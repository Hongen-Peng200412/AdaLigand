# AdaLigand Claude Entry

Project governance rules are defined in `AGENTS.md`. Read and follow `AGENTS.md` before doing non-trivial work.

In short: use `plan-log-governance` for plan/log/mapping/contract governance, use `execplan` for long-running execution logs, keep plans clean, keep history in logs and Git, and use repository-relative paths for local project files.
