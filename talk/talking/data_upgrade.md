
此文档写于 2026-8-9， 目的是进行数据管线的升级&重整，我们姑且称之为_v3。它可能牵动着后续的模型输入&监督、评估范围等等。注意，我们要保证在改动时旧的数据划分或产物仍能被所有程序&AI agengt读取，简言之，我们是升级而不是删除式覆盖。


# 用"完整性扫描脚本"，维护一个"产物齐全名单"all_valid_list
- 阅读日志，首先把那少数几个辅助标签没有打出来的PDB对应的产物文件夹名字加上后缀变成{pdb_id}_no_auxlabel、形状太小的PDB加上后缀变成{pdb_id}_too_small————这相当于留档。
- 同时，创建一个一个脚本，它可以扫描当前所有产物：包括密度三件套/标签/受体情况等等，并保证"所有产物"是灵活可叠加的。只要相应的配套产物齐全就记录到json里面，比如可以命名为 all_valid_list（和 keep_list 不同，后者需要经过过滤）。
这意味着根据数据处理的不同，"产物齐全名单"可能渐渐缩小，同时"完整性扫描脚本"也可能反复调用，相当于进行某一阶段的过滤和校验。
- 我同样想要建立一个全样本的"不退出"式的json名单 info.json ，初始时每个样本(pdb_id,emdb_id)的条目都是空的，只有在产生错误、匹配条目缺失、被keep_list的质量过滤时才增加相应原因——————这个建议初始的全空json由一次性临时样本产生，之前的历史失败由一次性临时脚本"补填"，之后由每个行动"顺手填上"，不增加额外专用逻辑，且它不进入正式链路仅仅作为留档记录。

# 增加标签
注意 C:\Users\15919\Desktop\AdaLigand\文档\规划文档\数据处理_v2.md 产出的 ligand_area.npz 。我想要在原本字段的基础上增加6个字段：{type}_mask(1,D,H,W)bool，每一个配体类别一个，代表这类配体所产生的ligand-area-mask，允许重叠。
这个标签将是未来多分类监督的基础。

# 下载受体的蛋白&核酸序列（fasta）
我想要受体的蛋白 & 核酸的链序列，它们必将用于后续的去冗余操作；同时，如果不需要很多"额外力气"，我同样想要维护"某个受体原子到某条链上的某个残基的映射(对应)关系"，有了它们就可以跑语言模型（如ESM-2、Luca等等）作为后续的额外一个模态（matcher如：talk\talking\Matcher_单候选闭集配体身份分类新方案.md，builder也自然加上）。也就是说我有如下期待: 解析 受体原子<————>序列残基<————>跑的ESM-2或luca等语言模型的特征 的映射关系、为每个(或几乎所有)残基给出语言模型的特征。


————————————————— 分隔线 ——————————————— 我们还需要考虑"执行顺序"(这一点我也没有下死命令)，但是我猜测，上面说的三条"较为基础"，所以可能需要先执行完成，然后跑"完整性扫描脚本"产生这一阶段的"产物齐全名单" ————————————————— 分隔线 ———————————————

# 按照新规则过滤/划分数据集————————这部分暂时都不做！（而且，涉及到去冗余的 convention，这必须问老板）
这一步分成三点：
- 产生keep_list，还有 all_valid_list, 但是对于keep_list，我想要试探性地尝试另外一套过滤规则：分辨率<5埃或4埃，cc加强为>0.7（预期过滤后大概剩余8000+）
- 进一步地，在keep_list(而不是 all_valid_list)的基础上根据 talk\talking\去冗余&验证测试集的构造.md 选出验证集、测试集(校准集可能因样本紧张，由验证集同样充当)、训练集。
- 另外建议在这一步就划分 box_pool————但是产生与加载box_pool的逻辑可能要修改。这包括但不限于：可以完全删除 "fraction"的相应逻辑了（因为我将不会再使用它）、训练时可能加一点逻辑保证每个global_batch都有一定bias正样本、重新划定box内部来源比例（如0:3:7）。
第二点需要预先得到fasta序列才可以做。

————————————————— 分隔线 ——————————————— 
# 跑配体语言模型
- 这一部分的行动草案写在 talk\talking\Matcher_单候选闭集配体身份分类新方案.md 里面了。注意，老版的"粗细分支二阶段训练"的matcher计划已经在不可行的边缘了，所以才提出新的行动草案。
- 受体语言模型可以考虑在这一步完成。

# 注意事项
- 我们每一步都允许有少量样本(<1%)因为代码的逻辑小瑕疵而失败：只要大部分样本能够成功就好；同时，每一步也都允许"某个样本因为超时(如3min没有产出结果)"而让程序自行跳过，我们同样允许这样的跳过(<1%)。
- 我们每做一步，都允许有少量样本或跳过，所以我建议每一步都"过滤" all_valid_list。同时写一个简单的通用脚本：过滤掉 keep_list、各个split的list中不在 all_valid_list 的部分（清除样本），这个样本要简便。注意如果按照我说的顺序做，box_pool内的样本完全不用过滤（因为即便语言模型的结果完全错误或缺失, 也不影响stage1的训练、且大概不影响stage2/3的E训练————如果影响了再说）