# Stage1 三项训练的入口链与差异检查记录

检查时间：2026-07-23 18:15–18:30（Asia/Shanghai）

检查对象：

- Slurm 作业 `321540`：新版 `Find_1`。
- Slurm 作业 `321107`：新版 `unet_c1`。
- Slurm 作业 `321743`：此前提交的 `Find_0`。

本记录来自只读检查。检查期间没有修改服务器文件，没有操作
`try_lock`、`kill_lock` 或 `after_lock`，也没有发送信号、提交作业或改变
Git 状态。

## 1. 事实标记

- **已确认**：直接读取 Slurm 状态、实际进程命令、脚本、源代码、配置文件、
  最终解析配置或运行产物得到。
- **由配置推导**：输入配置和实现已经确认，结论由二者直接推出，但本次没有
  重新执行数值实验。
- **尚未确认**：检查时尚未产生对应文件或没有足够证据。

## 2. 先看结论

### 2.1 三项训练是否走同一个训练入口

三项训练最终都进入 Pocket Plus 的 `src/train.py`，也都使用 Hydra 从
`configs/base.yaml` 开始组装配置。它们不是从同一个发布目录启动：

- `321540` 使用固定发布目录
  `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267`。
- `321107` 使用固定发布目录
  `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_9417bcf`。
- `321743` 使用作业启动时复制的运行目录
  `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/runtime/My_Project/Pocket_Plus`。

因此，“统一入口”应理解为相同的程序入口和 Hydra 组装方式，而不是三项训练
共享同一份服务器代码目录。

### 2.2 当前 warmup 是否已经统一为 0.005

**已确认：是。**

- `321540` 当前实际进程命令包含
  `train.scheduler.warmup_ratio=0.005`。
- `321107` 当前实际进程命令包含
  `train.scheduler.warmup_ratio=0.005`。
- `321743` 没有在命令行覆盖该字段；其最终解析配置
  `resolved_config.yaml` 中的值为 `0.005`。

新版 `Find_1` 和 `unet_c1` 此前使用 `0.025` 的运行已经被新的运行标识取代。
本记录后续比较的“当前新版训练”均指带有 `warmup005` 的运行。

### 2.3 最关键的实验差异

| 项目 | `321540` 新版 `Find_1` | `321107` 新版 `unet_c1` | `321743` 旧版 `Find_0` |
|---|---|---|---|
| 体素密度输入 | 56 个密度通道 | 1 个密度通道 `exp_clipnorm_nopost` | 56 个密度通道 |
| 原子/点分支 | 有 | 无 | 有 |
| 体素骨干最终通道 | 64 | 64 | 48 |
| 体素骨干末端多尺度卷积 | 关闭 | 关闭 | 始终执行 3×3×3、5×5×5、7×7×7 和融合卷积 |
| 体素输出头 | 两层 1×1×1 卷积，中间 ReLU | 同新版 `Find_1` | 每个已有输出头为单层 1×1×1 卷积 |
| 蛋白主链分类 | 5 类，启用 | 5 类，启用 | 无 |
| 核酸主链分类 | 7 类，启用 | 7 类，启用 | 无 |
| 配体距离回归 | 启用 | 启用 | 无 |
| 学习率 | `5e-5` | `1e-4` | `5e-5` |
| warmup 比例 | `0.005` | `0.005` | `0.005` |
| 单次前向的每卡批量 | 6 | 6 | 8 |
| GPU 数 | 2 | 1 | 2 |
| 梯度累积次数 | 4 | 8 | 3 |
| 有效全局批量 | 48 | 48 | 48 |
| 当前阶段 | CPC1；完成后自动进入 CPC2 | 单阶段正式训练 | CPC1；完成后自动进入 CPC2 |

## 3. 从 sbatch 到 `src/train.py` 的完整入口链

### 3.1 三个长期占用作业共用的锁控制结构

三个 sbatch 文件都不是直接写死一次训练命令，而是维持一个长期 GPU
资源占用，并在循环中检查以下文件：

- `pre_lock`
- `try_lock`
- `after_lock`
- `kill_lock`
- `run_cmd_<job-id>.sh`

三个 sbatch 文件的共同位置如下：

- 第 14 行：定义本轮资源范围目录
  `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations`。
- 第 15–20 行：定义 `run_cmd` 和四种锁文件。
- 第 29–53 行：执行锁控制循环。
- 第 36 行：使用
  `setsid stdbuf -oL -eL bash "$run_cmd"` 启动实际训练命令。

三个文件的差异主要是 Slurm 分区、GPU 型号、GPU 数量和作业名称。

### 3.2 `321540`：新版 `Find_1`

#### 入口链

1. Slurm 作业定义：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_alloc_h100_2_replan.sbatch`
2. 当前命令文件：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321540/run_cmd_321540.sh`
3. 固定发布目录：
   `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267`
4. Python 入口：
   `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267/src/train.py`
5. Hydra 总配置：
   `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267/configs/base.yaml`
6. CPC1 实验配置：
   `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267/configs/experiment/CPC1/Find_1.yaml`
7. 当前最终解析配置：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321540/auxiliary_supervision_1909267_warmup005/cpc1_resolved.yaml`
8. 当前运行目录：
   `/home/penghongen/My_Project/feedback_plus/logs/AdaLigand_Stage1-Find_1-CPC1/Find_1-CPC1____job321540_auxsup_1909267_warmup005_20260723T1545_CPC1`

#### `run_cmd_321540.sh` 的具体职责

- 第 4–12 行：定义资源范围、发布目录、证据目录和 CPC1/CPC2 运行目录。
- 第 14–23 行：检查目标目录碰撞并记录发布来源。
- 第 25–40 行：设置 Conda、`PYTHONPATH` 和 AdaLigand 数据位置。
- 第 42–55 行：定义共同训练参数。当前值包括两张 GPU、每卡批量 6、
  全局批量 48、10 个数据读取进程、学习率 `5e-5`、warmup 比例
  `0.005`。
- 第 57–68 行：选择 CPC1/CPC2 配置，并规定 CPC2 从本轮 CPC1 的
  `BEST.ckpt` 初始化。
- 第 70–102 行：生成最终解析配置并执行启动前断言。
- 第 120–124 行：启动 CPC1 正式训练。
- 第 128–132 行：CPC1 成功结束后启动 CPC2。

#### 当前实际进程

检查时，hnode01 上存在以下主进程：

```text
python -u src/train.py
  +experiment=CPC1/Find_1
  init_from=null
  project_name=AdaLigand_Stage1
  train.devices=2
  train.global_batch_size=48
  train.batch_size=6
  train.num_workers=10
  train.max_epochs=20
  train.val_per_epoch=30
  train.optimizer.lr=5.0e-5
  train.scheduler.warmup_ratio=0.005
  train.scheduler.stop_after_lr_reductions=4
  offline=false
```

### 3.3 `321107`：新版 `unet_c1`

#### 入口链

1. Slurm 作业定义：
   `/home/penghongen/My_Project/Pocket_Plus/tmp/adaligand_stage1_alloc_h100_1.sbatch`
2. 当前命令文件：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321107/run_cmd_321107.sh`
3. 固定发布目录：
   `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_9417bcf`
4. Python 入口：
   `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_9417bcf/src/train.py`
5. Hydra 总配置：
   `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_9417bcf/configs/base.yaml`
6. 实验配置：
   `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_9417bcf/configs/experiment/unet_c1.yaml`
7. 当前最终解析配置：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321107/auxiliary_supervision_unet_9417bcf_warmup005/resolved.yaml`
8. 当前运行目录：
   `/home/penghongen/My_Project/feedback_plus/logs/AdaLigand_Stage1-unet_c1/unet_c1____job321107_auxsup_unet_9417bcf_warmup005_20260723T1545_formal`

#### `run_cmd_321107.sh` 的具体职责

- 第 4–10 行：定义资源范围、发布目录、证据目录和运行目录。
- 第 12–21 行：检查目录碰撞并记录发布来源。
- 第 23–38 行：设置 Conda、`PYTHONPATH` 和数据位置。
- 第 40–56 行：定义一张 GPU、每卡批量 6、全局批量 48、20 个数据读取
  进程、学习率 `1e-4`、warmup 比例 `0.005` 和最多 4 次学习率降低。
- 第 59–82 行：生成最终解析配置并执行启动前断言。
- 第 101–105 行：启动正式训练。

#### 当前实际进程

检查时，hnode02 上存在以下主进程：

```text
python -u src/train.py
  +experiment=unet_c1
  init_from=null
  project_name=AdaLigand_Stage1
  train.devices=1
  train.global_batch_size=48
  train.batch_size=6
  train.num_workers=20
  train.max_epochs=20
  train.val_per_epoch=30
  train.optimizer.lr=1.0e-4
  train.scheduler.warmup_ratio=0.005
  train.scheduler.stop_after_lr_reductions=4
  offline=false
```

### 3.4 `321743`：旧版 `Find_0`

#### 入口链

1. Slurm 作业定义：
   `/home/penghongen/My_Project/Pocket_Plus/tmp/adaligand_stage1_alloc_h200_2.sbatch`
2. 当前命令文件：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/run_cmd_321743.sh`
3. 该作业创建的代码目录：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/runtime/My_Project/Pocket_Plus`
4. 两阶段调度脚本：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/runtime/My_Project/Pocket_Plus/tmp/stage1_formal_find_chain.sh`
5. 单阶段训练脚本：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/runtime/My_Project/Pocket_Plus/tmp/stage1_formal_train_stage.sh`
6. Python 入口：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/runtime/My_Project/Pocket_Plus/src/train.py`
7. Hydra 总配置：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/runtime/My_Project/Pocket_Plus/configs/base.yaml`
8. CPC1 实验配置：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/runtime/My_Project/Pocket_Plus/configs/experiment/CPC1/Find_0.yaml`
9. 当前最终解析配置：
   `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/formal/Find_0_CPC1/launch_1/resolved_config.yaml`
10. 当前运行目录：
    `/home/penghongen/My_Project/feedback_plus/logs/AdaLigand_Stage1-Find_0-CPC1/Find_0-CPC1____job321743_Find_0_CPC1_lr5e5_p2_val30_chunk2x_2gpu_m8_w1`

#### 三层脚本各自做什么

`run_cmd_321743.sh`：

- 第 4–9 行：建立本作业专用运行目录，从共享 Pocket Plus 目录复制代码；
  复制时排除 `.git` 和缓存。
- 第 11–15 行：激活 Conda 环境并进入运行目录。
- 第 17–20 行：运行 `Find_0` CPC1 显存探查。
- 第 22–23 行：进入正式两阶段脚本。

`stage1_formal_find_chain.sh`：

- 第 16–33 行：把 `Find_0` 映射到 CPC1 和 CPC2 实验配置。
- 第 35–37 行：调用单阶段脚本启动 CPC1。
- 第 39–45 行：读取 CPC1 结果和 `BEST.ckpt`。
- 第 48–50 行：用 CPC1 的 `BEST.ckpt` 初始化 CPC2。
- 第 57–70 行：保存两阶段结果记录。

`stage1_formal_train_stage.sh`：

- 第 55–71 行：构造全局批量 48、每卡批量 8、两张 GPU、10 个数据读取
  进程、20 个 epoch、每个 epoch 验证 30 次、学习率 `5e-5` 等覆盖项。
- 第 121–154 行：尝试连接 W&B；连接失败时允许转为离线记录。
- 第 155–161 行：设置唯一运行标识并保存最终解析配置。
- 第 175–176 行：启动 `src/train.py`。
- 第 264–285 行：检查 `BEST.ckpt` 和仅模型参数初始化结果。
- 第 287–303 行：保存本阶段结果。

#### 当前实际进程

检查时，hnode03 上存在以下主进程及其 DDP 子进程：

```text
python -u .../src/train.py
  +experiment=CPC1/Find_0
  init_from=null
  project_name=AdaLigand_Stage1
  train.devices=2
  train.global_batch_size=48
  train.batch_size=8
  train.num_workers=10
  train.max_epochs=20
  train.val_per_epoch=30
  train.optimizer.lr=5.0e-5
  train.scheduler.stop_after_lr_reductions=4
  offline=false
```

warmup 没有在命令行出现，但最终解析配置已确认是 `0.005`。

## 4. `src/train.py` 与 Hydra 如何组装最终实验

三个代码目录中的 `src/train.py` 在本次涉及的入口位置相同：

- 第 581–608 行：`_get_config_name()` 在没有额外环境变量时选择 `base`。
- 第 871 行：Hydra 使用 `config_path="../configs"` 和选出的配置名。
- 第 874 行：进入 `main()`。
- 第 890–893 行：设置训练随机种子。
- 第 895–907 行：创建运行目录，保存配置和源代码快照。
- 第 978–989 行：根据最终配置实例化 Dataset。
- 第 1124–1129 行：实例化模型。
- 第 1131–1140 行：执行仅模型参数的 checkpoint 初始化。
- 第 1153–1204 行：建立 W&B 记录。
- 第 1210–1238 行：建立 checkpoint 回调。
- 第 1263–1270 行：计算验证频率。
- 第 1505 行：调用 `trainer.fit(...)`。

三个 `configs/base.yaml` 都通过 `defaults` 列表依次选择模型组件、损失、
Dataset 和训练配置，最后由实验配置覆盖。应以“最终解析配置”为实际训练事实，
不能只看某一个 YAML 文件。

### 4.1 `Find_1` 的 Hydra 选择

`configs/experiment/CPC1/Find_1.yaml`：

- 第 3–22 行：选择模型各组件、辅助监督损失、`stage1_find` Dataset 和
  `stage1_cpc1` 训练配置。
- 第 18 行：损失配置为 `stage1_find_cpc1_auxiliary`。
- 第 19 行：Dataset 配置为 `stage1_find`。
- 第 20 行：训练配置为 `stage1_cpc1`。
- 第 30–50 行：设置 `Find_1` 名称和 16 个已知无效结构排除项。
- 第 52–66 行：设置体素最终通道 64、关闭末端多尺度卷积、启用蛋白、
  核酸和配体距离输出头。

### 4.2 `unet_c1` 的 Hydra 选择

`configs/experiment/unet_c1.yaml`：

- 第 3–22 行：明确不使用点分支、融合、原子特征嵌入和稀疏细化。
- 第 18 行：损失配置为 `stage1_unet_c1`。
- 第 19 行：Dataset 配置为 `stage1_unet_c1`。
- 第 20 行：训练配置为 `stage1_cpc1`。
- 第 30–53 行：保留 U-Net 循环计算，设置最终通道 64、关闭末端多尺度
  卷积、启用三个新输出头。
- 第 59–78 行：设置 Dataset 参数和相同的 16 个无效结构排除项。

### 4.3 `Find_0` 的 Hydra 选择

`configs/experiment/CPC1/Find_0.yaml`：

- 第 3–22 行：选择旧版 `Find_0` 原子嵌入、旧版 CPC1 损失、
  `stage1_find` Dataset 和 `stage1_cpc1` 训练配置。
- 第 18 行：损失配置为 `stage1_find_cpc1`。
- 第 19 行：Dataset 配置为 `stage1_find`。
- 第 20 行：训练配置为 `stage1_cpc1`。
- 第 30–48 行：设置旧版 `Find_0` 模型。

## 5. 数据与抽样差异

### 5.1 共同点

**已确认：**

- 三项训练读取相同的 AdaLigand 数据根目录、预处理目录和 BOX 池。
- `request_seed=3407`。
- 训练随机种子为 `3407`。
- `box_sample_fraction=1.0` 的新版训练没有创建比例抽样清单。
- 三项训练均开启随机空间旋转。
- 三项训练均未启用按类别平衡的采样器。

### 5.2 密度通道

- 新版 `Find_1` 和旧版 `Find_0` 都读取 56 个密度通道。
- 新版 `unet_c1` 只读取 `exp_clipnorm_nopost` 一个密度通道。

实际模型初始化日志还显示：

- 新版 `Find_1` 的原始密度通道数为 56，加入在线原子体素特征后，
  体素骨干输入为 107 通道。
- 新版 `unet_c1` 的体素骨干输入为 1 通道。
- 旧版 `Find_0` 的原始密度通道数为 56，加入旧版原子体素特征后，
  体素骨干输入为 105 通道。

### 5.3 每个 epoch 的 BOX 选择

**已确认：**

- 旧版 `Find_0` 使用 `request_seed + epoch` 从完整 BOX 池重新选择满足
  1:5:3 数量关系的 BOX。
- 新版代码在 `box_sample_fraction=1.0` 时仍使用相同的
  `request_seed + epoch` 方式，每个 epoch 重新选择 BOX。
- 新版两项训练在生成请求前会排除 16 个已知无效结构；旧版
  `Find_0` 没有这份排除列表。

因此，新版和旧版在比例为 1.0 时都保留了跨 epoch 重新选取 BOX 的行为，
但新版可用结构集合少了 16 个已知无效条目。

## 6. 模型差异

### 6.1 原子/点分支

- 新版 `Find_1`：有点分支和密度—原子信息融合。
- 新版 `unet_c1`：没有点分支、原子嵌入或融合，只训练体素 U-Net 和体素
  输出头。
- 旧版 `Find_0`：有点分支和密度—原子信息融合。

### 6.2 体素骨干

三项训练都使用 RAUNet 系列体素骨干，主要通道规划相同：

```text
[64, 256, 256, 256, 256, 256, 128, 64, 64]
```

三项训练都允许最多 3 次循环计算，并在训练时随机选择实际循环次数。

差异如下：

- 新版 `Find_1` 和 `unet_c1` 的最终体素特征为 64 通道，并关闭
  `enable_multiscale_output`。
- 旧版 `Find_0` 的最终体素特征为 48 通道。旧代码没有
  `enable_multiscale_output` 开关，因此固定执行 3×3×3、5×5×5、
  7×7×7 三个卷积，拼接后再执行 3×3×3 融合卷积。

### 6.3 原子体素特征

新版 `Find_1` 的
`configs/model/embed_head/Find_1.yaml` 已确认：

- 产生 49 个原子体素特征通道。
- 加入原子占据特征和原子中心特征。
- 使用 Gaussian 散射。
- Gaussian `sigma=0.7`。

旧版 `Find_0` 的
`configs/model/embed_head/Find_0.yaml` 已确认：

- 原嵌入模块不直接产生体素通道。
- 不加入新版的原子占据和中心特征。
- 在线原子体素化使用旧版硬散射规则。

### 6.4 输出头

新版实现位于：

`/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267/src/model/stage1_voxel_backbone.py`

- 第 96–116 行：建立配体区域、结合区域、蛋白主链、核酸主链和配体距离
  输出头；旧配置中的输出头层数字段不再控制新版结构。
- 第 151–161 行：每个输出头固定为
  `1×1×1 Conv → ReLU → 1×1×1 Conv`。
- 第 135–142 行：按各任务先验概率初始化最后一层偏置。

新版结构类别为：

- 蛋白：背景、N、CA、C、O，共 5 类。
- 核酸：背景、P、O5'、C5'、C4'、C3'、O3'，共 7 类。

蛋白和核酸每个非背景类别的初始概率均为 `0.01`；背景分别取得剩余的
`0.96` 和 `0.94`。配体距离头按照变换后目标约为 `1/11` 的概率初始化。

旧版实现位于：

`/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/runtime/My_Project/Pocket_Plus/src/model/stage1_voxel_backbone.py`

- 第 80–106 行：输出头深度仍由配置控制。
- `Find_0` 最终配置把配体区域和结合区域输出头的额外 3×3×3 卷积数设为
  0，因此两个头各自只剩一层 1×1×1 卷积。
- 旧版没有蛋白主链、核酸主链或配体距离输出头。

## 7. 损失与权重差异

### 7.1 CPC1 当前权重

| 损失 | 新版 `Find_1` | 新版 `unet_c1` | 旧版 `Find_0` |
|---|---:|---:|---:|
| 配体区域 | 1.0 | 1.0 | 1.0 |
| 配体距离 | 0.3 | 0.3 | 不存在 |
| 结合区域 | 0.1 | 0.1 | 0.1 |
| 蛋白主链 | 0.05 | 0.05 | 不存在 |
| 核酸主链 | 0.05 | 0.05 | 不存在 |
| 原子分类 | 1.0 | 0.0 | 1.0 |
| 伪标签/概率项 | 0.1 | 0.0 | 0.1 |

这意味着新版 `unet_c1` 虽然采用和新版 `Find_1` 相同的五项体素损失，
但它没有点分支，因此原子分类和伪标签/概率项不参加总损失。

### 7.2 分类损失的计算

新版蛋白和核酸损失均为：

```text
0.7 × focal + 0.3 × Tversky
```

- focal 的 `gamma=2`。
- Tversky 的 `alpha=0.5`、`beta=0.5`，是对称的 Dice 形式。
- 多分类 Tversky 会把每个非背景类别分别当作前景计算，再对这些类别取平均。
- 损失在体素维度取平均。

原有配体区域和结合区域也保留 focal 与对称 Tversky 的组合方式。

### 7.3 配体距离损失

**已确认：**

- Dataset 从 `ligand_dist.npz` 读取最近配体原子距离。
- 训练目标使用 `1 / (1 + distance)`。
- 无限距离变换为 0。
- 模型输出经过 sigmoid。
- 损失为所有体素上的平均 MSE。
- 权重为 `0.3`。

旧版 `Find_0` 没有这项标签、输出或损失。

### 7.4 CPC2

新版 `Find_1` 的
`configs/loss/stage1_find_cpc2_auxiliary.yaml` 明确把以下五项权重设为 0：

- 配体区域
- 结合区域
- 蛋白主链
- 核酸主链
- 配体距离

它仍继承 CPC1 的原子分类和伪标签/概率项。CPC2 从本轮 CPC1 的
`BEST.ckpt` 只加载模型参数，优化器、学习率调度器和训练步数重新开始。

旧版 `Find_0` 的 CPC2 同样把配体区域和结合区域权重设为 0，并保留原子
分类和伪标签/概率项；它也从 CPC1 的 `BEST.ckpt` 只加载模型参数。

`unet_c1` 没有 CPC2。

## 8. 优化器、批量与验证频率

### 8.1 共同点

- 有效全局批量均为 48。
- 混合精度均为 BF16。
- 最大 epoch 数均为 20。
- 每个 epoch 计划验证 30 次。
- CPC1 最多允许 4 次学习率降低后停止。

### 8.2 不同点

- 新版 `Find_1`：两张 GPU，每卡批量 6，梯度累积 4 次，学习率
  `5e-5`。
- 新版 `unet_c1`：一张 GPU，每卡批量 6，梯度累积 8 次，学习率
  `1e-4`。
- 旧版 `Find_0`：两张 GPU，每卡批量 8，梯度累积 3 次，学习率
  `5e-5`。
- 旧版 `Find_0` 的调度器耐心值为 2；新版当前基础配置的耐心值为 3。
- 三项训练当前 warmup 比例均为 `0.005`。

## 9. W&B、checkpoint 与源代码快照

### 9.1 新版 `Find_1`

当前运行明确请求 `offline=false`，运行标识为：

```text
job321540_auxsup_1909267_warmup005_20260723T1545_CPC1
```

检查截止时：

- 已创建运行目录。
- 已保存完整 `src_snapshot/src`。
- 进程仍在运行。
- 尚未在运行目录中看到 W&B 运行 ID。
- 尚未生成 checkpoint。

因此，“请求在线 W&B”已确认；“已经连接在线 W&B”在本次检查截止时尚未确认。

### 9.2 新版 `unet_c1`

当前运行明确请求 `offline=false`，运行标识为：

```text
job321107_auxsup_unet_9417bcf_warmup005_20260723T1545_formal
```

检查截止时：

- 已创建运行目录。
- 已保存完整 `src_snapshot/src`。
- 进程仍在运行。
- 尚未在运行目录中看到 W&B 运行 ID。
- 尚未生成 checkpoint。

因此，“请求在线 W&B”已确认；“已经连接在线 W&B”在本次检查截止时尚未确认。

### 9.3 旧版 `Find_0`

**已确认：**

- 在线 W&B 运行 ID：`w21l8dof`。
- W&B 地址：
  `https://wandb.ai/pencounkdual-111/AdaLigand_Stage1/runs/w21l8dof`。
- 已存在 `BEST.ckpt`、`last.ckpt` 和三个 epoch 0 的 TOP checkpoint。
- 当前从头训练，`init_from=null`。

旧版运行目录最初没有完整保存所有源代码；之后的
`backfill_provenance.json` 记录了一次“只补缺失文件”的回填，来源是
`321743` 的固定运行目录。已有 26 个文件经过校验，缺失源文件从该运行目录
补齐。由于最初复制代码时排除了 `.git`，本次无法从该运行目录确认精确 Git
提交；可以确认的是该运行目录中的具体代码和补齐后的运行快照。

### 9.4 共同 checkpoint 规则

三项训练使用的 `src/train.py` 都配置：

- 保存分数最高的 10 个 checkpoint。
- 保存 `last.ckpt`。
- 建立 `BEST.ckpt` 别名。
- 每 3 个 epoch 保存周期 checkpoint。

新版 `Find_1` 和 `unet_c1` 的发布目录分别固定到 Git 提交
`1909267` 和 `9417bcf`；每个运行目录还保存完整 `src` 快照、最终配置和文件
哈希清单。旧版 `Find_0` 固定的是作业专用运行目录，没有可核实的 Git 提交号。

## 10. Slurm 资源差异与一个记录不一致

| 作业 | 节点 | GPU | CPU | Slurm 命令文件 |
|---|---|---|---:|---|
| `321540` | hnode01 | H100 × 2 | 24 | `/home/penghongen/My_Project/tmp/adaligand_stage1_alloc_h100_2_replan.sbatch` |
| `321107` | hnode02 | H100 × 1 | 24 | `/home/penghongen/My_Project/Pocket_Plus/tmp/adaligand_stage1_alloc_h100_1.sbatch` |
| `321743` | hnode03 | H200 × 2 | 24 | `/home/penghongen/My_Project/Pocket_Plus/tmp/adaligand_stage1_alloc_h200_2.sbatch` |

`321743` 的 sbatch 源文件第 7 行写的是 `cpus-per-task=48`，但当前
`scontrol show job -dd 321743` 显示实际分配为 24 个 CPU。这是已确认的记录
不一致；本次只读检查没有判断它由提交时脚本版本变化还是调度系统参数覆盖造成。

## 11. 建议按以下顺序亲自复核

### 11.1 先确认作业身份

```bash
scontrol show job -dd 321540
scontrol show job -dd 321107
scontrol show job -dd 321743
```

重点核对 `Command`、`WorkDir`、`NodeList`、GPU 数和标准输出路径。

### 11.2 再看长期资源脚本与当前命令

依次查看每项训练的 sbatch 文件和
`allocations/<job-id>/run_cmd_<job-id>.sh`。当前真正生效的学习率、
warmup、实验名和发布目录首先在 `run_cmd` 中确定。

### 11.3 再沿 Hydra 配置检查

按以下顺序阅读：

1. `configs/base.yaml`
2. `configs/experiment/...yaml`
3. 实验配置引用的模型、Dataset、损失和训练 YAML
4. `src/train.py`
5. 本记录列出的 `resolved.yaml` 或 `resolved_config.yaml`

最终解析配置优先级高于单个 YAML，因为它已经合并 defaults 和命令行覆盖。

### 11.4 最后核对实际运行目录

每个运行目录中的以下文件共同确定一次真实实验：

- `config.yaml`
- `train.yaml`
- `src_snapshot/`
- 源代码快照清单
- `wandb/`
- `checkpoints/`

如果命令文件、最终解析配置和运行目录中的 `config.yaml` 三者不一致，应先
按时间和唯一运行标识确认当前进程究竟对应哪一个目录，不能只凭 W&B 展示名
判断。

## 12. 尚未确认的事项

截至本次检查结束：

- 两个带 `warmup005` 的新版运行尚未在运行目录中写出 W&B 运行 ID，因此
  在线连接状态尚未确认。
- 两个新版运行尚未生成首个 checkpoint，因此无法在本次记录中核对新
  checkpoint 的指标、保存次序或恢复结果。
- `321743` 的精确 Git 提交号无法从其不含 `.git` 的固定运行目录恢复；
  本记录只把该固定运行目录和运行快照视为代码事实。
- `321743` 的 CPU 请求文件与实际分配不一致，原因尚未确认。

## 13. 2026-08-01 阶段切换与运行产物索引

本节记录 2026-08-01 用户确认后的当前事实，并取代第 12 节中关于 W&B、checkpoint 和阶段切换的早期未确认状态。旧记录不删除，因为它们仍说明三项训练最初是如何启动和核验的。

### 13.1 本轮统一选择与阶段切换规则

- 只比较确实存在于运行目录 `checkpoints/` 中的 TOP checkpoint。
- 以 `val_score/global/voxel_ligand_PRAUC` 最大值选取阶段起点，不使用可能落后一轮验证的 `BEST.ckpt`。
- TOP 文件名中的 `score` 仍是旧代码保存的验证总损失，只用于识别文件，不再代表本轮选择标准。
- Find_0 与 Find_1 的 CPC2 都只加载选定 checkpoint 的模型参数；从第一步直接使用最高学习率 `5e-5`，不执行 warmup。
- `unet_c1` 不运行 CPC2。它自然结束后只记录配体区域 PR-AUC 最大值对应的 TOP checkpoint。
- 三个当前冻结运行副本不换版。它们在学习率下降停止回调的既知错误处自然退出时，保留错误、锁、运行目录和 allocation。

### 13.2 Job 321540：新版 Find_1

| 内容 | 地址或值 |
|---|---|
| Slurm | Job `321540`，`hnode01`，H100 × 2 |
| 冻结代码 | `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267` |
| 当前 CPC1 运行目录 | `/home/penghongen/My_Project/feedback_plus/logs/AdaLigand_Stage1-Find_1-CPC1/Find_1-CPC1____job321540_auxsup_1909267_warmup005_20260723T1545_CPC1` |
| 保留的错误 warmup 历史目录 | `/home/penghongen/My_Project/feedback_plus/logs/AdaLigand_Stage1-Find_1-CPC1/Find_1-CPC1____job321540_auxsup_1909267_20260723T1400_CPC1` |
| W&B | ID `owb4e114`；`https://wandb.ai/pencounkdual-111/AdaLigand_Stage1/runs/owb4e114` |
| Slurm 标准输出 | `/home/penghongen/My_Project/tmp/adaligand_s1_find2_h100x2_321540.out` |
| Slurm 标准错误 | `/home/penghongen/My_Project/tmp/adaligand_s1_find2_h100x2_321540.err` |
| 当前状态 | CPC1 继续运行；本轮结束后不启动 CPC2 |

当前 CPC1 目录中的 `config.yaml`、`train.yaml`、`src_snapshot/`、`wandb/` 和 `checkpoints/` 共同构成运行证据。2026-07-31 16:35 已知 `TOP_epoch_00_score_0.2998.ckpt` 的配体区域 PR-AUC 为 `0.656438`；此后又有 TOP 文件落盘，因此 Find_1 自然结束后必须重新逐个核对所有实际 TOP，不能提前把该文件认定为最终最优产物。本轮只记录比较结果和文件地址，不启动 Find_1 CPC2。

### 13.3 Job 321107：新版 unet_c1

| 内容 | 地址或值 |
|---|---|
| Slurm | Job `321107`，`hnode02`，H100 × 1 |
| 冻结代码 | `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_9417bcf` |
| 当前 CPC1 运行目录 | `/home/penghongen/My_Project/feedback_plus/logs/AdaLigand_Stage1-unet_c1/unet_c1____job321107_auxsup_unet_9417bcf_warmup005_20260723T1545_formal` |
| 保留的错误 warmup 历史目录 | `/home/penghongen/My_Project/feedback_plus/logs/AdaLigand_Stage1-unet_c1/unet_c1____job321107_auxsup_unet_9417bcf_20260723T1345_formal` |
| W&B | ID `3a323sl8`；`https://wandb.ai/pencounkdual-111/AdaLigand_Stage1/runs/3a323sl8` |
| Slurm 标准输出 | `/home/penghongen/My_Project/tmp/adaligand_s1_unet_321107.out` |
| Slurm 标准错误 | `/home/penghongen/My_Project/tmp/adaligand_s1_unet_321107.err` |
| 当前状态 | CPC1 继续运行；结束后不启动 CPC2 |

当前 CPC1 目录同样保存 `config.yaml`、`train.yaml`、`src_snapshot/`、`wandb/` 和 `checkpoints/`。2026-07-31 11:25 已知 `TOP_epoch_00_score_0.2017.ckpt` 的配体区域 PR-AUC 为 `0.565161`；自然结束后仍须在全部实际 TOP 中重新比较，记录最终选定文件，但不据此启动新训练。

### 13.4 Job 321743：旧版 Find_0 CPC1 与 CPC2

| 内容 | 地址或值 |
|---|---|
| Slurm | Job `321743`，`hnode03`，H200 × 2 |
| 冻结运行代码 | `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/runtime/My_Project/Pocket_Plus` |
| CPC1 运行目录 | `/home/penghongen/My_Project/feedback_plus/logs/AdaLigand_Stage1-Find_0-CPC1/Find_0-CPC1____job321743_Find_0_CPC1_lr5e5_p2_val30_chunk2x_2gpu_m8_w1` |
| CPC1 W&B | ID `w21l8dof`；`https://wandb.ai/pencounkdual-111/AdaLigand_Stage1/runs/w21l8dof` |
| CPC1 最终解析配置 | `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/formal/Find_0_CPC1/launch_1/resolved_config.yaml` |
| CPC1 标准输出/错误 | 同一 `launch_1/` 下的 `train.out` 与 `train.err` |
| CPC1 退出原因 | `LearningRateReductionStopper` 从 `on_validation_end` 调用 `LightningModule.log()`，Lightning 拒绝；退出码记录为 `137` |
| CPC2 选择文件 | CPC1 目录下 `checkpoints/TOP_epoch_00_score_0.2843.ckpt` |
| CPC2 选择值 | 配体区域 PR-AUC `0.6754766702651978` |
| 选择文件 SHA-256 | `87ec6080809a14e2558e10c0740c16371b363fed0f672081b389f46c64928b44` |
| CPC2 运行标记 | `job321743_Find_0_CPC2_prauc0675477_20260801T132852_w1` |
| CPC2 运行目录 | `/home/penghongen/My_Project/feedback_plus/logs/AdaLigand_Stage1-Find_0-CPC2/Find_0-CPC2____job321743_Find_0_CPC2_prauc0675477_20260801T132852_w1` |
| CPC2 W&B | ID `liibt044`；`https://wandb.ai/pencounkdual-111/AdaLigand_Stage1/runs/liibt044` |
| CPC2 最终解析配置 | `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/formal/Find_0_CPC2/launch_1/resolved_config.yaml` |
| CPC2 标准输出/错误 | 同一 `launch_1/` 下的 `train.out` 与 `train.err` |
| 阶段切换证据 | `/home/penghongen/My_Project/tmp/adaligand_stage1_20260721T024000/allocations/321743/transition_Find_0_CPC2_prauc0675477_20260801T132852` |
| CPC2 最终状态 | 用户根据 W&B 曲线判断收益较小后主动停止；Job `321743` 已离开 Slurm，不再监控或重启 |

阶段切换证据目录保存 CPC1 命令及摘要、选定 checkpoint 摘要、切换前的 `scontrol`、选择结果、CPC2 命令及摘要。2026-08-01 13:50 已确认 CPC2 严格加载 1,622 个模型参数键，冻结 123,277,630 个参数、训练 41,970 个参数；在线 W&B 已初始化。命令行与最终配置均为学习率 `5e-5`、`warmup_steps=0`、`warmup_ratio=0`、全局批量 48。2026-08-01 23:04 的只读核验确认 Job `321743` 已离开 Slurm；历史运行目录、W&B、checkpoint 和阶段切换证据继续保留，不执行清理、覆盖或重启。

### 13.5 只供下一次运行使用的 Pocket_Plus 修正

- 实现分支：`codex/stage1-prauc-monitor`，端点 `4ae9f5f4473114c2e5b3018d77404c8c00560234`。
- 学习分支：`Learn/stage1-prauc-monitor`，端点 `66469e263ca41bd2f2e941d34d42dcb2b8faea53`。
- 累计学习分支：`Learn/CUMULATIVE` 已快进到 `66469e2`。
- 两个端点的树对象均为 `5f96ab7e146bdc9a19c23817548ea137fbcb5e69`，专项测试均为 `31 passed`。
- Find_0、Find_1 和 `unet_c1` 的 `monitor_metric` 改为 `val_score/global/voxel_ligand_PRAUC`，`monitor_mode` 改为 `max`。
- `LearningRateReductionStopper` 保留学习率下降次数、checkpoint 状态、终止条件和控制台记录，但不再从 `on_validation_end` 调用 `LightningModule.log()`。
- 这些提交没有同步到 Job `321540`、`321107` 或 `321743` 的冻结代码目录，也没有修改本轮任何运行产物。

### 13.6 后续监控边界

`adaligand-stage1` heartbeat 每 5 小时只检查 Job `321540` 的新版 Find_1 CPC1 与 Job `321107` 的新版 `unet_c1`。每次分别核对 Slurm 身份、进程、GPU、W&B、当前运行目录、最终配置、TOP/last/BEST、锁和错误。Job `321743` 已由用户停止，不再检查、控制或重启；Find_1 CPC1 结束后也不启动 CPC2。不得执行 `scancel`、释放 allocation、覆盖旧目录、替换冻结代码或删除 CPC2 能力。

### 13.7 Find_0 CPC2 启动后的第一次短周期检查

检查时间为 2026-08-01 14:24 至 14:25。三个 Slurm Job 均为 `RUNNING`，每个 allocation 根目录都只有原有 `after_lock`，没有新的 `try_lock` 或 `kill_lock`。

- Job `321540` 的冻结代码路径与 CPC1 命令未变，摘要推进到 `global_step=20009`。最新验证总损失为 `0.30054771900177`，配体区域 PR-AUC 为 `0.6415252089500427`；五项训练损失均为有限值。两张 H100 分别占用约 80.514 GiB 和 79.910 GiB，W&B 内部日志持续写入，当前输出没有错误栈。
- Job `321107` 的冻结代码路径与 CPC1 命令未变，摘要推进到 `global_step=25409`。最新验证总损失为 `0.19972597062587738`，配体区域 PR-AUC 为 `0.5686334371566772`；五项训练损失均为有限值。H100 占用约 65.080 GiB、瞬时利用率 100%，W&B 内部日志持续写入，当前输出没有错误栈。
- Job `321743` 的 Find_0 CPC2 已经越过 checkpoint 读取和模型初始化并进入实际训练，摘要从启动时的 `global_step=0` 推进到 `119`。训练总损失为 `0.12919294834136963`，摘要中的学习率为 `5e-5`；最终解析配置仍为 `warmup_steps=0`、`warmup_ratio=0.0`、全局批量 48。主进程和两个分布式训练进程均来自冻结代码目录，W&B `liibt044` 持续写入；两张 H200 约占用 96.538 GiB 和 100.504 GiB，瞬时采样时一张卡利用率 100%。正式日志只有 Lightning 分布式同步建议和 DDP 参数检查警告，没有错误栈；CPC2 尚未保存首个 checkpoint。

这是 Find_0 CPC2 进入训练后的第 1 次连续健康检查。短周期健康计数为 1/5，heartbeat 保持每 30 分钟触发。

### 13.8 第二次短周期检查

检查时间为 2026-08-01 14:50 至 14:51。三个 Slurm Job 均为 `RUNNING`，每个 allocation 根目录仍只有原有 `after_lock`；三个当前进程的 `POCKET_RUN_STAMP` 均与各自唯一运行标记一致。

- Job `321540` 推进到 `global_step=20069`。五项训练损失均为有限值，最新验证总损失与配体区域 PR-AUC 仍为 `0.30054771900177` 和 `0.6415252089500427`。两张 H100 分别占用约 80.360 GiB 和 80.218 GiB，W&B 摘要持续写入。
- Job `321107` 推进到 `global_step=25493`。五项训练损失均为有限值，最新验证总损失与配体区域 PR-AUC 仍为 `0.19972597062587738` 和 `0.5686334371566772`。H100 占用约 65.080 GiB、瞬时利用率 100%，W&B 摘要持续写入。
- Job `321743` 的 Find_0 CPC2 推进到 `global_step=218`，训练总损失为 `0.15097272396087646`。摘要学习率仍为 `5e-5`；最终解析配置仍为 `warmup_steps=0`、`warmup_ratio=0.0`、全局批量 48。两个训练进程与两张 H200 均存在，当前正式错误日志没有错误栈；CPC2 尚未保存首个 checkpoint。

这是 Find_0 CPC2 进入训练后的第 2 次连续健康检查。短周期健康计数为 2/5，heartbeat 保持每 30 分钟触发。

### 13.9 第三次短周期检查

检查时间为 2026-08-01 15:20。三个 Slurm Job 均为 `RUNNING`，每个 allocation 根目录仍只有 `after_lock`。三个主训练进程的 `POCKET_RUN_STAMP` 与冻结代码工作目录均符合各自约定。

- Job `321540` 推进到 `global_step=20135`。五项训练损失均为有限值，最新验证总损失与配体区域 PR-AUC 仍为 `0.30054771900177` 和 `0.6415252089500427`。两张 H100 分别占用约 80.742 GiB 和 80.710 GiB，W&B 摘要持续写入。
- Job `321107` 推进到 `global_step=25595`。五项训练损失均为有限值，最新验证总损失与配体区域 PR-AUC 仍为 `0.19972597062587738` 和 `0.5686334371566772`。H100 占用约 65.080 GiB，W&B 摘要持续写入。
- Job `321743` 的 Find_0 CPC2 推进到 `global_step=326`，训练总损失为 `0.18076485395431519`。摘要学习率仍为 `5e-5`；最终解析配置仍为 `warmup_steps=0`、`warmup_ratio=0.0`、全局批量 48。两张 H200 分别占用约 118.184 GiB 和 133.228 GiB，两个训练进程均存在；正式错误日志自 13:50 后没有新增内容或错误栈。CPC2 尚未保存首个 checkpoint。

这是 Find_0 CPC2 进入训练后的第 3 次连续健康检查。短周期健康计数为 3/5，heartbeat 保持每 30 分钟触发。

### 13.10 第四次短周期检查

检查时间为 2026-08-01 15:50。三个 Slurm Job 均为 `RUNNING`，每个 allocation 根目录仍只有 `after_lock`。三个主训练进程的 `POCKET_RUN_STAMP` 与冻结代码工作目录继续符合各自约定。

- Job `321540` 推进到 `global_step=20204`。五项训练损失均为有限值，最新验证总损失与配体区域 PR-AUC 仍为 `0.30054771900177` 和 `0.6415252089500427`。两张 H100 分别占用约 80.898 GiB 和 80.096 GiB，W&B 摘要持续写入。
- Job `321107` 推进到 `global_step=25703`。五项训练损失均为有限值，最新验证总损失与配体区域 PR-AUC 仍为 `0.19972597062587738` 和 `0.5686334371566772`。H100 占用约 65.080 GiB、瞬时利用率 100%，W&B 摘要持续写入。
- Job `321743` 的 Find_0 CPC2 推进到 `global_step=443`，训练总损失为 `0.11972230672836304`。摘要学习率仍为 `5e-5`；最终解析配置仍为 `warmup_steps=0`、`warmup_ratio=0.0`、全局批量 48。两张 H200 分别占用约 126.888 GiB 和 142.812 GiB，第二张卡只剩约 0.959 GiB，但没有显存不足、进程退出或训练停滞证据。正式错误日志没有新增内容或错误栈；CPC2 尚未保存首个 checkpoint。

这是 Find_0 CPC2 进入训练后的第 4 次连续健康检查。短周期健康计数为 4/5，heartbeat 保持每 30 分钟触发。

### 13.11 第五次短周期检查

检查时间为 2026-08-01 16:20。三个 Slurm Job 均为 `RUNNING`，每个 allocation 根目录仍只有 `after_lock`。三个主训练进程的 `POCKET_RUN_STAMP` 与冻结代码工作目录继续符合各自约定。

- Job `321540` 推进到 `global_step=20270`。五项训练损失均为有限值，最新验证总损失与配体区域 PR-AUC 仍为 `0.30054771900177` 和 `0.6415252089500427`。两张 H100 分别占用约 80.900 GiB 和 80.962 GiB，W&B 摘要持续写入。
- Job `321107` 推进到 `global_step=25805`。五项训练损失均为有限值，最新验证总损失与配体区域 PR-AUC 仍为 `0.19972597062587738` 和 `0.5686334371566772`。H100 占用约 65.080 GiB、瞬时利用率 97%，W&B 摘要持续写入。
- Job `321743` 的 Find_0 CPC2 推进到 `global_step=545`，训练总损失为 `0.11528033018112183`。摘要学习率仍为 `5e-5`；最终解析配置仍为 `warmup_steps=0`、`warmup_ratio=0.0`、全局批量 48。两张 H200 分别占用约 135.082 GiB 和 143.068 GiB，第二张卡只剩约 0.703 GiB，但没有显存不足、进程退出或训练停滞证据。正式错误日志没有新增内容或错误栈；CPC2 尚未保存首个 checkpoint。

这是 Find_0 CPC2 进入训练后的第 5 次连续健康检查。连续健康计数达到 5/5，`adaligand-stage1` heartbeat 已改为每 5 小时触发；Find_1 如果转入 CPC2，则重新改为每 30 分钟检查并从零累计。

### 13.12 恢复五小时周期后的第一次检查

检查时间为 2026-08-01 21:25 至 21:26。三个 Slurm Job 均为 `RUNNING`，每个 allocation 根目录仍只有 `after_lock`。三个主训练进程的 `POCKET_RUN_STAMP` 与冻结代码工作目录继续符合各自约定。

- Job `321540` 推进到 `global_step=20945`。五项训练损失均为有限值，最新验证总损失与配体区域 PR-AUC 仍为 `0.30054771900177` 和 `0.6415252089500427`。两张 H100 分别占用约 80.812 GiB 和 80.878 GiB，W&B 事件文件与内部日志持续写入。
- Job `321107` 的摘要停在 `global_step=26303`，最近五项训练损失均为有限值，最新验证总损失与配体区域 PR-AUC 仍为 `0.19972597062587738` 和 `0.5686334371566772`。主训练进程仍存在，H100 占用约 65.080 GiB、瞬时利用率 97%；W&B 事件文件更新到 21:24，内部日志更新到 21:26，因此没有训练停滞证据。
- Job `321743` 的 Find_0 CPC2 推进到 `global_step=1334`，训练总损失为 `0.13490301370620728`。首次正式验证总损失为 `0.1431693732738495`，受体区域与配体区域 PR-AUC 分别为 `0.7405543327331543` 和 `0.6754766702651978`。运行在 21:20 保存首个 `TOP_epoch_00_score_0.1432.ckpt` 与 `last.ckpt`；摘要学习率仍为 `5e-5`，最终解析配置仍为 `warmup_steps=0`、`warmup_ratio=0.0`、全局批量 48。两张 H200 分别占用约 142.916 GiB 和 142.914 GiB，余量很小但没有显存不足。正式错误日志在首次验证时只增加警告，没有错误栈。

三个训练继续符合各自契约。`adaligand-stage1` heartbeat 保持每 5 小时触发。

### 13.13 CPC2 停止与监控范围收窄

2026-08-01，用户根据当前 W&B 损失曲线判断 CPC2 收益较小，主动停止 Find_0 CPC2，并决定本轮不再训练 Find_1 CPC2。这项决定只收窄本轮运行范围：Pocket_Plus 中的 CPC2 代码、配置、恢复接口以及已经形成的运行产物全部保留，不做额外修改或清理。

23:04 至 23:05 的只读核验结果如下：

- `squeue` 只返回 Job `321540` 与 `321107`；对 `321743` 查询 allocation 时，Slurm 返回作业编号无效，确认 Find_0 CPC2 已停止并离开调度系统。磁盘上原有 `after_lock_321743`、运行目录、W&B 和 checkpoint 属于历史证据，不触碰。
- Job `321540` 仍在 `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267` 运行 Find_1 CPC1，唯一运行标记保持为 `job321540_auxsup_1909267_warmup005_20260723T1545_CPC1`。摘要到达 `global_step=21041`，验证总损失为 `0.30054771900177`，配体区域 PR-AUC 为 `0.6415252089500427`；两张 H100 分别占用约 80.792 GiB 与 80.748 GiB，训练进程和 W&B 正常。
- Job `321107` 仍在 `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_9417bcf` 运行 `unet_c1`，唯一运行标记保持为 `job321107_auxsup_unet_9417bcf_warmup005_20260723T1545_formal`。摘要到达 `global_step=26399`，验证总损失为 `0.19810287654399872`，配体区域 PR-AUC 为 `0.57575523853302`；H100 占用约 65.080 GiB，训练进程和 W&B 正常。

从本节开始，heartbeat 只监控 Job `321540` 与 `321107`。任一运行自然结束后，只比较实际存在的 TOP checkpoints 并记录配体区域 PR-AUC 最高者及其地址；不启动 CPC2，也不把下一版代码部署到当前冻结运行。

### 13.14 两项 CPC1 的第一次五小时检查

检查时间为 2026-08-02 02:34 至 02:35。Job `321540` 与 `321107` 均为 `RUNNING`；Slurm 作业名称、节点、冻结发布目录和唯一运行标记符合各自约定。两个 allocation 根目录都只有原有 `after_lock`，没有 `try_lock` 或 `kill_lock`。

- Job `321540` 的 Find_1 CPC1 运行进程来自 `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267`。命令行与最终配置保持学习率 `5e-5`、`warmup_ratio=0.005`、全局批量 48；逐体素配体区域、受体结合区域、配体距离、蛋白主链和核酸主链损失权重仍为 `1.0`、`0.1`、`0.3`、`0.05`、`0.05`，受体原子继续使用既有 sigma=0.7 Gaussian scatter。摘要推进到 `global_step=21278`：训练总损失为 `0.18218788504600525`，配体区域、受体结合区域、配体距离、蛋白主链和核酸主链训练损失分别为 `0.0806870311498642`、`0.07949812710285187`、`0.0006529955426231027`、`0.0017028190195560455` 和 `0.000010965764886350371`；验证总损失为 `0.3003791570663452`，配体区域 PR-AUC 为 `0.6465026140213013`，蛋白与核酸主链宏平均 PR-AUC 为 `0.9973682761192322` 和 `0.9971734285354614`。01:01 保存了新的 `TOP_epoch_00_score_0.3004.ckpt` 与 `last.ckpt`；`BEST.ckpt` 仍对应更低验证总损失 `0.2998` 的旧 TOP，符合当前冻结代码以验证总损失选优的行为。两张 H100 分别占用约 80.656 GiB 和 80.846 GiB；W&B 摘要、事件文件和内部日志持续更新。
- Job `321107` 的 `unet_c1` 进程来自 `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_9417bcf`。命令行与最终配置保持学习率 `1e-4`、`warmup_ratio=0.005`、全局批量 48 和相同五项辅助监督权重。摘要推进到 `global_step=27251`：训练总损失为 `0.2268998771905899`，配体区域、受体结合区域、配体距离、蛋白主链和核酸主链训练损失分别为 `0.18272224068641663`、`0.18489527702331543`、`0.0015806052833795547`、`0.2784956693649292` 和 `0.22578296065330505`；验证总损失为 `0.19810287654399872`，配体区域 PR-AUC 为 `0.57575523853302`，蛋白与核酸主链宏平均 PR-AUC 为 `0.15546193718910217` 和 `0.2252635955810547`。8 月 1 日 22:41 保存了新的 `TOP_epoch_00_score_0.1981.ckpt` 与 `last.ckpt`；`BEST.ckpt` 暂时仍对应 `0.1990` 的旧 TOP，属于冻结回调的一次验证延迟，现有新 TOP 文件完整。H100 占用约 65.080 GiB、瞬时利用率 100%；W&B 摘要、事件文件和内部日志持续更新。

两项摘要没有除未启用 sparse-refine 面板之外的非有限数值。分配作业标准错误文件最后修改于 7 月 23 日，其中的 YAML、Hydra 与被 `kill_lock` 停止的记录均属于已经退出的启动尝试；当前 W&B 内部日志尾部没有错误。此次没有服务器写入、锁操作、作业控制或 CPC2 行为，heartbeat 保持每 5 小时触发。

### 13.15 两项 CPC1 的第二次五小时检查

检查时间为 2026-08-02 07:32 至 07:37。Job `321540` 与 `321107` 均为 `RUNNING`；Slurm 作业名称、节点、冻结发布目录和唯一运行标记符合各自约定。两个 allocation 根目录都只有原有 `after_lock`，没有 `try_lock` 或 `kill_lock`。

- Job `321540` 的 Find_1 CPC1 根训练进程继续从 `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267` 运行。命令行保持学习率 `5e-5`、`warmup_ratio=0.005` 和全局批量 48；运行目录 `config.yaml` 的 SHA-256 为 `ef79c88fdddbbb93a24b08be8210396893b384a60cba654bd2e8fbee7236fa3b`，五项损失权重仍为配体区域 `1.0`、受体结合区域 `0.1`、配体距离 `0.3`、蛋白主链 `0.05`、核酸主链 `0.05`，Gaussian scatter 开关仍为 `true`。W&B 摘要推进到 `global_step=22088`：训练总损失为 `0.2451460212469101`，五项训练损失依次为 `0.10679708421230316`、`0.11075326800346375`、`0.0006245375843718648`、`0.0022655113134533167` 和 `0.0032161250710487366`。最近验证总损失仍为 `0.3003791570663452`；配体区域、受体结合区域、蛋白主链和核酸主链 PR-AUC 分别为 `0.6465026140213013`、`0.7182583212852478`、`0.9973682761192322` 和 `0.9971734285354614`。本次没有新验证或 checkpoint；最近仍是 8 月 2 日 01:01 保存的 `TOP_epoch_00_score_0.3004.ckpt` 与 `last.ckpt`。两张 H100 分别占用约 80.612 GiB 和 80.542 GiB，瞬时利用率为 69% 和 86%；W&B 摘要与内部日志持续更新到 07:37。
- Job `321107` 的 `unet_c1` 根训练进程 PID `73776` 从 7 月 23 日持续运行，工作目录仍为 `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_9417bcf`。命令行保持学习率 `1e-4`、`warmup_ratio=0.005` 和全局批量 48；运行目录 `config.yaml` 的 SHA-256 为 `b4a37ac65c3732805184fa26d90318b9bebb5368aaf7c19df1685967c748d4c5`，五项损失权重没有变化。W&B 摘要当前为 `global_step=27620`：训练总损失为 `0.2178851068019867`，五项训练损失依次为 `0.17194195091724396`、`0.19969460368156433`、`0.0019239205867052078`、`0.2777489423751831` 和 `0.23018117249011993`。最近验证总损失仍为 `0.19810287654399872`；配体区域、受体结合区域、蛋白主链和核酸主链 PR-AUC 分别为 `0.57575523853302`、`0.6216362714767456`、`0.15546193718910217` 和 `0.2252635955810547`。本次没有新验证或 checkpoint；最近仍是 8 月 1 日 22:41 保存的 `TOP_epoch_00_score_0.1981.ckpt` 与 `last.ckpt`。摘要文件自 04:17 暂未刷新，但 W&B 二进制事件文件更新到 07:34、内部日志更新到 07:37，根训练进程占用约 68% CPU，H100 占用约 65.080 GiB、瞬时利用率 85%，没有训练停滞证据。

两项启用的训练损失、验证损失和 PR-AUC 均为有限值。两个分配作业的标准输出与标准错误文件仍停在 7 月 23 日，当前 W&B 内部日志尾部没有错误；本次没有服务器写入、锁操作、作业控制或 CPC2 行为。heartbeat 保持每 5 小时触发。

### 13.16 两项 CPC1 的第三次五小时检查

检查时间为 2026-08-02 17:31 至 17:34。Job `321540` 与 `321107` 均为 `RUNNING`；Slurm 作业名称、节点、冻结发布目录和唯一运行标记符合各自约定。两个 allocation 根目录都只有原有 `after_lock`，没有 `try_lock` 或 `kill_lock`。

- Job `321540` 的 Find_1 CPC1 继续从 `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_1909267` 运行。运行目录 `config.yaml` 的 SHA-256 仍为 `ef79c88fdddbbb93a24b08be8210396893b384a60cba654bd2e8fbee7236fa3b`；学习率 `5e-5`、`warmup_ratio=0.005`、全局批量 48、五项损失权重和 sigma=0.7 Gaussian scatter 没有变化。W&B 摘要到达 `global_step=23171`：训练总损失为 `0.2920948565006256`，配体区域、受体结合区域、配体距离、蛋白主链和核酸主链训练损失分别为 `0.11683323979377747`、`0.14222043752670288`、`0.0009072156390175223`、`0.0026279359590262175` 和 `0.0035408716648817062`。最新验证总损失为 `0.29281967878341675`；配体区域、受体结合区域、蛋白主链和核酸主链 PR-AUC 分别为 `0.6602762937545776`、`0.7388049960136414`、`0.9975420236587524` 和 `0.9981743693351746`。12:08 保存了新的 `TOP_epoch_00_score_0.2928.ckpt` 与 `last.ckpt`；`BEST.ckpt` 暂时仍指向验证总损失 `0.2998` 的旧 TOP，符合冻结回调的一次验证延迟。两张 H100 分别占用约 80.834 GiB 和 80.964 GiB；W&B 摘要与内部日志持续更新到 17:33。
- Job `321107` 的 `unet_c1` 继续从 `/home/penghongen/My_Project/tmp/aux_supervision_20260723/releases/Pocket_Plus_9417bcf` 运行。运行目录 `config.yaml` 的 SHA-256 仍为 `b4a37ac65c3732805184fa26d90318b9bebb5368aaf7c19df1685967c748d4c5`；学习率 `1e-4`、`warmup_ratio=0.005`、全局批量 48 和五项损失权重没有变化。W&B 摘要到达 `global_step=29060`：训练总损失为 `0.1751905381679535`，配体区域、受体结合区域、配体距离、蛋白主链和核酸主链训练损失分别为 `0.13402751088142395`、`0.1496044546365738`、`0.0009350805194117129`、`0.2695625126361847` 和 `0.2488785684108734`。最新验证总损失为 `0.19538724422454834`；配体区域、受体结合区域、蛋白主链和核酸主链 PR-AUC 分别为 `0.5682877898216248`、`0.6184656023979187`、`0.1795673817396164` 和 `0.2697680592536926`。16:56 保存了新的 `TOP_epoch_00_score_0.1954.ckpt` 与 `last.ckpt`；`BEST.ckpt` 正确指向 07:58 保存、验证总损失 `0.1952` 的当前最佳 TOP。H100 占用约 65.080 GiB、瞬时利用率 100%；W&B 摘要、二进制事件文件和内部日志持续更新到 17:33。

两项启用的训练损失、验证损失和 PR-AUC 均为有限值，未启用 sparse-refine 面板之外没有非有限数值。两个分配作业的标准输出与标准错误文件仍停在 7 月 23 日，当前 W&B 内部日志尾部没有错误；本次没有服务器写入、锁操作、作业控制或 CPC2 行为。heartbeat 保持每 5 小时触发。
