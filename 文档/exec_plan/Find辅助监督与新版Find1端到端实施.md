# 为 Find_1 增加结构与配体距离辅助监督并完成正式训练

本文是持续更新的 ExecPlan。实施过程中必须同步维护 `Progress`、`Surprises & Discoveries`、`Decision Log` 和 `Outcomes & Retrospective`；任何范围、接口、验证或服务器运行方式变化都要同时修改正文相关位置，并在文末记录修改原因。

本文依据 `C:\Users\15919\Desktop\AdaLigand\grill_with_memory\07-22-17-42.md` 中已经收敛的讨论结论，负责把这些结论实施为 AdaLigand 距离标签、Pocket_Plus 训练与推理代码、完整代码快照、正式训练和可恢复的运行记录。本文不实施六分类配体类型标签，不替换整个体素主干，不训练 Selector，也不重新运行 A–G 数据处理管线。CPC2 的代码与配置能力保留，但用户已于 2026-08-01 根据 W&B 曲线取消本轮继续训练 CPC2。

## Purpose / Big Picture

完成后，每个满足现有 Stage1 样本条件的 PDB 都有一份与实验密度网格对齐的 `ligand_dist.npz`。Pocket_Plus Dataset 从现有受体原子字段即时构造蛋白和核酸主链原子体素类别，并从距离文件裁切当前 `80×80×80` BOX。新版 Find_1 使用五个结构一致的逐体素输出头，同时学习现有配体区域、现有受体结合区域、配体反距离、蛋白主链原子和核酸主链原子。

用户可以通过四类结果确认交付成立：距离文件的字段与几何契约可以独立校验；本地和 Linux 测试及真实小样本前向—反向通过；任意 checkpoint 能优先使用运行目录中保存的完整 `src/` 快照进行推理；Job `321540` 与 `321107` 分别使用冻结代码继续运行新版 Find_1 CPC1 与新版 `unet_c1`，直至自然结束并保存可追溯产物。

## Progress

- [x] (2026-07-23 00:00+08:00) 完成 `grill with doc`，冻结辅助标签、损失、模型、快照、Git、CPU、GPU 与监控边界，并获得端到端实施许可。
- [x] (2026-07-23 00:20+08:00) 读取 AdaLigand `AGENTS.md` 触发的规划、代码结构、表达、双线 Git、ExecPlan、项目记忆与服务器交互 skills。
- [x] (2026-07-23 00:30+08:00) 冻结两个累计学习基点并建立隔离实现工作树：AdaLigand `799293c`，Pocket_Plus `de6a89f`。
- [x] (2026-07-23 01:10+08:00) 建立距离标签字段契约、纯计算、原子写入、校验、命令行入口、README 与 5 项专项测试；Linux 完整依赖环境验证仍在后续里程碑执行。
- [x] (2026-07-23 03:20+08:00) 完成 Pocket_Plus Dataset、固定类别契约、精确比例抽样、五个固定输出头、损失、验证指标、Find_1 两阶段配置和完整 `src/` 快照；实现提交为 `c462825`、`acfb018`。
- [x] (2026-07-23 03:20+08:00) 受限 subagent 完成 checkpoint 快照推理、可变 `voxel_final` 通道产物和 Selector 惰性输入，主 agent 审查后提交为 `b54a28d`。
- [x] (2026-07-23 03:20+08:00) 完成独立只读审计并修复缺失原子 NaN、比例抽样总数与元数据、空类别 PR-AUC、固定类别单一来源、输出头宽度、Find_1 专属学习率和 DDP 快照竞争；审计结论为无剩余代码级阻断或科学契约漂移。
- [x] (2026-07-23 03:25+08:00) 本地合并回归为 Pocket_Plus `78 passed`、AdaLigand 距离专项 `5 passed`，两个仓库 `compileall` 与 `git diff --check` 通过。Windows 全量收集缺少服务器专用依赖，保留到 Linux 环境执行。
- [x] (2026-07-23 03:37+08:00) 在真实 `10ad` 上用正式 CLI 生成并校验 `ligand_dist.npz`；28 秒完成，产物约 40 MiB，`failed=0`。
- [x] (2026-07-23 03:48+08:00) 在精确发布目录通过 Pocket_Plus `378 passed` 与 AdaLigand `306 passed`；真实 `10ad` Dataset 读出 56 通道输入和三项新标签，固定模型具有 64 通道 `voxel_final` 与五个两层 1×1×1 输出头。
- [x] (2026-07-23 03:58+08:00) 从 Job `321107`、`321540`、`321743` 各自 allocation 运行时源码只增补缺失快照文件，原有 26 个文件逐字核验且未覆盖；三种真实 checkpoint 均从补齐快照严格恢复成功。
- [ ] 完成服务器真实前向—反向验证；该步骤在接管 Job `321540` 后使用同一两张 H100 执行，不额外提交 GPU 作业。
- [x] (2026-07-23 11:30+08:00) 聚合全量距离生产的 12 个分片。原数组元素 `323027_6` 在全部目标文件原子落盘、写后验证长时间停留后取消；补验数组 `323275` 对 36 个长尾候选施加每样本 90 秒硬超时。最终 22,386 条状态为 22,369 success、12 skipped、5 unknown_failed，冻结 16 个训练排除 PDB。
- [x] (2026-07-23 12:05+08:00) 用唯一 `POCKET_RUN_STAMP` 接管 Job `321540`；旧 Find_1 进程以 143 退出，Slurm allocation、两张 H100 与 `after_lock_321540` 保留。
- [x] (2026-07-23 12:35+08:00) 按用户补充要求，把 `unet_c1` 的 U-Net 骨干保持不变，仅将最终特征、五个输出头、五项损失、学习率和 16 项训练排除清单与新版 Find_1 对齐；提交 `6c798d6`、`eef5c5b`，本地 34 项专项测试与精确发布目录 Linux 全套 `380 passed`。
- [x] (2026-07-23 12:42+08:00) 用 Job `321107` 的 `kill_lock` 停止旧 `unet_c1`，保留单张 H100、Slurm allocation 与 `after_lock_321107`；唯一新运行目录和精确发布目录均在启动前核验无碰撞。
- [x] (2026-07-23 14:05+08:00) Job `321107` 的新版 `unet_c1` smoke 完成四个训练批次，五项训练损失均为有限值；正式在线 W&B 训练已自动启动。
- [x] (2026-07-23 14:20+08:00) Job `321540` 从精确发布目录 `Pocket_Plus_1909267` 完成新版 Find_1 两卡 smoke：训练前验证、四个训练批次、五项损失与新增 PR-AUC 均完成，上一版本的 CPU/NCCL 错误未再出现。脚本已自动进入正式在线 W&B CPC1，CPC1、CPC2 最高学习率均为 `5e-5`。
- [x] (2026-07-23 14:28+08:00) 独立只读端到端审计完成，没有发现需要中止正式训练的科学契约漂移、无效数据、训练计算错误或目录身份冲突。两项正式训练仍在对应 Job 内运行，两个 `after_lock` 均保留。
- [x] (2026-07-23 14:33+08:00) 完成正式训练启动后的第 2 次健康检查。Find_1 CPC1 到达 `global_step=17`，`unet_c1` 到达 `global_step=95`；两者五项训练损失均为有限值，在线 W&B 持续更新，没有 traceback、显存不足或非预期 NaN。两个正式验证尚未开始，因此蛋白与核酸 PR-AUC 留待后续检查；当前也尚未到达 checkpoint 保存时点。
- [x] (2026-07-23 15:37+08:00) 完成连续第 3 次健康检查并把 heartbeat 从每 30 分钟改为每 5 小时。Find_1 CPC1 到达 `global_step=119`，`unet_c1` 到达 `global_step=260`；五项训练损失仍为有限值，在线 W&B 持续更新，两个 Job 的进程身份、唯一运行目录和 `after_lock` 均正确，没有 traceback、显存不足或非预期 NaN。Find_1 两张 H100 采样约为 80.8/81.6 GiB；两个训练仍未进入首次正式验证，也尚未生成 checkpoint。
- [x] (2026-07-23 18:16+08:00) 用户从 W&B 曲线发现 CPC1 继承的 `warmup_ratio=0.025` 不符合此前统一决定的 `0.005`。按用户授权，分别用 Job `321540` 与 `321107` 的 `kill_lock` 停止错误 warmup 运行，保留两个 allocation 和 `after_lock`；未运行新测试或 smoke，直接以唯一运行标记启动 `warmup_ratio=0.005` 的正式 Find_1 CPC1 与 `unet_c1`。两份 resolved 配置确认五项损失权重、学习率及其他已冻结字段未变。Pocket_Plus 默认配置和契约断言同步修正为提交 `aa1b9e9`。
- [x] (2026-07-23 18:56+08:00) 完成 warmup 纠正重启后的第 1 次健康检查。Find_1 CPC1 到达 `global_step=23`，`unet_c1` 到达 `global_step=41`；两条 `warmup_lr` 均从各自 0.33 倍初始学习率缓慢上升，五项训练损失有限，在线 W&B 持续更新，日志没有 traceback、显存不足或非预期 NaN。Find_1 两张 H100 采样约为 80.3/80.8 GiB，`unet_c1` 约为 62.7/81.6 GiB；尚未到 checkpoint 保存时点。
- [x] (2026-07-23 18:56+08:00) 只读 subagent 完成 Job `321540`、`321107`、`321743` 的差异和从 sbatch 到 resolved 配置的入口链路调查；唯一写入为 `C:\Users\15919\Desktop\AdaLigand\talk\检查记录.md`，没有修改或打断代码、配置、锁、作业、运行产物或 Git。
- [x] (2026-07-23 19:24+08:00) 完成 warmup 纠正重启后的第 2 次健康检查。Find_1 CPC1 到达 `global_step=68`，`unet_c1` 到达 `global_step=113`；两条 `warmup_lr` 继续平稳上升，五项训练损失有限，在线 W&B 持续更新，日志没有 traceback、显存不足或非预期 NaN。Find_1 两张 H100 采样约为 80.6/80.4 GiB，`unet_c1` 约为 62.7/81.6 GiB；首次正式验证指标和 checkpoint 仍未出现。
- [x] (2026-07-23 19:54+08:00) 完成 warmup 纠正重启后的第 3 次健康检查。Find_1 CPC1 到达 `global_step=119`，`unet_c1` 到达 `global_step=191`；两条 `warmup_lr` 连续上升，五项训练损失有限，在线 W&B 持续更新，日志和锁正常。Find_1 两张 H100 采样约为 80.9/81.0 GiB，`unet_c1` 约为 62.9/81.6 GiB；没有显存不足，首次正式验证指标和 checkpoint 仍未出现。
- [x] (2026-07-23 20:24+08:00) 完成 warmup 纠正重启后的第 4 次健康检查。Find_1 CPC1 到达 `global_step=164`，`unet_c1` 到达 `global_step=275`；两条 `warmup_lr` 继续按 `0.005` 预期上升，五项训练损失有限，在线 W&B 持续更新，日志和锁正常。Find_1 两张 H100 采样约为 80.6/80.6 GiB，`unet_c1` 约为 62.9/81.6 GiB；没有显存不足，首次正式验证指标和 checkpoint 仍未出现。
- [x] (2026-07-23 20:54+08:00) 完成 warmup 纠正重启后的第 5 次健康检查并把 heartbeat 改为每 5 小时。Find_1 CPC1 到达 `global_step=215`，`unet_c1` 到达 `global_step=347`；两条 `warmup_lr`、五项训练损失、在线 W&B、日志和锁持续正常。Find_1 两张 H100 采样约为 81.0/80.7 GiB，`unet_c1` 约为 62.9/81.6 GiB；没有显存不足，首次正式验证指标和 checkpoint 仍未出现。
- [x] (2026-07-23 20:55+08:00) 按用户要求把 Job `321743` 的旧版 Find_0 一并纳入后续 heartbeat。它继续按自身运行目录、resolved 配置、学习率、损失、W&B、checkpoint 和锁检查，不套用新版辅助监督字段；入口链路和三项任务差异见 `C:\Users\15919\Desktop\AdaLigand\talk\检查记录.md`。
- [x] (2026-07-24 02:01+08:00) 完成第一次三作业联合检查。Job `321540` 新版 Find_1 到达 `global_step=698`，Job `321107` 新版 `unet_c1` 到达 `global_step=1112`；两者学习率、五项损失、在线 W&B、日志、锁和显存正常，仍未进入首次正式验证或保存 checkpoint。Job `321743` 旧版 Find_0 到达 `global_step=5483`，正式验证损失与 F1/PRAUC 有限，已有 BEST、last 和 TOP checkpoints，W&B 在线；两张 H200 采样约为 142.1/142.8 GiB（各总计约 143.8 GiB），余量较小但没有显存不足。
- [x] (2026-07-24 07:04+08:00) 完成第二次三作业联合检查。三个 Slurm 作业均为 `RUNNING`，各自只保留 `after_lock`，错误扫描没有发现 traceback、显存不足或非预期 NaN。Job `321540` 新版 Find_1 到达 `global_step=1241`，五项训练损失有限，运行配置仍为 Gaussian `sigma=0.7`、`warmup_ratio=0.005`、最高学习率 `5e-5` 和全局批量 48；尚未进入首次正式验证或保存 checkpoint。Job `321107` 新版 `unet_c1` 到达 `global_step=1325`，首次正式验证总损失为 `0.273812`，蛋白主链与核酸主链宏平均 PR-AUC 分别为 `0.0381163` 和 `0.00681047`，已保存 `TOP_epoch_00_score_0.2738.ckpt` 与 `last.ckpt`。Job `321743` 旧版 Find_0 到达 `global_step=6233`，既有验证指标和 BEST、last、TOP checkpoints 继续有效；两张 H200 采样约为 142.4/142.6 GiB（各总计约 143.8 GiB），余量仍小但没有显存不足。
- [x] (2026-07-24 12:01+08:00) 完成第三次三作业联合检查。三个作业仍为 `RUNNING`，Slurm batch step、W&B 步数和 GPU 采样持续推进，各自只有 `after_lock`，错误扫描为空。Job `321540` 新版 Find_1 到达 `global_step=1430`，首次正式验证总损失为 `0.423916`，蛋白主链与核酸主链宏平均 PR-AUC 分别为 `0.111973` 和 `0.00329660`，已保存 `TOP_epoch_00_score_0.4239.ckpt` 与 `last.ckpt`；运行配置与五项损失保持不变。Job `321107` 新版 `unet_c1` 到达 `global_step=2180`，首次验证指标与 TOP、last checkpoints 保持有效。Job `321743` 旧版 Find_0 到达 `global_step=6773`，最新验证总损失为 `0.330061`，受体与配体区域 PR-AUC 分别为 `0.660388` 和 `0.606173`，已更新 TOP 与 last checkpoints；两张 H200 采样约为 141.7/141.4 GiB。两个 Find 均尚未从 CPC1 切换到 CPC2。
- [x] (2026-07-24 17:03+08:00) 完成第四次三作业联合检查。三个作业均为 `RUNNING`，配置、唯一运行目录、`after_lock` 和错误扫描没有变化。Job `321540` 新版 Find_1 到达 `global_step=1973`，五项训练损失保持有限，首次验证与 TOP、last checkpoints 继续有效。Job `321107` 新版 `unet_c1` 到达 `global_step=2630`；`wandb-summary.json` 在 14:43 后暂未刷新，但 W&B 二进制事件文件、内部日志和 GPU 采样持续更新到 17:03，GPU 利用率为 84%–100%，因此当前没有进程停滞证据。allocation 总日志中出现的 Hydra traceback 属于已经退出的旧启动尝试，不属于当前唯一运行目录。Job `321743` 旧版 Find_0 到达 `global_step=7529`，既有验证与 checkpoints 保持有效；两张 H200 最新采样约为 142.7/142.5 GiB。两个 Find 均尚未进入 CPC2。
- [x] (2026-07-24 22:04+08:00) 完成第五次三作业联合检查。三个作业均为 `RUNNING`，各自配置、唯一运行目录、`after_lock`、GPU 采样和 W&B 事件流持续有效。Job `321540` 新版 Find_1 到达 `global_step=2528`，训练损失有限，首次验证与 TOP、last checkpoints 保持有效；W&B 内部日志中的 traceback 是 7 月 23 日一次 GraphQL 请求被限流后的自动重试，当前摘要已继续刷新，不是训练错误。Job `321107` 新版 `unet_c1` 到达 `global_step=3236`，第二次验证总损失从 `0.273812` 降至 `0.241217`，受体与配体区域 PR-AUC 从 `0.370737`、`0.397535` 升至 `0.448923`、`0.456689`，蛋白主链宏平均 PR-AUC 从 `0.0381163` 升至 `0.0508417`，核酸主链宏平均 PR-AUC 从 `0.00681047` 降至 `0.00279204`；已保存新的 TOP 与 last checkpoints。Job `321743` 旧版 Find_0 到达 `global_step=8084`，最新验证总损失为 `0.328610`，并更新 TOP 与 last checkpoints；两张 H200 最新采样最高约为 142.8/143.0 GiB，余量很小但没有显存不足。两个 Find 均尚未进入 CPC2。
- [x] (2026-07-25 16:27+08:00) 完成第六次三作业联合检查。三个作业均为 `RUNNING`，精确发布身份、最终配置、唯一运行目录和各自唯一的 `after_lock` 正确；当前运行目录没有训练 traceback、显存不足或数据契约错误，W&B 事件流与 GPU 采样持续更新。Job `321540` 新版 Find_1 到达 `global_step=3971`，最新验证总损失降至 `0.343283`，蛋白与核酸主链宏平均 PR-AUC 分别升至 `0.976166` 和 `0.947399`，并保存新的 TOP 与 last checkpoints。Job `321107` 新版 `unet_c1` 到达 `global_step=5258`，第三次验证总损失降至 `0.231801`，蛋白与核酸主链宏平均 PR-AUC 分别为 `0.0619655` 和 `0.0283426`，并保存新的 TOP 与 last checkpoints；其摘要文件在 13:09 后暂未刷新，但 W&B 二进制事件文件和 GPU 采样持续更新到 16:28，单张 H100 利用率为 97%，没有进程停滞证据。Job `321743` 旧版 Find_0 到达 `global_step=10931`，最新验证总损失降至 `0.317618`，受体与配体区域 PR-AUC 分别为 `0.686995` 和 `0.615032`，并保存新的 TOP 与 last checkpoints。此次检查期间两张 H200 的最高显存占用为 143.070 GiB，单卡总显存为 143.771 GiB，余量很小但没有显存不足。两个 Find 均尚未进入 CPC2。
- [x] (2026-07-25 21:29+08:00) 完成第七次三作业联合检查。三个作业均为 `RUNNING`，精确发布身份、最终配置、唯一运行目录和各自唯一的 `after_lock` 正确；当前应用日志没有训练 traceback、显存不足或数据契约错误，W&B 事件流与 GPU 采样持续更新。Job `321540` 新版 Find_1 到达 `global_step=4628`，五项训练损失均为有限值，最近一次验证与 TOP、last checkpoints 继续有效。Job `321107` 新版 `unet_c1` 到达 `global_step=6197`，第四次验证总损失从 `0.231801` 降至 `0.221887`，受体与配体区域 PR-AUC 分别升至 `0.535934` 和 `0.504227`，蛋白与核酸主链宏平均 PR-AUC 分别升至 `0.0797290` 和 `0.0845545`；已保存 `TOP_epoch_00_score_0.2219.ckpt` 与新的 `last.ckpt`。Job `321743` 旧版 Find_0 到达 `global_step=11843`，最近一次验证与 TOP、last checkpoints 继续有效。此次检查期间两张 H200 的最高显存占用仍为 143.070 GiB，单卡总显存为 143.771 GiB，余量很小但没有显存不足。两个 Find 均尚未进入 CPC2。
- [x] (2026-07-26 10:07+08:00) 完成第八次三作业联合检查。三个作业均为 `RUNNING`，实际训练进程仍来自各自冻结的发布目录，命令行参数与最终配置保持既定学习率、`warmup_ratio=0.005`、全局批量 48 和对应损失契约；各自只存在 `after_lock`，当前应用日志错误扫描为空，W&B 摘要与 GPU 计算继续更新。Job `321540` 新版 Find_1 到达 `global_step=5864`，最新验证总损失降至 `0.334012`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.651325`、`0.599134`、`0.987488` 和 `0.975704`，并保存 `TOP_epoch_00_score_0.3340.ckpt` 与新的 `last.ckpt`。Job `321107` 新版 `unet_c1` 到达 `global_step=7889`，最新验证总损失降至 `0.219721`，对应四项 PR-AUC 分别为 `0.544096`、`0.507334`、`0.0870110` 和 `0.116536`，并保存 `TOP_epoch_00_score_0.2197.ckpt` 与新的 `last.ckpt`。Job `321743` 旧版 Find_0 到达 `global_step=14066`，最新验证总损失降至 `0.316924`，受体与配体区域 PR-AUC 分别为 `0.693115` 和 `0.623365`，并保存 `TOP_epoch_00_score_0.3169.ckpt` 与新的 `last.ckpt`。新版 Find_1 两张 H100 的最高采样显存占用为 80.844/81.559 GiB，旧版 Find_0 两张 H200 的最高采样占用为 143.054/143.771 GiB；均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-26 15:16+08:00) 完成第九次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前应用日志错误扫描为空，W&B 摘要与 GPU 计算继续更新。Job `321540` 新版 Find_1 到达 `global_step=6500`，五项训练损失保持有限，最近一次验证总损失 `0.334012` 及四项 PR-AUC 继续有效。Job `321107` 新版 `unet_c1` 到达 `global_step=8051`，最新验证总损失降至 `0.216680`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别升至 `0.552024`、`0.522347`、`0.0970976` 和 `0.143969`，并保存 `TOP_epoch_00_score_0.2167.ckpt` 与新的 `last.ckpt`。Job `321743` 旧版 Find_0 到达 `global_step=14678`，最新验证总损失降至 `0.310742`，受体与配体区域 PR-AUC 分别升至 `0.699974` 和 `0.644994`，并保存 `TOP_epoch_00_score_0.3107.ckpt` 与新的 `last.ckpt`。新版 Find_1 两张 H100 的最高采样显存占用为 80.734/81.559 GiB，旧版 Find_0 两张 H200 的最高采样占用为 143.064/143.771 GiB；均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-26 20:17+08:00) 完成第十次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前应用日志错误扫描为空，W&B 摘要与 GPU 计算继续更新。Job `321540` 新版 Find_1 到达 `global_step=6725`，最新验证总损失降至 `0.331998`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.654372`、`0.592297`、`0.991848` 和 `0.988387`，并保存 `TOP_epoch_00_score_0.3320.ckpt` 与新的 `last.ckpt`。Job `321107` 新版 `unet_c1` 到达 `global_step=8828`，最近一次验证总损失 `0.216680` 及四项 PR-AUC 继续有效。Job `321743` 旧版 Find_0 到达 `global_step=15389`，最近一次验证总损失 `0.310742` 及两项 PR-AUC 继续有效。新版 Find_1 两张 H100 的最高采样显存占用为 80.864/81.559 GiB，旧版 Find_0 两张 H200 的最高采样占用为 142.998/143.771 GiB；均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-27 01:17+08:00) 完成第十一次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数与 W&B 运行配置保持既定学习率、`warmup_ratio=0.005`、全局批量 48 和对应损失权重；各自只存在 `after_lock`，当前应用日志错误扫描为空，W&B 摘要与 GPU 计算继续更新。Job `321540` 新版 Find_1 到达 `global_step=7304`，五项训练损失有限，最近一次验证总损失 `0.331998`、四项 PR-AUC 以及 TOP、last checkpoints 继续有效。Job `321107` 新版 `unet_c1` 到达 `global_step=9206`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。Job `321743` 旧版 Find_0 到达 `global_step=15926`，最新验证总损失降至 `0.307528`，受体与配体区域 PR-AUC 分别升至 `0.703757` 和 `0.636299`，并保存 `TOP_epoch_00_score_0.3075.ckpt` 与新的 `last.ckpt`。本次采样中，新版 Find_1 两张 H100 最高占用 80.626/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.306/143.771 GiB；均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-27 06:16+08:00) 完成第十二次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前应用日志错误扫描为空，W&B 摘要与 GPU 计算继续更新。Job `321540` 新版 Find_1 到达 `global_step=7880`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。Job `321107` 新版 `unet_c1` 到达 `global_step=9662`，最新验证总损失降至 `0.215768`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.550378`、`0.516296`、`0.109687` 和 `0.165010`，并保存 `TOP_epoch_00_score_0.2158.ckpt` 与新的 `last.ckpt`。Job `321743` 旧版 Find_0 到达 `global_step=16733`，最近一次验证与 checkpoints 继续有效。本次采样中，新版 Find_1 两张 H100 最高占用 80.682/81.559 GiB，旧版 Find_0 两张 H200 最高占用 143.014/143.771 GiB；均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-27 11:16+08:00) 完成第十三次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前应用日志错误扫描为空，W&B 摘要与 GPU 计算继续更新。Job `321540` 新版 Find_1 到达 `global_step=8126`，最新验证总损失降至 `0.325117`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别升至 `0.682811`、`0.619510`、`0.993087` 和 `0.990706`，并保存 `TOP_epoch_00_score_0.3251.ckpt` 与新的 `last.ckpt`。Job `321107` 新版 `unet_c1` 到达 `global_step=10520`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。Job `321743` 旧版 Find_0 到达 `global_step=17354`，本次验证总损失为 `0.311083`，受体与配体区域 PR-AUC 为 `0.699321` 和 `0.643448`；结果有限但未优于此前 `0.307528`，新的 `TOP_epoch_00_score_0.3111.ckpt` 与 `last.ckpt` 均完整保存。本次采样中，新版 Find_1 两张 H100 最高占用 80.776/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.312/143.771 GiB；均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-27 16:17+08:00) 完成第十四次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前应用日志错误扫描为空，W&B 摘要与 GPU 计算继续更新。Job `321540` 新版 Find_1 到达 `global_step=8726`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。Job `321107` 新版 `unet_c1` 到达 `global_step=10604`，本次验证总损失为 `0.216707`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.559758`、`0.512020`、`0.100211` 和 `0.167001`；结果有限但未优于此前 `0.215768`，新的 `TOP_epoch_00_score_0.2167-v1.ckpt` 与 `last.ckpt` 均完整保存。Job `321743` 旧版 Find_0 到达 `global_step=18119`，最近一次验证与 checkpoints 继续有效。本次采样中，新版 Find_1 两张 H100 最高占用 80.800/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.708/143.771 GiB；均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-27 21:17+08:00) 完成第十五次三作业联合检查。三个作业均为 `RUNNING`，发布目录、最终配置、唯一运行目录与各自唯一的 `after_lock` 保持正确；当前正式运行的错误日志扫描为空，W&B 事件文件、摘要和 GPU 采样持续更新。Job `321540` 新版 Find_1 到达 `global_step=9206`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。Job `321107` 新版 `unet_c1` 到达 `global_step=11432`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。Job `321743` 旧版 Find_0 到达 `global_step=18728`，最新验证总损失降至 `0.302739`，受体区域与配体区域 PR-AUC 分别为 `0.712215` 和 `0.640414`，并保存新的 `TOP_epoch_00_score_0.3027.ckpt` 与 `last.ckpt`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.854/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.280/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-28 02:17+08:00) 完成第十六次三作业联合检查。三个作业均为 `RUNNING`，作业身份、发布目录、最终配置、唯一运行目录与各自唯一的 `after_lock` 保持正确；当前正式训练错误扫描为空，W&B 事件文件与 GPU 采样持续更新。Job `321540` 新版 Find_1 到达 `global_step=9575`，最新验证总损失降至 `0.317876`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.695172`、`0.638382`、`0.993398` 和 `0.993774`，并保存新的 `TOP_epoch_00_score_0.3179.ckpt` 与 `last.ckpt`。Job `321107` 新版 `unet_c1` 最近一次摘要仍为 `global_step=11837`，但 W&B 事件文件、内部日志和 GPU 计算持续推进到本次检查时刻，五项训练损失有限，没有进程停滞证据。Job `321743` 旧版 Find_0 到达 `global_step=19664`，最近一次验证与 checkpoints 继续有效。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.436/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.750/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-28 07:20+08:00) 完成第十七次三作业联合检查。三个作业均为 `RUNNING`，各自只存在 `after_lock`，当前正式训练错误扫描为空，W&B 事件文件、摘要和 GPU 计算持续更新。Job `321540` 新版 Find_1 到达 `global_step=10178`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。Job `321107` 新版 `unet_c1` 到达 `global_step=12350`，最新验证总损失降至 `0.212835`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.579582`、`0.532469`、`0.119576` 和 `0.199941`，并保存新的 `TOP_epoch_00_score_0.2128.ckpt` 与 `last.ckpt`。Job `321743` 旧版 Find_0 到达 `global_step=20270`，本次验证总损失为 `0.305076`，未优于此前 `0.302739`；受体区域与配体区域 PR-AUC 为 `0.715987` 和 `0.658177`，新的 TOP 与 last checkpoints 完整保存。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.752/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.336/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-28 13:10+08:00) 完成第十八次三作业联合检查。三个作业均为 `RUNNING`，各自只存在 `after_lock`，当前正式训练错误扫描为空，W&B 事件文件、内部日志与 GPU 计算持续更新。Job `321540` 新版 Find_1 最近一次摘要为 `global_step=10520`，五项训练损失有限，最近一次验证与 checkpoints 继续有效；摘要自 10:29 后暂未刷新，但 W&B 事件文件和内部日志更新到 13:10，两张 H100 保持计算，没有进程停滞证据。Job `321107` 新版 `unet_c1` 到达 `global_step=13151`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。Job `321743` 旧版 Find_0 到达 `global_step=21053`，最近一次验证与 checkpoints 继续有效。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.964/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.598/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1，三项 `BEST.ckpt` 状态没有变化。
- [x] (2026-07-28 18:11+08:00) 完成第十九次三作业联合检查。三个作业均为 `RUNNING`，各自只存在 `after_lock`，当前正式训练错误扫描为空，W&B 事件文件、摘要和 GPU 计算持续更新。Job `321540` 新版 Find_1 到达 `global_step=11048`，最新验证总损失降至 `0.311816`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.699857`、`0.623672`、`0.995299` 和 `0.993932`，并保存新的 `TOP_epoch_00_score_0.3118.ckpt` 与 `last.ckpt`。Job `321107` 新版 `unet_c1` 到达 `global_step=13574`，最新验证总损失降至 `0.205013`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.597136`、`0.552964`、`0.122539` 和 `0.198476`，并保存新的 `TOP_epoch_00_score_0.2050.ckpt` 与 `last.ckpt`。Job `321743` 旧版 Find_0 到达 `global_step=21749`，最新验证总损失降至 `0.301744`，受体区域与配体区域 PR-AUC 分别为 `0.727060` 和 `0.644007`，并保存新的 `TOP_epoch_00_score_0.3017.ckpt` 与 `last.ckpt`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.214/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.866/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1。
- [x] (2026-07-28 23:13+08:00) 完成第二十次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前正式训练错误扫描为空，W&B 事件文件、摘要和 GPU 计算持续更新。Job `321540` 新版 Find_1 到达 `global_step=11615`，五项训练损失有限，最近一次验证与 `TOP_epoch_00_score_0.3118.ckpt`、`last.ckpt` 继续有效。Job `321107` 新版 `unet_c1` 到达 `global_step=14465`，五项训练损失有限，最近一次验证与 `TOP_epoch_00_score_0.2050.ckpt`、`last.ckpt` 继续有效。Job `321743` 旧版 Find_0 到达 `global_step=22370`，训练损失有限，最近一次验证与 `TOP_epoch_00_score_0.3017.ckpt`、`last.ckpt` 继续有效。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.912/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.966/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1；三项 `BEST.ckpt` 仍分别对应 `0.3179`、`0.2128` 和 `0.3027`，暂时落后当前最佳 TOP 一次验证。
- [x] (2026-07-29 04:12+08:00) 完成第二十一次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前正式训练错误扫描为空，W&B 事件文件、摘要和 GPU 计算持续更新。Job `321540` 新版 Find_1 到达 `global_step=11852`，本次验证总损失为 `0.316934`，未优于当前最佳 `0.311816`；受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.682324`、`0.614820`、`0.996150` 和 `0.995899`，新的 `TOP_epoch_00_score_0.3169.ckpt` 与 `last.ckpt` 完整保存。Job `321107` 新版 `unet_c1` 到达 `global_step=14735`，本次验证总损失为 `0.209346`，未优于当前最佳 `0.205013`；对应四项 PR-AUC 分别为 `0.588629`、`0.537688`、`0.119717` 和 `0.190546`，新的 `TOP_epoch_00_score_0.2093.ckpt` 与 `last.ckpt` 完整保存。Job `321743` 旧版 Find_0 到达 `global_step=23249`，最新验证总损失降至 `0.301204`，受体区域与配体区域 PR-AUC 分别为 `0.724694` 和 `0.671256`，并保存新的 `TOP_epoch_00_score_0.3012.ckpt` 与 `last.ckpt`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.666/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.910/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1。新版 Find_1 与 `unet_c1` 的 `BEST.ckpt` 已分别刷新为当前最佳 `0.3118` 和 `0.2050`；旧版 Find_0 的 `BEST.ckpt` 暂时对应上一轮最佳 `0.3017`。
- [x] (2026-07-29 11:34+08:00) 完成第二十二次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前正式训练错误扫描为空，W&B 事件文件和 GPU 计算持续更新。Job `321540` 新版 Find_1 到达 `global_step=12752`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。Job `321107` 新版 `unet_c1` 最近一次摘要为 `global_step=15782`，五项训练损失有限；摘要自 09:43 后暂未刷新，但 W&B 事件文件与内部日志持续更新到 11:34，H100 保持计算，没有进程停滞证据。Job `321743` 旧版 Find_0 到达 `global_step=24242`，最新验证总损失降至 `0.299347`，受体区域与配体区域 PR-AUC 分别为 `0.722775` 和 `0.660897`，并保存新的 `TOP_epoch_00_score_0.2993.ckpt` 与 `last.ckpt`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.594/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.830/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1。新版 Find_1 与 `unet_c1` 的 `BEST.ckpt` 继续指向当前最佳 `0.3118` 和 `0.2050`；旧版 Find_0 的 `BEST.ckpt` 已刷新为上一轮最佳 `0.3012`，暂时落后新最佳一次验证。
- [x] (2026-07-29 16:35+08:00) 完成第二十三次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前正式训练错误扫描为空，W&B 事件文件和 GPU 计算持续更新。Job `321540` 新版 Find_1 最近一次摘要为 `global_step=13151`，五项训练损失有限；摘要自 14:55 后暂未刷新，但 W&B 事件文件、内部日志与两张 H100 持续推进到 16:35，没有进程停滞证据。Job `321107` 新版 `unet_c1` 到达 `global_step=16400`，本次验证总损失为 `0.205393`，未优于当前最佳 `0.205013`；受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.593386`、`0.556990`、`0.134876` 和 `0.211881`，新的 `TOP_epoch_00_score_0.2054.ckpt` 与 `last.ckpt` 完整保存。Job `321743` 旧版 Find_0 到达 `global_step=25001`，训练损失有限，最近一次验证与 checkpoints 继续有效。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.968/81.559 GiB，旧版 Find_0 两张 H200 最高占用 141.824/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1。新版 Find_1 与 `unet_c1` 的 `BEST.ckpt` 继续指向当前最佳 `0.3118` 和 `0.2050`；旧版 Find_0 的 `BEST.ckpt` 仍对应上一轮最佳 `0.3012`，暂时落后当前最佳 `0.2993` 一次验证。
- [x] (2026-07-29 21:36+08:00) 完成第二十四次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前正式训练错误扫描为空，W&B 事件文件、摘要和 GPU 计算持续更新。Job `321540` 新版 Find_1 到达 `global_step=13493`，最新验证总损失降至 `0.311039`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.705271`、`0.633604`、`0.995829` 和 `0.996088`，并保存新的 `TOP_epoch_00_score_0.3110.ckpt` 与 `last.ckpt`。Job `321107` 新版 `unet_c1` 最近一次摘要为 `global_step=17096`，五项训练损失有限，最近一次验证与 checkpoints 继续有效；W&B 事件文件、内部日志与 H100 持续推进到 21:36。Job `321743` 旧版 Find_0 到达 `global_step=25724`，最新验证总损失降至 `0.297060`，受体区域与配体区域 PR-AUC 分别为 `0.726640` 和 `0.653690`，并保存新的 `TOP_epoch_00_score_0.2971.ckpt` 与 `last.ckpt`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.832/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.572/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1。新版 Find_1 的 `BEST.ckpt` 暂时对应上一轮最佳 `0.3118`，新版 `unet_c1` 的 `BEST.ckpt` 继续正确，旧版 Find_0 的 `BEST.ckpt` 已刷新为上一轮最佳 `0.2993`，暂时落后当前最佳 `0.2971` 一次验证。
- [x] (2026-07-30 09:04+08:00) 完成第二十五次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前正式训练错误扫描为空，W&B 事件文件和 GPU 计算持续更新。Job `321540` 新版 Find_1 最近一次摘要为 `global_step=14465`，五项训练损失有限；摘要自 06:47 后暂未刷新，但 W&B 事件文件、内部日志与两张 H100 持续推进到 09:04，没有进程停滞证据。Job `321107` 新版 `unet_c1` 到达 `global_step=18413`，最新验证总损失降至 `0.202949`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.602145`、`0.551623`、`0.129276` 和 `0.216974`，并保存新的 `TOP_epoch_00_score_0.2029.ckpt` 与 `last.ckpt`。Job `321743` 旧版 Find_0 到达 `global_step=27569`，最新验证总损失降至 `0.294410`，受体区域与配体区域 PR-AUC 分别为 `0.726935` 和 `0.655561`，并保存新的 `TOP_epoch_00_score_0.2944.ckpt` 与 `last.ckpt`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.924/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.910/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1。三项 `BEST.ckpt` 分别对应 `0.3118`、`0.2050` 和 `0.2971`，均暂时落后当前最佳 TOP 一次验证。
- [x] (2026-07-30 14:04+08:00) 完成第二十六次三作业联合检查。三个作业均为 `RUNNING`，训练进程仍来自各自冻结的发布目录，命令行参数保持既定学习率、`warmup_ratio=0.005` 和全局批量 48；各自只存在 `after_lock`，当前正式训练错误扫描为空，W&B 事件文件、摘要和 GPU 计算持续更新。Job `321540` 新版 Find_1 到达 `global_step=14894`，最新验证总损失降至 `0.308903`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.713553`、`0.642585`、`0.996422` 和 `0.995750`，并保存新的 `TOP_epoch_00_score_0.3089.ckpt` 与 `last.ckpt`。Job `321107` 新版 `unet_c1` 到达 `global_step=19010`，本次验证总损失为 `0.203519`，未优于当前最佳 `0.202949`；对应四项 PR-AUC 分别为 `0.603420`、`0.552125`、`0.144904` 和 `0.223982`，新的 `TOP_epoch_00_score_0.2035.ckpt` 与 `last.ckpt` 完整保存。Job `321743` 旧版 Find_0 到达 `global_step=28193`，最新验证总损失降至 `0.291351`，受体区域与配体区域 PR-AUC 分别为 `0.734282` 和 `0.661356`，并保存新的 `TOP_epoch_00_score_0.2914.ckpt` 与 `last.ckpt`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.912/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.812/143.771 GiB，均未发生显存不足。两个 Find 仍在 CPC1。新版 Find_1 与旧版 Find_0 的 `BEST.ckpt` 分别暂时对应上一轮最佳 `0.3110` 和 `0.2944`；新版 `unet_c1` 的 `BEST.ckpt` 已刷新为当前最佳 `0.2029`。
- [x] (2026-07-30 19:11+08:00) 完成第二十七次三作业联合检查。Job `321540`、`321107` 和 `321743` 均为 `RUNNING`；冻结发布提交、唯一运行标记、最终配置和各自唯一的 `after_lock` 保持不变，当前正式训练错误扫描为空，W&B 事件文件、内部日志和 GPU 计算持续更新，两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=15479`，新版 `unet_c1` 到达 `global_step=19727`，旧版 Find_0 到达 `global_step=28949`；三项当前训练损失均为有限值，最近验证结果和 TOP、last checkpoints 继续有效，本次没有产生新的正式验证结果。新版 Find_1 的 `BEST.ckpt` 仍对应 `0.3110`，暂时落后当前最佳 `0.3089`；新版 `unet_c1` 的 `BEST.ckpt` 正确对应当前最佳 `0.2029`；旧版 Find_0 的 `BEST.ckpt` 仍对应 `0.2944`，暂时落后当前最佳 `0.2914`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.858/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 143.064/143.771 GiB，均未发生显存不足。
- [x] (2026-07-31 01:40+08:00) 完成第二十八次三作业联合检查。00:06 的原定检查、00:39 和 01:09 的两次重试均因 `10.102.33.220:10022` 连接超时而未取得服务器状态；01:39 恢复连接后完成全量只读核验。Job `321540`、`321107` 和 `321743` 均为 `RUNNING`，冻结发布提交、唯一运行标记、最终配置、各自唯一的 `after_lock`、W&B 活动和当前正式训练错误扫描正常，两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=15977`，最新验证总损失降至 `0.303946`，受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.715185`、`0.646304`、`0.996413` 和 `0.996518`，并保存新的 `TOP_epoch_00_score_0.3039.ckpt` 与 `last.ckpt`。新版 `unet_c1` 到达 `global_step=20525`，最新验证总损失降至 `0.201407`，对应四项 PR-AUC 分别为 `0.598554`、`0.551136`、`0.144395` 和 `0.230864`，并保存新的 `TOP_epoch_00_score_0.2014.ckpt` 与 `last.ckpt`。旧版 Find_0 到达 `global_step=30218`，本次验证总损失为 `0.294136`，未优于当前最佳 `0.291351`；受体区域与配体区域 PR-AUC 分别为 `0.734265` 和 `0.653966`，新的 `TOP_epoch_00_score_0.2941.ckpt` 与 `last.ckpt` 完整保存。新版 Find_1 与 `unet_c1` 的 `BEST.ckpt` 分别暂时对应上一轮最佳 `0.3089` 和 `0.2029`；旧版 Find_0 的 `BEST.ckpt` 已刷新为当前最佳 `0.2914`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.788/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.946/143.771 GiB，均未发生显存不足。heartbeat 恢复为每 5 小时。
- [x] (2026-07-31 11:25+08:00) 完成第二十九次三作业联合检查。10:51 的原定检查因 `10.102.33.220:10022` 两次 SSH 连接超时且 TCP 端口不可达而未取得服务器状态；11:24 的第一次定时重试恢复连接并完成全量只读核验。Job `321540`、`321107` 和 `321743` 均为 `RUNNING`，冻结发布提交、唯一运行标记、最终配置、各自唯一的 `after_lock`、W&B 活动和当前正式训练错误扫描正常，两个 Find 仍在 CPC1。新版 Find_1 最近摘要为 `global_step=17096`，五项训练损失有限，最近验证与 checkpoints 继续有效；摘要自 09:26 暂未刷新，但 W&B 事件文件、内部日志和两张 H100 持续推进到 11:25，没有停滞证据。新版 `unet_c1` 到达 `global_step=21869`，本次验证总损失为 `0.201678`，未优于当前最佳 `0.201407`；受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.598142`、`0.565161`、`0.154531` 和 `0.255415`，新的 `TOP_epoch_00_score_0.2017.ckpt` 与 `last.ckpt` 完整保存。旧版 Find_0 到达 `global_step=31877`，最新验证总损失降至 `0.284309`，受体区域与配体区域 PR-AUC 分别为 `0.740554` 和 `0.675477`，并保存新的 `TOP_epoch_00_score_0.2843.ckpt` 与 `last.ckpt`。新版 Find_1 的 `BEST.ckpt` 仍对应上一轮最佳 `0.3089`，暂时落后当前最佳 `0.3039`；新版 `unet_c1` 的 `BEST.ckpt` 已刷新为当前最佳 `0.2014`；旧版 Find_0 的 `BEST.ckpt` 已刷新为中间一次验证产生的 `0.2899` 最佳 TOP，暂时落后当前 `0.2843` 新最佳一次验证。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.926/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.670/143.771 GiB，均未发生显存不足。heartbeat 恢复为每 5 小时。
- [x] (2026-07-31 16:35+08:00) 完成第三十次三作业联合检查。Job `321540`、`321107` 和 `321743` 均为 `RUNNING`，精确发布目录或固定运行时代码、最终配置、各自唯一的 `after_lock`、W&B 活动和当前正式训练错误扫描正常，两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=17681`，最新验证总损失降至 `0.299771`；受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.734120`、`0.656438`、`0.996792` 和 `0.996809`，五项训练损失均为有限值，并保存新的 `TOP_epoch_00_score_0.2998.ckpt` 与 `last.ckpt`。新版 `unet_c1` 最近摘要仍为 `global_step=22358`，最近验证和 checkpoints 继续有效；摘要自 13:42 暂未刷新，但 W&B 内部日志和 H100 计算持续推进到 16:35，没有停滞证据。旧版 Find_0 到达 `global_step=32897`，最近验证总损失 `0.284309` 与 checkpoints 继续有效。新版 Find_1 的 `BEST.ckpt` 仍对应 `0.3039`，旧版 Find_0 的 `BEST.ckpt` 仍对应 `0.2899`，两者均暂时落后各自最新最佳一次验证；新版 `unet_c1` 的 `BEST.ckpt` 正确对应当前最佳 `0.2014`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.966/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.974/143.771 GiB，均未发生显存不足。
- [x] (2026-07-31 21:35+08:00) 完成第三十一次三作业联合检查。Job `321540`、`321107` 和 `321743` 均为 `RUNNING`；精确发布目录或固定运行代码、训练命令、最终配置、各自唯一的 `after_lock`、W&B 活动和当前正式训练错误扫描正常，两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=18338`，五项训练损失均为有限值，最近验证总损失 `0.299771` 与 checkpoints 继续有效。新版 `unet_c1` 到达 `global_step=23174`，最新验证总损失降至 `0.198961`；受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.600148`、`0.563620`、`0.156990` 和 `0.241991`，并保存新的 `TOP_epoch_00_score_0.1990.ckpt` 与 `last.ckpt`。旧版 Find_0 到达 `global_step=33779`，本次验证总损失为 `0.287442`，未优于当前最佳 `0.284309`；受体区域与配体区域 PR-AUC 分别为 `0.733609` 和 `0.662756`，新的 TOP 与 last checkpoints 完整保存。新版 Find_1 与 `unet_c1` 的 `BEST.ckpt` 分别暂时落后当前最佳 `0.2998` 和 `0.1990` 一次验证；旧版 Find_0 的 `BEST.ckpt` 已正确刷新为当前最佳 `0.2843`。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.750/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 143.052/143.771 GiB，均未发生显存不足。
- [x] (2026-08-01 02:35+08:00) 完成第三十二次三作业联合检查。Job `321540`、`321107` 和 `321743` 均为 `RUNNING`；精确发布目录或固定运行代码、训练命令、最终配置、各自唯一的 `after_lock`、W&B 活动和当前正式训练错误扫描正常，两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=18641`，本次验证总损失为 `0.305648`，未优于当前最佳 `0.299771`；受体区域、配体区域、蛋白主链与核酸主链 PR-AUC 分别为 `0.720618`、`0.634045`、`0.996900` 和 `0.997245`，新的 TOP 与 last checkpoints 完整保存。新版 `unet_c1` 最近摘要为 `global_step=23672`，五项训练损失均为有限值，最近验证总损失 `0.198961` 与 checkpoints 继续有效；摘要自 23:45 暂未刷新，但 W&B 内部日志和 H100 计算持续推进到 02:35，没有停滞证据。旧版 Find_0 到达 `global_step=34724`，本次验证总损失为 `0.290416`，未优于当前最佳 `0.284309`；受体区域与配体区域 PR-AUC 分别为 `0.734715` 和 `0.671311`，新的 TOP 与 last checkpoints 完整保存。新版 Find_1 与旧版 Find_0 的 `BEST.ckpt` 已正确对应当前最佳 `0.2998` 和 `0.2843`；新版 `unet_c1` 的 `BEST.ckpt` 仍对应上一轮最佳 `0.2014`，暂时落后当前最佳 `0.1990` 一次验证。本次 GPU 采样中，新版 Find_1 两张 H100 最高占用 80.928/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.884/143.771 GiB，均未发生显存不足。
- [x] (2026-08-01 11:20+08:00) 第三十三次联合检查确认旧版 Find_0 因 `LearningRateReductionStopper` 在 `on_validation_end` 调用 `LightningModule.log()` 而退出；Job `321743`、两张 H200、旧运行目录、错误日志、`try_lock_321743` 和 `after_lock_321743` 均保留。新版 Find_1 与 `unet_c1` 继续运行；三个冻结运行副本均不修改。
- [x] (2026-08-01 13:50+08:00) 按用户新决定，不再把完整训练状态续跑作为阶段切换前提。对旧版 Find_0 已在实际落盘的 TOP checkpoints 中按 `val_score/global/voxel_ligand_PRAUC` 最大值选择 `TOP_epoch_00_score_0.2843.ckpt`；该次验证的配体区域 PR-AUC 为 `0.6754766702651978`，文件 SHA-256 为 `87ec6080809a14e2558e10c0740c16371b363fed0f672081b389f46c64928b44`。同一 Job `321743` 已从该文件只加载模型参数并启动 CPC2；新运行目录、W&B、两张 H200 和训练进程均已核实。
- [x] (2026-08-01 13:50+08:00) 完成只供后续运行使用的 Pocket_Plus 修正：Find_0、Find_1 和 `unet_c1` 的 checkpoint 监控指标改为配体区域 PR-AUC，方向为最大；学习率下降停止回调不再从 `on_validation_end` 调用 `LightningModule.log()`。实现端点 `4ae9f5f` 与学习端点 `66469e2` 的树对象均为 `5f96ab7e146bdc9a19c23817548ea137fbcb5e69`，两端专项测试均为 `31 passed`；`Learn/CUMULATIVE` 已快进到 `66469e2`。这些修改没有部署到三个当前冻结运行副本。
- [x] (2026-08-01 23:04+08:00) 用户根据当前 W&B 曲线判断 CPC2 收益较小，已主动停止 Find_0 CPC2，并取消本轮 Find_1 CPC2。只读检查确认 Job `321743` 已离开 Slurm；Job `321540` 与 `321107` 仍分别在冻结发布目录内健康运行到 `global_step=21041` 与 `26399`。本次只收窄运行与监控范围，没有修改代码、配置、checkpoint 或既有运行目录，CPC2 能力继续保留。
- [x] (2026-08-02 02:35+08:00) 完成监控范围收窄后的第一次两作业检查。Job `321540` 与 `321107` 均为 `RUNNING`，实际进程来自 `Pocket_Plus_1909267` 与 `Pocket_Plus_9417bcf`，唯一运行标记、学习率、`warmup_ratio=0.005`、全局批量 48、五项损失权重和各自唯一的 `after_lock` 均正确。新版 Find_1 到达 `global_step=21278`，训练总损失 `0.182188`、验证总损失 `0.300379`、配体区域 PR-AUC `0.646503`；新版 `unet_c1` 到达 `global_step=27251`，对应数值为 `0.226900`、`0.198103` 和 `0.575755`。两项启用损失与指标均为有限值，W&B、GPU 和 checkpoint 持续更新，当前没有训练错误或显存不足；heartbeat 保持每 5 小时检查。
- [x] (2026-08-02 07:37+08:00) 完成第二次两作业五小时检查。Job `321540` 与 `321107` 仍为 `RUNNING`，冻结发布目录、唯一运行标记、学习率、`warmup_ratio=0.005`、全局批量 48、五项损失权重和各自唯一的 `after_lock` 没有漂移。Find_1 到达 `global_step=22088`，训练总损失 `0.245146`，最近验证总损失与配体区域 PR-AUC 仍为 `0.300379` 和 `0.646503`；`unet_c1` 到达 `global_step=27620`，训练总损失 `0.217885`，最近验证总损失与配体区域 PR-AUC 仍为 `0.198103` 和 `0.575755`。两项启用损失和指标均为有限值；Find_1 的 W&B 摘要持续刷新，`unet_c1` 的摘要自 04:17 暂未刷新，但主训练进程、W&B 二进制事件文件、内部日志和 H100 计算持续活动，没有停滞证据。两项没有新验证或 checkpoint，当前没有训练错误或显存不足；heartbeat 保持每 5 小时检查。
- [x] (2026-08-02 17:34+08:00) 完成第三次两作业五小时检查。Job `321540` 与 `321107` 仍为 `RUNNING`，冻结发布目录、唯一运行标记、配置文件哈希、学习率、`warmup_ratio=0.005`、全局批量 48、五项损失权重和各自唯一的 `after_lock` 没有漂移。Find_1 到达 `global_step=23171`，训练总损失 `0.292095`；最新验证总损失降至 `0.292820`，配体区域 PR-AUC 为 `0.660276`，并保存新的 `TOP_epoch_00_score_0.2928.ckpt` 与 `last.ckpt`。`unet_c1` 到达 `global_step=29060`，训练总损失 `0.175191`；最新验证总损失为 `0.195387`，配体区域 PR-AUC 为 `0.568288`，并保存新的 `TOP_epoch_00_score_0.1954.ckpt` 与 `last.ckpt`。两项启用损失和指标均为有限值，W&B、训练进程和 GPU 持续活动，当前没有训练错误或显存不足；heartbeat 保持每 5 小时检查。
- [x] (2026-08-20 23:36+08:00) 用户从共享任务根提交 Job `350185`：`Find_1.sh`、单张 H100、24 CPU、固定 `hnode01`、启用 `after_hold` 且不设置 `pre_lock`。该 Job 在优先级队列中等待，不影响节点上既有任务。
- [x] (2026-08-21 09:21--09:58+08:00) Job `350185` 获得 `hnode01` 的单张 H100 并进入稳定训练。冻结命令确认 `train.devices=1`、batch 6、全局 batch 48、梯度累积 8、24 workers、每 epoch 40 次 validation、学习率 `5e-5`；W&B `mpvawjsb` 从 `global_step=2` 连续推进到至少 38，训练总损失保持有限，H100 计算与显存占用稳定，当前运行错误扫描没有 Traceback、显存不足、NCCL 或 worker 崩溃。此时 `hnode01` 的三张 H100 分别由 Job `346003`、`348312_4` 和 `350185` 占用，H100 待调度队列为空，已经到达“双卡转换授权门”。
- [x] (2026-08-22 09:17--09:49+08:00) H100 分区既有待调度任务清空后，从服务器共享 Pocket_Plus 根目录提交 Job `351295`：`Find_1.sh`、双张 H100、64 CPU、固定 `hnode01`、启用 `after_hold` 且不设置 `pre_lock`。二次检查确认待调度队列中只有新 Job 后，按用户授权取消 Job `346003` 与 `350185`；他人的 Job `350573` 未被操作。旧任务回收一度使 `hnode01` 因 `Kill task failed` 进入 `MIXED+DRAIN`，节点随后由管理员恢复为可调度状态，Job `351295` 于 09:30:26 在原定节点启动。
- [x] (2026-08-22 09:49+08:00) Job `351295` 启动验收通过。发布副本为 `Pocket_Plus_fdb8a30fa196`，launch 为 `Find_1_job351295_20260822T093108_a1`，运行目录为 `Find_1-CPC1____Find_1_job351295_20260822T093108_a1_CPC1`；最终配置确认单节点、`devices=2`、64 CPU、每 rank 30 workers、batch 6、梯度累积 4、全局 batch 48、`gradient_clip_val=1000`、`val_per_epoch=40`、学习率 `1.0e-4` 和调度器绝对改善阈值 `0.003`。两个 NCCL rank 均已建立，W&B `yyz7c55e` 到达 `trainer/global_step=5`，训练总损失及各分项均为有限值，两张 H100 持续计算，未见显存不足、NCCL、DataLoader 或非有限值错误。后续按多段 300 秒命令进行低耗监视，只在验证、checkpoint、故障恢复或退出等明确事件时合并记录。
- [x] (2026-08-22 15:18+08:00) Job `351295` 完成第一次正式 validation，并在验证后继续训练。W&B `yyz7c55e` 到达 `global_step=1128`；验证总损失为 `0.403396`，配体体素、受体、蛋白主链和核酸主链 PR-AUC 分别为 `0.531660`、`0.576420`、`0.249253` 和 `0.012209`。`TOP_epoch_00_score_0.5317.ckpt` 与 `last.ckpt` 已完整落盘，大小分别为 1,466,222,905 和 1,466,223,097 字节；两张 H100 继续计算，启用损失和正式选择指标均为有限值，错误扫描没有显存不足、NCCL、DataLoader 或训练异常。`refined/unrefined` 面板的 `NaN` 继续属于未启用稀疏精修路径的既知语义，不参与损失、调度器或 checkpoint 选择。
- [x] (2026-08-22 21:05--23:54+08:00) Job `351295` 完成第二次正式 validation 并继续训练。W&B `yyz7c55e` 到达 `global_step=2864`；验证总损失降至 `0.372919`，配体体素、受体、蛋白主链和核酸主链 PR-AUC 分别为 `0.556626`、`0.610118`、`0.957787` 和 `0.000127`。`TOP_epoch_00_score_0.5566.ckpt` 与新的 `last.ckpt` 已落盘；`BEST.ckpt` 已刷新为上一最高分 `0.5317`，符合冻结回调的一轮延迟语义。Job 仍为 `RUNNING`，当前错误扫描为空。
- [x] (2026-08-23 03:00--04:00+08:00) Job `351295` 完成第三次正式 validation 并继续训练。W&B `yyz7c55e` 到达 `global_step=3590`；验证总损失降至 `0.327019`，配体体素、受体、蛋白主链和核酸主链 PR-AUC 分别为 `0.565112`、`0.647742`、`0.980424` 和 `0.838008`。`TOP_epoch_00_score_0.5651.ckpt` 与新的 `last.ckpt` 已落盘；`BEST.ckpt` 已刷新为上一最高分 `0.5566`。Job 仍为 `RUNNING`，当前错误扫描为空。
- [x] (2026-08-23 08:57--10:09+08:00) Job `351295` 完成第四次正式 validation。W&B `yyz7c55e` 到达 `global_step=4781`；验证总损失为 `0.347850`，配体体素、受体、蛋白主链和核酸主链 PR-AUC 分别为 `0.526410`、`0.617275`、`0.990466` 和 `0.980735`。本次未超过当前最高 `0.565112`，但新的 TOP 与 `last.ckpt` 已落盘，`BEST.ckpt` 已刷新为当前最高的 `TOP_epoch_00_score_0.5651.ckpt`。Job 继续运行，当前错误扫描为空。
- [x] (2026-08-23 15:18--15:49+08:00) 用户判断 `1.0e-4` 学习率偏大并同步新版 `Find_1.sh` 后，授权通过 `kill_lock` 在 Job `351295` 的既有双卡 allocation 内从头重启。第 1 次执行以预期退出码 137 停止，`after_lock_351295` 保持不变；删除自动生成的 `try_lock_351295` 后，第 2 次执行从发布 `Pocket_Plus_9a4533e3bf4c` 启动。新 launch 为 `Find_1_job351295_20260823T152130_a2`，运行目录为 `Find_1-CPC1____Find_1_job351295_20260823T152130_a2_CPC1`，W&B 为 `a87fe1ef`。最终配置确认单节点、`devices=2`、64 CPU、每 rank 30 workers、batch 6、梯度累积 4、全局 batch 48、学习率 `5.0e-5`、`gradient_clip_val=0.5`、`val_per_epoch=40`、`warmup_ratio=0.005`、调度器绝对改善阈值 `0.003` 和 `init_from=null`。两个 NCCL rank 正常建立；15:49 时 W&B 到达 `trainer/global_step=44`，训练总损失 `0.676343`，启用分项均为有限值，两张 H100 持续计算，未见显存不足、NCCL、DataLoader 或非有限值错误。第 1 次执行的四轮 validation 与 checkpoint 完整保留；这次停止是用户主动调整超参数，不是训练故障。
- [x] (2026-08-23 21:12--21:16+08:00) Job `351295` 的第 2 次执行完成首次正式 validation，并在验证后继续训练。W&B `a87fe1ef` 到达 `global_step=1142`；验证总损失为 `0.424685`，配体体素、受体、蛋白主链和核酸主链 PR-AUC 分别为 `0.512118`、`0.554270`、`0.046906` 和 `0.000668`，原子与伪原子 PR-AUC 分别为 `0.573507` 和 `0.532515`。`TOP_epoch_00_score_0.5121.ckpt` 与 `last.ckpt` 已完整落盘，大小分别为 1,466,222,905 和 1,466,223,097 字节；首次 validation 后尚未生成 `BEST.ckpt`，符合冻结回调在后续 validation 才刷新别名的语义。两张 H100 继续计算，启用损失与正式选择指标均为有限值，错误扫描没有显存不足、NCCL、DataLoader 或训练异常。`refined/unrefined` 面板的 `NaN` 仍属于未启用稀疏精修路径的既知语义，不参与损失、学习率调度或 checkpoint 选择。
- [x] 形成两个仓库的实现端点与学习端点，验证允许差异后推进各自 `Learn/CUMULATIVE`。
- [ ] 完成新版 Find_1 CPC1 与新版 `unet_c1` 正式训练的后续 heartbeat 监控；两者结束后只记录最终可用产物，不启动 CPC2。
- [ ] 收口映射索引、README、ExecPlan、`CLAUDE/memory/`、运行证据和 heartbeat。

## Surprises & Discoveries

- Observation: 新版 `unet_c1` 保存验证总损失为 `0.221887` 的 TOP checkpoint 后，`BEST.ckpt` 仍与上一轮 `TOP_epoch_00_score_0.2318.ckpt` 内容相同。
  Evidence: 2026-07-25 21:31+08:00 的服务器只读校验显示两个文件 SHA-256 均为 `90fb5d33…210`，而 `TOP_epoch_00_score_0.2219.ckpt` 为 `467535c4…4d7`。精确发布 `Pocket_Plus_9417bcf/src/train.py` 中 `BestCheckpointAlias` 在 `on_validation_end` 和 `on_train_end` 刷新别名；服务器当前 Lightning 的 `trainer/connectors/callback_connector.py::_reorder_callbacks` 明确把 `ModelCheckpoint` 移到其他 callback 之后执行，因此训练过程中别名会落后一轮验证，训练正常结束时会再次刷新。当前 TOP checkpoint 已完整保存，训练继续运行，这一行为不阻断当前训练；CPC1 结束后必须核对 `BEST.ckpt` 与最终 `best_model_path` 的内容一致性。
  Update: 2026-07-26 10:07+08:00 的只读校验显示该行为继续保持，并且另外两个训练也表现出同样的一轮延迟：新版 Find_1 的 `BEST.ckpt` 对应 `0.3433`，最新 TOP 为 `0.3340`；新版 `unet_c1` 的 `BEST.ckpt` 对应 `0.2219`，最新 TOP 为 `0.2197`；旧版 Find_0 的 `BEST.ckpt` 对应 `0.3171`，最新 TOP 为 `0.3169`。三份最新 TOP 均完整存在，当前训练不受阻；两个 Find 从 CPC1 切换到 CPC2 前，以及 `unet_c1` 正常结束后，都必须核对最终 `BEST.ckpt`。
  Update: 2026-07-26 15:16+08:00 的只读校验显示，新版 `unet_c1` 与旧版 Find_0 又分别保存了 `0.2167` 和 `0.3107` 的新 TOP，`BEST.ckpt` 仍各自对应上一轮 `0.2197` 和 `0.3169`；新版 Find_1 尚未产生新验证。该现象继续符合已查明的回调执行顺序，最新 TOP 文件均完整存在。
  Update: 2026-07-26 20:17+08:00 的只读校验显示，新版 Find_1 保存了 `0.3320` 的新 TOP，`BEST.ckpt` 对应上一轮 `0.3340`；新版 `unet_c1` 与旧版 Find_0 尚未产生下一次验证。三项最新 TOP 文件继续完整存在。
  Update: 2026-07-27 01:17+08:00 的只读校验显示，旧版 Find_0 保存了 `0.3075` 的新 TOP，`BEST.ckpt` 仍对应上一轮 `0.3107`；新版 Find_1 与 `unet_c1` 尚未产生下一次验证。三项最新 TOP 文件继续完整存在。
  Update: 2026-07-27 06:16+08:00 的只读校验显示，新版 `unet_c1` 保存了 `0.2158` 的新 TOP，`BEST.ckpt` 仍对应上一轮 `0.2167`；两个 Find 尚未产生下一次验证。三项最新 TOP 文件继续完整存在。
  Update: 2026-07-27 11:16+08:00 的只读校验显示，新版 Find_1 保存了 `0.3251` 的新最佳 TOP，`BEST.ckpt` 仍对应上一轮 `0.3320`；新版 `unet_c1` 的 `BEST.ckpt` 仍对应 `0.2167`，尚未指向 `0.2158`。旧版 Find_0 本次验证总损失 `0.311083` 未优于此前的 `0.307528`，`BEST.ckpt` 已在本次验证开始时刷新并与 `TOP_epoch_00_score_0.3075.ckpt` 内容相同，因此其最佳别名当前正确。三项当前最佳 TOP 文件均完整存在。
  Update: 2026-07-27 16:17+08:00 的只读校验显示，新版 `unet_c1` 本次验证总损失 `0.216707` 未优于此前的 `0.215768`，`BEST.ckpt` 已刷新并与 `TOP_epoch_00_score_0.2158.ckpt` 内容相同，因此其最佳别名当前正确。旧版 Find_0 的 `BEST.ckpt` 继续指向 `0.3075` 的最佳 TOP。新版 Find_1 尚未进行下一次验证，`BEST.ckpt` 仍对应 `0.3320`，尚未指向 `0.3251`。三项当前最佳 TOP 文件均完整存在。
  Update: 2026-07-27 21:17+08:00 的只读校验显示，旧版 Find_0 保存了新的 `0.3027` 最佳 TOP，`BEST.ckpt` 仍指向上一轮最佳 `0.3075`，再次符合已经查明的一次验证延迟。新版 Find_1 仍未进行下一次验证，`BEST.ckpt` 继续落后当前 `0.3251` 最佳 TOP；新版 `unet_c1` 的最佳别名继续正确。三项当前最佳 TOP 文件均完整存在。
  Update: 2026-07-28 02:17+08:00 的只读校验显示，新版 Find_1 保存了新的 `0.3179` 最佳 TOP，`BEST.ckpt` 已刷新为上一轮最佳 `0.3251`，仍符合一次验证延迟。旧版 Find_0 尚未进行下一次验证，`BEST.ckpt` 继续落后当前 `0.3027` 最佳 TOP；新版 `unet_c1` 的最佳别名继续正确。三项当前最佳 TOP 文件均完整存在。
  Update: 2026-07-28 07:20+08:00 的只读校验显示，新版 `unet_c1` 保存了新的 `0.2128` 最佳 TOP，`BEST.ckpt` 仍指向上一轮最佳 `0.2158`，符合一次验证延迟。旧版 Find_0 本次验证未产生新最佳结果，`BEST.ckpt` 已刷新为当前最佳 `0.3027`；新版 Find_1 尚未进行下一次验证，`BEST.ckpt` 继续落后当前 `0.3179` 最佳 TOP。三项当前最佳 TOP 文件均完整存在。
  Update: 2026-07-28 18:11+08:00 的只读校验显示，三项训练都保存了新的最佳 TOP：新版 Find_1 为 `0.3118`，新版 `unet_c1` 为 `0.2050`，旧版 Find_0 为 `0.3017`。三项 `BEST.ckpt` 均已刷新为各自上一轮最佳，分别是 `0.3179`、`0.2128` 和 `0.3027`，继续符合一次验证延迟；最新 TOP 与 last checkpoints 均完整存在。
  Update: 2026-07-28 23:13+08:00 的只读校验显示，三项训练尚未完成下一次验证；各自的最新 TOP、last 和 `BEST.ckpt` 内容与第十九次检查相同，W&B 事件文件与 GPU 计算仍在持续推进。该状态没有超出已经查明的一次验证延迟，继续在阶段切换或训练结束时核对最终别名。
  Update: 2026-07-29 04:12+08:00 的只读校验显示，新版 Find_1 与 `unet_c1` 本次验证均未产生新最佳结果，其 `BEST.ckpt` 已分别刷新为当前最佳 `0.3118` 和 `0.2050`。旧版 Find_0 保存了新的 `0.3012` 最佳 TOP，`BEST.ckpt` 仍对应上一轮 `0.3017`，继续符合一次验证延迟；三项最新 TOP 与 last checkpoints 均完整存在。
  Update: 2026-07-29 11:34+08:00 的只读校验显示，旧版 Find_0 保存了新的 `0.2993` 最佳 TOP，`BEST.ckpt` 已刷新为上一轮最佳 `0.3012`，继续符合一次验证延迟。新版 Find_1 与 `unet_c1` 尚未产生下一次验证，其最佳别名继续正确；三项最新 TOP 与 last checkpoints 均完整存在。
  Update: 2026-07-29 16:35+08:00 的只读校验显示，新版 `unet_c1` 本次验证总损失 `0.205393` 未优于当前最佳 `0.205013`，其 `BEST.ckpt` 继续正确。新版 Find_1 尚未产生下一次验证，其最佳别名继续正确；旧版 Find_0 也尚未进行下一次验证，`BEST.ckpt` 继续落后当前最佳 `0.2993` 一次验证。三项最新 TOP 与 last checkpoints 均完整存在。
  Update: 2026-07-29 21:36+08:00 的只读校验显示，新版 Find_1 与旧版 Find_0 分别保存了新的 `0.3110` 和 `0.2971` 最佳 TOP；两项 `BEST.ckpt` 分别对应上一轮最佳 `0.3118` 和 `0.2993`，继续符合一次验证延迟。新版 `unet_c1` 尚未产生下一次验证，其最佳别名继续正确；三项最新 TOP 与 last checkpoints 均完整存在。
  Update: 2026-07-30 09:04+08:00 的只读校验显示，新版 `unet_c1` 与旧版 Find_0 分别保存了新的 `0.2029` 和 `0.2944` 最佳 TOP；两项 `BEST.ckpt` 分别对应上一轮最佳 `0.2050` 和 `0.2971`。新版 Find_1 尚未产生下一次验证，`BEST.ckpt` 继续对应上一轮最佳 `0.3118` 而落后当前 `0.3110`。三项状态均继续符合一次验证延迟，最新 TOP 与 last checkpoints 完整存在。
  Update: 2026-07-30 14:04+08:00 的只读校验显示，新版 Find_1 与旧版 Find_0 分别保存了新的 `0.3089` 和 `0.2914` 最佳 TOP；两项 `BEST.ckpt` 分别对应上一轮最佳 `0.3110` 和 `0.2944`，继续符合一次验证延迟。新版 `unet_c1` 本次验证未产生新最佳结果，`BEST.ckpt` 已刷新为当前最佳 `0.2029`。三项最新 TOP 与 last checkpoints 均完整存在。
  Update: 2026-07-30 19:11+08:00 的只读校验未发现新的正式验证结果。新版 Find_1、`unet_c1` 和旧版 Find_0 的 `BEST.ckpt` 状态分别保持为“落后当前最佳一次验证”“正确指向当前最佳”和“落后当前最佳一次验证”；三项训练继续推进，当前最佳 TOP 与 last checkpoints 均完整存在。
  Update: 2026-07-31 01:40+08:00 的只读校验显示，新版 Find_1 与 `unet_c1` 分别保存新的 `0.3039` 和 `0.2014` 最佳 TOP，两项 `BEST.ckpt` 分别对应上一轮最佳 `0.3089` 和 `0.2029`。旧版 Find_0 本次验证总损失 `0.294136` 未优于当前最佳 `0.291351`，其 `BEST.ckpt` 已刷新并与 `TOP_epoch_00_score_0.2914.ckpt` 内容状态一致。三项最新 TOP 与 last checkpoints 均完整存在。
  Update: 2026-07-31 11:25+08:00 的只读校验显示，新版 `unet_c1` 本次验证总损失 `0.201678` 未优于当前最佳 `0.201407`，其 `BEST.ckpt` 已刷新为当前最佳 `0.2014`。旧版 Find_0 在连接中断期间先保存 `0.2899` 最佳 TOP，又保存当前 `0.2843` 新最佳 TOP；`BEST.ckpt` 已刷新为 `0.2899`，暂时落后新最佳一次验证。新版 Find_1 尚未产生下一次验证，`BEST.ckpt` 继续落后当前 `0.3039` 最佳 TOP。三项最新 TOP 与 last checkpoints 均完整存在。
  Update: 2026-07-31 16:35+08:00 的只读校验显示，新版 Find_1 已保存当前最佳 `0.2998` TOP 与对应 last checkpoint，但 `BEST.ckpt` 仍对应 `0.3039`；旧版 Find_0 的 `BEST.ckpt` 仍对应 `0.2899`，暂时落后当前最佳 `0.2843`；新版 `unet_c1` 的 `BEST.ckpt` 继续正确对应当前最佳 `0.2014`。这些状态仍符合训练期间别名可能落后一次验证的已知行为，最终收口时必须再次逐项核对。
  Update: 2026-07-31 21:35+08:00 的只读校验显示，新版 `unet_c1` 已保存当前最佳 `0.1990` TOP 与对应 last checkpoint，但 `BEST.ckpt` 仍对应上一轮最佳 `0.2014`；新版 Find_1 尚未产生下一次验证，`BEST.ckpt` 继续落后当前最佳 `0.2998`；旧版 Find_0 本次验证未产生新最佳，其 `BEST.ckpt` 已正确刷新为当前最佳 `0.2843`。三项 TOP 与 last checkpoints 均完整存在。
  Update: 2026-08-01 02:35+08:00 的只读校验显示，新版 Find_1 与旧版 Find_0 本次验证均未产生新最佳，其 `BEST.ckpt` 已分别正确刷新为当前最佳 `0.2998` 和 `0.2843`。新版 `unet_c1` 尚未产生下一次验证，`BEST.ckpt` 继续落后当前最佳 `0.1990` 一次验证。三项 TOP 与 last checkpoints 均完整存在。

- Observation: AdaLigand 与 Pocket_Plus 的常用工作区都已有未提交修改，Pocket_Plus 还包含当前正式训练相关的大量代码和配置变化。
  Evidence: 2026-07-23 的 `git status --short --branch` 显示 AdaLigand 有讨论和同步工具修改，Pocket_Plus 有 Dataset、模型、推理、Selector、配置和项目记忆修改。本文因此使用两个隔离 worktree，不在原工作区编辑或暂存文件。
- Observation: Pocket_Plus 尚未建立新名称 `Learn/CUMULATIVE`，当前累计学习基点仍名为 `Learn/model-cumulative`。
  Evidence: `Learn/model-cumulative` 与 `origin/Learn/model-cumulative` 均指向 `de6a89f`。本任务保留旧分支，并创建 `Learn/CUMULATIVE` 指向同一提交。
- Observation: 正式 centered 产物和 Selector 输入把 `voxel_final` 固定为 48 通道，但当前没有已经训练的 Selector 权重。
  Evidence: `src/artifacts/io.py`、`src/inference/centered.py` 和 `src/selector/model/input_fusion.py` 存在固定 48 的形状检查或 `nn.Linear(48, ...)`；用户确认尚未训练 Selector。
- Observation: 当前 Find_1 是 CPC1→CPC2 两段训练，而不是一个配置完成全部训练。
  Evidence: `configs/experiment/CPC2/Find_1.yaml` 从同名 CPC1 最佳 checkpoint 初始化，`configs/frozen_module/adaligand_stage2.yaml` 冻结体素主干，`configs/loss/stage1_find_cpc2.yaml` 把现有体素损失权重设为零。
- Observation: 当前 Windows 默认 Python 有 NumPy 与 SciPy，但没有 RDKit；原计划中的本地 `AdaLigand_stage1_py310` Conda 环境也不存在。
  Evidence: `python -m pytest tests/test_ligand_distance.py` 在把 Stage C 导入延迟到文件读取边界后为 `5 passed`；导入现有 Stage C 完整契约会因 `ModuleNotFoundError: rdkit` 中止。完整 A–G 测试必须使用服务器既有依赖环境。
- Observation: Stage C 允许 `present=False` 的配体坐标保存 NaN，距离生产只能检查 `present=True` 坐标是否有限。
  Evidence: 独立审计用 A–G 契约核对后发现第一版在应用 `present` 掩码前检查全部坐标；修正提交 `e5234c9` 使用真实 NaN 测试覆盖缺失原子。
- Observation: 单个 Slurm task 内运行两张 GPU 时，Lightning 子进程共享 `SLURM_PROCID=0`，必须优先使用 `RANK` 或 `LOCAL_RANK` 判断代码快照写入者。
  Evidence: 独立审计发现第二个 DDP 子进程可能重复创建 `src_snapshot`；`ExperimentManager` 现按 `RANK → LOCAL_RANK → SLURM_PROCID` 解析，并有继承组合测试。
- Observation: Job `321540` 仍为 `RUNNING`，`after_lock` 保留；同一 allocation 再次运行 Find_1 时，默认 `job321540` 标记会与旧运行目录重名。
  Evidence: 2026-07-23 02:55+08:00 的只读 `scontrol` 与 allocation 脚本检查确认两张 H100 和锁循环仍在。新版启动必须显式设置唯一 `POCKET_RUN_STAMP`，在触碰 `kill_lock` 前打印并证明新旧运行目录不同。
- Observation: 项目安全同步入口不删除远端旧文件，因此共享服务器代码目录可以保留本地分支已经删除的配置和测试，不能作为精确发布目录。
  Evidence: 共享目录测试额外收集旧 `Find_2` 配置与历史测试并得到与本地不同的测试数量；从空目录建立的提交命名发布副本分别通过 Pocket_Plus `378 passed` 与 AdaLigand `306 passed`。新版训练必须从精确发布副本启动，不从共享目录或旧 allocation runtime 猜测代码集合。
- Observation: 真实 `10ad` 中，第一个中心 BOX 的蛋白 N/CA/C/O 体素数分别为 968、965、945、943，核酸前景类别为空；配体反距离目标范围为 0.0143756–0.7884504。
  Evidence: CPU 作业 `323026` 从正式 BOX pool 的 `train/10ad.npz` 直接构造请求，读取新距离文件、三项标签和 56 通道密度输入，并成功实例化新版固定五头模型。
- Observation: 全量距离 array `323027` 的分片 2、3、4、7、11 各出现一条 `unknown_failed`；对应 PDB 是 `6k0a`、`7pel`、`9wqp`、`7ojf`、`9yx6`，五者均缺少正式 `density/{pdb_id}/exp.npz`，不是距离计算产生 NaN 或写盘失败。
  Evidence: `status.part_0002_of_0012.jsonl`、`status.part_0003_of_0012.jsonl`、`status.part_0004_of_0012.jsonl`、`status.part_0007_of_0012.jsonl` 与 `status.part_0011_of_0012.jsonl` 分别记录 `FileNotFoundError`；对应 Slurm task 本身均为 `COMPLETED 0:0`，程序汇总各报告 `failed=1`。在 12 个分片聚合完毕并处理训练样本排除前，不允许接管 Job `321540`。
- Observation: 分片 6 的 1,865 个目标文件已经全部原子落盘，但原作业停在少数超大文件的写后完整重读验证；`2w49` 的距离文件约 99 GB，分片峰值内存约 636 GB。
  Evidence: 原日志长时间停在 `Done 1829 tasks`，同时精确核对确认分片 6 的 1,865 个 `ligand_dist.npz` 均存在。取消 `323027_6` 后，数组 `323275` 对 36 个最大长尾候选执行独立 90 秒验证，25 个通过，11 个按超时跳过，无其他错误。
- Observation: Find_1 的第一份单步 smoke 把 `max_steps` 限制为 1，而候选 warmup 步数按 `round(total_steps × warmup_ratio)` 解析，因此得到 0 并在尚无验证阈值时错误进入自适应阈值路径。
  Evidence: 两个 DDP rank 均报 `p_best_by_class` 缺失；GPU 峰值约 29–30 GiB，不是显存不足。旧正式运行和新正式 CPC1 都保留 `warmup_steps: null` 与正的 warmup 比例，完整训练会解析出正数 warmup。重试只给单步 smoke 显式设置 `warmup_steps=1`，没有修改模型、候选逻辑或正式 CPC1 配置。
- Observation: 两次 Find_1 重试都在训练前验证结束时把 CPU 上的非分箱 PRAUC 标量交给 NCCL，两个进程均报 `No backend type associated with device type cpu`。
  Evidence: 旧 Job `321540` 的实际运行快照不但给 CPU 指标使用 Gloo 通信组，还在指标已经完成跨卡聚合后以 `sync_dist=False` 写入 Lightning。提交 `9f4012d` 只恢复了前半项；提交 `1909267` 同时恢复后半项，避免 Lightning 使用默认 NCCL 通信组二次同步 CPU 标量。正式 CPC1 的候选选择和 warmup 配置未改变。
- Observation: 旧 Job `321540` 的实际配置同时写有 `use_soft_splatting=true` 与 `use_gaussian_splatting=true`，旧代码按 Gaussian 优先级执行 sigma=0.7 的 `3×3×3` 写入；原训练计划误写成三线性写入。
  Evidence: 独立审计对比旧运行快照和新版代码后发现该差异。用户明确选择以旧运行的真实 Gaussian 行为为新基线；提交 `9417bcf` 恢复开关、优先级和两条前向路径，并把冲突规格同步为 Gaussian。
- Observation: 新版正式启动沿用了 `configs/train/stage1_cpc1.yaml` 中的 `warmup_ratio=0.025`，但用户此前统一决定的优化器学习率 warmup 比例是 `0.005`。
  Evidence: 两项旧运行的 resolved 配置均记录 `0.025`，W&B 的 `warmup_lr` 曲线也显示过长的线性增长。用户发现后要求立即重启；新运行 resolved 配置均记录 `0.005`，且五项损失权重逐项不变。
- Observation: Find_1 smoke 的 `refined` 与 `unrefined` 面板显示 `NaN`，但训练损失、验证损失、候选统计和已定义的 PR-AUC 均为有限值。
  Evidence: 当前 Find_1 没有启用 `ligand_sparse_refine_loss`，因此不会生成 `ligand_refine_target_C`，对应诊断缓冲区按既有定义把未启用面板记为 `NaN`。这些面板不参与损失、学习率调度、BEST checkpoint 选择或本次新增监督；smoke 的 `val_loss/global/total=0.76348`，蛋白主链宏平均 PR-AUC 为 `0.00133`。正式完整验证仍需确认核酸主链 PR-AUC 能在出现正类后记录。
- Observation: Find_1 两卡 smoke 和正式 CPC1 的单卡显存峰值接近 H100 容量上限。
  Evidence: smoke 峰值约为 80.98 GiB 与 81.56 GiB；14:28 的正式 CPC1 采样为 80.78 GiB 与 80.84 GiB，尚未发生显存不足。若后续样本触发显存不足，先按既定顺序把 density-cube 的 `hidden_channels` 从 64 调到 48，再考虑减小批量。
- Observation: 取消 Job `346003` 与 `350185` 时，Slurm 一度把 `hnode01` 标记为 `MIXED+DRAIN`，原因是 `Kill task failed`；此时新提交的 Job `351295` 显示 `ReqNodeNotAvail`。
  Evidence: 2026-08-22 09:19--09:30 的 `sinfo` 与 `scontrol show node hnode01` 显示 95 个 CPU 处于不可分配状态，节点原因由 `root` 写为 `Kill task failed`。没有改投节点、取消新任务或操作他人 Job；管理员恢复节点后，`hnode01` 变为 `MIXED` 且原因清空，Job `351295` 随即在原定节点取得两张 H100。该事件没有改变训练数据、模型或优化参数。
- Observation: 第一版 `unet_c1` 对齐配置已经构造三个新增输出头，但 Dataset 最初只在 `Find_1` 名称下返回蛋白、核酸与反距离目标。
  Evidence: 新增真实 `unet_c1` Dataset 测试首先复现缺少 `protein_mainchain_target`，随后把固定辅助标签返回范围精确扩展到 `Find_1` 与 `unet_c1`；34 项专项测试和 Linux 全套测试通过。

## Decision Log

- Decision: 蛋白分类头固定为背景、`N`、`CA`、`C`、`O`；核酸分类头固定为背景、`P`、`O5'`、`C5'`、`C4'`、`C3'`、`O3'`。训练配置不提供动态类别规则或 `class_id`。
  Rationale: 固定类别覆盖本轮科学目的，避免把小范围改动扩大成动态标签框架。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: 两个分类头在完整 `80³` BOX 上计算 `0.7×Focal + 0.3×Dice`，Focal `gamma=2`、无类别 alpha；Dice 对每个非背景类别分别计算后等权平均。
  Rationale: 完整网格监督训练空间定位能力，并沿用现有体素损失归约方式。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: `ligand_dist.npz` 保存 `float16` 原始 Å 距离；训练目标为 `1/(1+d/(1 Å))`，完整 BOX 平均 MSE。没有实际配体原子时磁盘距离全为正无穷，训练目标全为零。
  Rationale: 原始距离是稳定事实；反距离是有限、可训练的表达。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: 五个最高分辨率体素输出头统一为 `Conv1x1(C,C) → ReLU → Conv1x1(C,C_out)`。新版 Find_1 关闭 RAUNet 末端多尺度输出块，令 `voxel_final=c0` 并把通道数改为 64。
  Rationale: 最高分辨率的 3、5、7 体素宽空间卷积成本很高，体素主干此前已经混合空间信息。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: 现有配体区域、受体结合区域、配体反距离、蛋白原子、核酸原子损失权重分别为 `1.0:0.1:0.3:0.05:0.05`。新增 W&B 曲线只包含三项新增组合损失和两个验证宏平均 PR-AUC。
  Rationale: 保持现有主损失尺度，并限制监控改动范围。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: 新 Find_1 从头运行完整 CPC1→CPC2；新增辅助损失和 PR-AUC 只在 CPC1 启用，CPC2 冻结体素主干并把三项新增损失权重设为零。Find_1 两段最高学习率均为 `5e-5`；新版 `unet_c1` 继续使用 `1e-4`。
  Rationale: 保持 Find_1 的既有两阶段职责，同时让辅助监督只塑造 CPC1 体素表示。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: `box_sample_fraction=1.0` 完全保留当前逐 epoch BOX 选择；只有小于 1 时才生成并复用按比例和 seed 命名的训练、验证选择文件。首次正式新 Find_1 使用 1.0。
  Rationale: 默认训练不能因为新增消融开关改变样本多样性。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: 2026-08-21 的新版 Find_1 先以单张 H100 在 `hnode01` 自行排队并验证稳定；即使节点已经没有 H100 待调度任务，也只有在用户再次明确授权后，才可先提交双卡任务并取消 Job `350185` 与 `346003`。等待授权期间只读复核节点和训练状态。
  Rationale: 先证明正式入口和新数据链可以稳定训练，同时把终止已有训练、拼接两张 H100 的资源变更保留为独立授权动作。
  Date/Author: 2026-08-21，用户与 Codex。
- Decision: 2026-08-22 的双卡转换以“H100 分区没有既有待调度任务”为触发条件；先提交固定 `hnode01` 的双卡 Job，二次确认除新 Job 外仍无其他 H100 待调度任务后，再取消 Job `346003` 与 `350185`。新训练采用 64 CPU、每 rank 30 workers 和学习率 `1.0e-4`。
  Rationale: 先让双卡请求进入调度队列，再释放两张已占用 H100，可减少资源被其他新请求抢占的窗口；二次队列检查保护其他待调度作业，固定最终参数则保证冻结发布和运行配置可核验。
  Date/Author: 2026-08-22，用户与 Codex。
- Decision: 训练运行目录保存完整 `src/`、解析后的配置和来源清单。checkpoint 推理优先使用相邻快照；缺少快照时默认报错，只有显式选择才允许当前工作区代码。
  Rationale: 模型、Dataset、wrapper 与推理代码共同决定 checkpoint 的可复现行为。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: 正式产物按实际模型通道数处理 `voxel_final`，Selector 使用 `nn.LazyLinear` 接受第一次看到的通道数，不增加 64→48 投影。
  Rationale: 当前没有 Selector 权重需要兼容，通道数不应成为多个文件复制的常量。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: 距离标签 CPU 生产的同时运行上限为 192 核；96 核整节点不能立即取得时，使用每项约 16 核的 Slurm array 与 joblib loky。正式 GPU 训练只接管 Job `321540`，不提交其他 GPU 训练作业。
  Rationale: 小 CPU 任务更容易使用碎片资源，GPU allocation 必须保留现有两张 H100 和锁语义。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: 本次全量生产只对分片 6 的 36 个长尾候选使用 90 秒硬超时，不增加体素数上限，不重跑已经完成的 95% 样本。超时 PDB 与缺少实验密度的 PDB 一并进入冻结训练排除清单。
  Rationale: 当前优先快速启动训练；独立子进程硬超时能结束 SciPy 的长时间 C 计算，同时不改变已经落盘距离文件的字段、数值与单位契约。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: Job `321107` 重新训练 `unet_c1`；U-Net 骨干和五路中间特征接口不替换，只把最终体素特征设为 64 通道、关闭末端多尺度卷积、启用与 Find_1 相同的五个两层 `1×1×1` 输出头，并使用相同辅助损失、学习率和训练排除清单。
  Rationale: 用户要求比较同一 `unet_c1` 骨干在新版输出监督下的训练结果，不把任务扩大成体素骨干替换。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: Find_1 CPC1 与 `unet_c1` 的优化器学习率 warmup 比例统一为 `0.005`；Find_1 CPC2 继续无 warmup。此次纠正保留全部辅助损失权重、学习率、网络和数据设置。
  Rationale: 用户确认 `0.005` 才是此前统一决定的值，并要求不把调度器纠正扩大成其他训练改动。
  Date/Author: 2026-07-23，用户与 Codex。
- Decision: 本轮三个 CPC1 运行接受已经形成的运行历史和产物。阶段选择不使用可能落后一轮的 `BEST.ckpt`，而是在确实存在的 TOP checkpoint 中选择 `val_score/global/voxel_ligand_PRAUC` 最大者，并以明确文件路径只加载模型参数。Find_0 与 Find_1 进入 CPC2，`unet_c1` 不运行 CPC2。
  Rationale: 用户把配体区域 PR-AUC 确认为模型选择指标，并明确接受不恢复优化器、学习率调度器、全局步数和回调状态的阶段转换。
  Date/Author: 2026-08-01，用户与 Codex。
- Decision: Find_0 与 Find_1 的 CPC2 从第一步直接使用各自最高学习率，不执行 warmup。当前冻结运行保留原代码并允许在同一回调错误处自然退出；监控指标与非法日志调用的修正只进入下一版 Pocket_Plus。
  Rationale: CPC2 是新的训练阶段，不延续 CPC1 的 warmup；冻结代码和未来代码必须明确分离，避免运行中换版。
  Date/Author: 2026-08-01，用户与 Codex。
- Decision: 从 2026-08-01 23:04 起，本轮停止 Find_0 CPC2，且不再启动 Find_1 CPC2；后续只监控仍在运行的 Find_1 CPC1 和 `unet_c1`。该运行决定不删除、禁用或修改 Pocket_Plus 中的 CPC2 代码、配置与恢复能力。
  Rationale: 用户依据当前 W&B 损失曲线判断 CPC2 的收益不足以继续占用本轮计算资源，同时希望保留以后重新评估与复用 CPC2 的能力。
  Date/Author: 2026-08-01，用户。

## Outcomes & Retrospective

距离标签代码、Pocket_Plus 新版训练代码、完整快照和推理适配已经完成本地与 Linux 测试、独立审计和真实 `10ad` 验证。三个历史运行快照已经按各自实际执行源码只增补缺失文件，并通过真实 checkpoint 严格恢复。正式距离生产已经形成 22,386 条完整状态和 16 个 PDB 的冻结训练排除清单；证据位于本次运行的 `summary.json`、`ligand_dist_failures.json` 和 `timeout_repair_status.jsonl`。独立端到端审计未发现正式训练阻塞。首轮正式训练因错误继承 `warmup_ratio=0.025` 而由用户叫停；Job `321107` 与 `321540` 现已直接启动 `warmup_ratio=0.005` 的新版正式训练，两个 allocation 和 `after_lock` 均保留。

2026-08-01，旧版 Find_0 的 CPC1 因学习率下降停止回调的非法日志调用而结束。用户决定接受 CPC1 已有产物，按已落盘 TOP checkpoints 中最高配体区域 PR-AUC 选择阶段起点；Find_0 CPC2 已在原 Job `321743` 上启动，最高学习率从第一步生效且没有 warmup。新版 Find_1 与 `unet_c1` 继续使用各自冻结发布副本，前者自然结束后按相同规则进入 CPC2，后者自然结束后直接收口。下一版 Pocket_Plus 已独立修正 checkpoint 监控指标和回调错误，但没有部署到本轮三个运行。

同日 23:04，用户根据 W&B 曲线停止 Find_0 CPC2，并取消本轮 Find_1 CPC2。只读核验确认 Job `321743` 已离开 Slurm；Job `321540` 和 `321107` 继续运行，分别到达 `global_step=21041` 与 `26399`，训练进程、冻结代码、W&B 和 GPU 活动正常。后续只监控这两个 CPC1 运行并记录最终产物。这是本轮运行范围的变化，不是代码能力删减；下一版修正和现有 CPC2 代码、配置、checkpoint 与历史产物均保持不变。

截至 2026-08-02 17:34，两项 CPC1 仍在各自冻结发布副本中运行：Find_1 到达 `global_step=23171`，最新验证总损失与配体区域 PR-AUC 为 `0.292820` 和 `0.660276`；`unet_c1` 到达 `29060`，对应数值为 `0.195387` 和 `0.568288`。两项的当前训练损失、验证损失和已启用 PR-AUC 均为有限值，GPU 与 W&B 持续活动，并各自保存了新的 TOP 与 last checkpoint；尚未到达自然结束或最终产物收口时点。

## Context and Orientation

AdaLigand 实现工作树是 `C:\Users\15919\.codex\worktrees\019f8834-aux\AdaLigand`，分支是 `codex/auxiliary-supervision-find1`，共同基点是 `Learn/CUMULATIVE@799293c`。数据处理代码位于 `Data_Preprocessing/Ori_Data/adaligand_preprocessing/`，产物冷读契约位于 `Data_Preprocessing/Ori_Data/README.md`。

Pocket_Plus 实现工作树是 `C:\Users\15919\.codex\worktrees\019f8834-aux\Pocket_Plus`，分支同为 `codex/auxiliary-supervision-find1`，共同基点是新建的 `Learn/CUMULATIVE@de6a89f`。Dataset 位于 `src/datasets/stage1_dataset.py`，模型位于 `src/model/`，训练 wrapper 位于 `src/wrappers/`，正式产物位于 `src/artifacts/`，推理位于 `src/inference/`，Selector 位于 `src/selector/`。

受体原子局部坐标向下取整后得到该原子所属的唯一体素。蛋白和核酸标签不是只在这些体素上计算损失；目标原子所在体素为相应前景类别，BOX 内其余体素为背景。一个体素包含多个目标原子的极少数情况不建立专门分支，普通写入顺序留下的类别即可。

`ligand_dist.npz` 位于服务器正式数据根的 `density/{pdb_id}/ligand_dist.npz`。主数组 `distance` 形状为 `(1,Z,Y,X)`、类型为 `float16`，每个数值是对应实验密度体素中心到最近实际配体重原子的欧氏距离，单位 Å。实际配体重原子来自全部成功 occurrence 中 `present=True` 的原子。

Job `321540` 当前持有两张 H100。只有标签、代码、推理、测试和历史快照补齐全部完成后，才重新核实它仍在运行且实际承载旧 Find_1；核实通过后使用该运行目录的 `kill_lock` 停止旧进程，保留 `after_lock` 和 allocation，再启动新版 Find_1。不得执行 `scancel`。

复用 Job `321540` 时必须为新版训练显式设置唯一 `POCKET_RUN_STAMP`。启动脚本在创建 `kill_lock` 前先打印计划运行目录，并与旧 Find_1 路径比较；相同则停止，不允许覆盖旧运行目录或放宽快照拒绝覆盖保护。

## Plan of Work

第一个里程碑在 AdaLigand 中建立距离标签生产。先把距离的纯计算、字段契约、原子写入和校验分开，使小数组测试不依赖服务器。命令行入口只解析路径、分片和并行参数。代表性测试覆盖普通配体、没有实际配体原子、来源摘要变化、各向异性体素尺寸和断点复用。README 增加 `ligand_dist.npz` 的完整字段、坐标和缺失值说明。

第二个里程碑在 Pocket_Plus 中实现 Dataset 与模型训练。固定类别表只保存在一个模块；Dataset 根据现有逐原子坐标、残基类型、主链标志和原子名构造两张分类标签，并裁切距离图。`box_sample_fraction` 的默认路径不得生成选择文件。模型建立五个统一输出头，关闭多尺度输出块时不申请相关参数。wrapper 连接损失、权重和最少 W&B 指标。训练配置明确写出 64 通道、关闭多尺度块、学习率和辅助权重。

第三个里程碑扩大代码快照并并行完成推理适配。主 agent 先固定快照目录、来源清单和加载接口，再把 checkpoint 推理、正式产物可变通道和 Selector 惰性输入交给一个 subagent。subagent 只修改约定的推理、产物、Selector 与专项测试文件；主 agent 审查接口、测试和与训练代码的组合结果。历史 Job `321107`、`321540`、`321743` 只从各自实际执行代码或已证明提交补充缺失快照文件，绝不覆盖已有运行文件。

第四个里程碑完成验证。先运行两个 Windows 环境可执行的测试和静态检查，再安全同步到服务器项目目录，在 Linux 环境运行完整测试。代表性真实数据验证距离文件、Dataset 小批次、五个输出头、有限损失、非零梯度、CPC1→CPC2 严格加载、完整快照推理和 64 通道正式产物。真实发生 CUDA 显存不足时，先把 `both.yaml` 的两个 `hidden_channels` 从 64 改为 48；仍不足才降低单卡 batch 并提高梯度累积保持全局 batch。

第五个里程碑整理 Git 双线。两个仓库分别保留实现分支的真实提交；从共同基点建立 `Learn/auxiliary-supervision-find1`，按契约与测试、数据生产、Dataset 与模型、快照与推理、运行配置与证据的顺序重建。实现端点和学习端点只允许学习注释或学习文档差异，通过相同测试后快进各自 `Learn/CUMULATIVE`。

第六个里程碑生产正式距离标签并启动训练。CPU 作业先查看实时节点和 QoS；总申请量不超过 192，分片对 PDB 写入互斥并可断点续跑。最终失败为零时不增加过滤行为；仍有失败时写 `ligand_dist_failures.json`，在训练请求建立前排除对应 PDB。训练前重新核实 Job `321540`、运行目录、锁和 resolved 配置，随后按当时授权完成旧进程停止与新版 CPC1 启动。CPC2 能力经过验证并曾用于 Find_0，但用户后来取消本轮继续训练 CPC2。

第七个里程碑监控和收口。`warmup_ratio=0.005` 的训练启动后，把现有 `adaligand-stage1` heartbeat 指向当前 Codex 任务，每 30 分钟检查一次；连续五次健康后改为每 5 小时。当前 heartbeat 只检查 Find_1 CPC1 与 `unet_c1`；两者结束并记录最终状态后删除 heartbeat。同步更新本 ExecPlan、映射索引、项目状态、阶段 handoff 和必要的长期学习记录。

## Concrete Steps

在 AdaLigand 实现工作树运行轻依赖专项检查：

    cd Data_Preprocessing/Ori_Data
    python -m pytest tests/test_ligand_distance.py
    python -m compileall adaligand_preprocessing

在 Pocket_Plus 实现工作树运行：

    conda run -n Pocket_Plus_windows python -m pytest tests
    conda run -n Pocket_Plus_windows python -m compileall src

服务器使用 `/home/penghongen/anaconda3/envs/Pocket_Plus_centos7_cu121_allgpu` 运行 Pocket_Plus 测试。AdaLigand 距离生产使用项目现有 Python 3.10 完整依赖环境；正式命令必须在执行前从当前 sbatch、执行记录和远端环境重新核实，核实结果写回本文。

每个里程碑结束时执行：

    git status --short
    git diff --check

并把实际测试命令、通过数量、失败摘要、提交哈希和服务器证据路径写入本文。

## Validation and Acceptance

距离契约验收要求普通配体样本的若干体素能用直接 NumPy 计算得到相同最近距离；没有配体原子的样本全为正无穷；`distance`、空间字段和三个来源摘要由读取器重新验证。重复执行有效文件必须复用，损坏或来源变化必须明确失败或重建，不留下半成品目标文件。

Dataset 验收要求蛋白、核酸、混合受体、修饰或未知残基和无配体样本均能构造固定形状标签。蛋白和核酸前景体素计数可由受体原子直接核对；距离 BOX 与实验密度 BOX 使用相同起点。`box_sample_fraction=1.0` 的多个 epoch 保持当前不同 BOX 行为，小于 1 的相同 seed 复用同一选择文件且训练读取顺序仍随 epoch 改变。

模型验收要求五个头均为两层 `1×1×1` 卷积；关闭多尺度输出时对应参数不存在，`voxel_final` 为 64 通道。一次完整前向—反向必须得到有限的五项损失，启用的新增头具有非零梯度。CPC2 能严格加载 CPC1 最佳 checkpoint，新增辅助损失不进入 CPC2 total loss。

推理验收要求 checkpoint 有完整快照时使用该快照；缺少快照默认报错；一个进程只加载一套快照。64 通道 centered 文件能够写入、读取和校验，Selector 第一层在首次接收 64 通道特征时正确实例化。

正式训练验收要求 Job `321540` allocation 未释放，没有其他 GPU 训练作业被提交；resolved 配置显示 Find_1、64 通道、关闭多尺度输出、学习率 `5e-5`、`box_sample_fraction=1.0` 和五项正确权重。短期日志没有 traceback、NaN、CUDA 显存不足或数据契约错误，W&B 优先联网并在失败时保留完整 offline 日志。

## Idempotence and Recovery

距离标签按来源摘要复用，写入使用同目录临时文件与原子改名。CPU 分片只处理各自 PDB，重复提交不会覆盖来源相同的有效文件。最终失败 PDB 使用单独 JSON 清单，不修改 A–G keep list、BOX manifest、验证选择或逐 PDB BOX 池。

任何服务器同步只使用项目安全同步入口，不运行删除式同步。任何锁操作前重新读取 Job、进程、运行目录和锁状态。`kill_lock` 只用于用户明确授权的 Job `321540` 旧 Find_1；不删除 `after_lock`，不执行 `scancel`。

实现中发现标签科学定义、A–G 正式产物、现有训练样本或 Job 身份与本文冲突时停止相应副作用，保存证据并报告；不在昂贵运行中静默回退。

## Artifacts and Notes

当前共同基点：

    AdaLigand  799293c584586278ab5f313ce435533e2e1c3ff1
    Pocket_Plus de6a89f38e48553edfd440c76bf61823229d9205

当前实现分支：

    codex/auxiliary-supervision-find1

讨论结论：

    C:\Users\15919\Desktop\AdaLigand\grill_with_memory\07-22-17-42.md

## Interfaces and Dependencies

AdaLigand 最终必须提供一个长期生产函数，接收单个 PDB 的实验密度空间定义、成功配体实例和实际配体坐标，返回或写出本文定义的距离数组与来源字段；另有薄命令行入口支持 PDB 清单、分片、并行数、复用和校验。

Pocket_Plus 最终必须在 Dataset batch 中提供两个整数分类张量和一个浮点反距离张量，三者空间形状均为 `(80,80,80)`；Collator 堆叠为 `(B,80,80,80)`。模型前向返回蛋白、核酸和反距离 logits，wrapper 负责变换、损失与日志。固定类别顺序由单一代码常量定义，测试从同一常量取得预期编号。

完整快照目录固定为运行目录下 `src_snapshot/src/`，并保存来源清单。推理加载器必须把该目录中的 `src` 作为一个 checkpoint 专属实现使用，不在同一 Python 进程混用不同 checkpoint 的模块。

Revision note 2026-07-23：根据用户最终授权初始化本文，并记录隔离工作树、完整范围、固定科学定义、服务器资源纪律和验收要求。

Revision note 2026-07-23 01:10+08:00：记录距离标签第一版、5 项专项测试和 Windows 环境缺少 RDKit 的真实限制，并把 AdaLigand 本地命令改为当前能够执行的轻依赖检查。

Revision note 2026-07-23 03:25+08:00：记录 Pocket_Plus 实现、推理 subagent、独立审计修复、本地合并测试、实现提交和复用 Job `321540` 时必须使用唯一运行标记的启动门槛。

Revision note 2026-07-23 04:01+08:00：记录精确发布目录的 Linux 全套测试、真实 `10ad` 标签与 Dataset 证据、历史 checkpoint 快照补齐及严格恢复、全量距离 array `323027` 和共享目录不能作为精确发布目录的事实。

Revision note 2026-07-23 07:05+08:00：记录 array `323027` 已确认的两个缺失实验密度文件、对应正式状态证据及暂停训练接管的发布门。

Revision note 2026-07-23 07:35+08:00：把分片 4 新确认的 `9wqp` 纳入缺失实验密度文件集合。

Revision note 2026-07-23 08:35+08:00：把分片 11 新确认的 `9yx6` 纳入缺失实验密度文件集合，并确认分片 3、6、8 的累计 CPU 时间仍持续增长。

Revision note 2026-07-23 09:05+08:00：把分片 3 新确认的 `7pel` 纳入缺失实验密度文件集合；array 只剩分片 6，且其累计 CPU 时间仍持续增长。

Revision note 2026-07-23 11:35+08:00：记录分片 6 全部目标已落盘后的 90 秒长尾补验、16 个 PDB 的冻结训练排除清单、完整 22,386 条状态与正式证据摘要。

Revision note 2026-07-23 12:50+08:00：记录 Job `321540` 与 `321107` 的保留资源接管、`unet_c1` 输出监督对齐、Dataset 缺字段修复，以及 Find_1 单步 smoke 的 warmup 取整原因和测试专用修正边界。
Revision note 2026-07-23 13:35+08:00：记录 CPU PRAUC 的 NCCL/Gloo 适配遗漏、旧 Find_1 实际 Gaussian scatter 与书面规格冲突、用户确认的新损失权重、精确发布提交 `9417bcf` 及两个 smoke 同时启动。

Revision note 2026-07-23 14:10+08:00：记录 CPU PRAUC 在 Gloo 聚合后仍被 Lightning/NCCL 二次同步的完整根因、修复提交 `1909267`、Find_1 最高学习率回调到 `5e-5`，以及 `unet_c1` smoke 通过并进入正式训练。

Revision note 2026-07-23 18:20+08:00：记录 `warmup_ratio=0.025` 与用户既定 `0.005` 不符、两项运行经 `kill_lock` 保留资源重启，以及新的五次短周期健康检查要求。

Revision note 2026-07-24 07:08+08:00：记录第二次三作业联合检查、新版 `unet_c1` 的首次正式验证与首批 checkpoint，以及新版 Find_1 尚未进入首次正式验证的当前状态。

Revision note 2026-07-24 12:05+08:00：记录第三次三作业联合检查、新版 Find_1 的首次正式验证与首批 checkpoint，以及两个 Find 尚未进入 CPC2 的状态。

Revision note 2026-07-24 17:07+08:00：记录第四次三作业联合检查，并区分新版 `unet_c1` 当前运行证据与 allocation 总日志中更早的已退出启动错误。

Revision note 2026-07-24 22:08+08:00：记录第五次三作业联合检查、新版 `unet_c1` 第二次正式验证，以及新版 Find_1 历史 W&B 限流重试不影响当前训练的判定。

Revision note 2026-07-24：辅助监督实现已纳入 AdaLigand 与 Pocket_Plus 的本地 Git 双线重建。实现端点、学习端点和核验结果记录在 `文档/exec_plan/双线Git与学习注释重建.md`；本条只关闭“学习线未补全”事项，不关闭仍在进行的三项训练监控和两个 Find 的 CPC 阶段切换。

Revision note 2026-07-25 16:32+08:00：记录第六次三作业联合检查、三项训练的新验证结果，以及新版 `unet_c1` 摘要文件暂缓刷新但事件流与 GPU 计算继续推进的判定。

Revision note 2026-07-25 21:35+08:00：记录第七次三作业联合检查、新版 `unet_c1` 第四次验证，以及 `BEST.ckpt` 在训练中可能落后一轮验证但会在 `on_train_end` 再次刷新的回调行为。

Revision note 2026-07-26 10:12+08:00：记录第八次三作业联合检查、三项训练的新验证结果，并把训练期间 `BEST.ckpt` 落后一轮的核对范围扩展到三项训练。

Revision note 2026-07-26 15:20+08:00：记录第九次三作业联合检查，以及新版 `unet_c1` 和旧版 Find_0 的新验证结果与 checkpoint 状态。

Revision note 2026-07-26 20:21+08:00：记录第十次三作业联合检查，以及新版 Find_1 的新验证结果与 checkpoint 状态。

Revision note 2026-07-27 01:21+08:00：记录第十一次三作业联合检查，以及旧版 Find_0 的新验证结果与 checkpoint 状态。

Revision note 2026-07-27 06:20+08:00：记录第十二次三作业联合检查，以及新版 `unet_c1` 的新验证结果与 checkpoint 状态。

Revision note 2026-07-27 11:20+08:00：记录第十三次三作业联合检查、新版 Find_1 与旧版 Find_0 的新验证结果，以及三项训练当前不同的 `BEST.ckpt` 刷新状态。

Revision note 2026-07-27 16:21+08:00：记录第十四次三作业联合检查、新版 `unet_c1` 的新验证结果，以及其 `BEST.ckpt` 已刷新到当前最佳 TOP。

Revision note 2026-07-27 21:22+08:00：记录第十五次三作业联合检查、旧版 Find_0 的新最佳验证结果，以及其 `BEST.ckpt` 暂时落后新最佳 TOP 一次验证。

Revision note 2026-07-28 02:22+08:00：记录第十六次三作业联合检查、新版 Find_1 的新最佳验证结果，以及三项训练当前不同的 `BEST.ckpt` 刷新状态。

Revision note 2026-07-28 07:24+08:00：记录第十七次三作业联合检查、新版 `unet_c1` 与旧版 Find_0 的新验证结果，以及三项训练当前不同的 `BEST.ckpt` 刷新状态。

Revision note 2026-07-28 13:14+08:00：记录第十八次三作业联合检查，并区分新版 Find_1 暂未刷新的摘要文件与仍在持续推进的 W&B 事件流、内部日志和 GPU 计算。

Revision note 2026-07-28 18:15+08:00：记录第十九次三作业联合检查、三项训练各自的新最佳验证结果，以及三项 `BEST.ckpt` 均暂时落后新最佳 TOP 一次验证。
Revision note 2026-07-28 23:18+08:00：记录第二十次三作业联合检查、三项训练继续推进且没有新验证的状态，以及当前 GPU 余量和 `BEST.ckpt` 延迟状态。
Revision note 2026-07-29 04:17+08:00：记录第二十一次三作业联合检查、三项训练的新验证结果和各自不同的 `BEST.ckpt` 刷新状态。
Revision note 2026-07-29 11:39+08:00：记录第二十二次三作业联合检查、旧版 Find_0 的新最佳验证结果，以及新版 `unet_c1` 摘要暂缓刷新但训练仍在推进的证据。
Revision note 2026-07-29 16:40+08:00：记录第二十三次三作业联合检查、新版 `unet_c1` 的新验证结果，以及三项训练当前不同的摘要与 `BEST.ckpt` 状态。
Revision note 2026-07-29 21:41+08:00：记录第二十四次三作业联合检查、新版 Find_1 与旧版 Find_0 的新最佳验证结果，以及三项训练当前不同的 `BEST.ckpt` 状态。
Revision note 2026-07-30 09:09+08:00：记录第二十五次三作业联合检查、新版 `unet_c1` 与旧版 Find_0 的新最佳验证结果，以及三项 `BEST.ckpt` 均暂时落后当前最佳一次验证。
Revision note 2026-07-30 14:09+08:00：记录第二十六次三作业联合检查、三项训练的新验证结果，以及新版 `unet_c1` 的 `BEST.ckpt` 已刷新为当前最佳。
Revision note 2026-07-30 19:16+08:00：记录第二十七次三作业联合检查、三项训练继续推进且没有新验证的状态，以及当前 GPU 余量和 `BEST.ckpt` 状态。
Revision note 2026-07-31 00:10+08:00：记录原定第二十八次联合检查因服务器 SSH 与 TCP 端口均不可达而未完成，并把 heartbeat 临时缩短为每 30 分钟重试。
Revision note 2026-07-31 01:45+08:00：记录服务器连接恢复后的第二十八次三作业联合检查、三项训练的新验证结果和 `BEST.ckpt` 状态，并把 heartbeat 恢复为每 5 小时。
Revision note 2026-07-31 10:55+08:00：记录原定第二十九次联合检查再次因服务器 SSH 与 TCP 端口不可达而未完成，并把 heartbeat 临时缩短为每 30 分钟重试。
Revision note 2026-07-31 11:30+08:00：记录服务器连接恢复后的第二十九次三作业联合检查、新版 `unet_c1` 与旧版 Find_0 的新验证结果和 `BEST.ckpt` 状态，并把 heartbeat 恢复为每 5 小时。
Revision note 2026-07-31 16:40+08:00：记录第三十次三作业联合检查、新版 Find_1 的新最佳验证结果，以及三项训练当前不同的 `BEST.ckpt` 刷新状态。
Revision note 2026-07-31 21:40+08:00：记录第三十一次三作业联合检查、新版 `unet_c1` 的新最佳验证结果、旧版 Find_0 的本次验证结果，以及三项训练当前不同的 `BEST.ckpt` 刷新状态。
Revision note 2026-08-01 02:40+08:00：记录第三十二次三作业联合检查、两个 Find 的本次验证结果、新版 `unet_c1` 持续推进的证据，以及三项训练当前不同的 `BEST.ckpt` 刷新状态。

Revision note 2026-08-01 11:25+08:00：第三十三次联合检查发现旧版 Find_0 已于 07:03 停止。Job `321743` 的 allocation 仍为 `RUNNING`，但正式训练进程已经退出，`try_lock_321743` 与退出码 `137` 已落盘；正式错误日志显示真正触发点是 `src/train.py` 的学习率下降计数回调在 `on_validation_end` 中调用 `LightningModule.log()`，Lightning 明确拒绝该调用。新版 Find_1 与 `unet_c1` 当前仍分别推进到 `global_step=19727` 和 `24989`，损失、W&B、GPU 与 checkpoints 正常，但两个精确发布副本包含相同回调代码，因此在各自首次实际降低学习率时存在同一确定性失败风险。未执行 `scancel`、锁删除、目录覆盖、进程终止或服务器代码改写；恢复旧版 Find_0、以及是否在故障实际发生前重启两个新版训练，需要先确定 checkpoint 生命周期恢复方式，不能用仅加载模型参数的 `init_from` 冒充完整续训。

Revision note 2026-08-01 13:50+08:00：记录用户接受现有 CPC1 产物、改用配体区域 PR-AUC 选择已落盘 TOP checkpoint 的决定；记录 Find_0 从 `TOP_epoch_00_score_0.2843.ckpt` 启动无 warmup CPC2 的运行证据，以及下一版 Pocket_Plus 的监控指标、回调修正、双线端点和未部署边界。

Revision note 2026-08-01 14:25+08:00：记录 Find_0 CPC2 启动后的第一次短周期联合检查。Job `321743` 已推进到 `global_step=119`，训练损失均为有限值，最终配置与运行摘要共同确认学习率从 `5e-5` 直接开始、`warmup_steps=0`、`warmup_ratio=0`、全局批量 48；两个训练进程、两张 H200、在线 W&B 和唯一 `after_lock` 均正常，当前日志只有警告而没有错误栈。新版 Find_1 与 `unet_c1` 也继续健康运行到 `global_step=20009` 和 `25409`。短周期健康计数现为 1/5，heartbeat 继续每 30 分钟检查。

Revision note 2026-08-01 14:51+08:00：记录第二次短周期联合检查。Find_0 CPC2 推进到 `global_step=218`，训练总损失 `0.150973`，学习率仍为 `5e-5`，冻结代码、唯一运行标记、无 warmup 配置、两个训练进程、两张 H200、W&B 和日志均正常；新版 Find_1 与 `unet_c1` 分别推进到 `global_step=20069` 和 `25493`。三个 allocation 均只有 `after_lock`，短周期健康计数为 2/5。

Revision note 2026-08-01 15:20+08:00：记录第三次短周期联合检查。Find_0 CPC2 推进到 `global_step=326`，训练总损失 `0.180765`，无 warmup 配置、运行标记、冻结代码、W&B、两个训练进程和两张 H200 均正常，正式错误日志没有新增内容；新版 Find_1 与 `unet_c1` 分别推进到 `global_step=20135` 和 `25595`。三个 allocation 均只有 `after_lock`，短周期健康计数为 3/5。

Revision note 2026-08-01 15:50+08:00：记录第四次短周期联合检查。Find_0 CPC2 推进到 `global_step=443`，训练总损失 `0.119722`，冻结代码、唯一运行标记、无 warmup 配置、W&B、训练进程与错误日志均正常；两张 H200 最高占用约 142.812/143.771 GiB，余量很小但没有发生显存不足。新版 Find_1 与 `unet_c1` 分别推进到 `global_step=20204` 和 `25703`。三个 allocation 均只有 `after_lock`，短周期健康计数为 4/5。

Revision note 2026-08-01 16:20+08:00：记录第五次短周期联合检查。Find_0 CPC2 推进到 `global_step=545`，训练总损失 `0.115280`，冻结代码、唯一运行标记、无 warmup 配置、W&B、训练进程与错误日志均正常；两张 H200 最高占用约 143.068/143.771 GiB，余量很小但没有发生显存不足。新版 Find_1 与 `unet_c1` 分别推进到 `global_step=20270` 和 `25805`。三个 allocation 均只有 `after_lock`，连续健康计数达到 5/5，`adaligand-stage1` heartbeat 已改为每 5 小时检查。

Revision note 2026-08-01 21:26+08:00：记录恢复五小时周期后的第一次联合检查。Find_0 CPC2 推进到 `global_step=1334`，完成首次正式验证并保存 `TOP_epoch_00_score_0.1432.ckpt` 与 `last.ckpt`；验证总损失为 `0.143169`，受体区域与配体区域 PR-AUC 为 `0.740554` 和 `0.675477`。新版 Find_1 推进到 `global_step=20945`；`unet_c1` 摘要停在 `26303`，但训练进程、H100、W&B 事件文件和内部日志持续更新到 21:26。三个 allocation 均只有 `after_lock`，没有当前训练错误或显存不足，heartbeat 保持每 5 小时检查。

Revision note 2026-08-01 23:10+08:00：记录用户依据 W&B 曲线停止 Find_0 CPC2、取消本轮 Find_1 CPC2，并把 heartbeat 收窄为只监控 Find_1 CPC1 与 `unet_c1`；明确这次决定不修改或删除 CPC2 代码与能力，并写入只读服务器核验结果。

Revision note 2026-08-02 02:40+08:00：记录监控范围收窄后的第一次两作业检查；两项 CPC1 的冻结代码、配置、进程、W&B、损失、指标、checkpoint、锁和 GPU 均符合各自契约，heartbeat 继续每 5 小时触发。

Revision note 2026-08-02 07:42+08:00：记录第二次两作业五小时检查；Find_1 与 `unet_c1` 分别推进到 `global_step=22088` 与 `27620`，两项运行身份、配置、损失、指标、W&B、GPU、checkpoint 和锁继续符合契约。`unet_c1` 摘要暂缓刷新，但主进程、W&B 二进制事件文件、内部日志和 H100 计算持续活动，没有停滞证据。

Revision note 2026-08-02 17:38+08:00：记录第三次两作业五小时检查；Find_1 与 `unet_c1` 分别推进到 `global_step=23171` 与 `29060`，完成新一轮验证并各自保存新的 TOP 与 last checkpoint。两项运行身份、配置、损失、指标、W&B、GPU 和锁继续符合契约。

Revision note 2026-08-22 09:49+08:00：记录 Job `350185` 的单卡预验证、H100 队列触发、Job `351295` 的双卡提交、旧任务取消、`hnode01` 短暂 `MIXED+DRAIN` 后恢复，以及新版 Find_1 以学习率 `1.0e-4` 完成双 rank 启动验收并推进到 `global_step=5` 的证据。

Revision note 2026-08-22 15:18+08:00：记录 Job `351295` 的第一次正式 validation、四项主要 PR-AUC、首个 TOP 与 last checkpoint，以及验证后两卡继续训练且当前错误扫描为空的证据。

Revision note 2026-08-22 23:54+08:00：记录第二次正式 validation、配体体素 PR-AUC 提高到 `0.556626`、新 TOP/last checkpoint，以及 `BEST.ckpt` 刷新为上一最高分的证据。

Revision note 2026-08-23 04:00+08:00：记录第三次正式 validation、配体体素 PR-AUC 提高到 `0.565112`、新 TOP/last checkpoint，以及 `BEST.ckpt` 刷新为上一最高分的证据。

Revision note 2026-08-23 10:09+08:00：记录第四次正式 validation、未产生新最高分的结果、新 TOP/last checkpoint，以及 `BEST.ckpt` 已刷新到当前最高 `0.5651` 的证据。

Revision note 2026-08-23 15:49+08:00：记录用户因学习率偏大而授权 Job `351295` 通过 `kill_lock` 原 allocation 重启；冻结第 1 次执行的历史结果，并记录第 2 次执行的发布、launch、最终参数、双 rank、W&B 与首批稳定训练步证据。

Revision note 2026-08-23 21:16+08:00：记录 Job `351295` 第 2 次执行的首次正式 validation、六项 PR-AUC、首批 TOP/last checkpoint，以及验证后继续训练且当前错误扫描为空的证据。
