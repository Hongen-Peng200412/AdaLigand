# 计划执行映射

本文件是 AdaLigand 的 plan-centric 文档索引，用来说明计划书、执行日志、代码/契约文档和开放问题之间的关系。它不是决策日志、产物清单或详细漂移记录；这些内容应放在 `文档/exec_plan/`、代码旁契约 README 或 Git 历史中。

## 文档分层与文件夹语义

- **规划文档 `文档/规划文档/`** = 内容已稳的 clean spec；**讨论 `文档/讨论/`** = 内容仍在演进的规划稿（中性名，不含"过时/未成熟"含义）。**成熟度以本 mapping 的「位置·状态」列为准**；一份文档 churn 停下后再搬进 `规划文档/` 并翻状态。
- **执行日志 `文档/exec_plan/`**（过去时，记实现/决策/验证）、**契约 README**（代码旁，记当前接口现实）、**本 mapping**（meta 层，链接一切）。
- **跨文档边界"谁 owns 谁"就地声明在各文档头部**（每份文档头写清"我 owns X、不 owns Y、权威层级"）；本 mapping 只做索引与关系，**不承载边界规则正文**，不做决策日志/产物清单。`AGENTS.md` 只保留一行指向本文的指针，不承载易变内容。

## 总览

| Plan | Domain | 位置·状态 | Implementation Coverage | Primary Code / Contract | Open Threads |
| --- | --- | --- | --- | --- | --- |
| `文档/规划文档/数据处理_v2.md` | 数据 Stage A–G（整图/occurrence 级） | 规划文档·当前主规格 | 新 C 与 D–G 已实现；Stage C v4、22,386 全量 C、ABC gate 与正式 D/E 已完成，DE→F 三路独立审计通过。Stage F scratch v4 的 inventory、双新鲜进程门、零删除 audit、journal/fsync apply 与独立定向验收已闭合；正式 F 与尾段补算保留原 allocation/after+try，等待精确同步、远端全套和顺序恢复。E3 半体素修复已在本地直接复用 Pocket vendored 体素中心并升级 schema v3；`e90fccc` 删除自研候选边界且 300/300 oracle 零差异，`1780942` 窄修 frame preflight，`7202bc9` 冻结 22,309 目标 runner；最新本地全套 324 passed/10 platform skips，尚未完成远端/真实 oracle/正式迁移。Stage G 唯一 map-level schema v2 已实现/测试，316117 仅等待 analyze | `Data_Preprocessing/Ori_Data/`；契约 README `code/readme.md`；MRC lineage `code/mrc_pocket_legacy.source.json`；E3 voxel lineage `code/voxel_gt_pocket_legacy.source.json` | 按 `318350→316116` 顺序恢复 F、完成全量终态/质量和 exclusion 审计；并行完成 E3 远端验证、三路独立科学审计、真实 Pocket oracle、冻结迁移/release gate；随后 G analyze、正式分布、schema v2 数值配置与最终 QC |
| `文档/讨论/BOX-level数据契约.md` | BOX 四层存储 + CLG/Global Proposal/共享特征/selection/refinement 契约 | 讨论·核心逻辑已收敛 | 未开工（依赖 Stage1 重训） | 尚无代码；本文是盘上 schema 唯一权威 | ragged 物理容器 / box_index 形式 / feature-source 出口 / pocket 与 P membership 数值 |
| `文档/讨论/模型总规划_v2.md` | Stage2/3 **是什么**（任务/损失/两阶段课程/监督语义 §5.3·§8） | 讨论·接口已同步·待清理转正 | 未开工 | 尚无代码 | selection run、稀疏 PP 采样和密度 U-Net 接口待实现验证；回顾字样待清理 |
| `文档/讨论/Stage2_Stage3_迭代运算设计_讨论.md` | Stage2/3 **怎么算**（算子/批维/块/featurizer 契约/**监督全清单 §12**） | 讨论·收敛 | 未开工 | 尚无代码 | 读取 selection run、公共密度 U-Net 调制器和 CPU 闸待实现 |
| `文档/规划文档/Stage1训练与多阈值推理.md` | Stage1 基础训练 + 整图概率 + 组件森林/CLG + selector/反链 + 局部物化 | 规划文档·当前主规格 | 未开工（数据就绪后第一优先） | Pocket_Plus 分割主干 + 待新增 AdaLigand 推理/selector 模块；产物落 BOX 契约 | calibration 数值、feature 出口、物理 IO 与 selected-final 是否生产化 |

## 各 Plan

### `文档/规划文档/数据处理_v2.md`

- **Scope**：数据处理主规格，整图/occurrence 级产物（Stage A–G），不切 BOX。**新增边界**：也 owns 身份级/受体级**预计算标签**（配体全局描述子、49 维受体特征、centroid_atom、受体键表）——即"标签定义随生产方"（Q3）里预计算那一半。
- **Status**：旧 A–C 已完成一次正式运行；新 C 与 D–G 已实现。2,156-source repair、14-PDB 特定 rebuild、原 run id 全量 C、ABC gate 与正式 D/E 已完成；`316115` 为 `COMPLETED 0:0`，DE→F 的 status/gate、E artifact 风险分层和 exclusion/frame-mismatch 三路审计通过。Stage F scratch v4 已完成 inventory、双新鲜进程门、零删除 audit、journal/fsync apply 与独立定向验收；`316116/318350` 保留原 allocation 和精确 `after+try`，等待最新代码精确同步、远端全套及顺序恢复。E3 半体素修复由 `75d8f42/92fc2e8` 建立 Pocket vendored 中心、schema v3 与 E3 专用压缩原子覆盖；`e90fccc` 删除解析 bbox/`searchsorted` 并取得 300/300 完整网格 oracle 零差异，`1780942` 将生产 frame preflight 窄修为 Pocket 物理 BOX，`7202bc9` 增加冻结 22,309 目标、全局 partition claim 与独立 release gate 的 runner。最新 HEAD 本地全套 324 passed/10 个平台条件 skip；远端 Linux、三路独立科学审计、真实多图 Pocket oracle 与 22,309-ID 独立迁移仍待执行，77 个旧 known failure 不在迁移集合。Stage G 唯一 map-level schema v2 已实现并通过全套测试，`316117` 仍保持依赖等待和 analyze-only。
- **Execution Log**：`文档/exec_plan/数据下载与解析.md`（历史 A–C 实现与验证）；`文档/exec_plan/A-G数据流水线实现与全量运行.md`（implements/updates A–G，动态执行中）。
- **Contract**：`Data_Preprocessing/Ori_Data/code/readme.md`（Stage A–G 产物接口，含配体/口袋逐原子 Q 与聚合，冷读可懂）。
- **Open Threads**：以当前 A–G ExecPlan 的 `Progress` 为准；scratch v4 回收证据已闭合，下一步按 `318350→316116` 顺序恢复，再做 F 全量终态/质量及五条 Stage F exclusion 传播审计、F→G analyze 和最终独立 QC。E3 支线并行完成远端 Linux 全套、上下游对齐/Pocket 原法/产物差异三路独立科学审计、真实多图 Pocket 端到端比较、22,309-ID 冻结迁移及独立 release gate；旧 E status/`de_release`/exclusion 始终只读。未使用的 `mrc.py::grid_world_bounds` 留作 P2 维护收口，不阻断迁移。正式 G 分布后仍需用显式 schema v2 数值配置另行运行 filter/生成 `keep_list`，完成后回填最终 coverage 与证据报告。

### `文档/讨论/BOX-level数据契约.md`

- **Scope**：整图级产物 → BOX 级样本之间的盘上契约唯一权威，包括四层存储、BOX 身份、proposal/CLG、Group-parent 共享 voxel/P/receptor 表、GT overlap、selector score、selection result、可选 refinement、三档和模型对接。**owns 逻辑字段、shape、dtype 与追溯关系**；不 owns 候选算法、网络或反链目标。
- **Status**：核心逻辑已收敛，实现未开工；物理容器、feature-source 出口和少量空间规则仍需 profiling/calibration。
- **Related**：上游 = 数据处理_v2；产物来源 = `文档/规划文档/Stage1训练与多阈值推理.md`；下游消费 = 模型总规划 / 迭代运算。

### `文档/讨论/模型总规划_v2.md`

- **Scope**：Stage2/3 是什么——任务定义、双覆盖矩阵 + 对称 O、身份内匈牙利、两阶段课程、损失语义（§5.3/§8）。**监督全清单（含各辅助监督的挂点与标签来源指针）在迭代运算 §12**，不在本文。
- **Status**：Stage1/BOX 接口已同步；转正前仍需清理回顾字样（“替旧 A'/B'”等），并在实现期验证 selection run、稀疏 PP 采样和密度 U-Net 接口。
- **Related**：怎么算见迭代运算设计；盘上数据见 BOX 契约。

### `文档/讨论/Stage2_Stage3_迭代运算设计_讨论.md`

- **Scope**：Stage2/3 怎么算——算子词典、批维契约、Block 栈、chunk 策略、Emap2lig 复用。**featurizer 契约（§4.1）owns 运行时标签定义、指定 `selection_run_id` 的读取装配与 CPU 预算闸**；候选打分和反链选择由正式 Stage1 计划负责。**监督全清单 §12 owns“哪个模块 + 什么损失 + 标签来源指针”**。
- **Status**：收敛。
- **Related**：是什么见模型总规划；盘上数据见 BOX 契约。

### `文档/规划文档/Stage1训练与多阈值推理.md`

- **Scope**：Pocket_Plus Stage1 的新数据训练适配、训推共用 Dataset/DataLoader、完整图概率组装、micro-$F_\alpha$ 阈值标定、26-连通组件森林、CLG 确定性枚举、Group-parent 局部物化、Global Proposal selector、树上反链优化与可选 selected-final 精修。它还定义 selector/Stage2/Stage3 共用的可选轻量密度 U-Net 调制接口。
- **Status**：核心算法与接口已收敛，未实现；`min/max voxels`、PDB cap、网络宽度、pocket/P membership 和 selected-final 生产化由 calibration/消融冻结。
- **Related**：上游 = 数据处理_v2 与其 code-near README；存储 = BOX-level 数据契约；下游 = 模型总规划与 Stage2/Stage3 迭代运算。原 `文档/讨论/Stage1_Find_接缝与推理重写.md` 只保留转正指针。

## Reconciliation Trigger

当用户明确说"收口""回填计划""审计漂移"或等价表达时，agent 必须先读取相关计划书、执行日志、代码和本 mapping，向用户总结良性、中性、恶性和未完成差异，并请求用户确认。得到允许后，才可以回写计划书、执行日志、mapping 或契约 README。改动一份文档的成熟度（讨论 ↔ 规划文档）时，同步翻本表状态列。
