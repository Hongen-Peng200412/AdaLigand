# 计划执行映射

本文件是 AdaLigand 的 plan-centric 文档索引，用来说明计划书、执行日志、代码/契约文档和开放问题之间的关系。它不是决策日志、产物清单或详细漂移记录；这些内容应放在 `文档/exec_plan/`、代码旁契约 README 或 Git 历史中。

## 文档分层与文件夹语义

- **规划文档 `文档/规划文档/`** = 内容已稳的 clean spec；**讨论 `文档/讨论/`** = 内容仍在演进的规划稿（中性名，不含"过时/未成熟"含义）。**成熟度以本 mapping 的「位置·状态」列为准**；一份文档 churn 停下后再搬进 `规划文档/` 并翻状态。
- **执行日志 `文档/exec_plan/`**（过去时，记实现/决策/验证）、**契约 README**（代码旁，记当前接口现实）、**本 mapping**（meta 层，链接一切）。
- **跨文档边界"谁 owns 谁"就地声明在各文档头部**（每份文档头写清"我 owns X、不 owns Y、权威层级"）；本 mapping 只做索引与关系，**不承载边界规则正文**，不做决策日志/产物清单。`AGENTS.md` 只保留一行指向本文的指针，不承载易变内容。

## 总览

| Plan | Domain | 位置·状态 | Implementation Coverage | Primary Code / Contract | Open Threads |
| --- | --- | --- | --- | --- | --- |
| `文档/规划文档/数据处理_v2.md` | 数据 Stage A–G（整图/occurrence 级） | 规划文档·当前主规格 | 新 C 与 D–G 已实现；Stage C v4、22,386 全量 C、ABC gate 与 Stage D 已完成。Stage E 全量状态已写出，DE gate 因 18 个可恢复工程失败和 1 个待决 map/model frame mismatch 保留在精确 try-lock；F/G 未释放 | `Data_Preprocessing/Ori_Data/`；契约 README `code/readme.md`；MRC lineage `code/mrc_pocket_legacy.source.json` | E 工程恢复与 2zhc 契约决定；随后 F→G、正式分布与最终 QC |
| `文档/讨论/BOX-level数据契约.md` | BOX 四层存储 + blob 依赖标签 | 讨论·收敛演进中 | 未开工（阻塞于 Stage1 重训 + D–G） | 尚无代码；本文即落盘 schema 唯一权威 | ragged 物理格式 / box_index 形式 / 阈值 ρτ / over-save 下限 |
| `文档/讨论/模型总规划_v2.md` | Stage2/3 **是什么**（任务/损失/两阶段课程/监督语义 §5.3·§8） | 讨论·收敛·待清理转正 | 未开工 | 尚无代码 | 回顾字样待清理再转正 |
| `文档/讨论/Stage2_Stage3_迭代运算设计_讨论.md` | Stage2/3 **怎么算**（算子/批维/块/featurizer 契约/**监督全清单 §12**） | 讨论·收敛 | 未开工 | 尚无代码 | featurizer 契约（运行时标签 / blob 选择 / CPU 闸）待落实 |
| `文档/讨论/Stage1_Find_接缝与推理重写.md` | Stage1（Find）接缝集成（模型本体在 Pocket_Plus） | 讨论·即将开工 | 未开工（数据就绪后第一优先） | 模型 = Pocket_Plus 仓库；产物落 BOX 契约 | 二阶段推理 / CPU 并行 / blob 打分与筛选 / CPC 取消 / 落盘 schema |

## 各 Plan

### `文档/规划文档/数据处理_v2.md`

- **Scope**：数据处理主规格，整图/occurrence 级产物（Stage A–G），不切 BOX。**新增边界**：也 owns 身份级/受体级**预计算标签**（配体全局描述子、49 维受体特征、centroid_atom、受体键表）——即"标签定义随生产方"（Q3）里预计算那一半。
- **Status**：旧 A–C 已完成一次正式运行；新 C 与 D–G 已实现。2,156-source repair、14-PDB 特定 rebuild、原 run id 全量 C、ABC gate 和 Stage D 已完成。MRC 放行证据均通过；Stage E 已写 22,386 行完整状态，当前在 316115 的精确 try-lock 内恢复 18 个工程失败，并等待 2zhc frame mismatch 的显式科学决定，F/G 尚未释放。
- **Execution Log**：`文档/exec_plan/数据下载与解析.md`（历史 A–C 实现与验证）；`文档/exec_plan/A-G数据流水线实现与全量运行.md`（implements/updates A–G，动态执行中）。
- **Contract**：`Data_Preprocessing/Ori_Data/code/readme.md`（Stage A–G 产物接口，含配体/口袋逐原子 Q 与聚合，冷读可懂）。
- **Open Threads**：以当前 A–G ExecPlan 的 `Progress`、DE→F→G 和最终独立 QC 为准；完成后回填最终 coverage 与证据报告。

### `文档/讨论/BOX-level数据契约.md`

- **Scope**：整图级产物 → BOX 级样本之间的落盘契约唯一权威（四层存储、blob 依赖标签、三档、box_type 集合、四模型对接）。**owns 存储**（dtype/布局）；不 owns blob 产法（Stage1）/ 选择（featurizer）。
- **Status**：收敛演进中；实现未开工，阻塞于 Stage1 重训 + 数据 D–G。
- **Related**：上游 = 数据处理_v2；产物来源 = Stage1（`Stage1_Find_接缝与推理重写.md`）；下游消费 = 模型总规划 / 迭代运算。

### `文档/讨论/模型总规划_v2.md`

- **Scope**：Stage2/3 是什么——任务定义、双覆盖矩阵 + 对称 O、身份内匈牙利、两阶段课程、损失语义（§5.3/§8）。**监督全清单（含各辅助监督的挂点与标签来源指针）在迭代运算 §12**，不在本文。
- **Status**：收敛；转正前需清理回顾字样（"替旧 A'/B'"等）。
- **Related**：怎么算见迭代运算设计；盘上数据见 BOX 契约。

### `文档/讨论/Stage2_Stage3_迭代运算设计_讨论.md`

- **Scope**：Stage2/3 怎么算——算子词典、批维契约、Block 栈、chunk 策略、Emap2lig 复用。**featurizer 契约（§4.1）owns 运行时标签定义（图距离 / blob 形状）+ CPU 预算闸 + "用哪些 blob"选择旋钮**；**监督全清单 §12 owns"哪个模块 + 什么损失 + 标签来源指针"**。
- **Status**：收敛。
- **Related**：是什么见模型总规划；盘上数据见 BOX 契约。

### `文档/讨论/Stage1_Find_接缝与推理重写.md`

- **Scope**：Stage1 与 AdaLigand 之间的接缝/集成规格——换新数据/契约（模型不变）、推理重写（CPU 并行 + 二阶段中心化推理）、打分与二次筛选（存/用分离）、裁剪（CPC 取消、多分类头砍）。**owns 产法/语义**；模型架构在 Pocket_Plus 仓库、存储在 BOX 契约、选择在 featurizer。
- **Status**：即将开工（数据侧就绪后第一优先）。
- **Related**：模型本体 = Pocket_Plus（另一项目仓库）；产物落 BOX 契约第 3 层；下游 = Stage2/3。

## Reconciliation Trigger

当用户明确说"收口""回填计划""审计漂移"或等价表达时，agent 必须先读取相关计划书、执行日志、代码和本 mapping，向用户总结良性、中性、恶性和未完成差异，并请求用户确认。得到允许后，才可以回写计划书、执行日志、mapping 或契约 README。改动一份文档的成熟度（讨论 ↔ 规划文档）时，同步翻本表状态列。
