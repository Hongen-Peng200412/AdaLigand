# 计划执行映射

本文件是 AdaLigand 的 plan-centric 文档索引，用来说明计划书、执行日志、代码/契约文档和开放问题之间的关系。它不是决策日志、产物清单或详细漂移记录；这些内容应放在 `文档/exec_plan/`、代码旁契约 README 或 Git 历史中。

## 文档分层与文件夹语义

- **规划文档 `文档/规划文档/`** = 内容已稳的 clean spec；**讨论 `文档/讨论/`** = 内容仍在演进的规划稿（中性名，不含"过时/未成熟"含义）。**成熟度以本 mapping 的「位置·状态」列为准**；一份文档 churn 停下后再搬进 `规划文档/` 并翻状态。
- **执行日志 `文档/exec_plan/`**（过去时，记实现/决策/验证）、**契约 README**（代码旁，记当前接口现实）、**本 mapping**（meta 层，链接一切）。
- **跨文档边界"谁 owns 谁"就地声明在各文档头部**（每份文档头写清"我 owns X、不 owns Y、权威层级"）；本 mapping 只做索引与关系，**不承载边界规则正文**，不做决策日志/产物清单。`AGENTS.md` 只保留一行指向本文的指针，不承载易变内容。

## 总览

| Plan | Domain | 位置·状态 | Implementation Coverage | Primary Code / Contract | Open Threads |
| --- | --- | --- | --- | --- | --- |
| `文档/规划文档/数据处理_v2.md` | 数据 Stage A–G（整图/occurrence 级） | 规划文档·当前主规格 | 正式 run `adaligand_ag_20260711T154658` 已按原 DAG `316114→316115→316116→316117` 全部 `COMPLETED 0:0`。C 22,386 release-ready；D/E/F 与 E3 均闭合。F raw status 为 16,944 skipped、5,323 success、74 known、45 unknown；19 条原生 exclusion 与 45 条精确 waiver 分离。G analyze 状态为 22,251 success、90 known、45 raw unknown，生成 662,078 条候选；独立审计 summary SHA-256 `3a2fa87e…9d00` 重算集合和分布通过，`keep_list` 缺失。A–G 维护分支已经完成目录整理、冷读契约、Windows `297 passed, 4 skipped`、Linux `301 passed`、真实 Chimera/MapQ/MRC 验证、重构前后代表产物比较、依赖闭合的学习历史和独立审计；最终审计为 `PASS`，分支尚未推送 | `Data_Preprocessing/Ori_Data/adaligand_preprocessing/`；契约 README `Data_Preprocessing/Ori_Data/README.md`；历史执行记录 `文档/exec_plan/A-G数据流水线实现与全量运行.md`；维护 ExecPlan `文档/exec_plan/A-G数据管线维护与行为等价整理.md`；MRC/E3 来源清单位于 `adaligand_preprocessing/geometry/legacy/*.source.json` | A–G analyze-only 无开放生产项。schema v2 数值 filter/`keep_list` 须由用户看正式分布后显式启动；维护任务已经完成且不改变科学或产物契约，只有三条本地分支的非强制推送等待原任务另行确认 |
| `文档/讨论/BOX-level数据契约.md` | Stage1 训练预定位、完整图概率、组件森林/CLG、三类 centered BOX 与 selector/selection 盘上契约 | 讨论·当前 Stage1 盘上契约 | 代码已实现，并通过完整 CPU 回归与真实 A–G/GPU smoke；尚未生产正式全量产物 | Pocket_Plus 分支 `codex/adaligand-stage1` 的 `src/artifacts/`、`src/inference/`、`src/component_lineage/`、`src/selector/`；本文仍是 BOX 级盘上契约唯一权威 | 正式运行后实测压缩体量、随机读取与 25 TB 预算 |
| `文档/讨论/模型总规划_v2.md` | Stage2/3 **是什么**（任务/损失/两阶段课程/监督语义 §5.3·§8） | 讨论·Stage1 接口待同步 | 未开工 | 尚无代码 | 将旧 selection 寻址与公共密度调制描述同步到固定 Stage1 路径、consumer-owned DensityMUNetLite 与 V5+D；回顾字样待清理 |
| `文档/讨论/Stage2_Stage3_迭代运算设计_讨论.md` | Stage2/3 **怎么算**（算子/批维/块/featurizer 契约/**监督全清单 §12**） | 讨论·核心算子收敛·Stage1 接口待同步 | 未开工 | 尚无代码 | 将旧 selection 寻址和公共密度 U-Net 接口同步到固定 Stage1 路径、residual_swiglu/V5+D；CPU 闸待实现 |
| `文档/规划文档/Stage1训练实现计划.md` | `Find_0`/`Find_1` CPC1/CPC2 与 `unet_c1` 的冻结数据、统一 Dataset、训练和 checkpoint 接缝 | 规划文档·当前训练实施规格 | 代码已实现；363 项 CPU 回归、真实 `10ad` Dataset 与单张 A100 三 producer forward/backward/严格 voxel-only 等价 smoke 均通过；未提交正式训练 | 复用 Pocket_Plus 通用主干/wrapper；新增 AdaLigand Dataset/Collator、五套配置、loss/CPC 接缝与正式 loader | Stage G `keep_list` 冻结后做 batch preflight；正式训练等待用户明确授权 |
| `文档/exec_plan/Find辅助监督与新版Find1端到端实施.md` | Find_1 与 `unet_c1` 的蛋白、核酸原子辅助分类和配体距离回归；完整代码快照与新版正式训练 | 执行记录·进行中 | 本地实现与独立审计完成；AdaLigand Linux 全套 306 项通过；距离生产形成完整 22,386 条状态和 16 个 PDB 的冻结训练排除清单；首轮正式训练错误继承 `warmup_ratio=0.025`，现已保留 Job `321107`、`321540` 的 allocation 并直接重启 `warmup_ratio=0.005` 的唯一正式运行；连续 5 次短期检查和三次三作业联合检查均健康；新版 Find_1 与 `unet_c1` 均已完成首次正式验证并保存 TOP 与 last checkpoints，旧版 Find_0 的验证与 checkpoints 持续正常，两个 Find 尚未进入 CPC2；Find_1 精确发布提交为 `1909267`，Pocket_Plus 默认配置纠正提交为 `aa1b9e9` | AdaLigand `adaligand_preprocessing/labels/ligand_distance.py`；Pocket_Plus `src/auxiliary_supervision.py`、Dataset、模型、wrapper、artifact、inference 与 Selector 接口；服务器提交使用提交哈希命名的精确发布副本；三作业入口链路见 `talk/检查记录.md` | 每 5 小时分别按自身契约核对 Job `321107`、`321540`、`321743`，重点检查新版辅助指标趋势、两条 CPC1→CPC2 链、新版 `unet_c1` 后续验证以及 Find_0 的 H200 显存余量；随后完成 Git 学习线和最终收口 |
| `文档/规划文档/Stage1训练与多阈值推理.md` | 三 producer 完整图概率、阈值、组件森林/CLG、F1/CLG/Selected centered、selector 与反链 | 规划文档·当前 Stage1 推理与选择规格 | full-map/calibration/forest/CLG/centered/selector/反链代码与边界回归已实现，A100 最小闭环 smoke 通过；未执行正式全量生产 | Pocket_Plus 分支 `codex/adaligand-stage1` 的新 `src/inference/`、`src/evaluation/`、`src/component_lineage/`、`src/artifacts/` 与 `src/selector/`；盘上字段由 BOX 契约规定 | 已由 673,364 个 occurrence 得到 `Q95=682`、冻结 `max_voxels=1023`；正式 calibration/full production 与真实 Selector 训练待后续运行 |

## 各 Plan

### `文档/规划文档/数据处理_v2.md`

- **Scope**：数据处理主规格，整图/occurrence 级产物（Stage A–G），不切 BOX。**新增边界**：也 owns 身份级/受体级**预计算标签**（配体全局描述子、49 维受体特征、centroid_atom、受体键表）——即"标签定义随生产方"（Q3）里预计算那一半。
- **Status**：A–G analyze-only 正式全量运行已完成。`316114/316115/316116/316117` 全部 `COMPLETED 0:0`；E3 为 22,309/22,309，正式 F 为 22,386 行 raw 状态，成功 `f_release` 绑定 waiver SHA `805f4c49…0a04`。G 只运行 analyze，形成 22,386 行状态、662,078 条候选和质量分布，未写 `keep_list`。独立 job `321031` 重算 known/waived/eligible、候选集合和所有分位数通过，summary SHA-256 `3a2fa87e…9d00`。
- **Execution Log**：`文档/exec_plan/数据下载与解析.md`（历史 A–C 实现与验证）；`文档/exec_plan/A-G数据流水线实现与全量运行.md`（历史 A–G 实现与正式运行）；`文档/exec_plan/A-G数据管线维护与行为等价整理.md`（当前代码结构、README、脚手架和双线 Git 维护）。
- **Contract**：`Data_Preprocessing/Ori_Data/README.md`（Stage A–G 产物接口，含配体/口袋逐原子 Q 与聚合，可在没有运行历史的情况下直接查字段）。
- **Open Threads**：A–G analyze-only 无开放生产项。后续若要生成 `keep_list`，必须由用户依据正式分布另行给出显式 schema v2 数值配置。代码目录重组、run-specific 脚手架物理归档、重复 process-audit validator 合并及 `mrc.py::grid_world_bounds` 清理由当前维护 ExecPlan 跟踪；维护必须先冻结基线并通过完整回归和真实小规模验证。

### `文档/讨论/BOX-level数据契约.md`

- **Scope**：整图级产物 → Stage1 训练/推理样本之间的盘上契约唯一权威。训练侧保存紧凑 center/bias/context 预定位池；推理侧按 `stage1_model_name/split/pdb_id/centered_role/centered_box_index` 固定寻址，保存 probability、component forest/CLG、F1/CLG/Selected 三类 centered BOX、V/P/A 与 selector score/selection。**owns 逻辑字段、shape、dtype、ragged 关系与 `_COMPLETE` 原子完成语义**；不 owns 算法、网络或 loss。
- **Status**：契约已局部修订为固定 producer/split/PDB/role 寻址和每 PDB/role 一个聚合 `np.savez_compressed`，对应代码、冷读校验器和边界回归均已实现；363 项 CPU 回归与单张 A100 最小闭环通过。学习特征 FP16、概率/几何 FP32；正式全量产物尚未生产。
- **Related**：上游 = 数据处理_v2；产物来源 = `文档/规划文档/Stage1训练与多阈值推理.md`；下游消费 = 模型总规划 / 迭代运算。
- **Execution Log**：`文档/exec_plan/Stage1代码实现.md`（implements/updates 本契约，动态执行中）。

### `文档/讨论/模型总规划_v2.md`

- **Scope**：Stage2/3 是什么——任务定义、双覆盖矩阵 + 对称 O、身份内匈牙利、两阶段课程、损失语义（§5.3/§8）。**监督全清单（含各辅助监督的挂点与标签来源指针）在迭代运算 §12**，不在本文。
- **Status**：任务与监督核心仍有效，但 Stage1/BOX 接口需同步到本轮固定寻址、V5+D 与 consumer-owned DensityMUNetLite；转正前还需清理回顾字样（“替旧 A'/B'”等）。
- **Related**：怎么算见迭代运算设计；盘上数据见 BOX 契约。

### `文档/讨论/Stage2_Stage3_迭代运算设计_讨论.md`

- **Scope**：Stage2/3 怎么算——算子词典、批维契约、Block 栈、chunk 策略、Emap2lig 复用。**featurizer 契约（§4.1）owns 运行时标签定义、指定 `selection_run_id` 的读取装配与 CPU 预算闸**；候选打分和反链选择由正式 Stage1 计划负责。**监督全清单 §12 owns“哪个模块 + 什么损失 + 标签来源指针”**。
- **Status**：核心算子收敛；Stage1 输入寻址、多源前导融合和密度分支仍需按本轮三件套同步。
- **Related**：是什么见模型总规划；盘上数据见 BOX 契约。

### `文档/规划文档/Stage1训练实现计划.md`

- **Scope**：迫切启动 `Find_0`、`Find_1` 与 `unet_c1` 的直接实施规格，包括 PDB 分组 split/BOX pool、`1:5:3`、Dataset 直接 core+8 Å、共同 point embed/core-only voxel scatter、二值 target、统一 Dataset/Collator、五套训练、BEST、CPC hook 和正式 wrapper loader。
- **Status**：计划已完成本轮局部修订，Dataset/Collator、五套配置、loss/CPC 接缝、正式 checkpoint loader 与严格 voxel-only 入口均已实现；363 项 CPU 回归、真实 `10ad` Dataset smoke 和单张 A100 三 producer forward/backward/逐元素等价 smoke 已通过。正式全量训练等待用户明确授权及 Stage G `keep_list`、split/BOX pool/validation selection 冻结；正式 batch size 仍须在目标训练硬件上 preflight。
- **Related**：上游 = 数据处理_v2 与 A–G 执行日志；盘上位置契约 = BOX-level 数据契约；训练完成后由 Stage1 多阈值计划承接完整图推理。
- **Execution Log**：`文档/exec_plan/Stage1代码实现.md`（implements/updates 本计划，动态执行中；本轮不提交正式训练）。

### `文档/exec_plan/Find辅助监督与新版Find1端到端实施.md`

- **Scope**：在既有 Find_1 训练主线上增加固定蛋白主链类别、固定核酸主链类别和体素中心到最近实际配体重原子的距离监督；同时扩大训练代码快照、取消正式 `voxel_final` 的固定 48 通道假设，并运行新版 Find_1 CPC1→CPC2。用户随后把同一辅助监督扩展到 `unet_c1`：保留 U-Net 骨干，只对齐最终体素特征、输出头、损失、学习率和训练排除清单。
- **Status**：用户已批准端到端实施。代码、本地与 Linux 全套、真实 `10ad`、历史快照补齐及严格 checkpoint 恢复已经通过；正式距离生产已形成完整 22,386 条状态，并把 5 个缺少密度与 11 个 90 秒补验超时 PDB 冻结为训练排除清单。Job `321540` 与 `321107` 已在不释放 GPU allocation 的条件下停止旧进程并启动 `warmup_ratio=0.005` 的唯一正式运行；新版 Find_1 与 `unet_c1` 均已完成首次正式验证并保存 TOP 与 last checkpoints，两个 Find 尚未进入 CPC2。Job `321743` 的旧版 Find_0 也纳入每 5 小时的独立契约检查。Git 学习线和正式训练收口尚未完成。
- **Related**：当前训练基础规格为 `文档/规划文档/Stage1训练实现计划.md`；A–G 产物字段由 `Data_Preprocessing/Ori_Data/README.md` 说明；正式体素与 centered 产物字段由 `文档/讨论/BOX-level数据契约.md` 说明。
- **Execution Log**：本文自身是本轮活动 ExecPlan；进度、决定、意外发现、验证证据和训练状态持续写回本文。

### `文档/规划文档/Stage1训练与多阈值推理.md`

- **Scope**：承接三个冻结 producer，规定完整图 `probability_map`、micro-$F_\alpha$ 阈值、26-连通 arborescence、树副本 `D(g)` 式 CLG 枚举、F1/CLG/Selected 三类 centered、V5+D、CCLN selector 与精确反链 DP。基础训练由并列训练计划负责，盘上字段由 BOX 契约负责。
- **Status**：科学算法、depth1/2 的 32/64 node cap、calibration-first 两阶段运行和部分产物可消费语义已收敛；full-map、calibration、forest/CLG、三类 centered、V5+D Selector 与精确反链 DP 均已实现并通过边界回归和单张 A100 最小闭环。全量 673,364 个 occurrence 的统计给出 `Q95=682`，正式 `max_voxels=1023`。正式 calibration/full production、真实 Selector 训练，以及 V5+D 与三种条件损失加权的实证比较尚未运行。
- **Related**：上游 = 数据处理_v2 与其 code-near README；存储 = BOX-level 数据契约；下游 = 模型总规划与 Stage2/Stage3 迭代运算。原 `文档/讨论/Stage1_Find_接缝与推理重写.md` 只保留转正指针。
- **Execution Log**：`文档/exec_plan/Stage1代码实现.md`（implements/updates 本计划，动态执行中）。

### `文档/规划文档/Stage1实现细节手册.md`

- **Role**：本轮 Stage1 三件套的低权重、特例性附录，不是独立 plan，也不改变项目文档范式。
- **Scope**：只保存 Pocket_Plus 代码锚点、推荐模块落点、AI/工程师工作顺序、伪代码、测试用例和可续跑编排示例；不能承载或改写主文档中的科学公式、字段和配置决定。
- **Precedence**：与三件套冲突时无条件服从三件套；实现漂移仍按 `AGENTS.md` 先审计。

## Reconciliation Trigger

当用户明确说"收口""回填计划""审计漂移"或等价表达时，agent 必须先读取相关计划书、执行日志、代码和本 mapping，向用户总结良性、中性、恶性和未完成差异，并请求用户确认。得到允许后，才可以回写计划书、执行日志、mapping 或契约 README。改动一份文档的成熟度（讨论 ↔ 规划文档）时，同步翻本表状态列。
