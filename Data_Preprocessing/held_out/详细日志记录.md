# Held-out 去冗余与测试集服务器详细日志

本文记录 AdaLigand held-out 序列目录、MMseqs2 去冗余和三个测试视图的服务器执行事实。科学规则与产物字段分别以 `文档/规划文档/held-out去冗余与测试集构建.md` 和 `Data_Preprocessing/held_out/README.md` 为准；本文只保存实际命令、Job、事件、观测结果与后续动作。

## 固定执行边界

- 服务器项目目录：`/home/penghongen/My_Project/AdaLigand`。
- 正式输入根：`/storage/penghongen/AdaLigand/Ori_Data`。
- 正式输出根：`/storage/penghongen/AdaLigand/held_out`。
- PDB coverage：`mode=or`、`threshold=0.5`。
- 两个数组步骤：各 12 个数组元素，每个元素申请 8 CPU。
- 正式代码保持冻结。只有运行证据明确证明存在代码 bug 时才考虑修改；普通失败、环境问题和少量样本异常先诊断并记录。
- 不向科学主逻辑增加失败率门控、完成标记门控或 SHA 校验。

## 四步状态

| 步骤 | 任务 | 当前状态 | Job |
| --- | --- | --- | --- |
| 1 | 12 分片序列目录构建 | 已完成并验收 | `366076` |
| 2 | 目录合并与 RCSB 官方 FASTA smoke | 已完成并验收 | `366090` |
| 3 | 12 分片 MMseqs2 比对 | 已完成并验收 | `366094` |
| 4 | 冗余关系、身份证、`full_test/test_0/test_1` 合并 | 已提交，监视中 | `366110` |

## 事件记录

### 2026-08-31 16:37:01 +08:00　开始服务器执行

- 用户授权同步并运行完整四步流程，要求持续记录每一步事件和重复触发的检查命令。
- 已完整读取项目服务器交互规范、AdaLigand `训练与运行/README.md`、安全同步脚本、四个 held-out 任务脚本和统一 SSH helper。
- 当前本地正式代码位于 `Learn/CUMULATIVE`，除本日志外工作区没有未提交内容；本日志在用户指定位置持续追加。
- 下一动作：只读探测服务器、Conda 环境、输入文件、输出根、MMseqs2 和 Slurm 状态。

### 2026-08-31 16:38:46 +08:00　只读服务器探测完成

- 触发方式：本机统一 SSH helper，严格使用已固定的服务器主机密钥。
- 远端探测命令：

```bash
set -u
hostname
date --iso-8601=seconds
test -e /home/penghongen/My_Project/AdaLigand
test -x /home/penghongen/anaconda3/envs/AdaLigand_stage1_py310/bin/python
test -f /storage/penghongen/AdaLigand/Ori_Data/raw/pair_list.jsonl
test -f /storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_3/split/pdb_split/held_out.json
/home/penghongen/anaconda3/envs/AdaLigand_stage1_py310/bin/python -c "import sys,numpy,scipy,gemmi; print(sys.version.split()[0],numpy.__version__,scipy.__version__,gemmi.__version__)"
test -x /home/penghongen/software/mmseqs2/18-8cc5c/bin/mmseqs
find /storage/penghongen/AdaLigand/held_out -maxdepth 2 -type f -printf "%P\n"
squeue -u penghongen -h -o "%i|%P|%j|%T|%M|%D|%C|%R"
sinfo -h -p cpu -o "%P|%a|%l|%D|%C"
```

- 主机为 `master`，服务器时间为 `2026-08-31T16:38:46+0800`。
- AdaLigand 项目、Conda Python、`pair_list.jsonl` 和 held-out PDB split 均存在。
- 环境版本为 Python 3.10.20、NumPy 2.2.6、SciPy 1.15.2、Gemmi 0.7.5。
- 预定路径 `/home/penghongen/software/mmseqs2/18-8cc5c/bin/mmseqs` 不存在；正式输出根尚不存在。
- CPU 分区可用，4 个节点合计 384 CPU，其中 249 CPU 空闲。当前用户已有其他 GPU Job，但没有 held-out CPU Job。
- 下一动作：在用户软件目录安装 MMseqs2 18-8cc5c，并先运行版本检查；不修改 Conda 环境或项目代码。

### 2026-08-31 16:39:33 +08:00　MMseqs2 安装源探测完成

- 只读命令检查了 CPU 指令集和两个候选下载地址：

```bash
lscpu | grep -m1 "^Flags"
curl --fail --location --silent --show-error --range 0-0 --output /dev/null --write-out "%{http_code}|%{url_effective}\n" https://github.com/soedinglab/MMseqs2/releases/download/18-8cc5c/mmseqs-linux-avx2.tar.gz
curl --fail --location --silent --show-error --range 0-0 --output /dev/null --write-out "%{http_code}|%{url_effective}\n" https://mmseqs.com/archive/18-8cc5c/mmseqs-linux-avx2.tar.gz
```

- CPU 明确支持 AVX2。
- MMseqs2 官方 GitHub release 的 `18-8cc5c` AVX2 归档返回 HTTP 206，可下载；`mmseqs.com/archive` 候选返回 404，弃用。
- 下一动作：只从官方 GitHub release 下载到 `/storage/penghongen/tmp`，解包到 `/home/penghongen/software/mmseqs2/18-8cc5c`，随后执行 `mmseqs version`。

### 2026-08-31 16:40:24 +08:00　第一次安装触发未到达服务器

- 本机 PowerShell 在调用统一 SSH helper 时把远端 `trap` 引号误解析成 helper 参数，命令以退出码 1 停止。
- 失败发生在 SSH 建连之前，服务器没有下载、创建、移动或删除任何文件。
- 这不是正式代码 bug，不修改项目代码。下一次触发改用本机 LF 临时脚本，通过 `-InputFile` 交给远端 `bash -s`。

### 2026-08-31 16:44:26 +08:00　MMseqs2 安装完成

- 触发命令：

```powershell
& "$env:USERPROFILE\.codex\tools\Invoke-ProjectSsh.ps1" -Command "bash -s" -InputFile "C:\Users\15919\AppData\Local\Temp\held-out-install-mmseqs.sh"
```

- 临时脚本从 MMseqs2 官方 GitHub release 下载 `18-8cc5c` AVX2 归档，使用 `/storage/penghongen/tmp/mmseqs-18-8cc5c.*` 作为临时目录，并安装到 `/home/penghongen/software/mmseqs2/18-8cc5c`。
- `/home/penghongen/software/mmseqs2/18-8cc5c/bin/mmseqs` 已存在且可执行，`mmseqs version` 返回成功。
- 远端安装临时目录由脚本退出清理；本机 LF 临时脚本也已删除。
- 没有修改项目代码、Conda 环境或正式输出根。
- 下一动作：运行项目自带的安全同步 `与服务器交互/sync_code.ps1`。该入口不删除远端 AdaLigand 项目目录；同步后只读核对四个 held-out 脚本和 Python 导入。

### 2026-08-31 16:49:24 +08:00　安全同步完成

- 本机触发命令：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File ".\与服务器交互\sync_code.ps1"
```

- 同步入口依次确认远端 `rsync`、保留并创建 AdaLigand 项目目录、移除远端项目内旧 `.git`、增量上传本地项目、统一权限并修正 `.sh/.sbatch` 行尾。
- 传输 15,940,916 字节，总项目文件规模 29,061,817 字节；命令退出码为 0。
- 本次使用安全同步，没有删除远端 AdaLigand 项目目录，也没有操作 `/storage/penghongen/AdaLigand/Ori_Data` 或正式输出根。
- 下一动作：在服务器当前项目目录运行 held-out 的完整本地测试、四个 shell 语法检查、Python CLI 导入与 MMseqs2 版本检查。

### 2026-08-31 16:50:34 +08:00　服务器提交前检查通过

- 远端触发命令：

```bash
cd /home/penghongen/My_Project/AdaLigand
source /home/penghongen/anaconda3/etc/profile.d/conda.sh
conda activate /home/penghongen/anaconda3/envs/AdaLigand_stage1_py310
export PYTHONPATH=/home/penghongen/My_Project/AdaLigand/Data_Preprocessing/held_out
python -m pytest Data_Preprocessing/held_out
bash -n 训练与运行/sh/held_out_catalog_array.sh
bash -n 训练与运行/sh/held_out_catalog_finalize.sh
bash -n 训练与运行/sh/held_out_mmseqs_array.sh
bash -n 训练与运行/sh/held_out_finalize.sh
python -m held_out_pipeline.cli --help
/home/penghongen/software/mmseqs2/18-8cc5c/bin/mmseqs version
```

- Linux 环境完整收集 21 项测试，结果为 `21 passed in 7.67s`；Gemmi mmCIF 测试不再跳过。
- 四个正式 shell 均通过语法检查，Python CLI 可导入，MMseqs2 版本命令成功。
- 没有发现正式代码 bug，因此不修改代码。
- 下一动作：提交步骤 1 的 12 元素 CPU 数组，每个元素 8 CPU，随后持续检查 Slurm 状态、allocation 日志和 12 份分片 summary。

### 2026-08-31 16:51:43 +08:00　步骤 1 已提交

- 远端提交命令：

```bash
cd /home/penghongen/My_Project/AdaLigand
bash 训练与运行/submit_task.sh --sh held_out_catalog_array.sh --resource cpu --cpus 8 --array 0-11 --time 04:00:00
```

- Slurm 返回 `Submitted batch job 366076`。
- 提交系统确认为 full 模式、CPU、12 个数组元素、每个元素 8 CPU、无 `pre_hold/after_hold`；任务真正开始前由既有运行系统冻结 release。
- 持续检查命令固定为：

```bash
squeue -j 366076 -r -h -o "%i|%T|%M|%C|%R"
sacct -j 366076 -n -P --format=JobIDRaw,State,ExitCode,Elapsed,AllocCPUS,NodeList
find /storage/penghongen/AdaLigand/held_out/stage1/shards -maxdepth 1 -type f -name "summary_*.json" -printf "%f\n"
```

- 下一动作：重复触发上述检查，直到 12 个数组元素全部进入终态，再读取每份 summary 和 allocation 错误日志。

### 2026-08-31 16:54:13 +08:00　步骤 1 第 1 次持续检查

- 触发了已记录的 `squeue`、`sacct` 和分片 summary 列举命令。
- `366076_0` 至 `366076_11` 共 12 个数组元素全部为 `RUNNING`，每个元素 8 CPU；数组分布在 `cnode01` 和 `cnode02`。
- 已运行约 2 分 33 秒，尚未生成 `summary_*.json`；当前没有失败或异常终态。
- 下一动作：继续等待并重复同一组检查命令。

### 2026-08-31 16:57:06 +08:00　步骤 1 第 2 次持续检查

- 再次触发同一组 `squeue`、`sacct` 和分片 summary 检查命令。
- 数组索引 9、10、11 已分别以 `COMPLETED/0:0` 结束，并生成 `summary_009.json`、`summary_010.json`、`summary_011.json`。
- 数组索引 0 至 8 继续运行，已运行约 5 分 30 秒；没有失败或异常终态。
- 下一动作：继续等待剩余 9 个元素，不提前提交步骤 2。

### 2026-08-31 16:58:10 +08:00　步骤 1 第 3 次持续检查

- 再次触发同一组 `squeue`、`sacct` 和分片 summary 检查命令。
- `squeue` 已无活动元素；12 个数组元素全部为 `COMPLETED/0:0`，运行时长为 5 分 17 秒至 7 分 00 秒。
- `summary_000.json` 至 `summary_011.json` 和 `catalog_000.jsonl` 至 `catalog_011.jsonl` 全部存在。
- 下一动作：读取全部 summary、逐分片 JSONL 和 12 个 allocation 日志，完成步骤 1 深验收。

### 2026-08-31 17:02:42 +08:00　步骤 1 深验收通过

- 读取命令覆盖 12 份 summary、12 份 catalog JSONL 和 Job `366076` 对应的 12 个 allocation 目录。allocation 的 `err` 仅含既有 release 创建或复用提示，没有 Python traceback、任务错误或非零退出。
- 临时只读审计通过标准输入运行，检查了 summary 数量、分片索引、`shard_count=12`、`workers=8`、每份 JSONL 行数与 `assigned_pdb_count`、全局 PDB 唯一性。审计脚本运行后已从本机临时目录删除，没有进入项目或正式逻辑。
- 汇总结果：

| 指标 | 结果 |
| --- | ---: |
| summary 数 | 12 |
| 分配 PDB 总数 | 22,386 |
| held-out PDB 总数 | 2,497 |
| catalog PDB 记录数 | 22,386 |
| polymer entity 总数 | 198,055 |
| 序列状态 | `ok=22,386` |

- 步骤 1 没有序列解析失败，正式代码不需要修改。
- 下一动作：提交步骤 2，合并目录、生成 FASTA，并运行 3 个真实 RCSB 官方 FASTA smoke。

### 2026-08-31 17:04:05 +08:00　步骤 2 已提交

- 远端提交命令：

```bash
cd /home/penghongen/My_Project/AdaLigand
bash 训练与运行/submit_task.sh --sh held_out_catalog_finalize.sh --resource cpu --cpus 8 --time 02:00:00
```

- Slurm 返回 `Submitted batch job 366090`；资源为 1 个 CPU task、8 CPU、最长 2 小时，无资源保留锁。
- 持续检查命令固定为：

```bash
squeue -j 366090 -h -o "%i|%T|%M|%C|%R"
sacct -j 366090 -n -P --format=JobIDRaw,State,ExitCode,Elapsed,AllocCPUS,NodeList
tail -n 40 /home/penghongen/Feedback/AdaLigand/allocations/366090/out
tail -n 40 /home/penghongen/Feedback/AdaLigand/allocations/366090/err
```

- 下一动作：重复触发上述检查；Job 完成后读取 `stage1/summary.json`、序列失败清单、官方 smoke、FASTA 数量和分片完整性。

### 2026-08-31 17:05:21 +08:00　步骤 2 第 1 次持续检查

- 触发了已记录的 `squeue`、`sacct` 和 allocation 尾部日志命令。
- Job `366090` 在 `cnode01` 上为 `RUNNING`，使用 8 CPU，已运行约 1 分 23 秒。
- `out` 和 `err` 暂无输出，没有失败迹象。
- 下一动作：继续等待并重复同一组检查命令。

### 2026-08-31 17:06:45 +08:00　步骤 2 第 2 次持续检查

- 再次触发同一组 `squeue`、`sacct` 和 allocation 尾部日志命令。
- Job `366090` 已为 `COMPLETED/0:0`，总运行时长 3 分 06 秒。
- `stage1/summary.json` 报告 22,386 个 PDB、198,055 个 polymer entity、14,017 个参考 PDB、2,497 个 held-out PDB；参考和 held-out 序列失败数均为 0，官方 FASTA smoke 为通过。
- allocation 的 `err` 仅含既有 release 复用提示，没有任务错误。
- 下一动作：对逐 entity smoke、JSONL 行数、FASTA 分片、query/target 身份关系和核酸 U/T 派生视图做只读深验收。

### 2026-08-31 17:12:44 +08:00　步骤 2 深验收通过

- 主要产物行数：`sequence_catalog.jsonl=198,055`、`pdb_sequence_status.jsonl=22,386`、`held_out_base.jsonl=2,497`、`held_out_sequence_failures.jsonl=0`。
- RCSB 官方 FASTA smoke 使用 `10ad`、`10fz`、`10ay`，覆盖 25 个 entity；三个 PDB 和全部 entity 的本地/官方沉积序列均相等。
- FASTA 数量与关系：

| FASTA 集合 | entity 数 |
| --- | ---: |
| protein target | 132,592 |
| nucleic target | 9,627 |
| 12 份 protein query 合计 | 17,660 |
| 12 份 nucleic query 合计 | 1,612 |

- 两类 query 均为 12 个非空分片，query identity 各自无重复且全部属于对应 target。
- 第一次用普通 `grep` 检查核酸 `U` 时误命中了 FASTA header；随后尝试的单行 `awk` 又在本机 SSH helper 参数解析阶段失败，均未写服务器。改用临时只读 Python 审计后确认：自然 RNA 序列保留 `U`，MMseqs2 核酸派生序列不含 `U`。临时脚本已删除。
- 没有发现正式代码 bug，不修改代码。
- 下一动作：提交步骤 3 的 12 元素 MMseqs2 CPU 数组，每个元素 8 CPU；持续检查两个 easy-search、TSV 和分片 summary。

### 2026-08-31 17:14:02 +08:00　步骤 3 已提交

- 远端提交命令：

```bash
cd /home/penghongen/My_Project/AdaLigand
bash 训练与运行/submit_task.sh --sh held_out_mmseqs_array.sh --resource cpu --cpus 8 --array 0-11 --time 12:00:00
```

- Slurm 返回 `Submitted batch job 366094`。
- 提交系统确认为 full 模式、CPU、12 个数组元素、每个元素 8 CPU、最长 12 小时、无资源保留锁。
- 持续检查命令固定为：

```bash
squeue -j 366094 -r -h -o "%i|%T|%M|%C|%R"
sacct -j 366094 -n -P --format=JobIDRaw,State,ExitCode,Elapsed,AllocCPUS,NodeList
find /storage/penghongen/AdaLigand/held_out/stage2/mmseqs -maxdepth 1 -type f -name "summary_*.json" -printf "%f\n"
find /storage/penghongen/AdaLigand/held_out/stage2/mmseqs -maxdepth 1 -type f -name "*.tsv" -printf "%f|%s\n"
```

- 下一动作：重复触发上述检查；同时只读查看 allocation 日志中的 MMseqs2 当前阶段，不修改命令或参数。

### 2026-08-31 17:15:17 +08:00　步骤 3 第 1 次持续检查

- 触发了已记录的 `squeue`、`sacct`、summary 和 TSV 检查命令。
- `366094_0` 至 `366094_11` 共 12 个数组元素全部为 `RUNNING`，每个元素 8 CPU，分布在 `cnode01` 和 `cnode02`；已运行约 1 分 15 秒。
- 当时尚未看到 summary 或最终 TSV，未发现失败终态。
- 随后只读查看两个代表性 allocation 日志时，数组索引 0 和 11 已先完成 protein 与 nucleic 两条命令并正常释放资源。
- 下一动作：立即再次触发同一组检查，确认其余元素的终态与文件。

### 2026-08-31 17:16:39 +08:00　步骤 3 第 2 次持续检查

- `squeue` 已无活动元素；12 个数组元素全部为 `COMPLETED/0:0`，运行时长为 1 分 38 秒至 1 分 47 秒。
- `summary_000.json` 至 `summary_011.json`、12 个 protein TSV 和 12 个 nucleic TSV 全部存在且非空。
- 24 个 TSV 合计 5,071,858 行，其中 protein 为 4,889,250 行，nucleic 为 182,608 行。
- 抽查首尾记录均为九列：query、target、真实 identity、query coverage、target coverage、query/target 长度、alignment 长度和 E-value。
- 下一动作：核对 12 份 summary 的分片号、线程数、两类命令状态和 12 个 allocation 的成功退出信息。

### 2026-08-31 17:19:39 +08:00　步骤 3 深验收通过

- 12 份 summary 的 `shard_index` 精确覆盖 0 至 11，`threads` 全部为 8；每份均按 protein、nucleic 顺序记录两条 `status=completed` 命令，共 24 条。
- 每条 summary 指向的 TSV 均存在且非空；12 个 allocation 均记录“第 1 次执行成功”、未启用资源保留并已释放。
- 临时审计脚本只通过标准输入运行，结束后已删除，没有进入项目或正式逻辑。
- 没有发现正式代码 bug，不修改 MMseqs2 参数或 Python 代码。
- 下一动作：提交步骤 4，使用 `mode=or`、`threshold=0.5` 合并关系、身份证与三个测试视图。

### 2026-08-31 17:21:54 +08:00　步骤 4 已提交

- 远端提交命令：

```bash
cd /home/penghongen/My_Project/AdaLigand
bash 训练与运行/submit_task.sh --sh held_out_finalize.sh --resource cpu --cpus 8 --time 04:00:00 -- or 0.5
```

- Slurm 返回 `Submitted batch job 366110`；资源为 1 个 CPU task、8 CPU、最长 4 小时，无资源保留锁。
- 本次参数明确为 PDB coverage `mode=or`、`threshold=0.5`。
- 持续检查命令固定为：

```bash
squeue -j 366110 -h -o "%i|%T|%M|%C|%R"
sacct -j 366110 -n -P --format=JobIDRaw,State,ExitCode,Elapsed,AllocCPUS,MaxRSS,NodeList
tail -n 60 /home/penghongen/Feedback/AdaLigand/allocations/366110/out
tail -n 40 /home/penghongen/Feedback/AdaLigand/allocations/366110/err
find /storage/penghongen/AdaLigand/held_out -maxdepth 2 -type f -printf "%P|%s\n"
```

- 下一动作：重复触发上述检查；完成后分别验证冗余边、身份证、full_test 极大性及两个子集关系，不把验收写成生产门控。

### 2026-08-31 17:23:56 +08:00　步骤 4 第 1 次持续检查

- 触发了已记录的 `squeue`、`sacct`、allocation 尾部日志和最终文件检查命令。
- Job `366110` 在 `cnode01` 上为 `RUNNING`，使用 8 CPU，已运行约 1 分 37 秒。
- 动态命令准确传入 `or 0.5`；`err` 只有既有 release 复用提示，最终产物尚未写出。
- 下一动作：继续等待并重复同一组检查命令。

### 2026-08-31 17:27:00 +08:00　步骤 4 第 2 次持续检查

- 再次触发同一组状态、日志和最终文件检查命令。
- Job `366110` 继续为 `RUNNING`，已运行约 4 分 49 秒；无 traceback 或错误日志。
- `qualifying_entity_hits.jsonl` 已写出约 1.74 GB，其他最终关系、身份证和测试视图尚未出现。
- 该大文件来自既定 5,071,858 行 MMseqs2 输入的合格 entity 命中汇总；当前没有失败证据，不据此修改代码或参数。
- 下一动作：继续等待，同时用 `sstat` 只读查看当前内存，不干预进程。

### 2026-08-31 17:30:02 +08:00　步骤 4 资源只读检查

- 触发命令：

```bash
sstat -j 366110.batch -n -P --format=JobID,MaxRSS,AveRSS,MaxVMSize,AveCPU
```

- `366110.batch` 的 `MaxRSS` 与 `AveRSS` 均为 `8277152K`，`MaxVMSize` 为 `13580916K`，`AveCPU` 为 `00:06:29`。
- 即当时实际内存约 8.28 GB、虚拟内存约 13.58 GB；未看到内存异常终止或其他失败终态。
- 在登录节点执行的 `ps` 不会列出计算节点进程，因而不把空结果解释为作业结束。
- 本次仅探测状态，没有干预作业、修改正式代码或改变参数。
- 下一动作：重复触发步骤 4 的固定检查命令，等待明确终态。

### 2026-08-31 17:30:53 +08:00　步骤 4 第 3 次持续检查

- 再次触发步骤 4 已记录的 `squeue`、`sacct`、allocation 尾部日志和最终文件检查命令。
- Job `366110` 仍为 `RUNNING`，位于 `cnode01`，使用 8 CPU，已运行 9 分 15 秒。
- allocation 输出仍确认动态命令为 `held_out_finalize.sh or 0.5`；`err` 仅有 release 复用提示，未出现 traceback。
- `qualifying_entity_hits.jsonl` 大小仍为 1,735,674,376 字节；冗余边、身份证和三个测试视图尚未写出。
- 本轮没有失败证据，未修改正式代码、作业参数或运行状态。
- 下一动作：继续重复同一组只读检查；完成前不提前解释最终集合。

### 2026-08-31 17:32:22 +08:00　步骤 4 第 4 次持续检查

- 等待约 35 秒后，再次触发 `squeue`、`sacct`、`sstat`、最终文件和 `err` 尾部检查。
- Job `366110` 仍为 `RUNNING`，已运行 10 分 44 秒；`MaxRSS=9008124K`、`MaxVMSize=14311780K`、`AveCPU=00:12:08`。
- 当时实际内存约 9.01 GB；Slurm 未报告 OOM 或其他失败，`err` 仍只有 release 复用提示。
- 文件集合未发生变化，尚不能验收冗余关系与测试集。
- 本轮只读检查没有改变主逻辑、参数或作业。
- 下一动作：继续等待明确终态，并记录下一次持续检查。

### 2026-08-31 17:33:58 +08:00　步骤 4 第 5 次持续检查

- 等待约 35 秒后，重复触发状态、资源、文件和 allocation 尾部检查。
- Job `366110` 为 `RUNNING`，已运行 12 分 20 秒；`MaxRSS=9458096K`、`MaxVMSize=14761436K`、`AveCPU=00:13:47`。
- 当时实际内存约 9.46 GB，仍无 OOM、traceback 或非零退出；动态命令与 `or 0.5` 参数未变。
- 最终文件列表仍未变化，继续等待而不干预。
- 下一动作：再次重复同一组只读命令。

### 2026-08-31 17:35:36 +08:00　步骤 4 第 6 次持续检查

- 等待约 40 秒后，重复触发 `squeue`、`sacct`、`sstat`、最终文件和 `err` 尾部检查。
- Job `366110` 为 `RUNNING`，已运行 13 分 58 秒；`MaxRSS=9909524K`、`MaxVMSize=15213164K`、`AveCPU=00:15:39`。
- 当时实际内存约 9.91 GB；没有 OOM、traceback 或非零退出，文件列表尚未变化。
- 未对作业或正式代码采取干预。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:24:14 +08:00　步骤 4 第 32 次持续检查：开始落盘 PDB 边

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 1 小时 02 分 36 秒；`MaxRSS=17710224K`、`MaxVMSize=23013156K`、`AveCPU=01:13:33`。
- 首次观察到原子写入临时文件 `.redundancy_edges.jsonl.43180.tmp`，当时大小为 1,610,418,639 字节；这表示 PDB 对关系已开始落盘，但正式 `redundancy_edges.jsonl` 尚未完成替换。
- 当时实际内存约 17.71 GB；无失败状态或错误日志。临时文件不被当作完成产物。
- 未干预运行。
- 下一动作：继续轮询，等待临时边文件原子替换及 Job 明确终态。

### 2026-08-31 18:25:34 +08:00　步骤 4 明确失败

- 再次触发固定状态、日志和文件检查后，Job `366110` 明确进入 `FAILED/1:0`，运行 1 小时 03 分 36 秒，峰值内存 `17710224K`，allocation 已释放并清理锁。
- 临时边文件已原子替换为正式 `redundancy_edges.jsonl`，大小 3,736,282,840 字节；但身份证、full_test、test_0、test_1 和 stage2 summary 尚未写出。
- Python traceback 的终点为：

```text
ValueError: full_test 只有 124 个 PDB, 不足 200 个.
```

- 失败发生在全部 PDB 边构建和 full_test 贪心选择之后、test_0 抽样时；不是 MMseqs、序列解析、内存或 Slurm 资源问题。
- 该冲突影响全部最终视图，满足“明确 bug / 高比例影响”条件；但本时点尚未编辑正式代码。
- 下一动作：只读量化已有边、前置资格与 full_test=124 的来源，再选择保持科学契约且改动最小的恢复方式。

### 2026-08-31 18:27:00 +08:00　根因与双线 Git 端点确认

- 只读查看 `held_out_finalize.sh` 与 CLI：shell 将 `test_0_size=200` 写成固定目标并注明“不足时明确失败”，CLI 已有显式 `--test-0-size` 参数。
- 由于 full_test 必须保持内部无冗余且 test_0 必须是其子集，不能通过补入冲突 PDB 达到 200；也不应为凑数改变 `or + 0.5`、种子或 full_test 定义。
- 按双线 Git 流程检查三个工作树：实现端 `codex/held-out-sequence-redundancy`、学习端 `Learn/held-out-sequence-redundancy` 与累计端 `Learn/CUMULATIVE` 均干净；实现端与学习端树内容等价。累计端仅有本次用户要求的未跟踪 `详细日志记录.md`。
- 已决定的最小恢复语义：200 是 test_0 的目标上限；当 full_test 少于 200 时，test_0 取全部 full_test，仍使用独立随机子流给出固定顺序。冗余规则、full_test、seed 与 occurrence 规则不变。
- 下一动作：只在实现工作树完成最小代码、测试和说明修订，再按双线流程重建学习端。

### 2026-08-31 18:30:00 +08:00　最小修复已写入实现工作树

- 仅修改实现端 `codex/held-out-sequence-redundancy` 的 `sample_test_0`：删除 full_test 小于目标数时的异常，新增 `actual_sample_size=min(sample_size, len(full_test))`，继续使用原独立随机子流和无放回排列。
- 新增一个窄口径单元测试，验证 4 个 full_test 成员面对 200 上限时全部且仅被选中。
- 只同步更新直接相关的 shell 注释、README、READING 与既有实施记录；冗余边、full_test 贪心、seed、quality/assets/sequence/reference 资格和 occurrence 条件均未修改。
- 首次本地回归命令误落到 `D:\Pymol\python.exe`，该解释器没有 pytest，两条命令均在收集前以 `No module named pytest` 退出；这不是代码测试失败，也没有产生项目文件。
- 下一动作：定位项目既有本地 Python 环境后重跑；若本机没有可用 pytest，则使用同步后的服务器 Conda 环境验证。

### 2026-08-31 18:33:00 +08:00　本地回归与两遍主代理自查完成

- 定位到 `D:\Anaconda\envs\Pocket_Plus_windows\python.exe`，并从 `Data_Preprocessing/held_out` 包目录运行：

```powershell
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m pytest tests/test_selection.py
& 'D:\Anaconda\envs\Pocket_Plus_windows\python.exe' -m pytest
```

- 结果分别为 `5 passed` 与 `21 passed, 1 skipped`；跳过项仅因本地环境没有 Gemmi，服务器环境将在同步后执行完整 22 项。
- 期间曾从仓库根运行同一测试，因该包未安装且工作目录不在包根而出现 `ModuleNotFoundError: held_out_pipeline`；切到已有 `pyproject.toml` 的包目录后通过，确认是调用目录问题而非代码问题。
- 第一遍自查按修改文件顺序核对函数职责、位置、调用关系、嵌套与 Docstring：逻辑只留在原 `sample_test_0`，没有新增函数调用层级或隐藏分支；Docstring 收紧为 `N_test0=min(sample_size, N_full_test)`。
- 第二遍逐行核对科学变量注释：`sorted_ids`、`actual_sample_size`、随机生成器、`draw_indices` 及新增测试变量均有类型/形状/具体语义；未发现需补充的科学变量。
- `git diff --check` 无内容错误，仅报告 Windows 工作树既有 LF/CRLF 转换提示。
- 本机没有 Bash 可执行文件，`bash -n` 未运行；该项将在服务器 Conda/项目环境中验证。
- 下一动作：三名独立 subagent 分别对布局/Git、注释、逻辑进行 3 轮全面核查。

### 2026-08-31 18:40:00 +08:00　三路各 3 轮独立审查完成并窄口径修订

- 注释审查第 1、2 轮指出 Docstring 条目跨物理行、类型—形状标点和 `draw_indices` 索引对象说明不够精确；第 3 轮批准 shell 与文档。已按指定行窄改，没有扩展范围。
- 逻辑审查确认 `or + 0.5`、full_test 极大独立集、固定种子、`test_1 ⊆ test_0 ⊆ full_test` 与 occurrence 规则均未漂移，并批准 full_test=124 时 test_0 取全部成员；指出规划文档、mapping 和完整 finalize 测试仍保留旧的固定 200 契约。
- 布局/Git 审查批准 Python 职责、目录、依赖与函数位置，指出同一文档状态差异，并给出实现提交、学习端分层重建、树等价和累计端快进顺序。
- 已完成的窄改：
  - Docstring 用单行动态长度描述，不再引用未定义形状；变量注释改为 `list[str], (N,)` / `int64 ndarray, (N,)` 并写明索引对象。
  - 小集合 helper 测试加入反序输入确定性；完整 finalize 测试改为 `test_0_size=200>|full_test|`，并检查 test_0 等于 full_test、summary 数量与身份证排名。
  - 规划文档、mapping、实施记录统一为“至多 200，full_test 不足时取全部”，并记录前三步完成和 Job `366110` 的实际失败状态。
- 逻辑审查另建议拒绝负数 sample_size；该情况不是本次生产路径，且验证逻辑在本次修改前已同样不存在。依据用户“不额外扩充、不过度防御、不要新增门控”的要求，不新增该异常分支，交由逻辑审查者窄口径复核这一范围判断。
- 窄改后本地再次得到选择测试 `5 passed`、全套 `21 passed, 1 skipped`；`git diff --check` 无内容错误。
- 下一动作：将当前窄改分别发回三名审查者，只复核原问题，不再扩大范围。

### 2026-08-31 18:44:00 +08:00　窄口径复核批准与双线 Git 完成

- 三名审查者均只复核原问题并批准：注释格式全部闭合；规划、mapping 与运行状态一致；完整 finalize 测试覆盖 `200>|full_test|`；负数目标在用户范围约束下认定为非阻断既有问题。
- 实现线提交：`cf3e2e4 修复 test_0 小集合抽样契约`，包含 8 个直接相关文件。
- 学习线按理解顺序重建为：
  - `ae74324 说明 test_0 数量上限与首次运行事实`；
  - `f0c9a65 让 test_0 受 full_test 实际规模约束`；
  - `0e01739 验证 test_0 小集合完整抽样`。
- 实现端与学习端分别运行全套测试，均为 `21 passed, 1 skipped`。
- 首次读取提交 tree 的 PowerShell 命令因未给 `^{tree}` 加引号而被 PowerShell 错误解释；命令只读且没有改变 Git。加引号后两端 tree 均为 `fb6ed1b8b070579b119a0c5e797978ee3a1667bf`，路径差异为零。
- `Learn/CUMULATIVE` 已从 `89d95ee` fast-forward 到 `0e01739`。更新前先确认学习提交未占用 `详细日志记录.md`；更新后该日志仍是唯一未跟踪文件，没有暂存或提交。
- 下一动作：安全同步累计学习端，服务器先跑完整 22 项和 shell 语法，再重提 finalize。

### 2026-08-31 18:47:00 +08:00　修复版安全同步与服务器预检通过

- 从 `Learn/CUMULATIVE@0e01739` 再次运行既有安全同步脚本；同步、权限设置与 shell 行尾修复全部成功，未删除 `/storage/penghongen/AdaLigand/held_out` 中已有结果。
- 同步会按项目既有规则传输工作区文件，因此用户要求的 `详细日志记录.md` 也同步到服务器项目目录；它未进入 Git、release 主逻辑或输出根。
- 服务器预检命令：

```bash
cd /home/penghongen/My_Project/AdaLigand
source /home/penghongen/anaconda3/etc/profile.d/conda.sh
conda activate /home/penghongen/anaconda3/envs/AdaLigand_stage1_py310
cd Data_Preprocessing/held_out
python -m pytest
cd ../..
bash -n 训练与运行/sh/held_out_finalize.sh
```

- Linux Python 3.10.20 环境收集并通过全部 22 项测试，Gemmi 测试实际运行；`held_out_finalize.sh` 的 Bash 语法通过。
- 下一动作：使用原命令、原 `or + 0.5` 参数重提 finalize，并持续监视。

### 2026-08-31 18:48:00 +08:00　步骤 4 修复版已重提

- 提交命令与首次运行一致：

```bash
cd /home/penghongen/My_Project/AdaLigand
bash 训练与运行/submit_task.sh --sh held_out_finalize.sh --resource cpu --cpus 8 --time 04:00:00 -- or 0.5
```

- Slurm 返回 `Submitted batch job 366127`；full 模式、CPU、8 核、最长 4 小时、无资源保留锁。
- 本次仍从 24 个既有 MMseqs TSV 重新构建全部边，不引入恢复缓存或旁路脚本；正式参数仍为 `or + 0.5`。
- 持续检查命令固定为：

```bash
squeue -j 366127 -h -o "%i|%T|%M|%C|%R"
sacct -j 366127 -n -P --format=JobIDRaw,State,ExitCode,Elapsed,AllocCPUS,MaxRSS,NodeList
sstat -j 366127.batch -n -P --format=JobID,MaxRSS,AveRSS,MaxVMSize,AveCPU
find /storage/penghongen/AdaLigand/held_out -maxdepth 2 -type f -printf "%P|%s\n"
tail -n 30 /home/penghongen/Feedback/AdaLigand/allocations/366127/out
tail -n 20 /home/penghongen/Feedback/AdaLigand/allocations/366127/err
```

- 下一动作：重复触发上述只读命令，等待明确终态。

### 2026-08-31 19:04:51 +08:00　步骤 4 修复版第 1 次持续检查

- Job `366127` 在 `cnode01` 上为 `RUNNING`，使用 8 CPU，已运行 56 秒；`MaxRSS=13252K`。
- 输出根中的 `qualifying_entity_hits.jsonl` 与 `redundancy_edges.jsonl` 是首次 Job `366110` 已完成的旧阶段产物；当前检查不把它们的存在解释为本次已经推进到相同阶段。
- 尚未出现新身份证、三个测试视图或 stage2 summary，也没有失败终态。
- 下一动作：后续检查同时观察原子临时文件和修改时间，继续等待。

### 步骤 4 修复版持续检查表

| 检查时间 | Job 状态 / 已运行 | MaxRSS | 本次临时文件或正式文件事件 | 错误 |
| --- | --- | ---: | --- | --- |
| 2026-08-31 19:06:39 +08:00 | `RUNNING` / 2 分 44 秒 | 36,872 K | 无本次临时文件；两份旧文件修改时间仍分别为 17:24:48、18:24:52 | 无 |
| 2026-08-31 19:08:13 +08:00 | `RUNNING` / 4 分 18 秒 | 3,757,420 K | 无本次临时文件；旧文件修改时间未变 | 仅有新 release 创建提示，无运行错误 |
| 2026-08-31 19:09:43 +08:00 | `RUNNING` / 5 分 48 秒 | 6,453,284 K | `qualifying_entity_hits.jsonl` 已于 19:09:37 被本次原子替换，大小仍为 1,735,674,376 字节；旧边文件未变 | 无 |
| 2026-08-31 19:11:18 +08:00 | `RUNNING` / 7 分 23 秒 | 8,063,504 K | qualifying hits 修改时间稳定；旧边文件未变，无新临时文件 | 无 |
| 2026-08-31 19:12:49 +08:00 | `RUNNING` / 8 分 54 秒 | 8,285,916 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:14:21 +08:00 | `RUNNING` / 10 分 26 秒 | 8,587,596 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:15:56 +08:00 | `RUNNING` / 12 分 01 秒 | 8,915,812 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:17:35 +08:00 | `RUNNING` / 13 分 40 秒 | 9,098,736 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:19:07 +08:00 | `RUNNING` / 15 分 13 秒 | 9,589,952 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:20:35 +08:00 | `RUNNING` / 16 分 40 秒 | 9,918,216 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:22:10 +08:00 | `RUNNING` / 18 分 15 秒 | 10,304,192 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:23:36 +08:00 | `RUNNING` / 19 分 41 秒 | 10,616,628 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:25:09 +08:00 | `RUNNING` / 21 分 14 秒 | 10,944,312 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:26:42 +08:00 | `RUNNING` / 22 分 47 秒 | 11,265,840 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:29:00 +08:00 | `RUNNING` / 25 分 05 秒 | 11,405,436 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:32:27 +08:00 | `RUNNING` / 28 分 32 秒 | 11,639,496 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:34:05 +08:00 | `RUNNING` / 30 分 10 秒 | 11,761,540 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:35:45 +08:00 | `RUNNING` / 31 分 50 秒 | 11,872,536 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:37:16 +08:00 | `RUNNING` / 33 分 21 秒 | 12,126,912 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:38:49 +08:00 | `RUNNING` / 34 分 54 秒 | 12,252,084 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:40:17 +08:00 | `RUNNING` / 36 分 22 秒 | 12,345,816 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:41:47 +08:00 | `RUNNING` / 37 分 52 秒 | 12,613,852 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:43:18 +08:00 | `RUNNING` / 39 分 24 秒 | 12,878,896 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:44:49 +08:00 | `RUNNING` / 40 分 54 秒 | 13,171,036 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:46:19 +08:00 | `RUNNING` / 42 分 24 秒 | 13,371,100 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:47:53 +08:00 | `RUNNING` / 43 分 58 秒 | 13,718,252 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:49:23 +08:00 | `RUNNING` / 45 分 28 秒 | 14,006,800 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:50:55 +08:00 | `RUNNING` / 47 分 00 秒 | 14,219,516 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:52:25 +08:00 | `RUNNING` / 48 分 30 秒 | 14,490,804 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:53:58 +08:00 | `RUNNING` / 50 分 03 秒 | 14,792,528 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:55:28 +08:00 | `RUNNING` / 51 分 33 秒 | 15,123,332 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:57:03 +08:00 | `RUNNING` / 53 分 08 秒 | 15,425,348 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 19:58:47 +08:00 | `RUNNING` / 54 分 52 秒 | 15,746,860 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:00:21 +08:00 | `RUNNING` / 56 分 26 秒 | 16,043,824 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:02:09 +08:00 | `RUNNING` / 58 分 14 秒 | 16,248,592 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:03:33 +08:00 | `RUNNING` / 59 分 38 秒 | 16,356,396 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:05:18 +08:00 | `RUNNING` / 1 小时 01 分 23 秒 | 16,463,560 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:06:47 +08:00 | `RUNNING` / 1 小时 02 分 52 秒 | 16,613,976 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:08:17 +08:00 | `RUNNING` / 1 小时 04 分 23 秒 | 16,700,028 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:09:43 +08:00 | `RUNNING` / 1 小时 05 分 48 秒 | 16,829,844 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:11:14 +08:00 | `RUNNING` / 1 小时 07 分 19 秒 | 17,119,972 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:12:40 +08:00 | `RUNNING` / 1 小时 08 分 45 秒 | 17,186,124 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:14:05 +08:00 | `RUNNING` / 1 小时 10 分 10 秒 | 17,293,324 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 20:15:31 +08:00 | `RUNNING` / 1 小时 11 分 36 秒 | 17,379,360 K | 文件状态未变，无新临时文件 | 无 |
| 2026-08-31 约 20:20 +08:00 | 未取得有效快照 | — | 轮询末条命令误用了不存在的 `/home/penghongen/My_Project/AdaLigand/训练与运行/日志/held_out_finalize/366127/err`；`tail` 返回 1，使整组远端命令按失败返回。随后从 `submit_task.sh` 确认 full 模式日志位于 allocation 目录，并立即改回步骤 4 已记录的固定路径 | 监控命令路径错误，不是计算作业错误 |
| 2026-08-31 20:21:05 +08:00 | `RUNNING` / 1 小时 17 分 10 秒 | 17,698,252 K | 出现本轮原子临时文件 `.redundancy_edges.jsonl.*.tmp`，大小 3,379,048,448 字节；正式旧边文件尚未替换 | 无；allocation 的 `err` 为空 |
| 2026-08-31 约 20:22 +08:00 | 未取得有效快照 | — | 将 35 秒本地等待和远端查询放在同一调用中，超过本机单次等待窗口后只返回空输出；随后改为等待与查询分开触发 | 本地监控调用方式问题，不是计算作业错误 |
| 2026-08-31 20:23:31 +08:00 | `COMPLETED (0:0)` / 1 小时 17 分 39 秒 | 17,698,252 K | 本轮 `redundancy_edges.jsonl` 已于 20:21:12 原子替换，大小 3,736,282,840 字节；临时文件已消失 | 无运行错误；作业结束后 `sstat` 提示 step 已不再运行，最终资源值由 `sacct` 取得 |

- 表内每一行均由步骤 4 已记录的 `squeue`、`sacct`、`sstat`、临时文件、`stat` 和 `err` 检查命令产生。
- 下一动作：继续向本表追加持续检查事件，直到明确终态。

### 2026-08-31 20:24:49 +08:00　步骤 4 最终小文件与汇总只读检查

- 触发以下只读命令：输出根两层文件的大小和修改时间、`du -sh`、Python 读取四份小 JSON，以及 `wc -l held_out_identity.jsonl`。没有扫描两份数 GB 的 JSONL 内容。
- 最终产物齐全：`full_test.json`、`test_0.json`、`test_1.json`、`held_out_identity.jsonl`、`stage2/summary.json` 和 `stage2/_COMPLETE` 均于本轮落盘；`_COMPLETE` 只记录为完成事实，不作为发布门控。
- `stage2/summary.json` 报告：held-out 2,497 个、基础合格 315 个、full_test 124 个、test_0 124 个、test_1 108 个；模式为 `or`，阈值为 0.5，随机种子为 3407。
- 命中和边统计为：原始合格比对 5,071,858 条、定向 entity 命中 4,640,103 条、PDB 边 622,986 条、其中判冗余 388,797 条；关系分为 reference 568,377 条和 held-out internal 54,609 条。
- 排除原因计数为 reference redundant 1,986、quality failed 329、asset failed 279。三个原因可在同一 PDB 上同时出现，因此不把它们与 315 做互斥求和。
- `held_out_identity.jsonl` 恰有 2,497 行；`test_0` 数量符合 `min(200, |full_test|)=124`，`test_1` 是其中经 occurrence 条件保留的 108 个。
- 输出根当前占用约 5.7 GB；最大的正式文件是 3,736,282,840 字节的 `redundancy_edges.jsonl` 和 1,735,674,376 字节的 `qualifying_entity_hits.jsonl`。
- 下一动作：在计算节点运行项目外的只读全量审计，逐边核验独立性、极大性、拒绝见证和统计契约。

### 2026-08-31 20:29:50—20:30:57 +08:00　准备项目外全量审计

- 先只读确认以下两个精确目标不存在，避免覆盖已有文件：

```text
/storage/penghongen/tmp/adaligand_held_out_audit_20260831.py
/home/penghongen/My_Project/AdaLigand/训练与运行/sh/_audit_held_out_final_20260831.sh
```

- 通过统一 SSH helper 的标准输入分别传输临时 Python 审计脚本和 279 字节 simple 任务壳；服务器临时 Python 文件权限为 600，任务壳权限为 700。
- 审计脚本只读取 `/storage/penghongen/AdaLigand/held_out`，结果仅写入 Slurm 标准输出；不写正式输出、不加门控、不计算或记录 SHA。
- 项目外审计中的 `test_0` 数量断言已随正式契约改成 `len(test_0) == min(200, len(full_test))`；除此之外没有为本轮结果放宽任何断言。
- 远端执行 Python AST 解析和 `bash -n`，两份临时文件语法均通过。
- 这些文件不属于正式代码，作业终止后将按上述精确路径删除。
- 下一动作：通过 `submit_task.sh --simple` 提交 1 核 CPU 审计，避免在登录节点扫描数 GB 文件。

### 2026-08-31 约 20:32 +08:00　全量审计 Job 366151 已提交

- 提交命令：

```bash
cd /home/penghongen/My_Project/AdaLigand
bash 训练与运行/submit_task.sh --simple --sh _audit_held_out_final_20260831.sh --resource cpu --cpus 1 --time 02:00:00 --job-name held_out_final_audit
```

- Slurm 返回 `Submitted batch job 366151`；simple 模式、1 个 CPU、最长 2 小时、无资源保留锁，不创建 release 或 launch。
- 固定持续检查命令为：

```bash
squeue -j 366151 -h -o "%i|%T|%M|%C|%R"
sacct -j 366151 -n -P --format=JobIDRaw,State,ExitCode,Elapsed,AllocCPUS,MaxRSS,NodeList
sstat -j 366151.batch -n -P --format=JobID,MaxRSS,AveRSS,MaxVMSize,AveCPU
tail -n 30 /home/penghongen/SIMPLE_RUN/held_out_final_audit_366151.out
tail -n 20 /home/penghongen/SIMPLE_RUN/held_out_final_audit_366151.err
```

- 下一动作：持续触发上述只读检查，直至审计明确结束。

### 2026-08-31 20:33:50 +08:00　首次全量审计暴露临时断言错误

- Job `366151` 在运行 1 分钟后以 `FAILED (1:0)` 结束，MaxRSS 为 152,100 K；正式管线 Job `366127` 和正式输出均未变化。
- traceback 指向项目外临时审计脚本：它错误读取 `selection.rejection_edge["pdb_A"]`，触发 `KeyError: 'pdb_A'`。
- 随后并行触发两项只读核对：本地 `rg -n -C 6 "rejection_edge|rejected_by"` 检查正式生成代码；服务器逐条统计 2,497 份身份证中非空 `rejection_edge` 的字段签名和一个样本。
- 191 个非空拒绝见证均采用同一正式 schema：以 `other_pdb_id` 保存另一端，同时保存四个 coverage、两个 pass、`relation`、`redundant` 和 `max_coverage`；正式代码 `_strongest_edge` 的输出与之相符。
- 因此这是审计器自身的明确 bug，不是正式数据或正式代码 bug。只把临时断言改为检查 `other_pdb_id == rejected_by` 且 `relation == "held_out_internal"`；既有全图冲突集合仍独立验证该邻居确有直接冗余边。
- 没有修改任何正式代码、正式输出、参数或发布逻辑。
- 下一动作：覆盖服务器上同一路径的临时审计文件，语法检查后重新提交；临时任务壳无需变化。

### 2026-08-31 约 20:36 +08:00　窄修后的全量审计重新提交

- 本地与远端 AST 解析均通过后，按原精确路径覆盖项目外临时审计脚本；任务壳未修改。
- 使用与 Job `366151` 完全相同的 simple 提交命令重提，Slurm 返回 `Submitted batch job 366153`。
- Job `366153` 仍为 1 CPU、2 小时上限、无 release/launch，只读扫描同一正式输出。
- 持续检查命令中的 Job ID 与日志名相应替换为 `366153` 和 `held_out_final_audit_366153.{out,err}`；其余不变。
- 下一动作：持续等待 Job `366153` 的明确终态。

### Job 366153 持续检查表

| 检查时间 | Job 状态 / 已运行 | MaxRSS | stdout / stderr |
| --- | --- | ---: | --- |
| 2026-08-31 20:37:30 +08:00 | `RUNNING` / 47 秒 | 195,792 K | stdout 仅有 simple allocation 命令记录；stderr 为空 |
| 2026-08-31 20:38:52 +08:00 | `COMPLETED (0:0)` / 1 分 06 秒 | 233,056 K | stdout 给出 `status: passed` 的完整审计报告；无审计 traceback。作业结束后的 `sstat` 提示 step 已不再运行，最终资源值由 `sacct` 取得 |

- 下一动作：继续向本表追加持续检查事件，直至明确终态。

### 2026-08-31 20:38:52 +08:00　项目外全量审计通过

- Job `366153` 逐行重算并通过以下一致性检查：
  - 24 个 MMseqs TSV 共 5,071,858 条非空记录，等于 summary 的原始合格比对数；
  - `qualifying_entity_hits.jsonl` 共 4,640,103 条，等于定向 entity 命中数；
  - `redundancy_edges.jsonl` 共 622,986 条唯一关系边，其中 388,797 条在 `or + 0.5` 下判冗余；
  - 每条边的四个双向 coverage 均在 `[0, 1]`，`chain_pass`、`residue_pass` 与最终 `redundant` 均可由数值重新得到；
  - 关系计数为 reference 568,377、held-out internal 54,609，和 summary 完全一致；
  - 2,497 份身份证唯一，参考冗余、内部冗余、质量、资产、序列状态、occurrence 布尔值、排除原因和全部视图排名均与原始边及视图一致；
  - 315 个基础合格候选的 `greedy_rank` 是紧凑全排列；full_test 124 个成员之间没有冗余边，所有 191 个未接受的基础合格候选都存在指向已接受成员的直接冲突见证，因此该集合是极大独立集；
  - test_0 是 `min(200, 124)=124` 个无重复成员，test_1 是其严格满足 `1 < total_count < 100` 的保序子集，共 108 个；
  - 8 个 held-out PDB 没有达到长度阈值的可比链，但其中基础合格者为 0，因此没有进入 full_test。
- 审计最终报告为 `status: passed`。该报告来自独立临时脚本，不进入正式生产逻辑，也不构成发布门控。
- 下一动作：核对精确路径后删除本地和服务器临时审计脚本，保留 Slurm stdout/err 作为运行事实。

### 2026-08-31 20:40:15 +08:00　临时审计文件清理完成

- 删除前分别用 `readlink -f` 与 `stat` 确认两个服务器目标均解析为先前记录的精确绝对路径，且都是普通文件：临时 Python 脚本 13,546 字节、临时任务壳 279 字节。
- 随后仅删除这两个精确服务器文件，并逐一确认返回 `REMOVED`；未删除目录、Slurm stdout/err 或任何正式输出。
- 本机 `%LOCALAPPDATA%/Temp` 中对应的 Python 审计脚本和任务壳也通过补丁精确删除。
- 服务器的 simple Job `366151`、`366153` 日志保留在 `$HOME/SIMPLE_RUN`，可追溯首次审计器 bug 与最终通过结果。
- 下一动作：执行全作业状态、残留任务、临时文件、输出规模、阶段汇总和本地 Git 工作区的最终只读验收。

### 2026-08-31 18:22:47 +08:00　步骤 4 第 31 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 1 小时 01 分 09 秒；`MaxRSS=17610572K`、`MaxVMSize=22913888K`、`AveCPU=01:11:46`。
- 当时实际内存约 17.61 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:21:24 +08:00　步骤 4 第 30 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 59 分 46 秒；`MaxRSS=17469008K`、`MaxVMSize=22772776K`、`AveCPU=01:10:04`。
- 当时实际内存约 17.47 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:19:56 +08:00　步骤 4 第 29 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 58 分 18 秒；`MaxRSS=17366744K`、`MaxVMSize=22669584K`、`AveCPU=01:08:16`。
- 当时实际内存约 17.37 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:18:33 +08:00　步骤 4 第 28 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 56 分 55 秒；`MaxRSS=17248996K`、`MaxVMSize=22552388K`、`AveCPU=01:06:30`。
- 当时实际内存约 17.25 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:17:07 +08:00　步骤 4 第 27 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 55 分 29 秒；`MaxRSS=17140804K`、`MaxVMSize=22443980K`、`AveCPU=01:04:44`。
- 当时实际内存约 17.14 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:15:46 +08:00　步骤 4 第 26 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 54 分 08 秒；`MaxRSS=16864168K`、`MaxVMSize=22167532K`、`AveCPU=01:03:17`。
- 当时实际内存约 16.86 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:14:18 +08:00　步骤 4 第 25 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 52 分 40 秒；`MaxRSS=16678368K`、`MaxVMSize=21982052K`、`AveCPU=01:01:39`。
- 当时实际内存约 16.68 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:07:35 +08:00　事后深验收脚本已在临时目录准备

- 仅在 Windows 临时目录创建 `audit_held_out_final.py`，没有写入 AdaLigand 项目。
- 临时脚本将流式读取正式输出，交叉核对 TSV/JSONL 行数、边布尔判定、身份证计数、前置排除原因、full_test 独立性与极大性、拒绝见证、三个视图的嵌套与排名，以及 test_1 的严格 occurrence 条件。
- 脚本明确不把零 comparable chain 擅自变成排除条件，只报告其数量。
- 本地执行以下只读语法检查，结果为 `audit_temp_syntax=passed`：

```powershell
python -c "import ast, pathlib; p=pathlib.Path(r'C:\Users\15919\AppData\Local\Temp\audit_held_out_final.py'); ast.parse(p.read_text(encoding='utf-8')); print('audit_temp_syntax=passed')"
```

- 该脚本只会在步骤 4 正常完成后通过标准输入临时运行，随后删除；不会成为生产代码、门控或持久服务器文件。
- 下一动作：继续轮询 Job `366110`。

### 2026-08-31 18:12:57 +08:00　步骤 4 第 24 次持续检查

- 再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 51 分 19 秒；`MaxRSS=16562608K`、`MaxVMSize=21865976K`、`AveCPU=01:00:01`。
- 当时实际内存约 16.56 GB；CPU 时间继续增长，无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:07:20 +08:00　资格与 occurrence 字段只读核对

- 本地使用 `rg` 只读定位 `quality.passed`、`assets.passed`、`sequence.status`、`sequence.comparable_chain_count` 和 `ligands.strict_1_100_passed` 的既有契约。
- 确认 base eligibility 可由身份证自身字段与参考冗余事实独立复算；零 comparable chain 只作为统计事实，不会被审计脚本擅自新增为排除条件。
- 没有编辑正式代码或文档。

### 2026-08-31 18:07:31 +08:00　步骤 4 第 23 次持续检查

- 再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 45 分 53 秒；`MaxRSS=15842892K`、`MaxVMSize=21146524K`、`AveCPU=00:53:24`。
- 当时实际内存约 15.84 GB；CPU 时间持续增加，无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:05:21 +08:00　步骤 4 第 22 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 43 分 43 秒；`MaxRSS=15376964K`、`MaxVMSize=20681004K`、`AveCPU=00:51:10`。
- 当时实际内存约 15.38 GB；CPU 时间继续增长，无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 18:03:56 +08:00　步骤 4 第 21 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 42 分 18 秒；`MaxRSS=15001476K`、`MaxVMSize=20304672K`、`AveCPU=00:49:43`。
- 当时实际内存约 15.00 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:58:18 +08:00　PDB 边字段只读核对

- 在本地以 `rg` 定位并只读查看 `calculate_pdb_edge` 的返回字段。
- 确认每条边保存双侧 comparable chain/residue 分母、四个 coverage、三类一对一 matching、`chain_pass`、`residue_pass`、`pdb_coverage_mode`、阈值与最终 `redundant`。
- 后续临时审计可直接复算 `or + 0.5` 的布尔结果并检查 coverage 范围；不需要也不会向正式代码添加门控。
- 下一动作：继续轮询 Job `366110`。

### 2026-08-31 18:02:25 +08:00　步骤 4 第 20 次持续检查

- 再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 40 分 47 秒；`MaxRSS=14615100K`、`MaxVMSize=19918244K`、`AveCPU=00:47:54`。
- 当时实际内存约 14.62 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:54:35 +08:00　最终验收逻辑只读核对

- 在本地只读查看 `selection.py` 第 313 至 524 行和 `redundancy.py` 第 728 至 906 行，确认实际落盘顺序与字段来源。
- `qualifying_entity_hits.jsonl` 先于 PDB 边计算写出；因此该 1.74 GB 文件存在而其他最终文件尚未出现，符合既有实现的执行顺序，不能单独视为卡死或成功。
- 身份证与三个视图在 `redundancy_edges.jsonl` 之后写出；最终审计将按既有契约逐项交叉验证，而不编辑这些函数。
- 下一动作：继续轮询 Job `366110`。

### 2026-08-31 17:58:15 +08:00　步骤 4 第 19 次持续检查

- 等待后再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 36 分 37 秒；`MaxRSS=13703968K`、`MaxVMSize=19007772K`、`AveCPU=00:42:52`。
- 当时实际内存约 13.70 GB；无失败状态、错误日志或最终文件变化。
- 根据已核对的正式落盘顺序，作业仍处于 entity 命中转 PDB 对关系阶段；没有干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:52:20 +08:00　最终验收字段契约只读核对

- 等待步骤 4 时，在本地项目执行：

```powershell
rg --files Data_Preprocessing/held_out
rg -n "full_test|test_0|test_1|rejected_by|held_out_identity|redundancy_edges|summary" Data_Preprocessing/held_out -g '*.py'
```

- 只读确认最终验收可直接核对 `redundancy_edges.jsonl`、`held_out_identity.jsonl`、`full_test.json`、`test_0.json`、`test_1.json` 与 `stage2/summary.json` 的既有字段。
- 验收将检查 full_test 的独立性与极大性、`rejected_by` 直接邻居、`test_1 ⊆ test_0 ⊆ full_test`、固定 200 个 test_0、严格 `(1, 100)` occurrence 过滤和身份排名一致性。
- 该检查没有编辑任何正式文件；验收仍只作为运行后审计，不进入生产逻辑。
- 下一动作：继续轮询 Job `366110`。

### 2026-08-31 17:54:33 +08:00　步骤 4 第 18 次持续检查

- 再次触发固定的状态、资源、文件和错误日志检查。
- Job `366110` 为 `RUNNING`，已运行 32 分 55 秒；`MaxRSS=12808248K`、`MaxVMSize=18111808K`、`AveCPU=00:38:49`。
- 当时实际内存约 12.81 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:52:17 +08:00　步骤 4 第 17 次持续检查

- 等待后再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 30 分 39 秒；`MaxRSS=12330732K`、`MaxVMSize=17633820K`、`AveCPU=00:36:26`。
- 当时实际内存约 12.33 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:49:37 +08:00　步骤 4 第 16 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 27 分 59 秒；`MaxRSS=12084172K`、`MaxVMSize=17387852K`、`AveCPU=00:33:18`。
- 当时实际内存约 12.08 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:48:07 +08:00　步骤 4 第 15 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 26 分 29 秒；`MaxRSS=11806136K`、`MaxVMSize=17109380K`、`AveCPU=00:31:31`。
- 当时实际内存约 11.81 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:46:40 +08:00　步骤 4 第 14 次持续检查

- 等待约 40 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 25 分 02 秒；`MaxRSS=11675108K`、`MaxVMSize=16978448K`、`AveCPU=00:29:07`。
- 当时实际内存约 11.68 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:45:11 +08:00　步骤 4 第 13 次持续检查

- 等待约 35 秒后，再次触发固定的只读检查。
- Job `366110` 为 `RUNNING`，已运行 23 分 33 秒；`MaxRSS=11552448K`、`MaxVMSize=16855880K`、`AveCPU=00:27:27`。
- 当时实际内存约 11.55 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:42:26 +08:00　步骤 4 第 11 次持续检查与节点内存边界

- 等待约 30 秒后，重复触发状态、资源、文件和错误日志检查；另触发一次只读节点内存命令：

```bash
scontrol show node cnode01 | tr ' ' '\n' | grep -E '^(RealMemory|AllocMem|FreeMem)='
```

- Job `366110` 为 `RUNNING`，已运行 20 分 48 秒；`MaxRSS=11326304K`、`MaxVMSize=16629952K`、`AveCPU=00:24:18`。
- `cnode01` 报告 `RealMemory=2000000` MB、`FreeMem=1664232` MB；当前约 11.33 GB 的作业内存远未接近节点物理内存边界。
- 未见失败状态、错误日志或最终文件变化。该节点检查仅用于排除临近内存边界，没有改变资源申请或作业。
- 下一动作：回到固定只读检查，继续等待明确终态。

### 2026-08-31 17:43:55 +08:00　步骤 4 第 12 次持续检查

- 等待约 35 秒后，再次触发固定的状态、资源、文件和错误日志检查。
- Job `366110` 为 `RUNNING`，已运行 22 分 17 秒；`MaxRSS=11421572K`、`MaxVMSize=16724896K`、`AveCPU=00:26:05`。
- 当时实际内存约 11.42 GB；无失败状态、错误日志或最终文件变化。
- 未干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:41:04 +08:00　步骤 4 第 10 次持续检查

- 等待约 40 秒后，再次触发同一组只读命令。
- Job `366110` 为 `RUNNING`，已运行 19 分 26 秒；`MaxRSS=11218632K`、`MaxVMSize=16521568K`、`AveCPU=00:22:48`。
- 当时实际内存约 11.22 GB；未见 OOM、traceback、非零退出或最终文件变化。
- 没有干预运行。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:39:40 +08:00　步骤 4 第 9 次持续检查

- 等待约 35 秒后，再次触发同一组只读命令。
- Job `366110` 为 `RUNNING`，已运行 18 分 02 秒；`MaxRSS=10846628K`、`MaxVMSize=16149688K`、`AveCPU=00:20:15`。
- 当时实际内存约 10.85 GB；没有失败状态、错误日志或最终文件变化。
- 未干预作业。
- 下一动作：继续等待明确终态。

### 2026-08-31 17:37:02 +08:00　步骤 4 第 7 次持续检查

- 等待约 40 秒后，重复触发相同的状态、资源、文件和错误日志检查。
- Job `366110` 为 `RUNNING`，已运行 15 分 24 秒；`MaxRSS=10229480K`、`MaxVMSize=15532396K`、`AveCPU=00:17:18`。
- 当时实际内存约 10.23 GB，仍无 Slurm 失败状态或 Python 错误；最终文件列表未变化。
- 未干预运行。
- 下一动作：继续等待并记录下一次检查。

### 2026-08-31 17:38:22 +08:00　步骤 4 第 8 次持续检查

- 等待约 35 秒后，再次触发同一组只读命令。
- Job `366110` 为 `RUNNING`，已运行 16 分 44 秒；`MaxRSS=10588932K`、`MaxVMSize=15892276K`、`AveCPU=00:18:40`。
- 当时实际内存约 10.59 GB；没有失败状态、错误日志或最终文件变化。
- 未修改代码或运行参数。
- 下一动作：继续等待明确终态。

## 2026-08-31 20:42:39—20:44 +08:00　服务器运行最终收口

### 最终只读命令

```bash
sacct -X -j 366076,366090,366094,366110,366127,366151,366153 -n -P --format=JobIDRaw,JobName,State,ExitCode,Elapsed,AllocCPUS,NodeList
squeue -j 366076,366090,366094,366110,366127,366151,366153 -h -o "%i|%T|%M|%j"
find /storage/penghongen/AdaLigand/held_out -type f -name ".*.tmp"
find /storage/penghongen/AdaLigand/held_out -type f
find /storage/penghongen/AdaLigand/held_out/stage2/mmseqs -maxdepth 1 -type f -name "*.tsv"
du -sh /storage/penghongen/AdaLigand/held_out
stat /storage/penghongen/AdaLigand/held_out/stage1/_COMPLETE /storage/penghongen/AdaLigand/held_out/stage2/_COMPLETE
```

- 另以只读 Python 分别读取 `stage1/summary.json`、`stage2/summary.json` 和 `official_fasta_smoke.json`；本地执行 `git status --short --branch` 并确认两份本机临时审计文件均不存在。

### 作业终态

| 阶段 | Job | 终态 | 说明 |
| --- | --- | --- | --- |
| catalog 数组 | `366076` | 12/12 `COMPLETED (0:0)` | 每个任务 8 CPU |
| catalog 汇总与官方 FASTA smoke | `366090` | `COMPLETED (0:0)` | 3 分 06 秒 |
| MMseqs 数组 | `366094` | 12/12 `COMPLETED (0:0)` | 每个任务 8 CPU |
| 首次 stage2 汇总 | `366110` | `FAILED (1:0)` | 旧契约把 test_0 固定为 200，而实际 full_test 为 124；这是已记录并窄修的正式 bug |
| 修复版 stage2 汇总 | `366127` | `COMPLETED (0:0)` | 1 小时 17 分 39 秒，MaxRSS 17,698,252 K |
| 首次项目外审计 | `366151` | `FAILED (1:0)` | 临时审计器误读拒绝见证字段；正式输出未受影响 |
| 窄修后的项目外审计 | `366153` | `COMPLETED (0:0)` | 1 分 06 秒，MaxRSS 233,056 K，报告 `status: passed` |

- 上述 Job 与数组子任务当前活动数为 0；没有保留 allocation 或持续任务。

### 最终数据事实

- stage1：22,386 个 catalog PDB、198,055 个 polymer entity；14,017 个 reference PDB 与 2,497 个 held-out PDB 的序列失败数均为 0，全部 PDB sequence status 为 `ok`。
- 长度阈值后的 MMseqs 目标包括 132,592 个 protein entity 和 9,627 个 nucleic entity；24 个 TSV 共 5,071,858 条合格比对。
- 官方 RCSB FASTA smoke 为 `passed=true`：固定检查 `10ad`、`10fz`、`10ay` 三个 PDB，共核对 25 个 polymer entity，三个样本均通过。
- stage2 参数为 `or + 0.5`、seed 3407；4,640,103 条定向 entity 命中形成 622,986 条 PDB 边，其中 388,797 条判冗余。
- 2,497 份 held-out 身份证中，315 个满足统一前置条件；贪心得到 124 个成员的极大独立集 full_test，test_0 为其全部 124 个成员，test_1 为 occurrence 过滤后的 108 个成员。
- 独立审计确认 `test_1 ⊆ test_0 ⊆ full_test`、full_test 内无冲突边，并且每个未接受的基础合格候选都与 full_test 中某个已接受成员存在直接冗余边。
- 输出根共有 105 个文件、24 个 MMseqs TSV、0 个原子临时文件，占用约 5.7 GB；stage1/stage2 的 `_COMPLETE` 仅作为已完成事实留存，不被解释为门控。
- 本地工作区位于 `Learn/CUMULATIVE`，正式代码无未提交修改；唯一未跟踪文件是用户要求的 `Data_Preprocessing/held_out/详细日志记录.md`。本地与服务器临时审计脚本都已精确删除，Slurm 审计日志保留。

### 结论

- 本轮服务器运行与端到端验收完成。除 full_test 小于 200 时旧 test_0 硬门槛这一明确 bug 外，没有修改正式代码；没有新增成功率门控、发布门控、SHA 校验或旁路恢复逻辑。

## 2026-08-31　八组 split 产物重构与重跑

### 2026-08-31 22:33:13 +08:00　实施共同基点与本地基线

- 用户确认生成 `0.5/0.6 × chain/residue/or/and` 八组参数产物；根目录保存一份阈值无关的 PDB 边证据，八组验收后移除根目录旧 OR-0.5 参数副本。
- Git 只读核对确认 `Learn/CUMULATIVE` 与 `Learn/held-out-sequence-redundancy` 共同指向当前按提交者时间形成的最新提交；主工作区唯一未跟踪文件仍是本日志。
- 新实现 worktree 与学习 worktree 的计划目标路径均不存在，没有覆盖现有 worktree。
- 首次运行 `python -m pytest` 时命中 `D:\Pymol\python.exe`，该解释器没有 pytest；这属于本机解释器选择错误，不是代码测试失败。
- 改用已安装 pytest 的 `D:\Anaconda\python.exe -m pytest` 后，现有基线为 `21 passed, 1 skipped`；唯一跳过项是本机未安装 Gemmi 的 mmCIF 解析测试。
- 下一动作：从 `Learn/CUMULATIVE` 建立隔离实现分支，在隔离 worktree 中修改代码与契约文档。

### 2026-08-31 22:34—22:57 +08:00　共享边证据与八组合 split 实现

- 从 `Learn/CUMULATIVE@0e01739` 建立隔离实现分支 `codex/held-out-split-matrix`，工作树为 `C:\Users\15919\.codex\worktrees\held-out-split-matrix\AdaLigand`；没有改动主工作区中的正式代码。
- 服务器只读探测确认 `/storage/penghongen/AdaLigand/held_out/split` 已有六个空目录，缺少 `held_out_05_and` 与 `held_out_06_and`；AdaLigand、Pocket_Plus、Matcher 中没有发现消费旧根目录参数文件的代码引用。
- 将重计算拆成两个显式阶段：`edge-finalize` 从现有 24 个 MMseqs2 TSV 生成共享 `pdb_edge_evidence.jsonl` 与 `stage2/edge_summary.json`；`split-finalize` 只应用一个 mode/threshold 并把全部参数产物写到对应 split 目录。
- PDB coverage mode 扩展为 `chain`、`residue`、`or`、`and`；阈值仍为显式 `(0, 1]` 包含边界。没有新增失败率门控、发布门控、SHA、缓存或兼容副本。
- 新增 `held_out_split_array.sh`，数组索引 `0-7` 明确映射到 `0.5/0.6 × chain/residue/or/and` 八个目录；`held_out_finalize.sh` 只负责共享边证据。
- 更新合成测试，使选择阶段直接读取共享边证据；新增四种 mode、0.6 包含边界、参数产物不落共享根的断言。
- 使用 `D:\Anaconda\python.exe -m pytest tests\test_redundancy.py tests\test_selection.py -q` 运行局部回归，结果为 `14 passed`。
- 开始同步 README、阅读顺序、包职责、测试边界与主规格；服务器尚未同步或提交新 Job。
- 下一动作：完成执行记录与映射文档，运行全套本地验证，随后按要求执行两遍主代理自查。

### 2026-08-31 22:57—23:18 +08:00　资源核对与两遍主代理自查

- 只读查询旧 finalize Job `366127`：`ReqMem=2000000M`、`AllocCPUS=8`、终态 `COMPLETED (0:0)`；旧 `redundancy_edges.jsonl` 为 3.5 GB，旧 `held_out_identity.jsonl` 为 6.0 MB。由此为新共享边任务和 split 数组明确申请每任务 8 CPU、32 GB 内存，避免沿用整节点默认内存请求。
- 第一遍按文件顺序复核本轮全部修改函数的职责、位置、调用关系、嵌套和 Docstring。共享边构造入口收窄为只返回 summary；生产依赖保持 `cli -> redundancy/selection` 与 `selection -> classify_pdb_redundancy`，没有新增嵌套函数、缓存、兼容分支或门控。
- 第一遍同时增加共享边证据不含五个参数字段、split 只增加这五个字段的测试，固定参数无关事实与参数化产物的边界。
- 第二遍逐行核对 `classify_pdb_redundancy`、`calculate_pdb_edge`、`build_pdb_edge_evidence`、`finalize_identity_views` 和两份 shell 中的科学变量说明；补全形状符号、关键容器语义和逐路径注释，并把本轮代码注释中的中文标点全部改为英文标点加空格。
- 两遍自查后执行完整验证：Windows pytest 为 `21 passed, 1 skipped`，唯一跳过项是本机无 Gemmi；`compileall`、五份 shell 的 `bash -n`、`git diff --check` 和代码注释标点扫描全部通过。
- 当前尚未同步服务器或提交正式 Job；下一动作是保存独立审查前实现基线，然后按用户要求派发三类审查者各三轮全面核查。

### 2026-08-31 23:19—23:21 +08:00　独立审查前基线

- `git diff --cached --check` 通过后，在隔离实现分支提交 `c6d6696`，提交说明为 `feat(held-out): add parameterized split matrix`；提交包含 14 个代码、测试、shell 与契约文档文件。
- 提交后 `codex/held-out-split-matrix` 工作树干净；用户要求的《详细日志记录.md》仍只存在于主工作区，未进入实现提交。
- 同时派发第 1/3 轮三路全面独立核查：布局与 Git、代码注释、科学及端到端逻辑。三名审查者均只读检查 `0e01739..c6d6696`，不允许修改文件。
- 下一动作：收集第一轮问题并做最小修正；随后继续第 2/3 与第 3/3 轮全面核查。

### 2026-08-31 23:21—23:42 +08:00　第 1/3 轮独立审查与修复

- 布局/Git 与科学逻辑审查分别独立复现同一入口回归：当 `pdb_edge_evidence.jsonl` 为空时，非法 mode 或零阈值不会进入逐边分类校验，仍可能写出 `_COMPLETE`。该问题属于函数参数契约，不是失败率或发布门控。
- 最小修复是在 `finalize_identity_views` 开头直接检查四种 mode 和 `(0, 1]` 阈值；新增空共享边证据下零阈值必须失败且不得写 `_COMPLETE` 的入口测试。没有新增 helper、状态文件或门控链。
- 注释审查要求补全 split 冗余边对共享字段契约的引用，区分空容器初始化时的当前形状与循环结束后的最终长度，统一容器类型与形状之间的英文逗号，并恢复 shell 中 seed 与 test_0_size 的变量说明；这些修订均只涉及注释或显式参数变量。
- 同步修正 `训练与运行/sh/README.md` 的五步与 `0-7` split 数组说明，以及执行记录中上一轮 `cf3e2e4/0e01739` 和本轮 `c6d6696` 的双线 Git 事实；明确主工作区未跟踪日志未被夹带。
- 首轮修复后执行完整验证：pytest 为 `22 passed, 1 skipped`，唯一跳过项仍是 Windows 无 Gemmi；`compileall`、五份 shell 的 `bash -n` 与 `git diff --check` 均通过。
- 首轮修复以追加提交 `0ab822f` 保存，提交说明为 `fix(held-out): validate split parameters before edges`；提交后实现 worktree 干净。
- 下一动作：等待三名审查者对 `0e01739..0ab822f` 完成第 2/3 轮全量核查。

### 2026-09-01 00:06 +08:00　第 2/3 轮独立审查与修订

- 三名审查者完成对 `0e01739..0ab822f` 的第二轮全面核查。科学逻辑审查确认空证据参数校验、共享证据等价性、旧新 OR-0.5 逐字节等价、四种模式、两个阈值、三种匹配目标、八数组映射和无门控约束均正确；本轮没有发现新的算法缺陷。
- 注释审查发现若干空累加器被写成循环结束后的最终形状、类型与形状之间缺英文逗号、零计数类别缺键的契约未说明，以及测试中的矩阵轴语义不完整。现已按当前值与最终值分别改写注释，并补全 `relation_counts`、`exclusion_reason_counts` 与 `test_1.pdb_ids` 契约。
- 逻辑审查指出主规格把参考非冗余范围直接写成全部 14,017 个参考身份，会在存在解析失败时夸大实际比对范围。现已改为“与成功进入序列目录的参考 PDB 无冗余边”，并明确 14,017 是身份基准、实际失败数见 Stage1 summary；没有把失败率改成代码门控。
- Git 与布局审查确认代码职责和工作树状态通过，只要求回填首轮修复提交 `0ab822f`，并从映射开放事项中移除已经完成的两遍主审和第 1 轮审查。上述记录已同步。
- 第二轮修订后的完整 Windows pytest 为 `22 passed, 1 skipped`，唯一跳过项仍是本机没有 Gemmi；`compileall`、`git diff --check` 与代码注释中文全角标点扫描均通过。
- 首次 shell 语法命令因当前 PowerShell 的 `PATH` 中没有 `bash` 而未实际执行；这不是 shell 语法失败。随后明确使用 `C:\Program Files\Git\bin\bash.exe -n` 检查五份正式 shell，全部通过。
- 当前只改动注释和文档，尚未提交；下一动作是把第二轮修订保存为独立实现提交。
- `git diff --cached --check` 通过后，第二轮修订以追加提交 `bd6e146` 保存，提交说明为 `docs(held-out): close second review findings`；实现顺序现为 `c6d6696 -> 0ab822f -> bd6e146`。
- 下一动作：三名审查者分别对 `0e01739..bd6e146` 做第 3/3 轮全面核查；本轮仍不得修改文件，也不局限于前两轮问题。

### 2026-09-01 00:20 +08:00　第 3/3 轮全面审查与窄口径收口

- 注释审查批准：前两轮的结构化字段、零计数缺键、当前与最终形状、形状符号、类型分隔、矩阵轴和 shell 参数说明均已闭合，未发现中文全角标点或失败率门控。
- 科学逻辑审查批准：从合成 MMseqs2 TSV 重建确认共享证据等于旧 OR-0.5 边去除五个参数字段，旧新 OR-0.5 全部产物逐字节一致；四种 mode、两个阈值、双向 coverage、短链排除、三目标容量匹配和 `full_test` 极大性均通过独立核查。
- 布局与 Git 审查批准代码、目录、函数、CLI、五步 shell 和学习历史规划，只发现两处记录滞后：映射仍把已完成的第 2 轮列为开放事项，实施记录遗漏第二轮修订提交 `bd6e146`。
- 只修正上述两处状态记录，并以追加提交 `423a4ea` 保存，提交说明为 `docs(held-out): align final review status`；没有改动代码、测试或运行脚本。
- 已请求布局与 Git 审查者只对这两处原问题做窄口径复核，不再扩大审查范围。
- 原审查者首次窄口径复核批准。随后把正式映射的开放事项收窄为双线 Git 和服务器任务，并在实施记录中写明第 3 轮闭合；该状态回填以追加提交 `07b2c14` 保存，提交说明为 `docs(held-out): record review approval`。
- 原审查者对同一问题做最后一次窄口径确认并批准：映射开放项、第三轮状态与运行相关提交序列均准确。三轮全面独立审查及后续窄口径复核全部结束。

### 2026-09-01 00:24 +08:00　学习历史重建前安全核对

- `git worktree list --porcelain` 确认主工作区仍位于 `Learn/CUMULATIVE@0e01739`，唯一未跟踪文件是本日志；实现工作区位于 `codex/held-out-split-matrix@07b2c14` 且干净，既有上一轮 worktree 均保持不动。
- 全部本地与远端引用按提交者时间排序后，`07b2c14` 是唯一最新提交；本轮共同基点仍为 `0e01739`，`git merge-base` 与两个端点解析均符合预期。
- 计划学习 worktree `C:\Users\15919\.codex\worktrees\held-out-split-matrix-learn\AdaLigand` 不存在，不会覆盖已有目录。下一动作是从共同基点新建 `Learn/held-out-split-matrix`，按“文档契约 -> 共享边证据 -> 参数化 selection/CLI/shell -> 测试”重建线性学习历史。

### 2026-09-01 00:25—00:28 +08:00　学习历史重建与双端点验证

- 从 `0e01739` 新建隔离学习 worktree 与分支 `Learn/held-out-split-matrix`，依次形成四个提交：`42fb8a9` 文档与目录契约、`625cab8` 参数无关 PDB 边证据、`3958f43` 参数化选择/CLI/shell、`470ef24` 全部本轮测试。
- `git diff --exit-code 07b2c14 470ef24 --` 返回 0，两个端点的完整文件树逐路径一致，不存在需要列入允许清单的注释或文档差异。
- 首次从仓库根直接运行测试时，两个端点都因 `held_out_pipeline` 未安装到解释器环境而在收集阶段出现同样的 `ModuleNotFoundError`。这属于工作目录错误，不是代码失败或端点差异。
- 改回 `Data_Preprocessing/held_out` 后，在实现端点和学习端点分别运行同一套 pytest，均为 `22 passed, 1 skipped`；唯一跳过仍是 Windows 无 Gemmi。两端的 `compileall`、两个新 CLI help、五份 shell 语法和 `git diff --check` 也全部通过。
- 下一动作：核对 tree object、线性祖先关系、两端工作树和全部分支提交者时间；通过后以 `--ff-only` 推进 `Learn/CUMULATIVE`。
- 第一次 tree object 查询中，PowerShell 把未加引号的 `^{tree}` 解释为其他语法，查询未生效；加引号重跑后，实现端点与学习端点的 tree object 均为 `996e5f09cebaea9b9974b97a684a4af727d0efae`。
- `0e01739` 是 `470ef24` 的祖先，学习区间没有 merge commit；实现与学习 worktree 均干净，主工作区仍只有本日志未跟踪。按提交者时间，`Learn/held-out-split-matrix@470ef24` 是全部本地与远端引用中的唯一最新提交。
- 在主工作区执行 `git merge --ff-only Learn/held-out-split-matrix`，`Learn/CUMULATIVE` 从 `0e01739` 快进到 `470ef24`；没有 merge commit。`Learn/CUMULATIVE` 与 `Learn/held-out-split-matrix` 同指 `470ef24`，实现分支保留在 `07b2c14`。
- 快进后 `470ef24` 仍是按提交者时间形成的唯一最新提交；主工作区唯一未跟踪文件仍是本日志，未被纳入任何提交。双线 Git 本轮闭合。

## 2026-09-01　八组 split 服务器运行

### 2026-09-01 00:31 +08:00　同步前规则核对

- 重新完整读取 `pocket-plus-dev-workflow`、其 `references/workflow.md`、`project-server-interaction`、AdaLigand `训练与运行/README.md` 和 `与服务器交互/sync_code.ps1`。
- 正式任务继续使用完整模式 `训练与运行/submit_task.sh`；不使用普通 SSH 承载重任务，不使用 `--simple`、自动依赖、重试、锁复用或 `scancel`。
- 将执行本地安全同步命令：`& '.\与服务器交互\sync_code.ps1'`。该入口只增量上传 AdaLigand，不删除远端项目业务文件；本地用户日志会随项目文件上传，但仍不进入 Git。
- 安全同步完成：远端 rsync 检查、目录确认、增量上传、权限设置和 `.sbatch/.sh` 行尾修正全部成功；本次传输 239,723 字节，命令退出码为 0。

### 2026-09-01 00:34 +08:00　远端只读预检与 Linux 真测试

- 将通过用户级统一 SSH helper 执行一次轻量预检：核对主机、同步后的两个任务脚本、正式输出根与 split 子目录、磁盘空间、当前 Slurm 队列；随后在 `/home/penghongen/anaconda3/envs/AdaLigand_stage1_py310` 中从 `Data_Preprocessing/held_out` 运行完整 pytest，并对五份 shell 执行 `bash -n`。
- 触发命令入口：`& "$env:USERPROFILE\.codex\tools\Invoke-ProjectSsh.ps1" -Command $remoteCommand`。其中 `$remoteCommand` 只包含上述只读检查和测试，不运行正式数据构建。
- 远端主机为 `master`；当前 split 只有原先六个空目录，尚无两个 `and` 目录。根目录七份旧 OR-0.5 参数产物仍完整存在，其中旧冗余边文件 3,736,282,840 字节。
- `/storage` 当前可用 66 TB。用户队列中没有 held-out 同名运行任务；其余任务保持不动。
- 正式环境报告 Gemmi 0.7.5、SciPy 1.15.2；Linux 完整 pytest 为 `23 passed`，五份 shell 的 `bash -n` 通过，预检退出码为 0。

### 2026-09-01 00:35 +08:00　提交共享 PDB 边证据任务

- 将通过 SSH helper 在远端 AdaLigand 根执行：`bash 训练与运行/submit_task.sh --sh held_out_finalize.sh --resource cpu --cpus 8 --mem 32G --time 04:00:00`。
- 该任务使用完整 release/launch 模式，只重读现有 24 个 MMseqs2 TSV 并生成参数无关 `pdb_edge_evidence.jsonl` 与 `stage2/edge_summary.json`；不重跑目录或 MMseqs2，不读取 `_COMPLETE` 作为门控。
- 提交成功，Slurm Job ID 为 `366208`；完整模式、任务根、脚本、8 CPU、无 GPU、无 pre/after hold 均由提交器回显确认。release 将在资源实际开始执行前冻结。
- 后续持续状态命令统一通过 SSH helper 执行：`squeue -h -j 366208 ...`、`sacct -X -j 366208 ...`，并只读查看 `/home/penghongen/Feedback/AdaLigand/allocations/366208/{out,err}` 与 launch 记录；每次结果继续记在本节。
- 首次状态查询：`366208|RUNNING|0:53|8|32G|cnode01`，`sacct` 同报 RUNNING 与 `ExitCode=0:0`；stdout/stderr 为空，尚未出现 launch.json。
- 启动一个本地持续状态循环：每 40 秒通过统一 SSH helper 执行 `sacct -X -j 366208 --format=State,Elapsed,ExitCode -n -P`，只输出带本地时间戳的状态；检测到 `COMPLETED/FAILED/CANCELLED/TIMEOUT/OUT_OF_MEMORY/NODE_FAIL/PREEMPTED/BOOT_FAIL/DEADLINE` 后停止。该循环不修改服务器状态。
- 首次循环查询返回 `RUNNING|00:02:56|0:0`，但 helper 把原生命令结果写到 Host stream，PowerShell 变量得到空值，自动终态解析因此报 null 并失效。已用 Ctrl+C 停止该本地循环；这不是 Job 或代码错误。
- 改用一次 SSH helper 长连接，由远端 Bash 每 40 秒读取并解析同一 `sacct` 行，输出 `时间|状态|耗时|退出码`，遇终态后自行退出；仍为只读监控，不创建服务器文件。
- 由于 SSH helper 对长连接输出缓冲，另行触发一次只读 `sacct` 查询以提供可见状态：Job `366208` 在 `cnode01` 继续 RUNNING，耗时 `00:05:39`，8 CPU、32 GB、退出字段 `0:0`。
- 只读 `sstat`/allocation/launch 查询显示：batch 平均与最大 RSS 均约 7.9 GB，累计读 1,315,074,872 字节、写 1,984,654,551 字节；release 为 `AdaLigand_7924d8f4b13d`，launch 为 `held_out_finalize_job366208_20260901T004158_a1`。
- allocation stdout 只记录 release、launch 与动态命令，stderr 只记录 release 创建；未见 traceback 或错误。正式共享文件尚未出现，符合 `_write_jsonl` 先写同目录隐藏临时文件再原子替换的实现。
- 后续一次 `sacct/sstat/隐藏临时文件` 查询：Job 继续 RUNNING，耗时 `00:09:03`；最大 RSS 8,391,544 KB，累计读 1,315,127,031 字节、写 1,984,654,551 字节；尚无共享边隐藏临时文件，说明仍在内存构建阶段。
- 再次执行同一只读 `sacct/sstat`：耗时 `00:11:30`，状态 RUNNING；最大 RSS 8,910,060 KB，累计读 1,315,182,906 字节、写 1,984,654,551 字节，无异常终态。
- 持续 `sacct` 查询在耗时 `00:15:17` 时仍为 RUNNING，退出字段 `0:0`；没有新错误事件。
- 长连接终态监听持续运行且没有终态输出；一次性里程碑查询在耗时 `00:20:47` 时仍为 RUNNING，最大 RSS 11,140,208 KB，累计读 1,315,387,871 字节、写 1,984,654,551 字节。
- 里程碑查询在耗时 `00:25:13` 时仍为 RUNNING，最大 RSS 11,555,820 KB，累计读 1,315,488,453 字节、写 1,984,654,551 字节；资源余量充足。
- 里程碑查询在耗时 `00:30:48` 时仍为 RUNNING，最大 RSS 12,225,836 KB，累计读 1,315,611,436 字节、写 1,984,654,551 字节；没有异常状态。
- 里程碑查询在耗时 `00:34:42` 时仍为 RUNNING，最大 RSS 12,885,668 KB，累计读 1,315,700,830 字节、写 1,984,654,551 字节；没有异常状态。
- 里程碑查询在耗时 `00:38:37` 时仍为 RUNNING，最大 RSS 13,884,132 KB，累计读 1,315,786,553 字节、写 1,984,654,551 字节；运行趋势稳定。
- 里程碑查询在耗时 `00:42:50` 时仍为 RUNNING，最大 RSS 14,842,996 KB，累计读 1,315,879,718 字节、写 1,984,654,551 字节；仍在安全资源范围。
- 里程碑查询在耗时 `00:46:43` 时仍为 RUNNING，最大 RSS 15,797,552 KB，累计读 1,315,969,107 字节、写 1,984,654,551 字节；仍在安全资源范围。
- 里程碑查询在耗时 `00:50:35` 时仍为 RUNNING，最大 RSS 16,384,308 KB，累计读 1,316,054,815 字节、写 1,984,654,551 字节；仍低于旧任务约 17.7 GB 峰值和本次 32 GB 请求。
- 里程碑查询在耗时 `00:54:35` 时仍为 RUNNING，最大 RSS 16,786,824 KB，累计读 1,316,144,239 字节、写 1,984,654,551 字节；尚未出现共享边隐藏临时文件。
- 里程碑查询在耗时 `00:58:50` 时仍为 RUNNING，最大 RSS 17,338,480 KB，累计读 1,316,237,419 字节、写 1,984,654,551 字节；接近旧任务峰值但仍只有本次申请内存约一半。
- 里程碑查询在耗时 `01:02:50` 时仍为 RUNNING，最大 RSS 17,700,360 KB，累计读 1,316,326,838 字节、写 1,984,654,551 字节；与旧任务峰值吻合，尚无共享边隐藏临时文件。
- 远端长连接终态监听在 `2026-09-01 01:43:37 +0800` 返回 `COMPLETED|01:04:15|0:0` 并自行退出；期间每 40 秒的完整状态序列均为同一 Job RUNNING，未发生重启或异常状态。
- 终态核对确认 Job `366208` 为 COMPLETED、耗时 `01:04:15`、8 CPU、32 GB、节点 `cnode01`、退出码 `0:0`；allocation stdout 报告第一次执行成功并正常释放，stderr 只有 release 创建记录。
- 新共享 `pdb_edge_evidence.jsonl` 为 3,661,314,578 字节、622,986 行；`stage2/edge_summary.json` 为 200 字节。汇总为原始合格 alignment 5,071,858、定向 entity 命中 4,640,103、PDB 边 622,986，其中 reference 568,377、held_out_internal 54,609。
- 首条共享边字段抽查包含四个 coverage、三组 matching、可比 chain/残基分母、PDB 和 relation；`pdb_coverage_mode`、`pdb_coverage_threshold`、`chain_pass`、`residue_pass`、`redundant` 五个参数字段均不存在。

### 2026-09-01 01:44 +08:00　提交八组合 split 数组

- 共享边任务终态与基本产物核对通过后，将在远端 AdaLigand 根执行：`bash 训练与运行/submit_task.sh --sh held_out_split_array.sh --resource cpu --cpus 8 --mem 32G --array 0-7 --time 04:00:00`。
- 数组八个元素分别生成 `0.5/0.6 × chain/residue/or/and`，每项 8 CPU、32 GB；不建立自动依赖，不重跑 MMseqs2，不覆盖共享边证据。
- 提交成功，Slurm 数组 Job ID 为 `366228`；完整模式、脚本、数组范围 `0-7`、每项 8 CPU/32 GB、无 pre/after hold 均由提交器回显确认。
- 后续通过 SSH helper 持续查询 `sacct -X -j 366228` 与各数组元素 allocation 日志；只有八项均为明确成功终态后才进入产物审计。
- 首次 `squeue/sacct`：八项在提交后约 1 分 57 秒全部 RUNNING，每项 8 CPU/32 GB；数组 0-1 位于 `cnode01`，2-3 位于 `cnode02`，4-7 位于 `cnode04`。
- 启动一次 SSH helper 长连接：远端 Bash 每 40 秒执行 `squeue -h -j 366228 -r`；队列清空后输出 `sacct -X -j 366228` 的八项最终状态并退出。该循环只读且不创建文件。
- 数组终态：0-7 八项全部 `COMPLETED`、退出码 `0:0`，各耗时 3 分 25 秒至 3 分 40 秒。raw Job ID 依次为 `366229,366230,366231,366232,366233,366234,366235,366228`。
- 基础清点确认每个 split 都有 622,986 条 `redundancy_edges.jsonl`、2,497 条 `held_out_identity.jsonl`、三个视图、summary 和空 `_COMPLETE`；八项 stdout 均报告第一次执行成功，stderr 仅记录复用 release `AdaLigand_7924d8f4b13d`，没有残留隐藏临时文件。
- 八组 summary 依次为：
  - `held_out_05_chain`：冗余边 378,598，基础合格 330，full_test/test_0/test_1 为 130/130/113。
  - `held_out_05_residue`：冗余边 268,635，基础合格 373，full_test/test_0/test_1 为 143/143/128。
  - `held_out_05_or`：冗余边 388,797，基础合格 315，full_test/test_0/test_1 为 124/124/108。
  - `held_out_05_and`：冗余边 258,436，基础合格 389，full_test/test_0/test_1 为 153/153/136。
  - `held_out_06_chain`：冗余边 298,211，基础合格 413，full_test/test_0/test_1 为 179/179/149。
  - `held_out_06_residue`：冗余边 219,082，基础合格 408，full_test/test_0/test_1 为 159/159/138。
  - `held_out_06_or`：冗余边 319,218，基础合格 378，full_test/test_0/test_1 为 143/143/128。
  - `held_out_06_and`：冗余边 198,075，基础合格 445，full_test/test_0/test_1 为 197/197/165。
- 逐字节执行七项 `cmp -s`：新 `split/held_out_05_or` 的冗余边、身份证、full_test、test_0、test_1、summary 和 `_COMPLETE` 与旧根 OR-0.5 对应文件全部一致。

### 2026-09-01 01:52 +08:00　一次性独立科学审计准备

- 为避免把审计逻辑加入正式 held-out 包，在项目 `tmp/` 新建未跟踪脚本 `held_out_split_audit_20260901.sh`；它通过完整 Slurm release 运行，结果暂存 `/storage/penghongen/tmp/held_out_split_audit_20260901.json`，验收后精确删除本地脚本、远端项目副本和临时结果。
- 审计不调用生产 selection 函数，也不读取 `_COMPLETE` 作为条件。它流式同步读取共享边与八组参数边，逐行核对共享字段、四模式判定、0.6 对 0.5 子集、OR/AND 关系、短链与 identity/双向 coverage、一对一见证和 coverage 算术；随后独立重算 2,497 份身份证、固定种子贪心、极大性、test_0/test_1、summary 与见证字段。
- `bash -n` 首次即通过。第一次内嵌 Python 编译检查因 PowerShell 管道把中文源码编码为无效 surrogate 而失败；第二次又因命令行中的 `<<` 引号被 PowerShell 解释为重定向而失败。两次均只影响本地检查器，脚本未执行、未改动。
- 改用 PowerShell here-string 传递检查器源码后，内嵌 Python `compile(...)` 与 `bash -n` 均通过。
- 将再次执行安全同步命令 `& '.\与服务器交互\sync_code.ps1'` 上传这一份一次性审计脚本与最新日志；不做删除式同步。
- 第二次安全同步完成：增量发送 148,555 字节，远端权限与 shell 行尾修正成功，退出码 0。
- 将通过 SSH helper 在远端 AdaLigand 根执行：`bash 训练与运行/submit_task.sh --sh tmp/held_out_split_audit_20260901.sh --resource cpu --cpus 8 --mem 32G --time 04:00:00`。这是完整 release 模式的一次性只读审计任务。
- 远端 `bash -n` 再次通过，审计提交成功，Slurm Job ID 为 `366242`；完整模式、脚本路径、8 CPU/32 GB 和无 hold 均由提交器回显确认。
- 后续持续查询 `sacct -X -j 366242`、`sstat -j 366242.batch` 与 allocation 日志；审计脚本若发现任一不一致会以非零状态和明确消息结束。
- 初次状态为 `RUNNING|00:00:42|8|32G|cnode01|0:0`。另启动一次每 40 秒读取 `sacct` 的只读终态监听，遇明确终态后退出。
- 耗时 `00:02:04` 时仍 RUNNING；此时 batch CPU 约 20 秒、最大 RSS 28,940 KB，主要仍在 release/启动 I/O，stdout/stderr 暂为空。
- 耗时 `00:04:56` 时仍 RUNNING；batch CPU 约 2 分 15 秒，最大 RSS 450,196 KB，累计读 6,997,739,438 字节、写 248,983,239 字节。流式审计未整体载入九份大文件。
- 耗时 `00:09:07` 时仍 RUNNING；batch CPU 约 6 分 26 秒，最大 RSS 455,928 KB，累计读 22,661,631,658 字节、写 248,983,239 字节，大文件逐行核对已过半。
- 终态监听在耗时 `00:12:37` 返回 COMPLETED、退出码 `0:0`；batch 最大 RSS 459,688 KB，总读约 31,829.33 MB、写约 237.45 MB。stdout 报告审计 `status=passed`，stderr 只有 release 创建记录。
- 审计覆盖 198,055 个 polymer entity、622,986 条共享 PDB 边和 2,497 个 held-out PDB；八组的参数边、身份证、summary、固定种子视图均逐项重算一致，八个 full_test 全部 `independent=true`、`maximal=true`。

### 2026-09-01 02:25 +08:00　移除旧根目录 OR-0.5 参数副本

- 删除前置证据已经同时满足：八数组均 `COMPLETED 0:0`，每组基础文件与行数完整，旧新 OR05 七项逐字节一致，一次性科学审计 `status=passed`。
- 将通过 SSH helper 把 `/storage/penghongen/AdaLigand/held_out` 解析为固定真实路径，逐个验证以下七个目标是该根内的普通非符号链接文件，再使用单次显式 `rm --` 删除：`redundancy_edges.jsonl`、`held_out_identity.jsonl`、`full_test.json`、`test_0.json`、`test_1.json`、`stage2/summary.json`、`stage2/_COMPLETE`。不使用 glob，不删除任何共享文件或 split 文件。
- 这些是旧参数副本；其内容已由 `split/held_out_05_or` 逐字节保存，因此可恢复，但本轮不会建立兼容副本、硬链接或符号链接。
- 路径解析与七个普通文件验证通过后，显式删除全部七项；逐项复查均已不存在。删除总量约 3.74 GB，其中主要为旧 `redundancy_edges.jsonl` 3,736,282,840 字节。
- 删除后立即确认共享 `pdb_edge_evidence.jsonl` 3,661,314,578 字节、`stage2/edge_summary.json` 200 字节及新 OR05 冗余边 3,736,282,840 字节仍存在。

### 2026-09-01 02:26 +08:00　临时审计清理与最终目录验收

- 将精确删除本轮创建的本地 `tmp/held_out_split_audit_20260901.sh`、远端同步副本 `/home/penghongen/My_Project/AdaLigand/tmp/held_out_split_audit_20260901.sh` 和临时报告 `/storage/penghongen/tmp/held_out_split_audit_20260901.json`。Slurm release、launch 与 allocation 日志保留为审计证据。
- 删除后将只读核对根目录无七个旧参数路径、split 恰含八个目标目录、每个目录恰含七个既定文件、无隐藏临时文件，并汇总正式根占用。
- 本地一次性脚本、远端项目同步副本和 `/storage/penghongen/tmp` 临时报告均已精确删除；Slurm release `AdaLigand_2d9a4a1f2236`、launch `held_out_split_audit_20260901_job366242_20260901T021411_a1` 与 allocation 日志保留。
- 最终只读验收通过：根目录七个旧参数路径全部缺失；`split` 恰含八个 `held_out_<05|06>_<chain|residue|or|and>` 目录；每个目录恰含六个 JSON/JSONL 业务文件和一个空 `_COMPLETE`；隐藏临时文件为 0。
- 共享 `pdb_edge_evidence.jsonl` 和 `stage2/edge_summary.json` 仍存在；正式根共 156 个文件，占用约 34 GB。

### 本轮结论

- 正式服务器任务 `366208`、数组 `366228_0-7` 和独立审计 `366242` 全部 `COMPLETED 0:0`。没有因运行结果修改正式代码，没有重跑目录或 MMseqs2，没有新增失败率、发布、SHA、完成标记或兼容副本门控。
- 八组 `full_test` 都是固定种子贪心得到的极大独立集；`test_0` 是其至多 200 项无放回视图，本轮八组 full_test 均少于 200，因此 test_0 取全部成员；`test_1` 是严格 occurrence `(1, 100)` 的保序子集。
- 参数相关产物现在只位于 `split/held_out_<05|06>_<mode>/`；共享根只保留序列、MMseqs2、基础身份证与参数无关边证据。本轮服务器执行与端到端验收闭合。

### 2026-09-01 02:31 +08:00　正式文档与 Git 最终收口

- 在实现分支只回填正式服务器事实与映射状态，提交为 `73b5df4 docs(held-out): record split matrix production run`；没有修改代码、测试或运行脚本。
- 在学习分支以同一两份文档形成提交 `04b6f09 记录八组合正式运行与独立验收`。实现端点 `73b5df4` 与学习端点 `04b6f09` 的 tree object 均为 `0f40cc27f92a3e486cbb9507052117b65a058e0f`，路径级 diff 为空。
- 主工作区执行 `git merge --ff-only Learn/held-out-split-matrix`，`Learn/CUMULATIVE` 快进到 `04b6f09`；实现分支保留在 `73b5df4`。`04b6f09` 是按提交者时间形成的唯一最新提交。
- 最终主工作区 Windows pytest 为 `22 passed, 1 skipped`，唯一跳过仍是 Windows 未安装 Gemmi；`compileall` 通过。Linux 正式环境此前为 `23 passed`。
- 主工作区唯一未跟踪文件仍是本《详细日志记录.md》；实现和学习 worktree 均无未提交改动，本轮一次性审计脚本未残留。

### 2026-09-03　Held-out 脚本归位

- `held_out_catalog_array.sh`、`held_out_catalog_finalize.sh`、`held_out_mmseqs_array.sh`、`held_out_finalize.sh` 和 `held_out_split_array.sh` 已从 `训练与运行/sh/` 原样移动到 `Data_Preprocessing/held_out/sh/`，脚本内容未修改。
- 只更新了当前文档中的提交路径；上文历史命令保留原貌。本次没有同步服务器，也没有重新提交任务。
