# 批阅记录

- **源文件**：ligand的数据表示.md
- **源文件路径**：d:/OneDrive/Emap2lig/文档/说明/ligand的数据表示.md
- **源文件版本**：未知
- **批阅时间**：20260620_2041
- **批阅版本**：v1
- **批注数量**：0
  - 评论：0
  - 删除：0
  - 后插：0
  - 前插：0

---

## 操作指令

> 指令已按**从后往前**排列（倒序），请严格按照顺序从上到下逐条执行。
> 每条指令提供了「文本锚点」用于精确定位，请优先通过锚点文本匹配来确认目标位置，blockIndex 仅作辅助参考。

---

## 原始数据（JSON）

> 如需精确操作，可使用以下 JSON 数据。其中 `blockIndex` 是基于空行分割的块索引（从0开始），`startOffset` 是目标文本在块内的字符偏移量（从0开始），可用于区分同一块内的重复文本。

```json
{
  "fileName": "ligand的数据表示.md",
  "docVersion": "未知",
  "reviewVersion": 1,
  "annotationCount": 0,
  "rawMarkdown": "# ligand 的数据表示\n\n本文只解释当前 Emap2lig 推理代码中 ligand 如何从用户输入变成 Build 模型可以吃的数据。\n\n本阶段不泛讲 ligand 生物学背景，也不展开 SMILES 完整语法。这里的重点是：**代码里的每一步把数据从什么格式变成什么格式**。\n\n## 0. 一句话总览\n\nEmap2lig-Build 并不是直接把 SMILES 字符串送进模型。\n\n它先把用户输入的 ligand 统一转成一个结构化的 `LigandObject.npz`，再由 `LigandModelingDataset` 把它转成固定 shape 的张量：\n\n```text\n用户 ligand list\n-> LigandRecord\n-> RDKit Mol\n-> LigandObject.npz\n-> LigandModelingDataset.__getitem__()\n-> atom_feature / bond_feature / ref_pos / atom_mask / pair_mask\n-> Emap2lig.forward()\n-> sampled_atom_coords\n-> LigandWriter 写出 cif\n```\n\n先抓住一个核心：**ligand 在模型眼里主要是一个带 3D 参考构象的化学图**。\n\n这里的“图”不是密度图，而是 graph：\n\n```text\nnode = atom\nedge = bond\nnode feature = atom_feature\nedge/pair feature = bond_feature\nreference geometry = ref_pos\n```\n\n## 1. 用户最初能给什么\n\n入口格式见 `docs/input-format.md`，代码入口是：\n\n```text\nsrc/emap2lig/main.py::parse_ligand_list()\n```\n\n用户通过 `--ligand-list` 给一个 YAML 或单行文本。当前支持三类 ligand：\n\n| 用户输入类型   | 示例                 | 代码里的 type    | 含义                                                         |\n| -------- | ------------------ | ------------ | ---------------------------------------------------------- |\n| CCD      | `- CCD: ATP`       | `\"CCD\"`      | 用 PDB Chemical Component Dictionary 里的标准小分子定义。如：- CCD: ATP |\n| SMILES   | `- SMILES: CCO`    | `\"SMILES\"`   | 用一行字符串描述分子连接关系，再由 RDKit 生成 3D 构象。如：- SMILES: CC(\\=O)O      |\n| BRANCHED | 多个 residue + bonds | `\"BRANCHED\"` | 多个 CCD residue 拼成一个支链 ligand，例如糖链片段                        |\n\n`parse_ligand_list()` 不直接生成模型特征。它只把用户输入整理成 `LigandRecord`：\n\n```text\nLigandRecord(\n    type: \"CCD\" | \"SMILES\" | \"BRANCHED\",               # 配体来源类型。决定后继预处理走哪条解析路线。\n    name: str,                                         # 配体名。CCD 填三字母(如 \"ATP\")；SMILES/BRANCHED 自动分配(如 \"LIG1\")。\n    blobs: list[int] | None,                           # 绑定限制。指定只尝试与哪些检测出的 density blob 配对(如 [1, 2])。\n    smiles: str | None,                                # 拓扑结构。SMILES 路线填对应的一行字符串(如 \"CCO\")；CCD/BRANCHED 路线默认为 None。\n    residues: dict[int, str] | None,                   # 多单元组分(仅限 BRANCHED)。指明拼接子单元，序号->CCD名(如 {1: \"NAG\", 2: \"NAG\"})。\n    bonds: list[tuple[int, str, int, str]] | None,     # 残基间连键(仅限 BRANCHED)。定义子单元连接关系，格式：[(res1_idx, atom1, res2_idx, atom2)] (如 [(1, \"C1\", 2, \"O4\")])。\n)\n```\n\n来源定义：\n\n```text\nsrc/emap2lig/data/types.py::LigandRecord\n```\n\n1. **CCD**：查标准字典（提供高精度 3D 构象 + 标准原子名）。\n2. **SMILES**：掉包RDKit, 用字符串现画（无 3D 坐标，临时生成构象与原子名）。\n3. **BRANCHED**：把多个 CCD 拼成糖链（多单元结构，适合复杂糖基配体）。\n4. **等价性**：它们都能被 parse_ligand_list 统一转化为模型所需的 `LigandRecord` / `LigandObject` 格式，但**输入信息的丰富度与 3D 先验质量上 CCD > BRANCHED > SMILES**。\n\n对用于 BRANCHED 配体的专属字段 residues 和 bonds:\n\n来源为用户指定：用户在 YAML 配置文件中通过 \"序号. CCD名\" 的格式手动编写（通常为从 1 开始的递增整数），例如：\n\n\\- BRANCHED:\n\n&#x20;   residues:\n\n&#x20;     \\- \"1. NAG\"\n\n&#x20;     \\- \"2. NAG\"\n\n&#x20;   bonds:\n\n&#x20;     \\- [1, \"C1\", 2, \"O4\"]  # 代表 1 号 NAG 的 C1 原子与 2 号 NAG 的 O4 原子相连\n\n### 1.1 `blobs` 是什么\n\n`blob_id` 或 `blobs` 不是 ligand 化学信息。\n\n它只是用户指定“这个 ligand 只尝试哪些 density blob”。如果不指定，后续 `LigandModelingDataset` 会把这个 ligand 和所有 blob 组合，再用一个体积/原子数比例做粗过滤。\n\n## 2. `LigandRecord` 怎样变成 RDKit Mol\n\n入口是：\n\n```text\nsrc/emap2lig/main.py::prepare_ligand_dataset()\n```\n\n它按 `LigandRecord.type` 分派：\n\n| type         | 函数                        | 主要外部包/工具                                                  | 输出          |\n| ------------ | ------------------------- | --------------------------------------------------------- | ----------- |\n| `\"CCD\"`      | `prepare_CCD_data()`      | `get_ccd_mol()`，RDKit，pdbeccdutils/RCSB/HuggingFace cache | `<CCD>.npz` |\n| `\"SMILES\"`   | `prepare_SMILES_data()`   | `get_conformer_from_smiles()`，RDKit ETKDG/UFF             | `LIG1.npz`  |\n| `\"BRANCHED\"` | `prepare_BRANCHED_data()` | 多个 `get_ccd_mol()` + 用户给的 residue 间 bond                  | `LIG1.npz`  |\n\n这三条路最后都会走向同一种中间表示：`LigandObject`。\n\n### 2.1 CCD 路线\n\n代码：\n\n```text\nsrc/emap2lig/main.py::prepare_CCD_data()\nsrc/emap2lig/data/ccd.py::get_ccd_mol()\n```\n\nCCD code 是 PDB 对小分子/化学组分的标准编号，比如 `ATP`、`NAG`、`HEM`。\n\n代码会按顺序尝试：\n\n```text\nbulk CCD dictionary\n-> ~/.emap2lig/ccd/<CODE>.pkl\n-> RCSB CIF endpoint\n```\n\n拿到的是 RDKit `Mol`。它包含：\n\n```text\natoms\nbonds\natom names\n可能已有的 conformer 坐标\n```\n\n随后代码会：\n\n```text\nChem.MolToSmiles(ref_mol)  # 生成一个 SMILES 元信息\nChem.RemoveHs(ref_mol, sanitize=False)  # 去掉氢原子\nprocess_molecule(...)\n```\n\n重要点：**Build 的 atom 表示主要使用重原子，氢原子在这里被移除。**\n\n### 2.2 SMILES 路线\n\n代码：\n\n```text\nsrc/emap2lig/main.py::prepare_SMILES_data()\nsrc/emap2lig/data/ccd.py::get_conformer_from_smiles()\n```\n\nSMILES 是一种文本写法，用字符串描述分子的原子和连接关系。例如：\n\n```text\nCCO\n```\n\n对代码来说，SMILES 的作用不是“生物学解释”，而是让 RDKit 能还原出一个分子图。\n\n代码步骤：\n\n```text\nChem.MolFromSmiles(smiles)\n-> Chem.AddHs(mol)\n-> add_conformer(mol)\n-> Chem.RemoveHs(mol)\n-> _assign_canonical_atom_names(mol, smiles)\n```\n\n其中 `add_conformer()` 会尝试给分子生成 3D 坐标：\n\n```text\nRDKit ETKDGv3 embedding\n-> UFFOptimizeMolecule\n-> 如果失败，尝试 fallback\n```\n\n这里的 conformer 可以先理解为：**同一个分子连接关系下的一套参考 3D 坐标**。\n\nBuild 后面不是直接相信这套坐标作为最终答案，而是把它作为 ligand 的几何先验之一；真正输出坐标来自 diffusion 采样。\n\n### 2.3 BRANCHED 路线\n\n代码：\n\n```text\nsrc/emap2lig/main.py::prepare_BRANCHED_data()\nsrc/emap2lig/main.py::process_branched_ligand()\n```\n\nBRANCHED 适合多个 residue 组成的 ligand。用户给：\n\n```text\nresidues: 每个 residue 是哪个 CCD code\nbonds: 哪两个 residue 的哪些 atom 连起来\n```\n\n代码做的事是：\n\n```text\n每个 residue 单独用 get_ccd_mol() 取 RDKit Mol\n-> 提取每个 residue 的 atoms/bonds\n-> 把 atom index 按 residue 顺序拼接\n-> 把用户指定的 residue 间 bond 追加进去\n-> 写成一个 LigandObject\n```\n\n这里 `residue_id` 开始变重要：普通 CCD/SMILES ligand 通常全是 `residue_id=1`；BRANCHED ligand 会有多个 residue id。\n\n## 3. RDKit Mol 怎样变成 `LigandObject.npz`\n\n普通 CCD/SMILES 共用：\n\n```text\nsrc/emap2lig/main.py::process_molecule()\n```\n\n它调用：\n\n```text\n_extract_atom_features()\n_extract_bond_features()\n```\n\n然后写出：\n\n```text\nsrc/emap2lig/data/types.py::LigandObject\n```\n\n### 3.1 `LigandObject` 的字段\n\n`LigandObject` 是一个可序列化 dataclass，会通过 `np.savez_compressed` 写成 `.npz`。\n\n```text\nLigandObject(\n    smiles: str,                                       # 拓扑结构。SMILES 结构字符串（如 \"CCO\"，BRANCHED 时为空 \"\"）。\n    atom_names: list[str],                             # 原子名称。每个原子的字符串格式名字列表（如 [\"C1\", \"C2\", \"O1\"]），供写出 CIF 使用（与 atoms[\"name\"] 编码对应）。\n    atoms: np.ndarray,                                 # 原子数组。形状 (N,)，dtype=Atom 的结构化数组，保存原子信息（如元素号 6、参考坐标 [0.1, 1.2, 0.5]）。\n    bonds: np.ndarray,                                 # 键数组。形状 (E,)，dtype=Bond 的结构化数组，保存原子连接索引及键型。\n    name: str,                                         # 配体名称。最终输出配体模型文件的名称（如 CCD code \"ATP\" 或自动生成的 \"LIG1\"）。\n    residue_names: list[str],                          # 残基单元名称。CCD 残基名列表，普通小分子仅 1 项（如 [\"ATP\"]），BRANCHED 会有多个（如 [\"NAG\", \"NAG\"]）。\n    symmetries: list,                                  # 对称性信息。保留字段，当前实现统一默认返回空列表 []。\n    blobs: list[int] | None,                           # 绑定限制。此配体关联尝试的 density blob 编号列表（如 [1, 2] 或 None 尝试所有）。\n)\n```\n\n字段：\n\n| 字段              | 类型/形状                               | 含义                                                                                                    |                                                |\n| :-------------- | :---------------------------------- | :---------------------------------------------------------------------------------------------------- | ---------------------------------------------- |\n| `smiles`        | `str`                               | ligand 的 SMILES 结构元信息（如 `\"CCO\"`，BRANCHED 当前可为空 `\"\"`）                                                  |                                                |\n| `atom_names`    | `list[str]`，长度 `N`                  | 每个 atom 的名字列表（如 `[\"C1\", \"O4\"]`），供 `LigandWriter` 写出 mmCIF 使用，与 `atoms[\"name\"]`（`4i1` 整数编码，用于模型输入）相对应。 |                                                |\n| `atoms`         | `np.ndarray`，形状 `(N,)`，`dtype=Atom` | 原子表，保存每个 atom 的字段（见下）                                                                                 |                                                |\n| `bonds`         | `np.ndarray`，形状 `(E,)`，`dtype=Bond` | 键表，保存每条 bond（包含连接的两端原子索引，如 `0` 号与 `1` 号原子相连）                                                          |                                                |\n| `name`          | `str`                               | ligand 名，如 CCD code（如 `\"ATP\"`）或自动生成的 `\"LIG1\"`                                                         |                                                |\n| `residue_names` | `list[str]`                         | residue 名列表；普通小分子仅 1 项（如 `[\"ATP\"]`），BRANCHED 可能有多个（如 `[\"NAG\", \"NAG\"]`）                                |                                                |\n| `symmetries`    | `list`                              | 对称性信息；当前主要是保留字段，统一默认返回 `[]`                                                                           |                                                |\n| `blobs`         | \\`list[int]                        | None\\`                                                                                                | 用户指定的 blob 绑定关系限制（如 `[1, 2]`，为 `None` 时表示不作限制） |\n\n这里 `N` 是 ligand 的真实 atom 数，`E` 是真实 bond 数。\n\n### 3.2 `Atom` 结构化 dtype\n\n定义在：\n\n```text\nsrc/emap2lig/data/types.py::Atom\n```\n\n每个 atom 保存：\n\n| 字段           |          形状 | 新手解释                      | 后续用途                                 |\n| ------------ | ----------: | ------------------------- | ------------------------------------ |\n| `name`       |      `(4,)` | atom name 的 4 字符编码        | 进入 `atom_feature`                    |\n| `element`    |          标量 | 原子序数，如 C\\=6、O\\=8          | 进入元素 one-hot 和元素物理属性                 |\n| `charge`     |          标量 | formal charge，形式电荷        | 进入 `atom_feature`                    |\n| `coords`     |      `(3,)` | 当前坐标占位；此阶段通常是 0           | writer 会写预测坐标                        |\n| `ref_pos`    |      `(3,)` | RDKit/CCD conformer 的参考坐标 | 进入模型                                 |\n| `is_present` |     标量 bool | 是否在结构中存在；当前推理主要是保留字段      | 不作为主输入                               |\n| `chirality`  | `(7,)` bool | 手性类型 one-hot              | 进入 `atom_feature`                    |\n| `in_ring`    | `(4,)` bool | 是否在 3/4/5/6 元环中           | 进入 `atom_feature`                    |\n| `residue_id` |          标量 | 所属 residue 编号             | 进入 `atom_feature`，也用于 per-residue 增强 |\n\n### 3.3 `Bond` 结构化 dtype\n\n定义在：\n\n```text\nsrc/emap2lig/data/types.py::Bond\n```\n\n每条 bond 保存：\n\n| 字段        |          形状 | 含义                                                     |\n| --------- | ----------: | ------------------------------------------------------ |\n| `atom_1`  |          标量 | bond 一端 atom index                                     |\n| `atom_2`  |          标量 | bond 另一端 atom index                                    |\n| `type`    | `(5,)` bool | bond type one-hot：single/double/triple/dative/aromatic |\n| `in_ring` | `(4,)` bool | 这条 bond 是否在 3/4/5/6 元环中                                |\n\n注意：在 `LigandObject` 阶段，bond 是稀疏表 `(E,)`；到 Dataset 阶段会变成 dense pair tensor。\n\n## 4. `LigandObject` 怎样和 density blob 配对\n\n入口：\n\n```text\nsrc/emap2lig/main.py::run_structure_modeling()\nsrc/emap2lig/data/dataset.py::LigandModelingDataset\n```\n\n`run_structure_modeling()` 会准备两类文件：\n\n```text\nblob_*.npz          # DensityObject，由 Find 阶段得到\nligand_name.npz    # LigandObject，由 ligand list 得到\n```\n\n`LigandModelingDataset` 做的是组合：\n\n```text\n一个 DensityObject + 一个 LigandObject = 一个样本\n```\n\n如果用户没有指定 `blobs`，它会先尝试所有 blob-ligand pair，然后用一个经验比例过滤：\n\n```text\nratio = non_zero_voxels(instance_grid) / n_atoms\n保留 5 <= ratio <= 25 的 pair\n```\n\n直觉：blob 太小装不下这个 ligand，或 blob 太大不像这个 ligand，都先过滤掉。\n\n## 5. Dataset 返回的原始字段： 它是当一个 LigandObject 被加载并与某一个特定的检测到的密度 DensityObject (Blob) 配对时产生的结构。\n\n核心函数：\n\n```text\nsrc/emap2lig/data/dataset.py::LigandModelingDataset.__getitem__()\n```\n\n它加载：\n\n```text\nDensityObject.load(blob_i.npz)\nLigandObject.load(ligand_j.npz)\n```\n\n然后先构造一个 `features` 字典。\n\n### 5.1 density 侧字段\n\n| 字段              |      形状/类型     | 含义                                                                                 |\n| :-------------- | :------------: | :--------------------------------------------------------------------------------- |\n| `input_density` | `(1, D, H, W)` | 当前 blob 裁剪出的单通道连续电子密度图（`float32` 实数，**非 0/1 掩码**；对应 `DensityObject.density_grid`）。 |\n| `voxel_size`    |     `(3,)`     | x/y/z 方向 voxel 大小。                                                                 |\n| `global_origin` |     `(3,)`     | 当前裁剪块原点的世界坐标。                                                                      |\n| `object_id`     |   标量/list 元信息  | blob 编号。                                                                           |\n\n> **注意**：`DensityObject.instance_grid`（即 0/1 掩码，对应当前检测出的 blob 边界）**并没有**作为键值打包进 Dataset 返回的 `features` 字典中，因此模型前向计算和 Batch 数据里都不直接包含它。它是作为临时辅助变量，在 Dataset 内部被“消费”并转换为其它特征：1) **计算 blob 几何中心**：通过它计算 `groundtruth_center`（写入 `features[\"groundtruth_center\"]`），并将世界坐标系平移到以该中心为原点（修改 `global_origin`）。\n> 2\\) **采样空间提示点**：在掩码为 1 的体素内随机采样 `multiplicity` 个三维坐标点作为空间引导提示（写入 `features[\"prompt_points\"]`）。\n\n### 5.2 ligand 原始字段\n\n| 字段           | 类型                       | 含义&例子                                                                                                                    |\n| :----------- | :----------------------- | :----------------------------------------------------------------------------------------------------------------------- |\n| `atoms`      | `np.ndarray(dtype=Atom)` | 原子表，含有每个原子具体属性特征。                                                                                                        |\n| `bonds`      | `np.ndarray(dtype=Bond)` | 键表，稀疏边表。                                                                                                                 |\n| `atom_names` | `list[str]`              | 原子名称列表（字符串格式，如 `[\"C1\", \"O4\"]`），主要供 `LigandWriter` 写出 mmCIF 使用。与 `Atom[\"name\"]`（`4i1` 字符编码，用于模型输入特征 `atom_feature`）不同但对应。 |\n| `smiles`     | `str`                    | 分子的 SMILES 结构元信息（BRANCHED 时可为空）。例如：`\"CCO\"` (代表乙醇)。                                                                       |\n| `class_name` | `str`                    | 配体本身的化学分类/代号（即 `ref_mol_object.name`，非唯一，**允许重复**）。例如多个不同位点均可配 `\"ATP\"`。                                                  |\n| `identifier` | `str`                    | blob-ligand pair 联合唯一标识，格式为 `{blob_id}_{class_name}`。由于 blob_id 唯一，故其**绝对唯一**。例如：`\"3_ATP\"` (代表 3 号 blob 配 ATP)。         |\n| `symmetries` | `list`                   | 对称性保留字段。当前实现统一默认返回 `[]`。                                                                                                 |\n\n> [!WARNING]\n> **关于重复 CCD 代码的覆盖 Bug**：\n> 如果用户在 YAML 列表中写了多个具有相同 CCD 代码的条目并绑定不同的 `blob_id`（例如 `- CCD: ATP, blobs: 1` 和 `- CCD: ATP, blobs: 2` 作为独立元素在列表中并列），在 `prepare_ligand_dataset` 中，后一个条目在生成 `preprocess/ligands/ATP.npz` 时会**直接覆盖**前一个。这会导致前一个的 `blobs` 绑定信息丢失。\n> **正确写法**：如果同一个化学配体需要配对多个指定的 blob，应当合并写成列表形式，如：`- CCD: ATP, blobs: [1, 2]`。\n\n这些字段中，化学图和参考坐标会进入模型提取特征，其余元信息主要给 writer 还原写出使用。\n\n## 6. `_process_features()`：真正转成模型张量\n\n核心函数：\n\n```text\nsrc/emap2lig/data/dataset.py::LigandModelingDataset._process_features()\n```\n\n默认参数：\n\n```text\nmax_atoms = 200\npad_to_max = True\n```\n\n所以真实 atom 数 `N` 会被 padding 到固定长度 `max_len=200`。\n\n输出字段：\n\n| 字段             |            默认形状 | 含义                                     |\n| -------------- | --------------: | -------------------------------------- |\n| `atom_feature` |    `(200, 149)` | 每个 atom 的单体特征；padding 行为 0             |\n| `bond_feature` | `(200, 200, 9)` | 每对 atom 的 pair/bond 特征；无键或 padding 为 0 |\n| `ref_pos`      |      `(200, 3)` | 参考 conformer 坐标                        |\n| `atom_mask`    |        `(200,)` | 真实 atom 为 True，padding 为 False         |\n| `pair_mask`    |    `(200, 200)` | 真实 atom pair 为 True                    |\n| `n_atoms`      |              标量 | padding 前真实 atom 数                     |\n| `residue_id`   |        `(200,)` | 每个 atom 所属 residue 编号                  |\n\n### 6.1 `atom_feature` 的 149 维\n\n每个 atom 变成 149 维：\n\n```text\natom name code       4\nelement one-hot    128\nformal charge        1\nchirality one-hot    7\nring membership      4\nresidue_id           1\nis_metal             1\ncovalent radius      1\nvdW radius           1\natomic weight        1\n----------------------\ntotal              149\n```\n\n新手要点：\n\n1. `element one-hot` 告诉模型这是 C、N、O、P、S 等哪种元素。\n2. `charge` 给形式电荷。\n3. `chirality` 是三维手性信息的离散编码。\n4. `ring membership` 告诉模型 atom 是否处于常见环大小中。\n5. `residue_id` 主要帮助多 residue ligand。\n6. 半径、原子量、是否金属来自 `gemmi.Element`。\n\n### 6.2 `bond_feature` 的 9 维\n\n`LigandObject.bonds` 原来是 `(E,)` 的稀疏边表。\n\n`_process_features()` 把它写成 dense pair tensor：\n\n```text\nbond_feature: (200, 200, 9)\n```\n\n最后一维：\n\n```text\nbond type one-hot   5\nring membership     4\n----------------------\ntotal               9\n```\n\n对每条 bond `(i, j)`，代码会对称写入：\n\n```text\nbond_feature[i, j, :]\nbond_feature[j, i, :]\n```\n\n因为模型里的 pair representation 同时使用 `(i, j)` 和 `(j, i)`。\n\n### 6.3 `ref_pos` 不是最终预测坐标\n\n`ref_pos` 来自 RDKit/CCD conformer，是参考构象。\n\n它的作用是给模型一个 ligand 内部几何先验，例如哪些 atom 大概相距多远。\n\n最终输出坐标不是 `ref_pos` 直接平移得到的，而是 diffusion 从噪声开始采样出的：\n\n```text\nsampled_atom_coords: (B * multiplicity, 200, 3)\n```\n\n### 6.4 mask 为什么重要\n\n因为所有 ligand 都 padding 到 `max_atoms=200`，模型必须知道哪些行是真 atom。\n\n```text\natom_mask: (200,)\npair_mask: (200, 200) = outer(atom_mask, atom_mask)\n```\n\npadding atom 不应该参与 attention、pair 更新、loss 或输出还原。\n\n## 7. 坐标中心化和 prompt point\n\n`_process_features()` 后，Dataset 还会做三件事：\n\n```text\n_center_random_augment_ref_coords()\n_center_groundtruth_coords()\n_generate_prompt_points()\n```\n\n### 7.1 参考构象增强\n\n`_center_random_augment_ref_coords()` 对 `ref_pos` 做中心化和随机增强。\n\n代码按 `residue_id` 分组处理，所以 BRANCHED ligand 的多个 residue 可以分别增强。\n\n### 7.2 blob 中心化\n\n`_center_groundtruth_coords()` 用 `instance_grid` 计算当前 blob 的中心：\n\n```text\ngroundtruth_center: (3,)\nglobal_origin = global_origin - groundtruth_center\n```\n\n直觉：模型在以 blob 中心附近为原点的局部坐标系里工作。\n\nwriter 输出时会把中心加回去：\n\n```text\nbatch[\"global_origin\"] + batch[\"groundtruth_center\"]\n```\n\n### 7.3 prompt point\n\n`_generate_prompt_points()` 从 blob 的 instance mask 内随机采样点：\n\n```text\nprompt_points: (multiplicity, 3)\n```\n\n`multiplicity` 表示每个 blob-ligand pair 生成多少个 conformer。\n\n如果 batch size 是 1，进入模型前通常是：\n\n```text\nprompt_points: (1, multiplicity, 3)\n```\n\n模型 forward 会 reshape 成：\n\n```text\n(B * multiplicity, 3)\n```\n\nprompt point 的作用：告诉 instance module 和 diffusion module “这次采样以 blob 中的哪个点作为空间提示”。\n\n## 8. DataLoader batch 后的关键字段\n\n`run_structure_modeling()` 中：\n\n```text\nDataLoader(\n    dataset,\n    batch_size=1,\n    collate_fn=collate_fn,\n)\n```\n\n所以模型看到的典型字段形状是：\n\n| 字段              |               典型 batch 形状 | 是否直接进模型    |\n| --------------- | ------------------------: | ---------- |\n| `input_density` |         `(B, 1, D, H, W)` | 是          |\n| `global_origin` |                  `(B, 3)` | 是          |\n| `voxel_size`    |                  `(B, 3)` | 是          |\n| `atom_feature`  |           `(B, 200, 149)` | 是          |\n| `bond_feature`  |        `(B, 200, 200, 9)` | 是          |\n| `ref_pos`       |             `(B, 200, 3)` | 是          |\n| `atom_mask`     |                `(B, 200)` | 是          |\n| `pair_mask`     |           `(B, 200, 200)` | 是          |\n| `prompt_points` |    `(B, multiplicity, 3)` | 是          |\n| `atoms`         | list of structured arrays | writer 用   |\n| `atom_names`    |                      list | writer 用   |\n| `identifier`    |                list[str] | writer/日志用 |\n\n## 9. 模型怎样消费 ligand features\n\n核心入口：\n\n```text\nsrc/emap2lig/model/model.py::Emap2lig.forward()\n```\n\n### 9.1 ConformerEmbedder\n\n代码：\n\n```text\nsrc/emap2lig/model/modules/conf_embedder.py::ConformerEmbedder.forward()\n```\n\n输入：\n\n```text\nref_pos:       (B, 200, 3)\natom_feature: (B, 200, 149)\nbond_feature: (B, 200, 200, 9)\natom_mask:    (B, 200)\npair_mask:    (B, 200, 200)\n```\n\n输出：\n\n```text\natom_init_feats: (B, 200, d_atom_hidden)\npair_init_feats: (B, 200, 200, d_pair_hidden)\n```\n\n它做的事：\n\n1. 把 `atom_feature` 线性投影成 atom embedding。\n2. 把 `bond_feature` 线性投影成 pair embedding。\n3. 从 `ref_pos` 计算 atom pair 的相对位置和距离。\n4. 把单 atom 信息也加到 pair 表示上。\n\n所以这里已经把“化学图 + 参考构象”融合成模型内部表征。\n\n### 9.2 PairFormer\n\n代码：\n\n```text\nsrc/emap2lig/model/modules/pairformer.py\n```\n\n输入：\n\n```text\natom_feats\npair_feats\natom_mask\npair_mask\n```\n\n它进一步更新 atom/pair 表征。直觉上，atom 之间通过 pair/bond/几何关系互相通信。\n\n本阶段只需要知道：PairFormer 消费的是 ligand 的 atom/pair 表示，不直接看 SMILES 字符串。\n\n### 9.3 InstanceSegModule\n\n代码：\n\n```text\nsrc/emap2lig/model/modules/instance_seg.py::InstanceSegModule.forward_embedding()\n```\n\n输入中和 ligand 相关的是：\n\n```text\natom_features: (B, 200, d_atom_hidden)\natom_mask:     (B, 200)\nprompt_point:  (B * multiplicity, 3)\n```\n\n同时它也看 density：\n\n```text\ninput_density: (B, 1, 48, 48, 48)\nglobal_origin: (B, 3)\nvoxel_size:    (B, 3)\n```\n\n输出里最重要的是：\n\n```text\ninstance_output:       (B * multiplicity, 1, 48, 48, 48)\nselected_point_feats:  (B * multiplicity, 8192, 64)\nselected_point_coords: (B * multiplicity, 8192, 3)\nglobal_features:       (B * multiplicity, 1, 256)\n```\n\n直觉：InstanceSegModule 在当前 blob 里，结合候选 ligand 的 atom features 和 prompt point，预测“这个 ligand instance 应该占据哪些密度点”，再选出 top-k voxel points 给 diffusion 用。\n\n这是未来加入 receptor condition 的重要观察点之一。\n\n### 9.4 DiffusionModule\n\n代码：\n\n```text\nsrc/emap2lig/model/modules/diffusion.py::AtomDiffusion.sample()\n```\n\n输入包括：\n\n| 参数名 | 类型/形状 | 含义 | 初学者易懂例子 |\n| :--- | :--- | :--- | :--- |\n| `atom_inputs` | `[B, N_a, 128]` | `ConformerEmbedder` 的初始原子表征特征（由原始的 149 维原子化学特征进行线性投影而来）。 | 每个原子的 128 维特征向量（如 `[0.23, -0.45, ...]`），仅包含配体本身的化学和类别特征，不含 3D 坐标。 |\n| `atom_feats` | `[B, N_a, 128]` | 经过 `PairFormer` 多层信息传播后更丰富的原子单体特征。 | 融合了分子内原子所处化学环境（如周围有多少单键、双键环结构）后的 128 维特征向量。 |\n| `pair_feats` | `[B, N_a, N_a, 64]` | 经过 `PairFormer` 交互精化后的原子对特征。 | 描述第 `i` 个原子与第 `j` 个原子之间的共价键型或参考拓扑距离偏置的 64 维特征。 |\n| `selected_point_feats` | `[B*M, 8192, 64]` | 由 `InstanceSegModule` 从密度图中筛选出的 8192 个最可能属于配体区域的体素（Voxel）特征。 | 每个体素周围密度的灰度级别和局部结构编码（如 `64` 维浮点数特征）。 |\n| `selected_point_coords` | `[B*M, 8192, 3]` | 对应 `selected_point_feats` 中 8192 个体素点的三维世界坐标。 | 体素点的物理三维坐标（如 `[0.5, -1.2, 3.4]` Å），用于辅助扩散模型对齐真实的电子密度。 |\n| `global_features` | `[B*M, 1, 256]` / `[B*M, 96]` | 整个密度裁剪块 (Blob) 的全局平均池化特征。 | 编码当前 Blob 整体质量、体积粗略范围的特征向量，充当全局几何上下文引导。 |\n| `atom_mask` | `[B, N_a]` (bool) | 原子有效性掩码。标明该行是真实的原子，还是用于凑齐 `max_atoms` 的 padding 填充物。 | 配体若只有 15 个原子，则前 15 个为 `True`，后面的 padding 填充为 `False`。 |\n| `ref_pos` | `[B, N_a, 3]` | 候选配体的 RDKit 或 CCD 3D 参考构象坐标（经随机坐标旋转和中心化增强）。 | 原子的参考 3D 空间坐标（如 `[1.2, 0.4, -2.1]` Å），作为模型采样的位置和相对几何先验。 |\n| `prompt_points` | `[B*M, 3]` | 在当前密度 Blob 范围内随机采样得到的空间提示点坐标。 | 例如 `[-0.8, 2.5, 1.1]` Å。通过此提示点诱导不同次数的坐标扩散采样（多重复性 `M`）。 |\n\n*(注：其中 `B` 为 Batch 大小，`N_a` 为最大原子填充长度，`M` 为单配对的多次重复采样数 `multiplicity`。)*\n\n输出：\n\n```text\nsampled_atom_coords: (B * multiplicity, 200, 3)\n```\n\n关键点：diffusion 初始化不是从 `ref_pos` 开始，而是：\n\n```text\natom_coords = torch.randn_like(ref_pos) * init_sigma\n```\n\n也就是从噪声坐标开始，逐步去噪到 ligand 原子坐标。`ref_pos`、atom/pair features、density selected points、prompt point 都是条件信息。\n\n## 10. 输出怎样还原成结构文件\n\n代码：\n\n```text\nsrc/emap2lig/data/io/writer.py::LigandWriter\n```\n\nwriter 会接收模型预测：\n\n```text\nsampled_atom_coords: (B * multiplicity, 200, 3)\natom_mask:           (B, 200)\natoms:               原始 LigandObject.atoms\ngroundtruth_center:  (B, 3)\n```\n\n它会：\n\n```text\n用 atom_mask 去掉 padding atom\n-> 把预测坐标写回 LigandObject.atoms[\"coords\"]\n-> 把局部坐标加回 groundtruth_center\n-> 写出 mmCIF\n-> 保存 pred_mask.mrc 和 prompt.cmm\n-> 记录 consistency_iou\n```\n\n所以 `LigandObject.atoms` 在这里又变重要：它保留了 atom name、element、residue 等结构写出需要的信息。\n\n## 11. 当前阶段最该记住的接口\n\n如果只记一张表，记这张：\n\n| 阶段         | 数据对象                  | 形状/结构                                             | 主要代码                        |\n| ---------- | --------------------- | ------------------------------------------------- | --------------------------- |\n| 用户输入       | YAML/text             | CCD/SMILES/BRANCHED                               | `parse_ligand_list()`       |\n| 解析记录       | `LigandRecord`        | dataclass                                         | `types.py`                  |\n| 分子对象       | RDKit `Mol`           | atoms + bonds + conformer                         | `prepare_*_data()`、`ccd.py` |\n| 序列化中间层     | `LigandObject.npz`    | `atoms(N,)`、`bonds(E,)`                           | `process_molecule()`        |\n| Dataset 张量 | `features`            | `atom_feature(200,149)`、`bond_feature(200,200,9)` | `LigandModelingDataset`     |\n| 模型输入       | batch dict            | 加 batch 维                                         | `Emap2lig.forward()`        |\n| 模型输出       | `sampled_atom_coords` | `(B*M,200,3)`                                     | `DiffusionModule.sample()`  |\n| 结构输出       | mmCIF                 | 去 padding 后真实 atom                                | `LigandWriter`              |\n\n## 12. 为后续 Pocket_Plus 接入保留的观察点\n\n本阶段不展开接入方案，只记录接口点：\n\n1. Pocket_Plus 替代 Find 时，最小目标是生成等价的 `DensityObject.npz` 列表。\n2. Emap2lig-Build 当前从 `DensityObject` 需要：\n   ```text\n   density_grid\n   instance_grid\n   voxel_size\n   global_origin\n   object_id\n   ```\n3. receptor condition 最可能影响两处：\n   ```text\n   InstanceSegModule.forward_embedding()\n   DiffusionModule.sample() / diffusion conditioning blocks\n   ```\n4. ligand 表示本身已经比较独立：只要能生成同样的 `LigandObject` 和 Dataset features，SMILES/CCD 解析链路可以先不动。\n\n这几个点先作为后续学习路标，不在本文展开设计。",
  "annotations": []
}
```