Question: 是否把总方法冻结为“备份现有 skills → 本轮 grill 形成可执行 v0 → 实施 agent 只依照 v0 整理 AdaLigand → 独立审计只按原始症状检查问题是否消失 → 根据实践修订 v1 → 用 v1 完成最终 Learn 线与治理收口”的版本化共演闭环？
Decision: 是，接受并采用该版本化共演闭环。
Reason: 让规则在实施前生效，同时通过真实项目和独立症状审计校正，避免事后补救或让未经实践的抽象规则直接成为最终规范。

Question: 是否把 v0 skill 体系组织为“一个总入口、三个规则权威、一个阅读适配器”：总入口区分开发/纯学习模式；代码结构 skill 统一函数到目录和生命周期规则；注释 skill 成为生产/Learn 注释的唯一权威；Git 双线 skill 统一 Work/Learn 与等价检查；source-reading 只保留阅读顺序、数据流追踪和学习完成标准？
Decision: 是，接受并采用该 skill 组织方式。
Reason: 对使用者保持一条完整流水线，同时让结构、注释和 Git 各有唯一权威，消除现有 skill 之间的覆盖式冲突。

Question: 是否把每轮 Git 双线固定为“累计 Learn 终点作为共同基点 → Work 真实实现枝 → 从同一基点按逻辑顺序重建新 Learn 段 → 集中冻结测试快照 → 自动验证终点等价 → 累计 Learn 前移 → 下一轮 Work 再从新 Learn 叉出”，同时永久保留旧 Work 历史并让 main 退出日常推进？
Decision: 基本同意并采用该闭环；累计学习分支改用不含 `model` 的中性名称，具体名称另行锁定。
Reason: 延续 Pocket_Plus 已验证的学习树干/实现枝交替模型，同时让分支名适用于数据处理、模型、工具和文档等所有项目工作。

Question: 累计学习分支是否统一使用中性名称 `Learn/cumulative`？
Decision: 是，并将精确名称定为更醒目的 `Learn/CUMULATIVE`。
Reason: 该名称不绑定模型等具体对象，并能在分支图中清楚标识下一轮工作的累计学习基点。

Question: 是否用“一个文件一个可用白话说明的主要任务、通常 1–3 个公开入口、只按独立语义/测试/复用/副作用边界抽函数、按不同产物/依赖/生命周期/修改原因拆文件、数量只触发审查”取代“每文件硬性只能有一两个中心函数”，并禁止无边界的单文件 `utils.py`？
Decision: 大致接受；中心职责与非硬计数原则成立，但允许并希望用 `utils/` 文件夹收容工具性函数，并在需要时拆成 `io.py` 等子模块，以便从目录上区分主次。`utils/` 的准入边界另行锁定。
Reason: 人类阅读不仅需要模块内部有中心，也需要在目录层直接看出哪些是主流程、哪些是次要支撑代码。

Question: `utils/` 是否只收容被多个长期生产模块复用、且不决定领域/科学语义的通用机制，并按 IO、路径、哈希、并行等用途拆分；领域规则、具体 artifact schema、外部工具适配、运行编排、run-specific 修复和仅供单模块使用的局部 helper 不进入 `utils/`，且 `utils/` 不反向依赖上层模块？
Decision: 是，接受该 `utils/` 准入与依赖边界。
Reason: 保留从目录上区分主次的阅读价值，同时避免 `utils/` 演变成无所有权的杂物间。正式 skill 原文必须保持项目无关，不写 AdaLigand Stage 等脱离上下文无法理解的名称；项目实例只留在项目文档、实施计划或验收材料中。

Question: 是否把代码生命周期固定为“长期生产代码进入正式 package/CLI；默认关闭的可复用工具进入 `ops/`；一次性脚手架进入 `tmp/<task-id>/`，可在 Work 历史提交但不得被生产入口默认调用；任务结束时只允许补齐契约后提升到 `ops/` 或从最终活跃树删除，由 Git/日志/evidence 保存”，且 Learn 主要阅读和高密度注释只覆盖长期生产代码？
Decision: 是，接受并采用该生命周期与退出模型。
Reason: 允许 AI 在真实工作中自由产生必要脚手架，同时通过显式目录、最小范围说明和最终提升/删除门保证主路径长期保持可读。

Question: Learn 高密度变量注释是否采用“数据承载代码高密度、控制代码按需”的范围：覆盖数据预处理科学变换与 artifact 构建、Dataset/collate/增强、模型 forward/loss/科学指标和推理张量/坐标/结果组装；非标量首次获得关键语义及 shape/dtype/单位/轴序/坐标/对齐语义变化时说明，科学标量也解释；薄 CLI、明显控制变量、ops/tmp/测试不机械覆盖？
Decision: 是，接受该覆盖范围与密度规则。
Reason: 高密度学习注释应覆盖真正承载数据含义的完整链条，而不能只从 Dataset 开始，否则数据预处理中的 artifact、坐标、mask 和 offsets 仍然难以阅读。该决定只是注释覆盖政策，完整 skill 还必须通过统一的通用正例、反例和改写示例落实格式与例外。

Question: 注释 skill 是否以 `code-comment-style-cn` 的类型/shape/语义与结构化返回值格式为底稿，在同一 skill 内用 production、Learn 数据、Learn 模型/数学、专项契约和反例改写等 references 组织通用正反例；把 source-reading 中的近源、shape transition、低层数学自包含等有效内容迁入统一格式，并删除 skill 之间的覆盖式冲突政策？
Decision: 是，接受并采用该示例体系与唯一权威政策。
Reason: 抽象规则不足以稳定约束 agent；统一且分场景加载的正反例既能提高可执行性，也避免多个 skill 各自复制并逐渐漂移出不同格式。

Question: 变量注释是否采用“类型与必要 dtype → 当前 shape 或 shape 变化 → 语义 → 近源”的统一顺序，并以圆括号表示当前 shape、方括号箭头表示本行变化；坐标/mask/index/offsets 补专属语义，编码增加短例，默认使用代码上方的一项一义注释？
Decision: 基本接受；近源在代码已直接展示时不得机械重复，常见且上下文一致的 `torch.Tensor`/`np.ndarray` 类型也不应逐变量重复。类型、dtype 和近源的强制条件另行锁定。
Reason: 注释应把视觉注意力留给 shape 和真正新增的语义，而不是重复赋值表达式或整份文件中始终相同的容器类型。

Question: 变量注释是否以“当前 shape 或 shape 变化 + 语义”为最小核心，只在类型/dtype不同、行为敏感、容器不明显或发生转换时补类型，在来源跨函数/文件/artifact或经过非直观编码时补近源，并禁止重复本行右侧已经直接展示的来源？
Decision: 是，接受并采用该条件式类型、dtype 与近源规则。
Reason: 让类型和来源成为真正的边界/异常信息，避免固定前缀与语法复述遮蔽 shape、对齐关系和领域语义。

Question: Docstring 是否按“长期生产模块有模块说明；公开/跨模块边界有完整多行契约；非显然科学变换有可独立理解的输入输出；artifact 构建/读写/校验逐字段写 dtype、shape、单位、对齐、offsets、路径与写入行为；薄 CLI/转发和简单局部 helper 从简”的分级规则执行？
Decision: 基本接受并采用该分级规则；副作用和“不负责什么”不设为固定必填栏目，只在存在非显然写盘、网络、子进程、全局状态或容易混淆的边界时说明。
Reason: 让重要边界和 artifact 契约足够完整，同时避免轻量函数和普通模块被固定栏目与样板文字淹没。

Question: 注释和 Docstring 是否加入“具体名词规则”：已知真实实体时必须使用实际实体名，避免用未定义的行/条目/对象/数据/结果/上游/下游等抽象词；index、offsets、mask 必须点名作用数组、维度和实体；通用容器先定义每个元素代表什么，技术术语首次出现时用白话解释？
Decision: 是，接受并采用该具体名词与首次定义规则。
Reason: 让代码注释也满足项目文档的自包含要求，使读者能从局部文字定位到真实数据实体，而不是依赖含糊黑话猜测。

Question: 当前管线入口 README 是否同时作为唯一权威 artifact 契约，包含管线/目录/入口与每种产物的路径、字段、dtype、shape、单位、空值、轴序、主键、对齐、offsets、producer 和 validator；运行历史、临时命令和 Learn 阅读顺序分别留在 ExecPlan/evidence、ops runbook 和 Learn 指南？
Decision: 是，接受该文档分工；README 的首要功能是让用户不读代码就能立即知道会生成哪些文件以及每个文件包含哪些字段和语义，producer/validator 只作为进一步追查实现的入口，不能替代完整字段说明。
Reason: artifact 契约应以产物为中心并可独立冷读；当前难读主要来自稳定契约与运行历史混杂，而不是字段表本身过多。

Question: Data_Preprocessing 整理是否限定为“源码结构可以重组，但 A–G 科学定义、阈值、样本选择、artifact 路径/字段/dtype/shape/单位/数值语义/默认落盘及外部工具科学参数默认不变”；真实实现缺陷冻结证据后另行决定，辅助头/损失也使用独立 Work 流，不与等价重构混合？
Decision: 是，接受并采用该严格行为保持边界。
Reason: 为结构重构建立可验证的科学基线，防止目录、注释和契约整理成为未审议行为变化的掩护。

Question: 是否把平面 `code/` 改成直接位于 `Data_Preprocessing/Ori_Data/adaligand_preprocessing/` 的正式 Python package，增加最小 `pyproject.toml`，移除 `sys.path.insert` 和裸模块 import；保留 `scripts/` 中每阶段独立的薄 CLI，并使用同级 `ops/`、`tmp/`、`slurm/`、`tests/`，不增加额外 `src/` 层或旧 `code/*.py` 兼容 shim，同时保持 artifact 路径与内容不变？
Decision: 是，接受并采用该 package 与顶层目录结构。
Reason: 直接 package 在建立正常 import/测试边界的同时减少一层目录，更符合人类沿数据处理管线阅读的需求。

Question: `adaligand_preprocessing/` 是否采用“`stages/` 作为 A–G 人类阅读主线，复杂阶段才建子目录；`external_tools/`、`artifacts/`、`execution/`、`utils/` 作为只能被阶段调用而不反向依赖阶段的共享支撑层”的阶段优先布局，最终文件映射仍由实际依赖和生命周期清单决定？
Decision: 是，接受并采用该阶段优先 package 布局与依赖方向。
Reason: 让读者能直接沿数据处理顺序理解主线，同时把外部交互、产物机制和执行控制从科学代码中清楚分离。

Question: 当前已完成使命的 run-specific code/scripts/sbatch/tests 是否在冻结正式 commit、ExecPlan 和服务器 evidence 后逐文件审计，并从最终活跃树删除而不移入可执行 archive；只有证明跨任务复用、去除硬编码、补齐测试和短 runbook 的工具才提升到 `ops/`，且删除前确认无运行作业依赖、删除后重跑契约/完整测试/必要真实 smoke，Learn 不重建或注释历史脚手架？
Decision: 是，接受并采用该既有脚手架收口流程。
Reason: 用 Work Git 历史和外部证据保存真实运行过程，同时让最终生产树和 Learn 阅读线不再背负已经退出的恢复与迁移代码。

Question: 精确文件拆分与去向是否不在 grill 中逐一审批，而由实施阶段先建立包含当前用途、调用者、生命周期、职责、测试、目标路径、动作和理由的全量文件清单，再由实施 agent 按已锁定原则决定并由独立症状审计复核；只有触及科学/artifact 契约、无法证明等价、运行依赖不明或需要突破既定规则时才暂停询问？
Decision: 是，接受并采用该实施授权与升级边界。
Reason: 用户负责可读性、科学与治理边界，agent 在完整可审计清单内负责大规模但行为保持的工程映射，避免把 grill 退化成逐文件命名会议。

Question: 等价重构是否采用“保存当前 Windows 基线；逐阶段专项与 artifact 契约测试；最终 Windows/Linux 全套、CLI import/帮助/fixture smoke、代表性 Chimera/MapQ/MRC 真实 smoke、冻结 fixture 的路径/schema/数值/状态比较和 Work/Learn 等价检查”，不重跑完整 22k+ A–G；implementation hash 等必然 provenance 变化与科学内容分开判断，删除历史测试导致的计数变化必须逐项对应？
Decision: 是，接受并采用该分层验证门。
Reason: 已完成正式 run 继续绑定旧冻结实现；结构重构用全面自动契约和代表性真实执行证明等价，避免无必要地重复全量科学计算。

Question: 文档治理是否采用“科学规划文档默认不因等价重构改写；原 A–G ExecPlan 保留历史；本次新建维护 ExecPlan并更新 mapping；当前 README 与运行历史分离；错误的旧 learn.md 从最终生产树删除并在 `Learn/CUMULATIVE` 重建；新科学漂移另行分类决定”的方式？
Decision: 基本接受；上述角色分工成立，但 README 不能只做删除 Job/事故/run 历史的局部编辑，必须另行锁定更完整的重写与事实复核范围。
Reason: 当前 README 的问题不仅是历史混入，还包括状态矛盾、过时口径、字段来源与空值语义等需要重新核验的内容。

Question: README 是否不沿用旧文逐段删改，而从规划、最终代码、测试 fixture 和代表性真实产物重新建立 artifact 清单，逐产物核验路径、实体、字段 dtype/shape/单位/轴序/含义、对齐、offsets、稀疏表示、空值和条件字段，并说明必要的跨产物主键、发布/状态/provenance 与当前完成边界，最后通过不读代码的独立冷读审计？
Decision: 大致接受从事实重新建立 artifact 契约和冷读审计，但严格沿用先前界定的 README 职责：重点是无上下文、自包含地说明有哪些产物及每个字段是什么；producer 只需在适当位置给简短代码指针，validator、consumer 和实现调用关系不做详细展开，除非其规则本身是理解 artifact 语义所必需。
Reason: README 是 artifact 契约与冷读入口，不是实现依赖图或代码追踪手册；额外实现细节会再次遮蔽用户最关心的产物内容。

Question: skills 是否按“原地扩展 python-writing-style-cn、原地统一 code-comment-style-cn、新建 dual-track-git-workflow、收窄 source-reading、新建总入口、yaml 独立、备份后用 skill-creator 校验”的迁移方案执行？
Decision: 基本接受，但需调整三点：陌生 GitHub 代码学习是统一 AI 代码工作流的子场景，不应继续形成独立且重叠的 source-reading 权威，需重新规划其与双线工作流的关系；总入口是否写入顶层 AGENTS.md 及名称是否改为 AI-workflow 类名称另行决定；现有 skills 已由用户备份到 `C:\Users\15919\Desktop\skills`，获得最终授权后可直接修改 `.codex/skills` 原位置。
Reason: 用户需要一套同时覆盖 AI 编写和学习代码的统一工作方式，而不是保留多个彼此调用但仍可能重叠的入口。

Question: 是否取消独立 source-reading，把其阅读方法迁入名为 ai-code-workflow 的双模式总入口；该入口在实现模式调用结构、production 注释和双线 Git，在学习模式调用阅读 reference、Learn 注释和同一双线 Git，dual-track-git-workflow 只负责两种场景共用的 Git 机制？
Decision: 基本接受 source-reading 不再独立、学习与实现共用双线 Git，但不接受为显然由用户提示词给出的“学习还是实现”再建立 profile 名词或专门判断机制；是否还需要 ai-code-workflow 总入口需按更简洁的直接组合方案重定。
Reason: 模式通常由用户任务明确表达，额外的 profile 和路由层会制造概念与维护成本，而不是帮助 agent 做正确工作。

Question: 是否删除 ai-code-workflow 总入口与 production/Learn profile 名词，最终只保留“python-writing-style-cn 统一实现结构、code-comment-style-cn 统一普通注释并在用户明确学习时追加高密度规则与阅读 reference、dual-track-git-workflow 同时服务实现和学习、yaml 独立”，删除 source-reading，并由顶层 AGENTS.md 直接列出实现与学习各自应组合哪些 skills？
Decision: 是，接受并采用该简化 skill 体系与 AGENTS 直接组合方式。
Reason: 用户提示词已足以表明任务是实现还是学习；去掉路由 skill 和 profile 名词可以保留完整规则链，同时减少重复触发和维护层级。

Question: 顶层 AGENTS.md 是否增加一段包含 Python skill 组合、Learn/CUMULATIVE、production/ops/tmp 落点、Data_Preprocessing README 职责以及 v0→v1 同步说明的项目入口规则，而通用细节不复制？
Decision: 不采用该方案；这些内容仍混合了应由 python-writing-style-cn、dual-track-git-workflow 和表达规则拥有的实质政策，也夹带当前 v0→v1 任务说明。顶层 AGENTS.md 应重写为极简活跃触发索引：点名 plan-log-governance 并保留少量项目关键路径；点名复活后的 ai-code-workflow，由它映射其它代码相关 skills。具体职责归属与精确文本另行锁定。
Reason: AGENTS.md 应只回答“本项目何时调用哪个治理入口”，不重复规则、不保存当前一次性工作过程，也不成为多个 skill 内容的第二副本。

Question: 是否锁定唯一职责归属：python-writing-style-cn 管代码结构与 production/ops/tmp 生命周期；表达 skill 管人类可读技术表达；dual-track-git-workflow 管 Work/Learn；plan-log-governance 管计划/日志/mapping/drift；ai-code-workflow 只映射 skills；AGENTS.md 只保留项目触发与关键路径，并将 Self-Containment、Drift/Closeout、Run-Scoped 规则分别迁往其唯一所有者？
Decision: 几乎接受该职责图，但增加三项修订：本轮重写的个人 skills 应以中文正文为唯一权威；README 不得由 plan-log-governance 与表达 skill 保存两套重复要求，需锁定单一所有者；AGENTS.md 可以保留一句克制的“无隐藏上下文、自包含、少黑话”最高层表达原则。精确归属与文字继续逐项锁定。
Reason: 极简触发索引仍可保留一条项目普遍适用的人类可读性原则，但所有可执行细则和文档类型规则必须只有一个权威来源。

Question: README 的创建条件与全部写作要求是否只由表达 skill 管理；plan-log-governance 只在计划/日志/mapping 中记录 README 路径和覆盖关系，并在需要刷新时引用表达 skill，不再保存 Contract README Rules；ai-code-workflow 只负责触发映射？
Decision: 基本接受并采用 README 单一所有权：表达 skill 负责创建条件、内容与冷读写法；plan-log-governance 只负责关系与状态，不复制要求；ai-code-workflow 只映射。另需把 AGENTS.md 中允许保留的人类可读性原则写得更明确、更严格。
Reason: README 要求只保留一份才能避免漂移；项目入口仍可用短而强的通用原则要求所有面向用户的文字具体、自包含并避免模糊黑话。

Question: 顶层 AGENTS.md 是否保留一段强制所有聊天、代码注释、Docstring、README、讨论/规划文档和执行记录在无此前上下文时可理解，优先具体实体名、首次解释术语、尽量避免 row/item/object/proposal/route/upstream/downstream，并禁止用英文或中英混杂代替准确解释的项目级硬规则？
Decision: 是，接受该强制表达规则；代码符号、字段名、路径和命令保留原文，但必须另有准确的人类可读说明。
Reason: 这是适用于项目全部人类可读输出的最高层原则，足够重要且简短，可以直接留在极简 AGENTS.md 中；具体格式与例子仍由表达 skill 唯一维护。

Question: 是否采用“中文权威正文 + 分层指针 + 单一规则所有者”的 skill 与 AGENTS.md 架构：technical-expression-style-cn 指向代码、文档和 YAML 三个具体表达 skill；plan-log-governance、python-writing-style-cn、dual-track-git-workflow 与 ai-code-workflow 各守单一职责；AGENTS.md 只保留项目触发入口和一条强表达原则？
Decision: 基本接受，但将 technical-expression-style-cn 改为不保存具体规则的表达入口，分别指向 code-comment-style-cn、document-style-cn 和 yaml-config-style-cn；删掉原本的 CLAUDE.md 这个文件，不在新 AGENTS.md 中提及。
Reason: 代码、一般文档和 YAML 的局部格式与示例差异足以由三个独立 skill 维护，而表达入口只负责组合，避免单个 skill 过大；CLAUDE.md 当前只重复 AGENTS.md，没有独立治理价值。

Question: 是否确认最终表达分层为 `technical-expression-workflow` 指向 `code-comment-style-cn`、`document-style-cn` 和 `yaml-config-style-cn`，并删掉原本的 CLAUDE.md 而但是 `CLAUDE/memory/` 等项目记忆功能完全照常使用？
Decision: 是，确认该表达分层与删除范围。
Reason: 表达入口只负责按文件类型选择具体规范；顶层 `CLAUDE.md` 没有独立事实，而历史项目记忆与该兼容入口不是同一事项。

Question: 是否在继续讨论代码整理与辅助训练前，先把顶层 `AGENTS.md` 和相关个人 skills 按已经确认的职责重构为可直接长期使用的成熟通用版本？
Decision: 是，立即实施；skill 正文不得写入当前项目、A–G 阶段、本轮 grill 或所谓过渡版本信息，也不得因为存在后续真实使用审计而把当前版本当作半成品。
Reason: 规则应在真实项目实施前生效，但第一次投入使用的检查点仍必须按最终通用规范的质量完成；后续只有发现真实缺陷时才修订。

Question: 本轮“相关个人 skills 使用中文权威正文”是否包括 `execplan`？
Decision: 不包括；`execplan` 明确允许并继续保持 English-first。其余本轮新建或重构的代码治理 skills 使用中文权威正文。
Reason: `execplan` 是允许保留英文权威规范的既有例外，不需要为了统一表面语言改写稳定规范。

Question: 是否接受 A–G 管线整理的完整实施契约：以 `817940a` 为共同基点建立 `Learn/CUMULATIVE`、`codex/adaligand-data-preprocessing-maintenance` 和 `Learn/data-preprocessing-maintenance`，在隔离 worktree 中完成严格行为保持的 package 重组、脚手架收口、README 重建、文档治理、分层验证、端点等价检查、隔离服务器验证与最终非强制推送？
Decision: 是，整组接受；但现在只冻结实施契约，不立即整理代码。实际由哪个对话、工作树或 subagent 执行，以及是否与辅助监督和小型训练并行，必须先单独确定。
Reason: A–G 的范围、分支、文档和验证边界已经清楚，但数小时工作的任务拆分与协调方式会直接影响文件隔离、用户可见性和两个长期工作方向的并行安全。
