Question: Find_2 的 56D encoded atom feature 应继续使用当前 Find_1 的三线性 soft splat，还是改成真正的 3×3×3 Gaussian 投影？
Decision: Find_2 改成真正的 3×3×3 Gaussian 投影；如果未来决定取消并重提 Find_1 的双 H200 训练，重提的 Find_1 也改用真正的 3×3×3 Gaussian 投影。
Question: Find_2 的 Gaussian 投影是否只输出 56 个 encoded 通道，与 density56 逐元素直接相加，并完全不再追加 2D occupancy 或 3D centroid 输出通道？
Decision: 是。位置编码仍保留在 atom MLP 内部，但 Gaussian 投影只输出 56D encoded grid，并与 density56 直接相加。
Question: Find_2 是否复用现有 gauss_scatter_to_voxel_grid 的数值定义，即固定 3×3×3 邻域、sigma_voxel=0.7、逐原子权重和归一为 1、多原子贡献直接相加，并且不增加 gate、缩放或体素均值归一化？
Decision: 是。
Question: Find_2 是否使用独立的 CPC1/CPC2 配置、run/W&B/checkpoint 身份，voxel_embed_as_tune 默认关闭且只由 Find_2 开启，CPC1 从头训练并由同名 CPC1 BEST strict model-only 初始化同名 CPC2；下游正式 producer 契约是否暂缓扩展？
Decision: 前三项同意；下游不暂缓。Find_2 从一开始作为独立 producer 接通下游枚举。达成全部共识后，下游适配由一个 subagent 实施，并由另一个 subagent 独立审计，主线负责汇总结论而不重复审计。
Question: 是否给 Stage1EmbedHead 增加默认关闭的 use_gaussian_splatting，并将其与 voxel_embed_as_tune 解耦；未来 Find_1 只打开 Gaussian、保持 49D+raw49 residual+2D occupancy 后与 density56 拼接，Find_2 同时打开 Gaussian 与 tune、采用 56D 无 voxel raw residual且与 density56 相加，point 路径和完整/voxel-only 等价性保持不变？
Decision: 接受。现有 Gaussian 选择只属于 online raw atom scatter，embed-head 路径没有可复用的 Gaussian 开关，因此新增默认关闭的 use_gaussian_splatting；其余语义按问题所述执行。
Question: 是否停止当前双 H200 Find_1，改为新版 Find_1 与 Find_2 两条单 H200 链；两者先以 micro6、GBS48、accum8 做真实 5-step smoke，再按 CPC1 每 epoch 25 次 validation/4 次 LR 下降与 CPC2 同名 BEST strict model-only/1 次 LR 下降完整训练，并在正式启动后密集监控至少 30 分钟且达到至少 50 optimizer step再恢复 heartbeat？
Decision: 基本同意。Find_0 与 unet_c1 不动；新版 Find_1 与 Find_2 采用上述 smoke、训练和密集监控制度。资源切换不直接拆用旧 allocation，而先通过新单卡 H200 sbatch 确认新资源。
Question: 是否允许一次短暂 3×H200 过渡，并按“先只提交新版 Find_1 单 H200；Slurm 真正 RUNNING 后立即 scancel 旧 Job 321108；再提交 Find_2 单 H200；若新 Find_1 两分钟仍 PENDING 则取消新 Job并保留旧 Job”的顺序切换？
Decision: 接受并授权。H200 不超过 2 或 3 张表示不允许长时间同时占用，不是逐时刻硬限制；资源交接期间允许短时间过渡。
Question: Find_2 下游是否采用统一 producer 名单模块，集中维护当前 Find_0、Find_1、Find_2、unet_c1，并由该名单派生 Find 子集，供 Dataset、inference、CLI 和测试复用；Selector 仍保留逐 producer YAML，且不引入 registry、dataclass、插件系统或动态配置发现？
Decision: 确认并接受。采用单一 tuple 与派生 Find 子集的轻量列表适配法，在不改变原有语义和功能的前提下消除重复枚举并支持未来新增模型。
Question: Find_2 是否把有效 voxel 输出维度在 voxel_embed_as_tune=true 时直接硬编码为 56，只跳过 voxel raw49 residual并保留 point 路径；density 与 embed 在完整/voxel-only 路径直接相加且不增加专用检查、fallback或兼容层；测试覆盖旧行为、Gaussian、56D相加、point兼容、通道/配置/名单，下游再做真实 Dataset/DataLoader、前后向和5-step GPU smoke？
Decision: 确认并接受。
Question: 资源交接是否改为同时提交新版 Find_1 与 Find_2 两个单 H200 无时限 sbatch并停在 pre_lock；至少一个真正 RUNNING 后直接 scancel 321108，不再使用旧 Job 的 kill_lock；若两分钟内均未 RUNNING则取消两个新 Job并保留旧 Job；允许交接时极短4×H200重叠，旧 Job取消后长期回到2×H200？
Decision: 确认并接受。
Question: 是否确认已经达成共享理解，正式结束 grill，并授权按全部已记录决定开始实现、测试、调度和监控？
Decision: 确认并授权开始行动；若后续没有 heartbeat，可以重新创建，并在完成每个里程碑后更新 ExecPlan、handoff 等外部记忆。
