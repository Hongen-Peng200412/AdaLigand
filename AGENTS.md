# AdaLigand Agent 入口

本文件只保存项目级触发入口和关键位置。具体执行规则只在对应 skill 中维护，不在这里复制。

## 规划、执行记录与收口

涉及规划文档、执行记录、映射索引、计划与实现差异、计划回填或收口时，使用 `plan-log-governance`。

关键位置：

- 规划文档：`文档/规划文档/`
- 执行记录：`文档/exec_plan/`
- 计划与实现映射：`文档/mapping/计划执行映射.md`

## 代码工作

涉及代码编写、修改、重构、调试、审查、解释或学习注释时，使用 `ai-code-workflow`，由它调用本次任务需要的具体 skills。

## 面向用户的文字

编写或修改任何面向用户的技术文字时，使用 `technical-expression-workflow`。

任何面向用户的中文或英文文字——包括聊天、代码注释、Docstring、README、讨论文档、规划文档和执行记录——都必须在不依赖未写出的对话、运行历史或作者心中隐含信息时可以理解。优先点名具体文件、数组、字段、函数、实体和处理步骤；无法避免的术语必须在首次出现时解释。特别少用甚至不用 `row（行）`、`proposal`、`route`、`upstream/downstream` 等含义不明确的词；保留必要的代码符号、路径和命令，但不得以英文或中英混杂代替准确解释。
