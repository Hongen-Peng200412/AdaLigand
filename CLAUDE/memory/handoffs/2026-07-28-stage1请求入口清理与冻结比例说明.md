# Handoff: Stage1 请求入口清理与冻结比例说明

Date: 2026-07-28

## Current State

`C:\Users\15919\Desktop\Pocket_Plus` 当前位于 `Learn/CUMULATIVE`，HEAD 为 `80eedc7ba85bad99a4bdb82ff613a3ecee450af5`。本轮涉及的五个文件已经由用户暂存，工作区没有这些文件的未暂存修改：

- `src/datasets/ops/stage1_box_pool.py`
- `src/datasets/readme.md`
- `src/datasets/stage1_dataset.py`
- `src/datasets/stage1_requests.py`
- `tests/datasets/test_stage1_dataset.py`

本轮没有提交 Git commit、没有修改分支指针、没有启动或更改服务器任务。

## Completed

- 删除 `stage1_requests.py` 中不属于当前正式管线的 JSON、JSONL、多文件请求读取入口。
- 删除请求文件缺少 `require_targets`、`role`、`occurrence_id` 或 `candidate_index` 时的静默回退。
- 冻结比例 NPZ 现在只打开一次，并要求包含当前写入函数保存的全部请求字段和元数据字段。
- 单 PDB BOX NPZ 必须包含 `pdb_id`，并且该值必须与 `manifest.json` 中声明的 PDB 编号一致；不再从文件名推断身份。
- `Stage1Dataset` 测试改为直接传入 `ResolvedStage1Crop`，测试生成的单 PDB BOX NPZ 补齐正式契约要求的 `pdb_id`。
- `src/datasets/readme.md` 新增“未来可能用到的冻结比例npz”，写明训练与验证文件的绝对保存路径、命名规则、生成和复用条件，以及全部字段的数据类型、形状、缺失值和对齐关系。
- 重新运行语法检查和相关测试：`49 passed`。

## Decisions

- 当前正式训练请求来源只有 `box_pool/train` 目录；当前正式验证请求来源只有 `box_pool/validation_selection.npz`。
- 推理不依赖已删除的 JSON 请求入口，而是直接向 `Stage1Dataset` 传入内存中的 `ResolvedStage1Crop` 序列。
- `box_sample_fraction=1.0` 不生成冻结比例文件：训练请求继续随训练周期变化，验证使用完整固定请求。
- `box_sample_fraction<1.0` 时，训练和验证分别生成并复用 `train_selection_{fraction}_seed{seed}.npz` 与 `validation_selection_{fraction}_seed{seed}.npz`。
- 本轮不拆分 `stage1_requests.py`，不重构文件布局，也不处理未获授权的其他兼容参数。

## Open Questions

- 当前暂存内容在 `src/datasets/ops/stage1_box_pool.py:651` 和 `src/datasets/stage1_requests.py:633` 各有一处行尾空格。它们不影响 Python 行为或数据契约；本轮没有改动用户已经暂存的文字。

## Next Actions

1. 用户审阅并提交 `C:\Users\15919\Desktop\Pocket_Plus` 当前暂存内容。
2. 后续从新的正式 Stage1 数据根目录配置小于 `1.0` 的 `box_sample_fraction`，使用冻结比例文件启动小数据集训练。
3. 启动前核对训练与验证冻结比例文件的实际绝对路径、`request_seed`、请求数量和来源摘要；不要恢复已经删除的 JSON/JSONL 请求兼容入口。

## Files To Reopen

- `C:\Users\15919\Desktop\Pocket_Plus\src\datasets\stage1_requests.py`
- `C:\Users\15919\Desktop\Pocket_Plus\src\datasets\stage1_dataset.py`
- `C:\Users\15919\Desktop\Pocket_Plus\src\datasets\readme.md`
- `C:\Users\15919\Desktop\Pocket_Plus\tests\datasets\test_stage1_dataset.py`
- `C:\Users\15919\Desktop\AdaLigand\talk\短交互\process_1.md`
