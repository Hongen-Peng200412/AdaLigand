# Handoff: CryoAtom2 calibration 两个单 A800 任务重新提交

日期：2026-09-12

## 当前状态

本交接取代同日“首分片已提交”交接的旧 Job 和待提交状态。100 个 calibration PDB 分成两个原序交错分片，各 50 项。2026-09-12 12:00:27 已成功提交 379402_0（QOS=h200g4）和 379403_1（QOS=a100g2），12:00:49 均 PENDING(Priority)。两者均 nvlink 分区、1 张 A800、16 CPU、TimeLimit=UNLIMITED、无依赖、仅 after_hold。按用户最新要求不传 --mem，Slurm 显示 MinMemoryNode=0、默认 TRES mem=1000000M。尚未分配节点或生成 release/launch。

## 已完成

输入 `/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_3/split/pdb_split/calibration.json` 为 100 个不同小写 PDB 的 JSON 列表；原始图及唯一图对应完整，完整序列目录中有 715 蛋白质、38 RNA、10 DNA entity，短序列不剔除。只解压原始图，不读真实坐标，不改官方科学参数。两处清单读取兼容、两个薄 shell、既有测试参数化，8 项测试、主代理两遍自查和独立两轮全面审查通过；QOS 参数化窄查也批准。

## 决定

nvlinkg8 全组提交额度已满，h200g4 只余 1 名额；用户随后明确允许其他 QOS。因此两个短命令分别指定 h200g4/a100g2，硬件仍 A800。最初带 96G 的 379399_0 已被用户取消；刚提交的 379401_1 也按用户“移除96G并重新提交两个任务”的要求由主代理取消，两者均未运行预测。不得跟踪或恢复旧申请。

## 已执行正式命令

服务器项目根 `/home/penghongen/My_Project/AdaLigand`：

```bash
bash 测评数据代码/cryoatom2原始预测结构/submit_calibration.sh 0 h200g4
bash 测评数据代码/cryoatom2原始预测结构/submit_calibration.sh 1 a100g2
```

两个命令均已执行成功，不得再执行。检查命令与历史失败命令单独见执行记录，不是正式命令的一部分。

## 下一步

监视 379402_0、379403_1，启动后核对 release/launch、真实单卡进程、50/50 分片和输入命令，直到100项完成并重新解析200份最终/raw CIF，检查非空有限坐标、完整来源、覆盖和日志统计。稳定期每轮60或90分钟，用多段 Start-Sleep -Seconds 300 静默等待，不设置 heartbeat、不结束任务。结束后保留 after_hold，用户尚未授权释放这两个新任务。goal保持active。

## 文件与产物位置

- `文档/exec_plan/CryoAtom2校准集受体重建执行记录.md`：当前任务日志，服务器同步为下述日志根的任务日志.md。
- `测评数据代码/cryoatom2原始预测结构/README.md`、submit_calibration.sh、sh/predict_calibration.sh、predict.py：当前契约与入口。
- 输入图 `/storage/penghongen/AdaLigand/Ori_Data/raw/emdb_maps`，对应同 raw 目录 pair_list.jsonl；完整蛋白质/RNA/DNA 序列 `/storage/penghongen/AdaLigand/held_out/sequence_catalog.jsonl`。
- 实际产物 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/calibration/cryoatom2_artifact/<pdb>/<run_stamp>/`。
- 全部日志统计 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/calibration/运行日志与统计/`，包含提交前输入核验.json、提交前代码摘要.json、默认内存提交参数检查.txt、正式提交资源核验.txt；未来控制记录在 slurm/allocations、launches、releases。

当前生产文件 SHA256：predict.py 4a5a1eed1c97bc3b51bf05d4a1ab6c128241a1437d0397ef0de78281213b2e30；submit_calibration.sh 3dd07f08ab9265a510873e0e1e56f95c1b5784809727a97e2ba5d6bb29f48d3a；sh/predict_calibration.sh 18184a8feb4ebeb99c350bb90bd537b158c63d53b38b1dec74a5eb1f33175692。

AdaLigand Git 起点 Learn/CUMULATIVE=1fcd70063ce0652b406ed634cd9a0f68b3e32319，仍为按提交者时间唯一最新提交。本轮明显小型适配按 skill 例外留工作区，不自行提交。旧 test_0 的179项任务已完成，不操作旧资源。
