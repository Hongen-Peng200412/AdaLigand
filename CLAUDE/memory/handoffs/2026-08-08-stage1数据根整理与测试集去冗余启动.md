# Handoff: Stage1 数据根整理与测试集去冗余启动

Date: 2026-08-08

## Current State

Stage1 第二版训练准备根 `/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_2` 已补齐当前正式 `final_keep_list.jsonl` 与完整 `split/`。该根的 BOX 请求比例为 `0 center + 5 bias + 5 context`；旧根 `/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation/box_pool` 的比例为 `1 center + 5 bias + 3 context`。

`stage1_preparation` 继续原地保留，不再计划归档。Job 338368 仍直接读取该目录中的 train、validation、calibration split，因此不得在任务运行期间移动或删除该根。

## Completed

- 当前 `final_keep_list.jsonl` 共有 577,483 个 occurrence、18,288 个 PDB。train、validation、calibration、held-out 分别为 13,714、200、100、4,274 个 PDB；四个集合互斥，按 `(pdb_id, candidate_id)` 合并后精确等于 keep list。
- `final_keep_list.jsonl` 和 `split/` 已复制到第二版准备根；复制时逐文件比较一致，没有删除旧来源。
- 服务器目录 `/storage/penghongen/AdaLigand/Ori_Data/matcher` 与 `matcher_v2` 已在确认活跃作业没有命令、环境或打开文件引用后，移动到 `/storage/penghongen/AdaLigand/Ori_Data/OTHER/matcher` 与 `OTHER/matcher_v2`。没有创建旧路径软链接。
- 已在第二版准备根创建 `README.md`，说明样本身份、划分来源、旧根保留边界和 Matcher 归档位置。
- Job 335493 的 Slurm 名称带有历史 Matcher 字样，但其当前动态命令实际是 Find_0 train F1 全局分片 1/50 推理；不得再根据旧作业名把它认定为 Matcher 生产者。

## Decisions

- `stage1_preparation_box_pool_2` 作为当前 Stage1 训练、后续测试集构建的自包含数据根。
- `stage1_preparation` 保留原地，通过 README 说明来龙去脉，不再以“清理 storage”为由移动。
- Matcher 旧硬编码路径允许失效；未来再次运行独立 Matcher 前，调用方必须显式改用 `OTHER/` 下的新位置。
- 序列去冗余不从 `receptor_tokens.npz` 自行重建序列。优先采用 RCSB/PDB 或成熟序列聚类软件提供的 polymer entity 级序列与聚类结果，并保留多阈值中间结果。
- 冗余分组内不使用分辨率或 Q-score 排序代表；使用记录了随机种子的随机代表。

## Open Questions

- 正式蛋白测试集是否采用 RCSB 官方 DIAMOND 序列簇的 `30% identity + 80% member coverage`，还是先比较官方 30/40/50/70/90/95/100% 多档结果后再冻结。
- RCSB 官方蛋白簇排除 DNA、RNA 和少于 10 个氨基酸的短肽；核酸受体与短肽需要单独规则，不能静默混入或排除。
- 是否只下载并冻结 RCSB 官方聚类文件，还是同时在服务器安装 DIAMOND，用于未来运行官方列表之外的自定义阈值。

## Next Actions

1. 从 held-out 4,274 个 PDB 出发，把 train、validation、calibration 合并为已经暴露的 reference 集合。
2. 下载并冻结 RCSB 各档 protein polymer entity 聚类文件，投影到 Stage1 四个 PDB 集合，保存每档 reference 冲突、held-out 内部分组和测试集规模。
3. 每个 held-out PDB 冗余连通分量使用固定随机种子选择一个代表，并保存全部分组与随机抽取记录，以便未来换阈值或重抽。
4. 单独统计核酸和短肽覆盖缺口，冻结对应规则后再发布最终 `test.json` 与 `_COMPLETE`。

## Files To Reopen

- `文档/exec_plan/Stage1第二版BOX池与Find_0重训练实施.md`
- `talk/Stage1第二版BOX池与Find_0重训练检查记录.md`
- `CLAUDE/memory/projects/adaligand.json`
- `/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_2/README.md`
- `/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_2/split/held_out_pool.json`
