# AdaLigand E3 全量迁移启动与 Stage F 并行交接

Date: 2026-07-16 20:52+08:00
Status: active；A–G 与 E3 均尚未最终完成

## 当前主线

- 正式 A–G run：`adaligand_ag_20260711T154658`。
- `316116` 在 `cnode04` 用 CPU96/F12 运行正式 Stage F，是唯一 22,386 行状态与 `f_release` writer。
- `318350` 在 `cnode01` 用另一台 CPU96/F12 顺序执行尾段补算 v1→v2；guard/child PGID 仍须按原证据监控。
- `316117` 继续依赖等待，只允许 Stage G analyze；不得执行示例阈值或写 `keep_list`。

## E3 冻结身份

- repair run：`adaligand_ag_20260711T154658_e3v3_v1`。
- 目标：旧 Stage E 合格集合恰 22,309；77 个 known failure 不复活。
- target IDs SHA-256：`35108a7a879e4b3fd62bf6ff703a4e065eb86d7cc63cb4f45fd6f32dd6e7a10b`。
- target manifest SHA-256：`183134147f4b577a8ff86e318dc99243a09c1d0ddfe8a95e1cf8ce1212211f10`。
- launch manifest SHA-256：`86851f67e69d29f0330f03fb922ee5490cf0445eaa9ed22e0e0011ed5d663ac0`。
- authorization manifest SHA-256：`c179829b3f10a1f7b7df2d4ab09808856a23a178aa223107f0833f45ca7f5aa7`。
- 旧 Stage E status、`de_release`、exclusion、pair-list SHA 仍按 target manifest 只读绑定，不得覆盖。

## Pilot 与独立审计

- `318882_0`（实际 job `318886`）于 16 分 20 秒内完成 465/465 success，状态 SHA-256 `072f66a560effc0f07b90cf59e849d1c327c64d0b0311109c1ec41ead7e6cbc4`。
- pilot 落盘审计检查 465 份 schema v3 几何身份；28,115 个 ZIP 成员全部是 `ZIP_DEFLATED`，summary SHA-256 `880f1f8df4d04ed6ef944b1fb4dc7e79efbfd94e737fd5482495197684d13f93`。
- 三项独立终验均 PASS：C→E3→F/G 无错位；当前实现忠实调用 Pocket 祖传中心函数；7b14/7nll 的 v2→v3 差异由半体素修复解释，v3 对独立 Pocket-center oracle 为零误差。
- 182/22,309 个 origin-risk 候选属于已记录的小范围祖传风险，继续迁移且不排除；没有发现需要用户批准的广泛硬伤。
- 第三项只读审计因 SSH 客户端超时留下的 `83206/87039` 已按精确归属回收并确认 `/proc` 消失；未触碰其他进程，也不得重启同类扫描。

## 正在运行的 E3 作业

- `318882_[1-47%3]` 已放行，每分片 16 CPU、1 A100、最多 3 分片并发；启动时 1/2/3 分别位于 gnode08/06/07。
- `318883` 已 release 但只因 `afterok:318882` 保持 dependency waiting；它执行 22,309 份 source-aware 全量重建验证和独立 release。
- 全局 writer lock、authorization seal 与 launch manifest 在 gate/Slurm 终态前不得手工删除或改写。

## 继续执行

1. 每两小时轻量检查 `316116/318350/316117/318882/318883` 的 squeue/sacct、日志增量、正式 F guard/PGID 与精确锁；不运行 clean sync。
2. E3 任一分片出现非 success/skipped 时，array 会失败并阻断 gate；先冻结该分片 status/log/artifact 证据，不得绕过 afterok。
3. `318882` 全部成功后，让 `318883` 自行启动；gate 必须验证 48 份 status、22,309 ID 恰覆盖一次、schema v3/source/几何/ZIP_DEFLATED 全部正确，并冻结 release summary。
4. gate 与全部授权 job 终态确认后，使用 canonical launch control 的直接 `squeue` 快照和两阶段 release 释放全局 writer lock；不得手工删锁冒充 release。
5. 正式 F 完成后独立审计四终态、五条 run-only exclusion、四 CC、配体/6 Å 口袋 Q、空口袋 schema v3、MapQ/provenance；`f_release` 通过后才接受 `316117`。
6. Stage1 数据就绪必须同时满足 A–G 最终 gate 与 E3 独立 release；任一单独完成都不等价于总目标完成。
