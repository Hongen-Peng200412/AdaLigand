# Handoff: Find_1 低学习率重训第四次验证

Date: 2026-08-24

## Current State

Job `351295` 的第 2 次执行仍在 `hnode01` 使用两张 H100 训练。当前只有 `/home/penghongen/Feedback/Pocket_Plus/allocations/351295/after_lock_351295`。14:56 检查时 W&B `pencounkdual-111/AdaLigand_Stage1/a87fe1ef` 已从第四次 validation 的 `global_step=4514` 恢复到 `global_step=4535`，服务器错误扫描为空。

## Completed

第四次正式 validation 的总损失为 `0.321514`。配体体素、受体、蛋白主链、核酸主链、原子和伪原子 PR-AUC 分别为 `0.576911`、`0.639860`、`0.983063`、`0.880060`、`0.664888` 和 `0.582926`。配体体素 PR-AUC 比第三次 validation 提高约 `0.015089`，超过绝对改善阈值 `0.003`；`warmup_lr` 已达到最高学习率 `5.0e-5`。

`TOP_epoch_00_score_0.5769.ckpt` 与 `last.ckpt` 已于 14:49--14:50 完整落盘，大小均为 1,466,224,438 字节。`BEST.ckpt` 已刷新为上一轮的 `TOP_epoch_00_score_0.5618.ckpt`，当前最高 TOP 等待后续 validation 刷新别名。

## Decisions

继续保持当前科学契约与训练参数，不触发重启或学习率修正。`refined/unrefined` 面板的 `NaN` 仍来自未启用的稀疏精修路径，不参与损失、调度器判断或 checkpoint 选择。

## Open Questions

无新增科学契约问题。后续观察第五次 validation、`BEST.ckpt` 是否刷新到 `0.5769` TOP，以及最高学习率阶段的验证改善与调度器计数。

## Next Actions

继续使用多段独立的 300 秒命令低耗守护 Job `351295`。只在 validation、checkpoint、学习率下降、故障恢复或训练退出等明确事件时合并更新记录。

## Files To Reopen

- `文档/exec_plan/Find辅助监督与新版Find1端到端实施.md`
- `文档/mapping/计划执行映射.md`
- `CLAUDE/memory/projects/adaligand.json`
- `CLAUDE/memory/handoffs/2026-08-24-find1低学习率重训第三次验证.md`
