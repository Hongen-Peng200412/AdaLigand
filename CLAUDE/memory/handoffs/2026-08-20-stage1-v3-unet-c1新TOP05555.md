# Handoff: Stage1 第三版 unet_c1 新 TOP 0.5555

Date: 2026-08-20

## Current State

Slurm Job `346737` 上的 Stage1 第三版 `unet_c1` a4 继续稳定训练。本对话的 active goal 仍是持续监视该 Job、在既定范围内修复可恢复故障，并在训练正常完成后验收。

- Job：`346737`，`RUNNING`，节点 `hnode02`，单张 H100，16 CPU，`after_lock_346737` 保留。
- W&B：`hqumqkex`；2026-08-20 21:35 为 `trainer/global_step=17,024`、`epoch=0`。
- 16:52 validation：配体体素 PRAUC `0.555503`，受体 PRAUC `0.613929`，蛋白主链和核酸主链宏平均 PRAUC 分别为 `0.136529` 和 `0.162392`。21:17 后一轮配体体素 PRAUC 为 `0.554266`，未产生更高分。
- 当前最高 TOP：`TOP_epoch_00_score_0.5555.ckpt`；`BEST.ckpt` 已在 21:17 validation 后刷新并与该 TOP 逐字节一致。
- H100 快照：利用率 100%，显存 80,708/81,559 MiB；35 个训练相关 Python 进程存活。
- a4 错误切片为空，没有 CUDA OOM、共享内存错误、Traceback 或 RuntimeError。

## Completed

- 确认 0.5555 高于此前最高 TOP 0.5511，属于新的关键训练事件。
- 对 `BEST.ckpt` 与新 TOP 进行了大小、时间、SHA-256 和逐字节比较；`BEST.ckpt` 仍对应 0.5511，新 TOP 文件完整但两者内容不同。
- 只读检查冻结的 `BestCheckpointAlias` 语义及项目既有运行记录，确认别名在训练期间可能落后一次 validation，并会在下一次 validation 或 `on_train_end` 刷新。这不是训练中断条件，不改代码、不重启任务。
- 21:17 validation 后再次核查：`BEST.ckpt` 已刷新为 0.5555，其 SHA-256 与新 TOP 同为 `568bcbe06b40f958a62eb03966a39393e55cf2a6d3be4feff18866972b4b810c`，逐字节比较返回一致；别名延迟已正常闭合。
- 新指标、checkpoint 状态和别名语义已经写入 `文档/exec_plan/Stage1第三版训练启动与监视记录.md` 与计划执行映射。

## Decisions

- 当前可用于表示最佳模型的是 `TOP_epoch_00_score_0.5555.ckpt`，不能把暂时落后的 `BEST.ckpt` 当作当前最高分证据。
- 训练最终结束时再次核对最高 TOP、最终别名和 `last.ckpt`。
- 继续使用 60 分钟命令自然睡眠，不创建 heartbeat。
- batch 8 继续保持；只有出现明确 CUDA OOM 才降为 6。
- 不删除 `after_lock_346737`，不使用 `scancel`，不操作其他 Job。

## Next Actions

1. 执行 60 分钟命令自然睡眠，期间不发送消息。
2. 醒来后检查 Job 状态、H100、W&B summary、checkpoint、a4 错误切片与锁。
3. 训练正常结束后完成 Slurm、W&B、checkpoint、错误日志和结束原因的联合验收，再收口 goal。

## Files To Reopen

- `C:\Users\15919\Desktop\AdaLigand\文档\exec_plan\Stage1第三版训练启动与监视记录.md`
- `C:\Users\15919\Desktop\AdaLigand\CLAUDE\memory\handoffs\2026-08-20-stage1-v3-unet-c1新TOP05555.md`
- `C:\Users\15919\Desktop\Pocket_Plus\src\train.py`
- `C:\Users\15919\Desktop\Pocket_Plus\训练与运行\sh\unet_c1.sh`
