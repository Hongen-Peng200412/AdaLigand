# Handoff: BOX 契约最小化 + 迭代文档转正收敛

Date: 2026-06-29

## Current State

经多轮 grill 收敛，把 Stage2/3 设计的几大方案落进文档（"转正"前的清理）。三份文档已改、一份草案已删。文档仍在"未成熟或过时的讨论"目录，但已按"自包含、无回顾字样"写。下一步可据此起 Stage2 代码骨架 / exec_plan，以及（数据集 D–G 做完、Stage1 重训后）做两形式推理产 Stage2/3 数据。

## Completed（本会话改动）

- **重写 `文档/未成熟或过时的讨论/BOX-level数据契约.md`**（拿草案当骨架，clean）：
  - 行 = 有 blob 的 box（≤1，无 blob 不进训练）；列 = 图级全 occurrence。
  - 删 `member_occurrences` 与 `main_occurrence_id`；`box_id` 改 `(pdb_id, box_type)` 内唯一。
  - 每 box：枚举存稀疏 `occ_distances`（按 `centroid_atom`、in-box = atom 中心在框内）；推理存"这一个 blob"（box 级 = mask 中心离 box 中心最近者；全图 = 一框一 blob）+ 稀疏 coverage。
  - α/β/O 全派生；口袋以 blob 锚（密度档）/ box 中心（受体档）。
  - UNet 两源特征按 box 存、只存预测 ligand-area 内、完整不截断。
  - bias 配方：ligand-area 内截断高斯采样 × 比例 + 全局随机 floor。
  - context 只按受体比率过滤、无 clean 旗。diff 不落盘、exp/sim 整图 1Å。
  - occurrence 两套中心（centroid_voxel 已有 / centroid_atom 派生）；四类 box_type（含 infered_box）。
- **定点编辑 `Stage2_Stage3_迭代运算设计_讨论.md`**：删"本版关键修订"+ 所有回顾字样；§1.5 改四层（前导 once / trunk per-Block / 粗 / 细），前导 phase-aware + repr seed 来源说明；门 = 硬冻 k epoch + 之后 ramp；§8 打分 `coarse_match_recall` + `coarse_match_precision` 都拼进 O/A/B 三头；砍 A'/B' 二值为默认（留三不管论证）。
- **小改 `规划文档/数据处理_v2.md`**：删 diff 落盘（§0/§7 E2/§9/§10）；E3 `mask_{cid}` 转必存、`centroid_{cid}` → `centroid_voxel_{cid}`。
- **删除** `BOX契约_最小版草案_讨论20260629.md`（已合并进正文）。

## Decisions（核心收敛）

- 统一锚：所有 box（含 infered_box）一个 blob 一行、图级 occurrence 当列；不存 main/member，α/β/O 派生。
- 两套 occurrence 中心：voxel 中心（recrop/prompt）/ atom 中心（距离·in-box）。
- α/β 在落 blob 时顺手算、稀疏落盘（只存有覆盖条目）。
- 口袋跟 blob 走（训推同源、无域差）。
- UNet 特征必须落（Stage1 UNet 重、不能现算），但只落 ligand-area 内、按 box。
- trunk 四层分离 + 前导 phase-aware（阶段一前导含 seed 阶段二冻；后段从头重嵌阶段二新训）。
- 砍 A'/B' 二值（分类有 O、回归有连续 A/B，A'/B' 三不管）。

## Open Threads（本轮未做，留后续）

- **centroid_atom 物化**：当前派生自 C4 `coords`+`present`，未物化、未改 C 代码（C 即将跑）。若将来想物化进 `ligand_coords.npz`，下次动 C 时加 `centroid_atom_{cid}=mean(coords[present])`，非必须。
- **§8 pipeline 三条（本轮明确不动，待单独 grill）**：(1) 阈值标定——先全图推理一部分调阈值，再做 box 级 blob 产生与落盘；(2) blob 级硬过滤——用周围受体 binding 概率重打分去低分 blob；(3) Stage1 推理重写——训推共用 builder、修 CPU 并行、砍多分类、落盘契约与 BOX 契约一起定。
- **`模型总规划_v2.md`** 仍有"替旧 A'/B'"等回顾字样，它转正时需同样清理（本轮不在范围）。
- 数据集 D–G 未开工；Stage1 待新数据重训。

## Governance Note

`数据处理_v2.md` 是规划文档，本轮按用户明确授权小改（D–G 未实现、非漂移收口）。改动理由记于本 handoff + Git，规划文档正文保持 clean。

## Files To Reopen

- `文档/未成熟或过时的讨论/BOX-level数据契约.md`（最小化转正稿）
- `文档/未成熟或过时的讨论/Stage2_Stage3_迭代运算设计_讨论.md`（四层 trunk / 门 / 三头拼粗码 / 砍 A'B'）
- `文档/规划文档/数据处理_v2.md`（diff 删、mask 必存、两套质心）
- `文档/未成熟或过时的讨论/模型总规划_v2.md`（待转正清理）

## Next Actions

1. 起 Stage2 第一版代码骨架 / exec_plan（据迭代文档）。
2. 单独 grill §8 pipeline 三条（阈值标定 / blob 硬过滤 / Stage1 推理重写）。
3. 数据集 D–G 开工；Stage1 重训后做 box 级 + 全图两形式推理，按 BOX 契约落盘。
