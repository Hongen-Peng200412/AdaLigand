# Handoff: Stage1 第三版 unet_c1 Job 346737 接管与稳定启动

Date: 2026-08-18

## Current State

用户明确授权只接管 Slurm Job 346737，并在其上运行带受体主链辅助损失的 Stage1 第三版 `unet_c1`。当前任务仍在独立任务根目录运行：

- 任务根目录：`/storage/penghongen/tmp/stage1_v3_ablation_replacement_20260817T1845/task_root/Pocket_Plus`
- 运行目录：`/storage/penghongen/tmp/stage1_v3_ablation_replacement_20260817T1845/runtime/mainchain_official_346737`
- 正式发布代码：`Pocket_Plus_b150098bfd62`
- 正式第三次启动记录：`/storage/penghongen/tmp/stage1_v3_ablation_replacement_20260817T1845/feedback/launches/346737/tmp_stage1_mainchain_job346737_20260817T205907_a3`
- W&B：[`lolou7cc`](https://wandb.ai/pencounkdual-111/AdaLigand_Stage1/runs/lolou7cc)

2026-08-18 02:49 的一小时唤醒检查显示：Slurm 仍为 `RUNNING`，H100 利用率约 83%、显存约 62.9 GB，主进程和 16 个 Dataset worker 仍存活；正式启动标记之后没有新增错误。02:52 通过 `srun --jobid=346737 --overlap` 定向核对到 GPU 计算进程 `PID 76836`，H100 利用率约 97%、显存约 62.9 GB、进程 CPU 约 97%，因此训练进程确实仍在计算。W&B 原始运行文件和 `debug-internal.log` 仍在增长，但可读 summary 仍停留在 `trainer/global_step=1505`、`epoch=0`，尚未发现 checkpoint。因此目前确认的是“训练进程和 GPU 活跃、summary 刷新滞后”，还不能据此判定训练停滞；在证据明确前不得停止或修改该任务。

## Completed

- 保留用户在三个数据准备文件中的本地规范化修改，并通过官方安全同步入口复制到 346737 的独立任务根目录。
- 核对发布代码中 `unet_c1.sh`、`src/train.py`、`src/datasets/stage1_dataset.py` 和 `src/datasets/stage1_requests.py` 的 SHA-256 一致。
- 消费用户授权的 `kill_lock_346737`，终止旧尝试并建立新的 `try_lock_346737`；原有 `after_lock_346737` 保留。
- 原子写入并通过 `bash -n` 校验 `run_cmd_346737.sh`，随后移除 `try_lock_346737` 以启动正式执行。
- 正式运行参数已核实：单卡 H100、16 CPU、16 workers、`prefetch_factor=4`、`batch_size=6`、`global_batch_size=48`、20 epochs、每 epoch 30 次 validation；`protein_mainchain_loss_weight=0.05`、`nucleic_mainchain_loss_weight=0.05`、`ligand_distance_loss_weight=0.3`，使用第三版 box pool。
- 记录了此前临时 a1/a2 尝试；a2 曾出现 `torch_shm_manager: Invalid argument`，正式 a3 已使用独立临时目录重新启动。
- 已将正式启动和首轮稳定检查写入 `文档/exec_plan/Stage1第三版训练启动与监视记录.md`，并更新计划执行映射。

## Decisions

- 本轮只负责 Job 346737；不检查、不接管、不修改 346667、346668 或其他任务。
- 不使用 heartbeat，不设置周期性外部监控；采用命令启动的自然睡眠。任务稳定后每轮睡眠 1 小时，醒来只做一次状态检查。
- 只在出现明确事件（启动、错误、epoch/validation/checkpoint、退出或资源锁变化）时补写执行记录和 handoff，不把未变化的轮询写成流水账。
- `after_lock_346737` 作为任务边界和授权记录保留；没有用户新的明确授权，不删除锁、不重启、不改训练参数。

## Open Questions

- W&B summary 长时间停在 `global_step=1505`，但原始运行文件、进程和 GPU 仍活跃。需要在下一次检查中结合训练输出、W&B 文件时间戳和 checkpoint 目录判断是否只是 summary flush 延迟。
- 若确认训练停滞或出现新错误，应先记录证据并向用户汇报，不自行扩大操作范围。

## Next Actions

1. 只检查 Job 346737 的 Slurm、锁、主进程/worker、GPU、W&B summary、运行输出和 checkpoint。
2. 若出现有效进度或 checkpoint，补充执行记录，并在该关键节点再写一份新的 handoff 或更新本 handoff；若无明确变化且无故障，继续一小时自然睡眠。
3. 任务结束、失败或需要资源动作时，先停止自动动作并向用户报告证据，等待下一步授权。

## Files To Reopen

- `C:\Users\15919\Desktop\AdaLigand\文档\exec_plan\Stage1第三版训练启动与监视记录.md`
- `C:\Users\15919\Desktop\AdaLigand\文档\mapping\计划执行映射.md`
- `C:\Users\15919\Desktop\AdaLigand\文档\规划文档\Stage1第三版训练IO实施计划.md`
- `C:\Users\15919\Desktop\AdaLigand\CLAUDE\memory\projects\adaligand.json`
- 服务器任务根目录下的 `run_cmd_346737.sh`、W&B 运行目录和 `feedback/allocations/346737`
