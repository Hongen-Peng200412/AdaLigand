# Handoff: Matcher 正式训练已提交

Date: 2026-08-04

## Current State

不依赖 Stage1 推理产物的 Anchor/A-only O/O′ Matcher 正在复用 Slurm Job `335493` 的 gnode10 A800 allocation。第一次执行在 Phase1 step 4069 后因显存不足退出；用户授权从头重新训练。第二次执行已使用显存安全训练协议、新输出根和 W&B online 启动，epoch 0 候选准备已经完成，Phase1 已推进到 `global_step=177`；连续训练健康核对为 `1/3`。

## Completed

- 提交前确认正式输出根不存在，且没有同名 Job。
- 已用完整 release/launch 模式提交 A800×1、CPU 16、QOS `cpu96`的 Job `335493`。
- 已确认 `Partition=nvlink`、`TimeLimit=UNLIMITED`、`TresPerNode=gres:gpu:a800:1`，命令中没有 `--time`。
- 已创建只绑定线程 `019fc69d-c101-7d20-9ab2-4d16e4433419` 的 heartbeat `matcher`。
- 已核对服务器上该 Job 实际读取的旧四锁 runner：任务结束或失败后会创建 `try_lock_335493` 保留 allocation。
- release 为 `AdaLigand_edd59b6263cb`，launch 为 `train_anchor_O_O_prime_job335493_20260804T051115_a1`，`after_lock_335493` 存在。
- 首次运行核对未发现 NaN、OOM、Traceback 或其他错误。Python 栈位于 `AnchorPocketDataset.set_epoch(0)` 的候选准备，10 秒内继续读取约 0.58 GB；A800 暂时空闲是因为尚未进入优化步，不是训练进程退出。
- epoch 0 候选准备已在 `06:03` 后完成：保留 12,678 个 PDB，跳过 203 个零候选 PDB，接受 161,759 个 bias 候选和 179,602 个 context 候选。Phase1 已产生前三个正式优化步，所有 loss 有限，主进程占用约 25.8 GiB 显存；连续训练健康次数为 `1/3`。
- 连续三次训练健康检查均通过，heartbeat 已改为每 5 小时。最近核对 Phase1 已到 `global_step=753`，无 NaN、OOM、Traceback 或异常退出。
- 首次验证位于 `global_step=684`，`BEST.json` 当前记录 `F1_O_O_prime=0.08603167`、precision `0.13746224`、recall `0.06260750`、`O_threshold=0.04`，并指向 `checkpoint_step_00000684.pt`；该早期坐标不代表最终效果。
- 后续 step 1368、2052、2736、3419 的验证 F1 连续改善为 `0.11188`、`0.13414`、`0.13656`、`0.14742`。当前 BEST 位于 step 3419，precision `0.21019`、recall `0.11352`、`O_threshold=0.01`；Phase1 已进入 epoch 1 并推进至约 step 3682。
- 最近一次 `nvidia-smi` 快照显示主进程约占用 80,122 MiB，作业仍持续计算且没有 OOM，但显存余量很小；后续 heartbeat 继续观察，不自动改变训练配置。
- Phase1 随后在 epoch 1 完成 `global_step=4069` 后，于下一批次 backward 的 A 图网络边更新 `LayerNorm` 发生 CUDA OOM：已分配 78.11 GiB、保留但未分配 643.63 MiB、仅余 63.81 MiB，额外请求 494 MiB 失败。训练进程已退出，Phase2 未开始，最后可用 BEST 仍为 step 3419、F1 `0.14742`。
- runner 已创建 `try_lock_335493` 并保留 `after_lock_335493`；Slurm Job 仍为 `RUNNING`，A800 allocation 没有释放，GPU 当前空闲。不得删除任一锁或擅自续跑，等待用户确认诊断与恢复方案。
- 用户授权从头重训。新协议保留模型结构与 `map_channels=[24,48,72,96]`，采用 occurrence 装箱 48、Map 候选分块 32、Map activation checkpoint 和 Phase2 FinePair 分块 1024；新输出根为 `/storage/penghongen/AdaLigand/Results/matcher/anchor_O_O_prime_v1/seed_3407_occ48/`。
- 新增 W&B online 薄集成：项目 `AdaLigand_Matcher`，共同 group `anchor_O_O_prime_v1_seed_3407_occ48`，Phase1/Phase2 使用独立 run name；`events.jsonl` 仍是正式本地事件记录。服务器已确认 W&B 0.17.3、登录身份和网络可用。
- 完整本地回归为 43 项通过、1 项 CUDA 专项跳过；服务器定向训练测试 5 项通过；契约与可读性双审查无阻断。实现端点 `1eba879` 与学习端点 `a4dde27` tree 相同，`Learn/CUMULATIVE` 已推进到 `a4dde27`。
- 第二次执行 release 为 `AdaLigand_1126746ccd75`，launch 为 `train_anchor_O_O_prime_job335493_20260804T194729_a2`，W&B online run ID 为 `93omhb5u`。`try_lock_335493` 已按授权删除，`after_lock_335493` 保留；Phase1 已进入正式优化。
- 第二次执行的 epoch 0 候选准备保留 12,678 个 PDB，跳过 203 个零候选 PDB；得到 161,759 个 bias 候选，并在 180,268 个 context 请求中接受 179,602 个。Phase1 已到 `global_step=177`，最近 `loss_total=1.30499`，各损失均为有限值；GPU 快照约占用 35.4 GiB，未发现 NaN、OOM 或 Traceback，连续训练健康核对为 `1/3`。

## Decisions

- 排队阶段每 3 小时检查一次；排队不计入训练健康次数。
- 首次检测到 `RUNNING` 后立即改为每 30 分钟一次，再累计三次连续训练健康。
- 连续三次健康后，heartbeat 改为每 5 小时一次。
- heartbeat 只读监控；不自动修改、取消、重提或切换训练，也不删除 `try_lock_335493` 或对应 `after_lock`。
- 72.910 GiB reserved 峰值不再视为正式训练启动阻断。

## Next Actions

1. 候选准备完成后每 30 分钟核对 Phase1 的 events、loss、显存、GPU 活动、W&B 以及 NaN/OOM/Traceback；候选准备期不计训练健康次数。
2. 实际优化连续三次健康后把 heartbeat 改为每 5 小时，并记录新的 BEST、Phase1 完成与 Phase1→Phase2 接力状态。
3. 在 Phase1 BEST、Phase1→Phase2 接力、Phase2 BEST 和最终 validation 等实质节点追加执行记录；无实质变化时不制造文档噪声。

## Files To Reopen

- `文档/exec_plan/Matcher_Anchor_OOPrime端到端实施.md`
- `configs/matcher/anchor_O_O_prime_phase1.yaml`
- `configs/matcher/anchor_O_O_prime_phase2.yaml`
- `训练与运行/sh/matcher/train_anchor_O_O_prime.sh`
- `/home/penghongen/Feedback/AdaLigand/`
- `/storage/penghongen/AdaLigand/Results/matcher/anchor_O_O_prime_v1/seed_3407_occ48/`
