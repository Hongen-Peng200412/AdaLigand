# Handoff: 辅助监督决定 + 全项目文档治理落地

Date: 2026-07-08

## Current State

经一轮 grill（Q1–Q7）+ 辅助监督取舍，把决定**顺着边界规则派进全套文档**并落盘。核心是三条治理归位：转正稿留在中性 `文档/讨论/`（用户已改名，不再是"未成熟"）、跨文档边界就地声明在各文档头 + mapping 只做索引、AGENTS.md 保持最小指针不加内容。下一步可据此起 Stage2 代码骨架，以及（数据 D–G 完 + Stage1 重训后）产 Stage2/3 数据。

## Completed（本会话改动）

- **新建 `文档/讨论/Stage1_Find_接缝与推理重写.md`**：Stage1 = Pocket_Plus（另一仓库）的**接缝/集成规格**（不复述架构）。三处改动：换新数据/契约（模型不变）、推理重写（CPU 并行 + 二阶段中心化推理）、裁剪（CPC 取消、多分类头砍）。打分与二次筛选：**不删、过量保存 + 套打分**（老粗分数 + 可选迷你二分类网，丢弃中间高斯档）；**存/用分离**（打分器归 Stage1、选择归 featurizer、添油只存分数）。
- **扩 `文档/mapping/计划执行映射.md`** 为全 plan 索引（数据/BOX/模型总规划/迭代运算/Stage1）+ 文件夹语义（讨论=演进、规划文档=已稳，churn 停才搬 + 翻状态）。
- **改 `文档/讨论/BOX-level数据契约.md`**：§4.2 blob 加 `scores` 套打分（只增不改）+ over-save、去掉"硬过滤/删"；§4.3′ 逐配体原子覆盖标签（定义写这）；§4.4 加 `la_voxel_prob`；§4.5 逐受体原子 binding 概率（受体上下文，供 Stage2 输入 + 迷你网）；§9.1 infered_box 细化为两阶段中心化重推；§2.2 centroid_atom 改"已定物化"；第1层表加受体键表行。
- **改 `文档/规划文档/数据处理_v2.md`（转正文档）**：C4 物化 `centroid_atom`；C3 加受体键表；新增 §5.5 C5 受体 49 维特征；§5.2″ C2b 配体描述子（Wiener/图能量/Rg/计数 + atom_local，去重预计算）；§0 表 / §9 目录 / §1 决策（D28/D29）/ 头部边界同步。
- **改 `文档/讨论/Stage2_Stage3_迭代运算设计_讨论.md`**：§12 监督全清单加"标签来源"列 + binding 纠正（A 图边感知目标、可选受体档默认开、**非冗余**）+ 新增行（配体全局描述子/blob 形状/逐原子覆盖）+ 逐 PP 否决；§2.1 A 图边 = 落盘键 ∪ 运行时 radius；§4.1 featurizer 契约（运行时标签定义 / blob 选择旋钮 / CPU 预算闸）；头部边界。
- **改 `文档/讨论/模型总规划_v2.md`**：头部边界行（监督全清单在迭代运算 §12；本文 owns 是什么 §5.3/§8）。
- **归档** `BOX契约_最小版草案_讨论20260629.md` → `文档/讨论/留档/`（用户视为已删/被 BOX-level 取代）。
- AGENTS.md **不动**（已是最小指针）。

## Decisions（核心收敛，Q1–Q7）

- 跨文档治理内容：**局部声明（各文档头）+ mapping 索引 + AGENTS.md 一行指针**，不新建桥接文档、不进 AGENTS.md。
- 文件夹：讨论=演进 / 规划文档=已稳；转正 = churn 停才搬 + 翻 mapping。模型三份暂留讨论。
- 辅助监督标签：**定义随生产方**（预计算 → 数据处理/BOX；运行时 → 迭代运算 §4.1 featurizer），§12 只指针；运行时标签带 CPU 预算闸（PP≈8096、A≈3k 扛不住就退预计算）。
- Stage1 = 接缝规格；语义归 Stage1、存储归 BOX、选择归 featurizer；二阶段中心化推理闭合训/推域差；打分器/选择/存储三分。
- 辅助监督取舍：配体侧 Emap2lig 全套 + 克制三项全局描述子（MW/#原子、Wiener、Rg）；blob 形状运行时；逐配体原子覆盖（BOX 新字段）；binding 重预测**采纳为可选、受体档默认开**（A 图边感知论证，纠正我先前"弱/冗余"的误读——文档只对逐 PP 说冗余）；逐 PP/ligand_area 重预测否决（PP 冻结）；口袋描述子/持久同调作可选钩子不进 v1。

## 数据处理_v2（转正文档）漂移分类（governance）

均为**用户显式授权**：centroid_atom 物化 = **良性**（补此前刻意忽略的量）；受体键表 / 49 维受体特征步 / 配体描述子 = **新增 scope**（非漂移）。计划正文保持 clean，理由记于本 handoff + Git。

## Open Threads / Next

- Stage1 §8：阈值标定、over-save 下限、scores 集成员、迷你网架构与训练时机、受体上下文通道——先看分布再定。
- 模型总规划_v2 回顾字样（"替旧 A'/B'"）待清理再转正。
- 数据 D–G 未开工；Stage1 待新数据重训（数据后第一优先）。
- 参照算法梳理文档：用户定为与本项目无关，**移出治理范围**，不管/不折/不删。

## Files To Reopen

- `文档/讨论/Stage1_Find_接缝与推理重写.md`
- `文档/讨论/BOX-level数据契约.md`（§4.2/§4.3′/§4.4/§4.5/§9.1）
- `文档/规划文档/数据处理_v2.md`（§5.2″/§5.3′/§5.4/§5.5、D28/D29）
- `文档/讨论/Stage2_Stage3_迭代运算设计_讨论.md`（§12/§4.1/§2.1）
- `文档/mapping/计划执行映射.md`
