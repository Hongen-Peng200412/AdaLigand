# Matcher v2 双模式实施中

## 当前目标

在 `codex/matcher-v2-dual-context` 实现一套共享 Matcher。`ground_truth_context` 读取真实配体、GT 配体 10 Å 受体包络 A 与中心 48³ 实验密度；`stage1_context` 额外读取 Find F1-centered 的 V、P、A 条件与完整图概率派生 PP。先把模式一训练、推理和评估跑通，再从模式一权重继续训练模式二。

## 已完成

- `matcher_v2/contracts.py` 把 Stage3 可复用的 `PocketInput` 与 Matcher 专属槽位/标签分开。
- 两个显式 Dataset 位于 `matcher_v2/data_ground_truth.py` 和 `matcher_v2/data_stage1.py`；没有 Dataset 注册器或通用多模态插件框架。
- `matcher_v2/model.py` 复用已验证的图、U-Net、TypedAttention、粗细 Block，实现六个独立预测头。A 的 Stage1 概率与特征形成逐原子增量；V/P/PP 形成 BOX 候选增量。两者末层均为零初始化，条件缺席时不经过增量网络。
- `matcher_v2/objectives.py` 实现 A/B/O 连续 SmoothL1、A′/B′/O′ focal gamma=2、身份内 Hungarian、深监督、实体辅助和 FinePair 辅助。
- `matcher_v2/decode_ground_truth.py` 在同一 PDB 内联合决定一对一匹配与空槽位；验证以 0.003 步长扫描解码后的 F1 与 PR-AUC，checkpoint 冻结阈值，独立评估不重新选择阈值。
- 正式配置位于 `configs/matcher_v2/mode1/`；正式脚本位于 `训练与运行/sh/matcher_v2/mode1/`，W&B online，脚本不设置 Slurm 时间限制。
- 本地新版加既有图/U-Net 共 22 项测试通过；覆盖完整模型严格零增量、Phase1→Phase2、Phase2 resume 冻结、同身份 occurrence 交换、六头 FinePair、PP 四尺度梯度和验证调度。训练入口已补充 BF16 验证、精确五次验证、在线 W&B、`events.jsonl`、resolved YAML 与断点续训。
- `matcher_v2/README.md` 已写明两个数据入口、当前模式一不启用自生成点，以及 Stage3 可直接复用的叶子。

## 已核对的真实字段

服务器旧 Find F1-centered 真实宽度是：`voxel_final=48`、`P_feat_L2+P_feat_L3=64+48=112`、`A_feat_L1+A_feat_L2+A_feat_L3=64+64+48=176`。模式一 A–G `exp.npz` 使用 `grid`、`voxel_size` 和 `origin`。新版 Stage1 第二版 BOX manifest 含 train 13,710、validation 200 个 PDB。

修正后真实 `10ad` 双模式共同模型前向已通过，两者都得到有限的 `[2,16]` 六头输出。第二轮契约复审无 mode1 科学阻断。正式模式一清单最终由 CPU Job `336690` 成功生成：train 12,881 个 PDB / 180,382 个 occurrence、validation 188 / 2,914、calibration 89 / 1,386。

第二轮可读性复审同样无正式训练阻断。旧主干复用只作为首轮训练的过渡边界；不要在此时复制或重写整份旧模型。审查后的合并回归为 `55 passed, 1 skipped`。

旧 heartbeat 已删除，Job `335493` 的旧 Anchor 训练在 step 5261 后通过精确 `kill_lock` 结束，A800 allocation 由 `try_lock` 保留。临时画像 attempt 3 暴露并促成修复六头损失的设备不一致；attempt 4 使用 release `AdaLigand_e0072b2df641` 成功完成。普通 46-occurrence batch 的优化步为 7.78 秒、峰值保留显存 6.04 GiB；最大 `6v22` 的 100-occurrence 单 PDB 优化步为 6.00 秒、峰值保留显存 8.83 GiB。两项均无 OOM，远低于 71.397 GiB 人工门槛，不需要改变正式模型或运行参数。

## 下一步

实现端点 `codex/matcher-v2-dual-context@acdaba9` 与学习端点 `Learn/matcher-v2-dual-context@defbb26` 的 Git tree 完全一致；学习端点回归为 `55 passed, 1 skipped`，`Learn/CUMULATIVE` 已快进到 `defbb26`。

Job `335493` attempt 5 已使用 release `AdaLigand_aa34cc600a27`、launch `train_anchor_O_O_prime_job335493_20260805T041302_a5` 启动正式模式一训练。输出根为 `/storage/penghongen/AdaLigand/Results/matcher_v2/ground_truth_context/seed_3407_occ48`；Phase1 W&B online run id 为 `matcher-v2-gt-3407-p1`。训练进程、W&B service 和 GPU 活动均已确认，当前处于 epoch 0 候选准备期。

首次正式验证已在 `global_step=879` 完成：precision=0.15913688、recall=0.08098833、F1=0.10734592、PR-AUC=0.03771764、阈值=0.135。`latest.pt`、`best.pt` 与 `BEST.json` 已落盘，没有新 NaN、OOM 或 Traceback；连续优化健康次数为 1/3。

连续三次实际优化健康检查已经通过，覆盖至 `global_step=2637`；automation `matcher-v2` 已改为每 5 小时监控。最近一次 `global_step=3516` 的验证 F1 为 0.09458749，当前 BEST 仍是 step 879 的 F1=0.10734592；训练进程、W&B、GPU 和锁均正常。

5 小时检查时 Phase1 已进入 epoch 2、`global_step=10547`。当前 BEST 位于 step 6151：precision=0.13731959、recall=0.11427591、F1=0.12474246、PR-AUC=0.03478840、阈值=0.06。训练进程、W&B、A800 与锁正常，无新 NaN、OOM 或 Traceback。

Phase1 已在 `global_step=11428` 因 `scheduler_stop` 正常完成并写入 `COMPLETED.json`，最终 BEST 为 step 6151 的 F1=0.12474246。正式脚本已自动进入 Phase2；检查时位于 epoch 1、`global_step=7028`，当前 BEST 为 precision=0.19331586、recall=0.10123542、F1=0.13288288、PR-AUC=0.05658531、阈值=0.132。Phase2 进程、W&B、A800 和锁均正常，无新 NaN、OOM 或 Traceback。

后续检查时 Phase2 已进入 epoch 3、`global_step=14076`。当前 BEST 位于 step 12310：precision=0.22332671、recall=0.11564859、F1=0.15238526、PR-AUC=0.07005863、阈值=0.12。训练进程、W&B、A800 与锁正常，无新 NaN、OOM 或 Traceback。

1. automation `matcher-v2` 每 30 分钟检查当前对话对应的正式任务；候选准备不计健康次数，实际优化连续三次健康后改为每 5 小时。
2. 监控 NaN、OOM、Traceback、显存、事件、BEST/checkpoint 与 Phase1→Phase2；不要删除 `after_lock` 或未来可能出现的 `try_lock`。
3. Phase2 完成后，正式脚本自动运行 validation 与 calibration 推理；验收两个 `metrics.json` 后再判定端到端任务完成。

## 2026-08-06 资源移交

本交接末尾此前描述的 Matcher v2 活跃状态已经失效。Job `335493` attempt 6 的运行日志出现 `Too many open files`；用户随后明确授权把该 allocation 的 A800 转交给 Find_0 train F1 分片 1/50。主 agent 于 03:45 使用精确 `kill_lock_335493` 结束 Matcher 进程，等待 runner 创建 `try_lock_335493` 后切换动态命令；没有 `scancel`，`after_lock_335493` 继续保留。

Matcher v2 代码、配置、既有 checkpoint、BEST、W&B 与历史产物没有被修改或删除，但 Job `335493` 已不再运行 Matcher，原 `matcher-v2` heartbeat 已删除。以后若恢复 Matcher，必须另行选择资源并从本交接记录的既有产物判断恢复点；不得删除当前 Find_0 进程的锁、恢复旧动态命令或把 Job `335493` 继续记作 Matcher 活跃任务。
