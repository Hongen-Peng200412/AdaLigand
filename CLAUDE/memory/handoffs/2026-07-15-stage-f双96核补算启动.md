# AdaLigand Stage F 双 96 核补算启动交接

Date: 2026-07-15 16:29+08:00
Status: active; A–G 尚未完成

## 当前结论

正式流程仍处于 Stage F，不是 G。正式 job `316116` 在 `cnode04` 以 CPU96、F12×MapQ np8 运行；独立尾段补算 job `318350` 已在 `cnode01` 用另一台 CPU96 零等待启动。用户当前总分配为 192 CPU，达到 `Cpu96` 用户 QoS 的 `MaxTRESPU cpu=192`。`316117` 仍为 dependency waiting，且只允许运行 G analyze。

## 冻结身份

- 正式 run：`adaligand_ag_20260711T154658`
- 正式 F job：`316116`
- 补算 run：`adaligand_ag_20260711T154658_fsupp96_v1`
- 补算 job：`318350`
- 尾段：`[19386,22386)`，实际 2,990 PDB
- 碰撞停止阈值：正式完成 17,386，guard 2,000 tasks
- plan SHA-256：`1d5c12172629bcba2af65a379c2c78d9bf7141fdcdd505e699add8b58b1dff4f`
- ID SHA-256：`acacde79c2a5a8727949cdc0a986930aa8404419a8edaabfb264f4f05dacea80`
- pair list SHA-256：`6c736180f5268e96329c62d9fceb8cc5c5917935677b3ddd815202e5891035f8`
- Stage F exclusion SHA-256：`3b10abb5cc69bca0689cb89cdb6e0bde21620504c11479edcbfb51c83ec68ee8`
- 证据目录：`/storage/penghongen/AdaLigand/Ori_Data/reports/runs/adaligand_ag_20260711T154658/stage_f_tail_supplement_20260715_v1/`

## 启动证据

- `318350` 于 `2026-07-15T16:23:59+08:00` 在 `cnode01` 进入 RUNNING；`316116` 同时保持 `cnode04` RUNNING。
- 两项各 96 CPU，总分配 192；补算 TimeLimit=UNLIMITED。
- `after_lock_318350` 与 `/home/penghongen/child_pgid_318350` 存在；try/kill 不存在。
- 补算日志确认 formal/supplement run、plan/ID SHA 与 `Parallel(n_jobs=12)`；只有无害的 `C.UTF-8` locale warning。
- Windows 全套为 224 passed、2 skipped；Linux 全套为 226 passed。

## 继续监控

1. 每两小时只读检查 `316116/318350/316117` 的 squeue/sacct、正式/补算日志增长、精确 locks 和质量三件套增量。
2. 不修改运行中的代码/命令，不运行 clean sync，不操作非精确归属锁。
3. 补算正常时不手工干预。守卫会每 300 秒检查正式日志，在 17,386 阈值前收口 child PGID 并写 stop marker。
4. 若 `318350` 失败，只处理它自己的日志、run、locks 与 retry；绝不触碰正式 `316116`。
5. 正式 F 完成后，以正式 22,386 行 status/release 为准，独立审计五条 run-only exclusion、unknown/duplicate/silent missing、四 CC、配体/6 Å 口袋 Q、schema v3 空口袋、MapQ sigma=.4 np=8 与 provenance。
6. 只有正式 `f_release` 通过后才接受 `316117`；G 只 analyze，不执行示例阈值、不写 `keep_list`。

## 资源记忆

`cpu` 分区有 4×96 CPU 节点，不 oversubscribe。两台 CPU96 已达到用户 QoS 的 192 CPU 上限；用户额外授权的 48 CPU 是测试/审计/备用额度，当前不能在同一 QoS 并发使用。任何 repair/补算并发都必须先查冻结实测与项目记忆，不能重新拍脑袋设低并发或把平均 CPU 当峰值容量。
