# Handoff: MRC 合约放行与 DE 正式启动

Date: 2026-07-12

## Current State

- 正式 run 为 `adaligand_ag_20260711T154658`，DAG 保持 `316114 → 316115 → 316116 → 316117`，没有取消或重提。
- 316114 已 `COMPLETED 0:0`；22,386/22,386 C release-ready，1,761 个 B 失败均为显式 `download_failed`。
- 316115 保留原 96 核 allocation，MRC marker 已放行，当前并发运行 D64/E24；316116 F12 与 316117 G analyze 继续按 `afterok` 等待。

## Completed

- Pocket Plus 六个 MRC 祖传函数保持逐函数源码/portable AST 零差异。
- 正式 header audit 覆盖 22,274 张唯一 EMDB 图；仅 EMD-11978/12465 两张为 mixed-axis，五张缺图均对应 B known failure。
- 7b14/7nll 真实 Chimera 1.19 smoke 通过 canonical/sim 同 shape、actual voxel、非零 origin、标准轴、`nstart=0` 和真三维内容 QC。
- 本地和服务器 Python 3.10 全套均为 172 tests passed；远端持久证据 run 为 `adaligand_mrc_release_evidence_20260712T201306`。
- 独立 release audit 判定无阻断；`/home/penghongen/mrc_contract_release_316115` 权限 0600，SHA-256 为 `2ca92614cb53a9f08058a5186afe677264928b6a64ba2a4b60a044d8cea6b6b2`。run_cmd 自行删除 pre_lock 并启动 D64/E24。
- 累计工作树已经按任务拆成独立 Git checkpoint；唯一保留未提交的是职责不清的 `文档/讨论/数据处理_v2————提问.md`。

## Decisions

- Pocket Plus 六函数是可信祖传基线；普通路径保持原样。只有全量审计确认的两张 mixed 图走显式 `np.any` 薄兼容，vendored 函数不改。
- Pocket grid 幅值不改；EMDB contour 保存 native/scale/canonical 三值，F 只消费 canonical 值并逐项绑定当前 E1 provenance。
- release marker 是本次 run 的受检证据，不是未来修改祖传代码或放宽科学契约的许可。

## Next Actions

1. 低噪声监控 316115 的 stage_d/stage_e status、DE release gate、未知失败和产物增量；正常运行不干预。
2. 316115 完成后独立审计 D/E，再确认 316116 复用既有 F12 run_cmd 与 MapQ `sigma=0.4,np=8`。
3. F 完成后审计四 CC、配体 Q、6 Å occurrence 口袋 Q、schema v3 空口袋 null/status 与 provenance，再让 316117 只运行 G analyze。
4. A–G 完成后做全量 QC/G 分布、ExecPlan/mapping/README/项目记忆收口；不猜阈值，不写最终 keep_list。

## Files To Reopen

- `文档/exec_plan/A-G数据流水线实现与全量运行.md`
- `文档/规划文档/数据处理_v2.md`
- `Data_Preprocessing/Ori_Data/code/readme.md`
- `与服务器交互/other/readme.md`
- `CLAUDE/memory/projects/adaligand.json`
