# Find_1 第三版双卡重训首次验证

## 当前状态

2026-08-22 15:18，Slurm Job `351295` 完成第一次正式 validation，并在验证结束后继续使用 `hnode01` 的两张 H100 训练。W&B 运行 `yyz7c55e` 到达 `trainer/global_step=1128`；当前没有显存不足、NCCL、DataLoader、非有限训练损失或进程退出错误。

## 第一次 validation

| 指标 | 数值 |
| --- | ---: |
| 验证总损失 | `0.403396` |
| 配体体素 PR-AUC | `0.531660` |
| 受体区域 PR-AUC | `0.576420` |
| 蛋白主链宏平均 PR-AUC | `0.249253` |
| 核酸主链宏平均 PR-AUC | `0.012209` |

原子、伪原子和配体体素等启用的验证损失与选择指标均为有限值。`refined/unrefined` 面板仍为 `NaN`，原因是当前 Find_1 没有启用配体稀疏精修路径；这些面板不参与训练损失、学习率调度器或 checkpoint 选择。

## Checkpoint

- `TOP_epoch_00_score_0.5317.ckpt`：1,466,222,905 字节，生成于 15:18:06。
- `last.ckpt`：1,466,223,097 字节，生成于 15:18:09。
- 运行目录：`/home/penghongen/Feedback/Pocket_Plus/logs/AdaLigand_Stage1-Find_1-CPC1/Find_1-CPC1____Find_1_job351295_20260822T093108_a1_CPC1`

训练在 checkpoint 保存后继续，两张 H100 的显存约为 79--81 GiB，并保持计算活动。后续仍按多段 300 秒命令进行 60 分钟低耗监视，只在新 validation、TOP/BEST/last 变化、故障恢复或退出时更新记录。

## 相关记录

- `CLAUDE/memory/handoffs/2026-08-22-find1双卡重训启动.md`
- `文档/exec_plan/Find辅助监督与新版Find1端到端实施.md`
- `文档/mapping/计划执行映射.md`
