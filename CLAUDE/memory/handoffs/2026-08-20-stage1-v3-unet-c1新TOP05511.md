# Handoff: Stage1 第三版 unet_c1 新 TOP 0.5511

Date: 2026-08-20

## Current State

Slurm Job `346737` 上的 Stage1 第三版 `unet_c1` a4 继续稳定训练。本对话的 active goal 仍是持续监视该 Job、在既定范围内修复可恢复故障，并在训练正常完成后验收。

- Job：`346737`，`RUNNING`，节点 `hnode02`，单张 H100，16 CPU，`after_lock_346737` 保留。
- W&B：`hqumqkex`；2026-08-20 00:22 为 `trainer/global_step=11,624`、`epoch=0`。
- 最新 validation 配体体素 PRAUC：`0.551052`。
- 当前最高 TOP：`TOP_epoch_00_score_0.5511.ckpt`，发布时间为 2026-08-19 23:15；同一时间更新了 `last.ckpt`。
- H100 快照：利用率 98%，显存 80,454/81,559 MiB；33 个训练相关 Python 进程存活。
- a4 错误切片为空，没有 CUDA OOM、共享内存错误、Traceback 或 RuntimeError。

## Completed

- 2026-08-19 19:41 后执行 60 分钟命令自然睡眠。睡眠 SSH 会话结束前，本机到服务器的私网路由暂时超时；统一 SSH 入口按有限重试规则停止，没有关闭主机密钥检查。
- 再执行 30 分钟命令睡眠后，SSH 连接恢复。Slurm 记录证明 Job 在断连期间始终为 `RUNNING`；训练进程、W&B、checkpoint 和锁没有受影响。
- 新 TOP 0.5511、最新 validation 指标和连接事件已经合并写入 `文档/exec_plan/Stage1第三版训练启动与监视记录.md`。

## Decisions

- SSH 私网路由超时是本机监视连接故障，不是训练 bug；不得因此重启训练或改写任务。
- 继续使用 60 分钟命令自然睡眠，不创建 heartbeat。
- batch 8 继续保持；只有出现明确 CUDA OOM 才降为 6。
- 不删除 `after_lock_346737`，不使用 `scancel`，不操作其他 Job。

## Next Actions

1. 执行 60 分钟命令自然睡眠，期间不发送消息。
2. 醒来后检查 Job 状态、H100、W&B summary、checkpoint、a4 错误切片与锁。
3. 没有明确事件时不追加日志；出现新最佳、epoch 变化、学习率下降、故障或正常结束时再更新执行记录和 handoff。

## Files To Reopen

- `C:\Users\15919\Desktop\AdaLigand\文档\exec_plan\Stage1第三版训练启动与监视记录.md`
- `C:\Users\15919\Desktop\AdaLigand\CLAUDE\memory\handoffs\2026-08-19-stage1-v3-unet-c1持续监视goal.md`
- `C:\Users\15919\Desktop\Pocket_Plus\训练与运行\sh\unet_c1.sh`
