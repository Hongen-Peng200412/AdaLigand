# Handoff: Stage1 第三版 unet_c1 新 TOP 0.5696

Date: 2026-08-21

## Current State

Slurm Job `346737` 上的 Stage1 第三版 `unet_c1` a4 继续稳定训练。本对话的 active goal 仍是持续监视该 Job、在既定范围内修复可恢复故障，并在训练正常完成后验收。

- Job：`346737`，`RUNNING`，节点 `hnode02`，单张 H100，16 CPU，`after_lock_346737` 保留。
- W&B：`hqumqkex`；2026-08-21 06:16 为 `trainer/global_step=19,277`、`epoch=0`。
- 05:59 validation：配体体素 PRAUC `0.569573`，受体 PRAUC `0.631303`，蛋白主链和核酸主链宏平均 PRAUC 分别为 `0.166307` 和 `0.221338`。
- 当前最高 TOP：`TOP_epoch_00_score_0.5696.ckpt`；同轮 `last.ckpt` 已发布。
- `BEST.ckpt` 已刷新为此前最高分 0.5594，并与 `TOP_epoch_00_score_0.5594.ckpt` 的 SHA-256 和逐字节比较一致；0.5696 等待下一次 validation 刷新别名。
- H100 快照利用率 93%，显存 80,708/81,559 MiB；35 个训练相关 Python 进程存活。训练与验证损失有限，a4 错误切片为空。

## Completed

- 确认 0.569573 高于此前最高分 0.559395，绝对提升约 `0.010178`，明显超过调度器绝对改善阈值 `0.003`。
- 确认上一轮暂时落后的 `BEST.ckpt` 已按冻结回调语义正常刷新为 0.5594。
- 新 TOP 和 `last.ckpt` 文件大小均约 500 MB，时间戳与 W&B validation 一致。
- 新指标与 checkpoint 状态已经写入 `文档/exec_plan/Stage1第三版训练启动与监视记录.md` 与计划执行映射。

## Decisions

- 当前可用于表示最佳模型的是 `TOP_epoch_00_score_0.5696.ckpt`；在下一次 validation 刷新前，不把暂时落后的 `BEST.ckpt` 当作当前最高分证据。
- 下一次 validation 后复核 `BEST.ckpt` 是否刷新为 0.5696；训练最终结束时再次核对最高 TOP、最终别名和 `last.ckpt`。
- 继续使用 60 分钟命令自然睡眠，不创建 heartbeat。
- batch 8 继续保持；只有出现明确 CUDA OOM 才降为 6。
- 不删除 `after_lock_346737`，不使用 `scancel`，不操作其他 Job。

## Next Actions

1. 执行 60 分钟命令自然睡眠，期间不发送消息。
2. 醒来后检查 Job 状态、H100、W&B summary、checkpoint、a4 错误切片与锁。
3. 下一次 validation 后核对 `BEST.ckpt` 与 `TOP_epoch_00_score_0.5696.ckpt` 是否一致，并观察学习率状态。
4. 训练正常结束后完成 Slurm、W&B、checkpoint、错误日志和结束原因的联合验收，再收口 goal。

## Files To Reopen

- `C:\Users\15919\Desktop\AdaLigand\文档\exec_plan\Stage1第三版训练启动与监视记录.md`
- `C:\Users\15919\Desktop\AdaLigand\CLAUDE\memory\handoffs\2026-08-21-stage1-v3-unet-c1新TOP05696.md`
- `C:\Users\15919\Desktop\Pocket_Plus\src\train.py`
- `C:\Users\15919\Desktop\Pocket_Plus\训练与运行\sh\unet_c1.sh`
