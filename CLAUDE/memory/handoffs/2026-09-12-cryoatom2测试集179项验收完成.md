# Handoff: CryoAtom2 测试集 179 项验收完成

Date: 2026-09-12

## Current State

AdaLigand 的 held_out_06_chain/test_0 全部 179 个 PDB 已完成 CryoAtom2 原生结构预测及最终验收：成功 179、失败 0、运行中 0、未开始 0。179 份默认最终 CIF 和 179 份 raw CIF 均已独立重新解析通过，原子非空、坐标有限。本结论说明文件与数值有效，不表示预测结构达到特定精度；Find_1 下游特征、模拟密度和性能对照尚未开展。

## Completed

测试清单为 `/storage/penghongen/AdaLigand/held_out/split/held_out_06_chain/test_0.json`。每个 PDB 的图对应关系来自 `/storage/penghongen/AdaLigand/Ori_Data/raw/pair_list.jsonl`，图来自同目录 `emdb_maps/emd_<编号>.map.gz`。完整序列来自 `/storage/penghongen/AdaLigand/held_out/sequence_catalog.jsonl`。最终核对保留 385 个蛋白质、52 个 RNA、60 个 DNA entity，包括 3/4/7 条短序列；FASTA 与源序列完全一致，未知字符按官方读取规则记录。输入图仅解压，未预先重采样，没有输入真实受体或配体坐标，没有修改官方科学参数。

结果根为 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/test_0_chain06`：

- `cryoatom2_artifact/<pdb>/<run_stamp>/`：实际结构产物。该次目录下 `<run_stamp>.cif` 为默认最终结构，`<run_stamp>_raw.cif` 为 raw 结构；根下恰好 179 个 PDB、358 份 CIF，没有残留日志或失败中间产物。
- `运行日志与统计/pdb/<pdb>/<run_stamp>/`：来源 inputs.json、实际序列 FASTA、命令参数 command.json、stdout/stderr 和本次 status.json。对应 PDB 下 latest.json 与本次状态一致，全部退出码 0、无 error，stderr 全空。
- `运行日志与统计/summary.json`：最终全清单状态及两种 CIF 路径；最后一次独立 summarize 返回 179 成功，其余三种状态均为 0。
- `运行日志与统计/最终来源与覆盖验收.json`：179 项来源、完整序列、命令、状态、临时图清理、实际产物目录和三份运行身份的核查报告；含源清单摘要、生产脚本摘要、逐 PDB 耗时与运行内结构规模。
- `运行日志与统计/任务日志.md`：本地执行记录的同步副本，保存实际命令、来源、运行历史、验收与资源释放证据。

三项使用同一冻结副本 `运行日志与统计/slurm/releases/AdaLigand_9a152cd14d18/AdaLigand`，内容 SHA256 为 `9a152cd14d1810bcc6c8aa1c12f4b933ef92ac6d2bf08d959597fed51b0782ad`。服务器 release manifest 的 Git 字段为空；对应本地代码由三个生产脚本的 LF 统一后摘要逐项验证，不把空字段解释为已经记录远端 Git 提交。

| 分片 | 实际 Job | 运行名 | 已完成 PDB |
| --- | --- | --- | --- |
| 374480_0 | 377521 | predict_job377521_20260911T011918_a1 | 60 |
| 374480_1 | 377793 | predict_job377793_20260911T011942_a1 | 60 |
| 374480_2 | 374480 | predict_job374480_20260911T030130_a1 | 59 |

各自 launch 位于 `运行日志与统计/slurm/launches/<实际Job>/<运行名>/launch.json`，来源清单位于 `运行日志与统计/runs/<运行名>.json`。全部分片、Job、release、单卡 A100/8 CPU 身份与产物对应；96G 内存、无时间限制。独立门控 374469 已通过并退出 0:0，不计入正式 179 项。

## Decisions

正式提交命令只执行过一次，工作目录为 `/home/penghongen/My_Project/AdaLigand`：

```bash
bash 测评数据代码/cryoatom2原始预测结构/submit.sh
```

测试、门控、统计与锁命令分别保存在执行记录，不能拼入正式入口。不要因恢复上下文再次执行正式提交。

用户最后资源指令优先于较早的“全部保留”要求：只释放分片 2，分片 0、1 交其他 AI agent 接管。2026-09-12 01:18:11 在确认 Job374480 的预测进程退出、动态命令与锁归属及最终统计通过后，仅删除其 `运行日志与统计/slurm/allocations/374480/after_lock_374480`。Slurm 确认 01:18:20 COMPLETED 0:0，runner 清理本 Job 活动锁与动态命令。377521、377793 的原预测进程此前均已核实退出；本任务未释放、也不再查询这两个已交接作业的当前状态。

Git 实现端点 `495a4de2cc0a3c528c5a397d3d25d38bdc49c35b` 与学习端点 `cfc9ef56d5e4900baec1b36459ddf49740f41cb9` 的完整树仍完全一致，为 `7df8644353b68f3b99f91d33fa7fbe69a2abef4a`。其他任务已经推进 Learn/CUMULATIVE 至 `9f95ede38dd59783f871837c79dd8e42d496a680`，它仍为全部可达提交按提交者时间的唯一最新。本任务生产代码和测试保持不变，README 已补充 pre_hold 说明；两遍自查、两轮独立审查及两端测试证据保持有效。最终文字收口留在累计分支工作区，不触碰其他 agent 的可视化修改。

## Next Actions

本轮结构生成与验收无剩余工作。后续若开展 Find_1 受体替换实验，应按 PDB 使用上述最终 CIF，并另行确定受体特征及模拟密度流程；不能把本次结构文件验收当作模型性能对照结果。

## Files To Reopen

- [执行记录](../../../文档/exec_plan/CryoAtom2测试集受体重建执行记录.md)
- [规划文档](../../../文档/规划文档/CryoAtom2测试集受体重建.md)
- [产物与代码契约](../../../测评数据代码/cryoatom2原始预测结构/README.md)
- [计划执行映射](../../../文档/mapping/计划执行映射.md)
- 服务器结果根下 `运行日志与统计/summary.json` 与 `最终来源与覆盖验收.json`。
