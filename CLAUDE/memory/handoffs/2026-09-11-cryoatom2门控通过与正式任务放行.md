# Handoff: CryoAtom2 门控通过与正式任务放行

Date: 2026-09-11

## Current State

03:02 核查：独立 GPU 门控 374469 已在 gnode08 完成，退出码 0:0，耗时 38 秒。三个正式数组元素均已核实资源、动态命令和锁归属并放行，实际并行运行。03:00 的统计快照中 6 项成功结构已重新解析通过，2 项运行中、171 项尚未开始、0 失败；第三项随后开始处理 9lfa，因此该快照需要在后续更新。目标保持 active，全集验收尚未完成。

| 数组元素 | 实际 Job ID | 节点 | 分片样本数 | pre_lock 放行时间 |
| --- | --- | --- | --- | --- |
| 374480_0 | 377521 | gnode08，GPU IDX:5 | 60 | 2026-09-11 01:19:12 |
| 374480_1 | 377793 | gnode05，GPU IDX:5 | 60 | 2026-09-11 01:20:55 |
| 374480_2 | 374480 | gnode07，GPU IDX:4 | 59 | 2026-09-11 02:59:25 |

每项资源为 1 张 A100、8 核 CPU、96G 内存、无时间限制，保留 after_hold。三个数组元素彼此无依赖，可按资源到达时间并行运行。

## Completed

三项共用冻结副本 `运行日志与统计/slurm/releases/AdaLigand_9a152cd14d18/AdaLigand`。实际运行名依次为 `predict_job377521_20260911T011918_a1`、`predict_job377793_20260911T011942_a1`、`predict_job374480_20260911T030130_a1`；launch.json 位于 `slurm/launches/<实际JobID>/<运行名>/`，runs 来源清单位于 `运行日志与统计/runs/<运行名>.json`。完整路径及内容哈希已写执行记录。服务器 release 的 Git 字段为空，本地 Git 对应关系以执行记录和预测文件一致性证据为准。

计算节点与 master 的时钟存在数分钟差异，运行名和机器时间戳保留原值，不能据不同节点的绝对时间戳推断锁操作顺序或计算等待时长。

门控日志包含 RUNet、CryoNet、CryoNet_no_seq 的权重读取成功记录，以及 ESM2、RNA-FM 的前向成功记录和 RUNTIME_GATE_PASSED。日志位于结果根的 `运行日志与统计/门控/374469.out`、`374469.err`，不计入 179 个正式结果。

三项的动态命令均执行 `${TASK_PROJECT_ROOT}/测评数据代码/cryoatom2原始预测结构/sh/predict.sh`；TASK_PROJECT_ROOT 指向实际执行前生成的冻结副本。服务器 predict.py 与本地已审查版本统一 LF 后一致。代码未修改；实现端点 495a4de2cc0a3c528c5a397d3d25d38bdc49c35b 与学习/累计端点 cfc9ef56d5e4900baec1b36459ddf49740f41cb9 的既有完整树等价证据见执行记录。

## Decisions

测试清单为 `/storage/penghongen/AdaLigand/held_out/split/held_out_06_chain/test_0.json`，共 179 个不同 PDB。原始图对应关系与图文件分别位于 `/storage/penghongen/AdaLigand/Ori_Data/raw/pair_list.jsonl` 和同目录的 `emdb_maps`；完整蛋白质、RNA、DNA 序列来自 `/storage/penghongen/AdaLigand/held_out/sequence_catalog.jsonl`。仅解压原始图，不预先重采样，不输入真实受体或配体坐标，不改变官方科学参数。

结果根为 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/test_0_chain06`。实际逐 PDB 产物保存于 `cryoatom2_artifact`，其余输入、来源、日志、统计、控制记录和冻结副本统一保存于 `运行日志与统计`。正式结构同时保留最终 CIF 与 raw CIF；失败保留证据及分母身份。

已执行过一次的正式提交命令如下，工作目录为 `/home/penghongen/My_Project/AdaLigand`，返回数组 374480，禁止为恢复观察而重复提交：

```bash
bash 测评数据代码/cryoatom2原始预测结构/submit.sh
```

门控命令、只读检查和 pre_lock 操作均单列在执行记录，不属于上述正式提交入口。三项删除的是 `运行日志与统计/slurm/allocations/pre_lock_<实际JobID>`；after_lock 位于其下的 `<实际JobID>/` 子目录，继续保留。

## Next Actions

1. 继续检查 377521、377793、374480 的逐 PDB 日志、状态和实际进程。首次 PDB 分别为 9ter、30yu、9lfa；三项输入与启动记录已核实，不需要重复放行或提交。
2. `运行日志与统计/summary.json` 是运行中快照。03:00 已复核成功 9ter、9k9g、9q33、23xk、9r7l、9wfu；继续核实其余样本，不能把正常步骤进度或文件存在当作最终结构通过验收。
3. 稳定运行或排队后，每轮静默等待 60 或 90 分钟，由多次 Start-Sleep -Seconds 300 组成。等待期间不发消息、不设置 heartbeat，观察超时后继续核查同一存活会话，不能据此重提作业。
4. 全部 179 项结束后，复核最终和 raw CIF 可解析、原子非空、坐标有限，核对完整覆盖、失败原因及产物与日志分离，再完成统计和最终收口。Find_1 特征、模拟密度和性能对照属于下一任务。

## Files To Reopen

- `文档/exec_plan/CryoAtom2测试集受体重建执行记录.md`，服务器副本为结果根的 `运行日志与统计/任务日志.md`。
- `测评数据代码/cryoatom2原始预测结构/README.md`、`predict.py` 和 `sh/predict.sh`。
- `训练与运行/README.md`，用于核对 allocation、release、launch 和锁协议。
- 结果根的 `运行日志与统计/slurm/allocations/377521/`、`377793/`、`374480/`，以及 `slurm/launches/`、`runs/`、`pdb/` 和 `summary.json`。
