# Handoff: CryoAtom2 三个单卡正式任务已提交

Date: 2026-09-08

## Current State

用户追加授权立即将正式任务入队，同时启用 pre_hold 与 after_hold。正式数组 `374480` 已提交，含 `374480_0`、`374480_1`、`374480_2` 三个独立任务；每任务 1 张 A100、8 核 CPU、96 GiB 内存，无时间限制，分片分别含 60、60、59 个 PDB。各分片没有相互依赖。获得资源后先等待 pre_lock 放行，尚未产生正式预测结果。

独立门控 `374469` 保留；它只申请 1 张 A100、8 核 CPU、96 GiB 内存、20 分钟，结束即释放。正式数组没有门控作业依赖，因此可提前排队。目标已恢复 active；此前“资源阻塞”handoff 的未提交状态是历史状态。

## Completed

本地代码 `测评数据代码/cryoatom2原始预测结构/` 已上传到服务器 `/home/penghongen/My_Project/AdaLigand/` 的同名目录。两遍主代理自查、两轮三方向独立审查与首版双线 Git 验收通过。Python 7 项契约测试通过；新增 pre_hold 后的无卡提交参数检查再次通过。门控和真实整图推理均尚未验收。

首版实现端点 `54201da` 与学习/累计端点 `34a6481` 文件树完全一致。此后按用户追加指令新增的 pre_hold 开关、注释及运行记录暂按小型修改例外留在累计工作区；不要把历史端点验收说成已覆盖这次工作区差异。

## Decisions

输入清单是 `/storage/penghongen/AdaLigand/held_out/split/held_out_06_chain/test_0.json`，原始图来自 `/storage/penghongen/AdaLigand/Ori_Data/raw/emdb_maps`，对应关系为同 raw 目录的 pair_list.jsonl；完整聚合物序列来自 `/storage/penghongen/AdaLigand/held_out/sequence_catalog.jsonl`。仅解压原始图，保留完整蛋白质/RNA/DNA 序列，不读取真实受体或配体坐标。

结果根是 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/test_0_chain06`。逐 PDB 实际产物放 `cryoatom2_artifact`；输入、来源、日志、统计、Slurm 控制和冻结副本统一放 `运行日志与统计`。主结果为官方默认最终 CIF，同时保留 raw CIF；179 项均须有成功或明确失败记录。

## 正式运行命令

已在服务器 `/home/penghongen/My_Project/AdaLigand` 执行一次，返回 `374480`：

```bash
bash 测评数据代码/cryoatom2原始预测结构/submit.sh
```

该入口不调用测试或门控；门控命令仅记录于任务日志的“测试与门控命令”。

## Next Actions

1. 独立查询门控 374469 与正式数组 374480_0/1/2。确认门控退出码及 `运行日志与统计/门控/374469.out` 中的 RUNTIME_GATE_PASSED，再核实正式 allocation 的 pre_lock、动态命令、资源身份及开始条件。
2. 稳定排队或正常计算时，每轮等待 60 分钟，由 12 次 300 秒睡眠组成；睡眠期间不发消息、不设置 heartbeat，并保持 5 分钟内能接收用户消息。不要因稳定排队结束目标或重复生成 handoff。
3. 正式启动后记录每个 Job 的 release、launch 和实际运行名。等待全部 179 项结束后复核最终及 raw CIF，输出完整覆盖及失败统计；按用户要求保留 after_hold 资源。最终完成时更新日志及 handoff。

## Files To Reopen

- `文档/exec_plan/CryoAtom2测试集受体重建执行记录.md`；其服务器副本在结果根的 `运行日志与统计/任务日志.md`。
- `测评数据代码/cryoatom2原始预测结构/README.md`。
- `训练与运行/README.md`，用于核实锁协议。
