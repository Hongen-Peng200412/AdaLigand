# Handoff: Find 辅助监督端到端实施启动

Date: 2026-07-23

## Current State

用户已经批准端到端实施蛋白主链、核酸主链和配体距离三项辅助监督。距离生产代码、Pocket_Plus 新版训练代码、完整代码快照和推理适配已完成本地与 Linux 测试、真实 `10ad` Dataset 验证和独立审计。正式距离生产已经收口为 22,386 条状态：22,369 success、12 skipped、5 unknown_failed；5 个缺少实验密度和 11 个 90 秒补验超时 PDB 组成冻结的 16 项训练排除清单。历史 Job `321107`、`321540`、`321743` 的快照已按各自运行时源码只增补缺失文件并通过真实 checkpoint 严格恢复。

Job `321540` 与 `321107` 已在保留 Slurm allocation、GPU 和 `after_lock` 的条件下停止旧训练。Job `321107` 的新版 `unet_c1` 与 Job `321540` 的新版 Find_1 都已完成各自 smoke，并进入正式在线 W&B 训练。`unet_c1` 保留原 U-Net 骨干，只对齐最终体素特征、五个输出头、五项损失和 16 项排除清单。Find_1 使用精确发布目录 `Pocket_Plus_1909267`；该提交恢复 CPU PRAUC 的 Gloo 聚合后禁止 Lightning/NCCL 二次同步，并按用户最新决定把 Find_1 CPC1、CPC2 最高学习率改为 `5e-5`。两个新运行目录都使用唯一 `POCKET_RUN_STAMP`。

独立只读端到端审计没有发现需要中止训练的科学契约漂移、无效数据、训练计算错误或目录身份冲突。Find_1 当前显存余量很小：smoke 峰值约 80.98/81.56 GiB，正式 CPC1 在 14:28 的采样约 80.78/80.84 GiB；后续若出现显存不足，先把 density-cube 的 `hidden_channels` 从 64 降为 48。

15:37 前连续 3 次健康的首轮正式训练已被用户叫停，因为 W&B 曲线暴露两者错误继承 `warmup_ratio=0.025`，而此前统一决定的是 `0.005`。18:16 已分别用 Job `321540` 与 `321107` 的 `kill_lock` 停止错误运行，保留 allocation 和 `after_lock`，不执行新测试或 smoke，直接启动 `warmup_ratio=0.005` 的正式 Find_1 CPC1 与 `unet_c1`。新 resolved 配置确认五项损失权重、学习率、网络和数据设置未变。当前 heartbeat 重新设为每 30 分钟；新运行连续 5 次健康后再改为每 5 小时。

18:56 完成 warmup 纠正后的第 1 次健康检查：Find_1 CPC1 为 `global_step=23`，`unet_c1` 为 `global_step=41`；两条学习率曲线、五项损失、在线 W&B、错误扫描、锁和显存均正常，尚未生成 checkpoint。只读 subagent 已把三个 Job 的差异与 sbatch→run_cmd→`src/train.py`→Hydra 配置→resolved 配置链路写入 `C:\Users\15919\Desktop\AdaLigand\talk\检查记录.md`，未修改或打断运行。

19:24 完成第 2 次健康检查：Find_1 CPC1 为 `global_step=68`，`unet_c1` 为 `global_step=113`；学习率曲线、五项损失、在线 W&B、错误扫描、锁和显存继续正常。首次正式验证指标和 checkpoint 尚未出现。

19:54 完成第 3 次健康检查：Find_1 CPC1 为 `global_step=119`，`unet_c1` 为 `global_step=191`；学习率曲线、五项损失、在线 W&B、错误扫描、锁和显存继续正常。首次正式验证指标和 checkpoint 尚未出现。

20:24 完成第 4 次健康检查：Find_1 CPC1 为 `global_step=164`，`unet_c1` 为 `global_step=275`；学习率曲线、五项损失、在线 W&B、错误扫描、锁和显存继续正常。首次正式验证指标和 checkpoint 尚未出现。

20:54 完成第 5 次健康检查：Find_1 CPC1 为 `global_step=215`，`unet_c1` 为 `global_step=347`；学习率曲线、五项损失、在线 W&B、错误扫描、锁和显存继续正常。首次正式验证指标和 checkpoint 尚未出现；heartbeat 已改为每 5 小时。

用户随后要求每次 heartbeat 同时检查 Job `321107`、`321540`、`321743`。Job `321743` 是旧版 Find_0，两张 H200，运行时代码、resolved 配置和运行目录均与新版辅助监督任务独立；必须按它自身的损失和阶段契约检查，不要求出现三项新增辅助损失。三项作业的入口链路见 `C:\Users\15919\Desktop\AdaLigand\talk\检查记录.md`。

2026-07-24 02:01 完成第一次三作业联合检查。新版 Find_1 为 `global_step=698`，新版 `unet_c1` 为 `global_step=1112`，两者仍未进入首次正式验证或保存 checkpoint；学习率、五项损失、在线 W&B、日志、锁和显存均正常。旧版 Find_0 为 `global_step=5483`，正式验证结果有限，已有 BEST、last 和 TOP checkpoints，W&B 在线；两张 H200 约占 142.1/142.8 GiB（各总计约 143.8 GiB），余量较小但未发生显存不足。

2026-07-24 07:04 完成第二次三作业联合检查。三个作业均为 `RUNNING`，各自只有 `after_lock`，错误扫描为空。新版 Find_1 为 `global_step=1241`，五项训练损失有限，Gaussian `sigma=0.7`、`warmup_ratio=0.005`、最高学习率 `5e-5` 和全局批量 48 保持不变；尚未进入首次正式验证或保存 checkpoint。新版 `unet_c1` 为 `global_step=1325`，首次正式验证总损失 `0.273812`，蛋白主链与核酸主链宏平均 PR-AUC 分别为 `0.0381163` 和 `0.00681047`，已保存 TOP 与 last checkpoints。旧版 Find_0 为 `global_step=6233`，既有验证指标与 checkpoints 正常；两张 H200 约占 142.4/142.6 GiB，余量仍小但未发生显存不足。

2026-07-24 12:01 完成第三次三作业联合检查。三个作业仍为 `RUNNING`，Slurm batch step、W&B 步数和 GPU 采样持续推进，各自只有 `after_lock`，错误扫描为空。新版 Find_1 为 `global_step=1430`，首次正式验证总损失 `0.423916`，蛋白主链与核酸主链宏平均 PR-AUC 分别为 `0.111973` 和 `0.00329660`，已保存 TOP 与 last checkpoints。新版 `unet_c1` 为 `global_step=2180`，首次验证与 checkpoints 继续有效。旧版 Find_0 为 `global_step=6773`，最新验证总损失 `0.330061`，受体与配体区域 PR-AUC 分别为 `0.660388` 和 `0.606173`，已更新 TOP 与 last checkpoints；两张 H200 约占 141.7/141.4 GiB。两个 Find 尚未进入 CPC2。

2026-07-24 17:03 完成第四次三作业联合检查。新版 Find_1 为 `global_step=1973`，新版 `unet_c1` 为 `global_step=2630`，旧版 Find_0 为 `global_step=7529`；三者均为 `RUNNING`，配置、唯一运行目录、`after_lock`、运行目录错误扫描和 GPU 采样正常，两个 Find 尚未进入 CPC2。`unet_c1` 的 `wandb-summary.json` 自 14:43 暂未刷新，但 W&B 二进制事件文件、内部日志和 GPU 采样持续更新到 17:03，GPU 利用率为 84%–100%，没有进程停滞证据。allocation 总日志中的 Hydra traceback 属于已经退出的旧启动尝试，不属于当前唯一运行目录。Find_0 两张 H200 最新约占 142.7/142.5 GiB。

2026-07-24 22:04 完成第五次三作业联合检查。新版 Find_1 为 `global_step=2528`，新版 `unet_c1` 为 `global_step=3236`，旧版 Find_0 为 `global_step=8084`；三者均为 `RUNNING`，各自配置、唯一运行目录、`after_lock`、GPU 采样和 W&B 事件流正常，两个 Find 尚未进入 CPC2。`unet_c1` 第二次验证总损失从 `0.273812` 降至 `0.241217`，受体、配体区域和蛋白主链 PR-AUC 提高，核酸主链宏平均 PR-AUC 降至 `0.00279204`，并保存新的 TOP 与 last checkpoints。Find_1 日志中的 traceback 是 7 月 23 日 W&B GraphQL 限流后的自动重试，当前摘要持续刷新，不是训练错误。Find_0 两张 H200 最高约占 142.8/143.0 GiB，余量很小但未发生显存不足。

2026-07-25 16:27 完成第六次三作业联合检查。三个作业均为 `RUNNING`，精确发布身份、最终配置、唯一运行目录和各自唯一的 `after_lock` 正确，当前运行目录错误扫描为空，W&B 事件流与 GPU 采样持续更新。新版 Find_1 到达 `global_step=3971`，最新验证总损失降至 `0.343283`，蛋白与核酸主链宏平均 PR-AUC 分别升至 `0.976166` 和 `0.947399`，已保存新的 TOP 与 last checkpoints。新版 `unet_c1` 到达 `global_step=5258`，第三次验证总损失降至 `0.231801`，蛋白与核酸主链宏平均 PR-AUC 分别为 `0.0619655` 和 `0.0283426`，已保存新的 TOP 与 last checkpoints；摘要文件在 13:09 后暂未刷新，但 W&B 事件文件和 GPU 计算持续推进到 16:28。旧版 Find_0 到达 `global_step=10931`，最新验证总损失降至 `0.317618`，受体与配体区域 PR-AUC 分别为 `0.686995` 和 `0.615032`。两张 H200 此次检查期间最高占用 143.070/143.771 GiB，余量很小但未发生显存不足；两个 Find 尚未进入 CPC2。

2026-07-25 21:29 完成第七次三作业联合检查。三个作业均为 `RUNNING`，精确发布、最终配置、唯一运行目录、W&B、GPU 和各自唯一的 `after_lock` 正常，两个 Find 尚未进入 CPC2。新版 Find_1 到达 `global_step=4628`，五项训练损失有限；新版 `unet_c1` 到达 `global_step=6197`，第四次验证总损失降至 `0.221887`，受体、配体区域、蛋白主链和核酸主链 PR-AUC 分别升至 `0.535934`、`0.504227`、`0.0797290` 和 `0.0845545`。旧版 Find_0 到达 `global_step=11843`，既有验证与 checkpoints 继续有效。H200 最高占用仍为 143.070/143.771 GiB，未发生显存不足。`unet_c1` 的 `BEST.ckpt` 当前仍对应上一轮 `0.2318` TOP checkpoint；精确发布代码会在每次验证和 `on_train_end` 刷新别名，当前新的 `0.2219` TOP 文件已完整保存，因此不阻断训练，但 CPC1 结束后必须核对最终别名与 `best_model_path` 一致。

2026-07-26 10:07 完成第八次三作业联合检查。三个作业均为 `RUNNING`，发布目录、训练参数、最终配置、W&B、GPU、唯一运行目录和各自唯一的 `after_lock` 正常，当前应用日志错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=5864`，最新验证总损失 `0.334012`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.651325`、`0.599134`、`0.987488` 和 `0.975704`。新版 `unet_c1` 到达 `global_step=7889`，最新验证总损失 `0.219721`，对应四项 PR-AUC 为 `0.544096`、`0.507334`、`0.0870110` 和 `0.116536`。旧版 Find_0 到达 `global_step=14066`，最新验证总损失 `0.316924`，受体与配体区域 PR-AUC 为 `0.693115` 和 `0.623365`。三项训练均保存新的 TOP 与 last checkpoints；训练中的 `BEST.ckpt` 均落后最新 TOP 一轮，因此两个 Find 切换 CPC2 前和 `unet_c1` 结束后必须核对最终别名。新版 Find_1 两张 H100 最高占用 80.844/81.559 GiB，旧版 Find_0 两张 H200 最高占用 143.054/143.771 GiB，均未发生显存不足。

2026-07-26 15:16 完成第九次三作业联合检查。三个作业均为 `RUNNING`，发布目录、训练参数、W&B、GPU、唯一运行目录和各自唯一的 `after_lock` 正常，当前应用日志错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=6500`，五项训练损失有限，最近验证结果继续有效。新版 `unet_c1` 到达 `global_step=8051`，最新验证总损失降至 `0.216680`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.552024`、`0.522347`、`0.0970976` 和 `0.143969`。旧版 Find_0 到达 `global_step=14678`，最新验证总损失降至 `0.310742`，受体与配体区域 PR-AUC 为 `0.699974` 和 `0.644994`。`unet_c1` 与 Find_0 均保存新的 TOP 与 last checkpoints；训练中的 `BEST.ckpt` 继续落后最新 TOP 一轮。新版 Find_1 两张 H100 最高占用 80.734/81.559 GiB，旧版 Find_0 两张 H200 最高占用 143.064/143.771 GiB，均未发生显存不足。

2026-07-26 20:17 完成第十次三作业联合检查。三个作业均为 `RUNNING`，发布目录、训练参数、W&B、GPU、唯一运行目录和各自唯一的 `after_lock` 正常，当前应用日志错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=6725`，最新验证总损失降至 `0.331998`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.654372`、`0.592297`、`0.991848` 和 `0.988387`，并保存新的 TOP 与 last checkpoints。新版 `unet_c1` 到达 `global_step=8828`，旧版 Find_0 到达 `global_step=15389`，两者最近一次验证与 checkpoints 继续有效。训练中的 `BEST.ckpt` 继续落后最新 TOP 一轮。新版 Find_1 两张 H100 最高占用 80.864/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.998/143.771 GiB，均未发生显存不足。

2026-07-27 01:17 完成第十一次三作业联合检查。三个作业均为 `RUNNING`，发布目录、训练参数、W&B、GPU、唯一运行目录和各自唯一的 `after_lock` 正常，当前应用日志错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=7304`，新版 `unet_c1` 到达 `global_step=9206`，五项训练损失均为有限值，最近一次验证与 checkpoints 继续有效。旧版 Find_0 到达 `global_step=15926`，最新验证总损失降至 `0.307528`，受体与配体区域 PR-AUC 升至 `0.703757` 和 `0.636299`，并保存新的 TOP 与 last checkpoints。训练中的 `BEST.ckpt` 继续落后最新 TOP 一轮。新版 Find_1 两张 H100 最高占用 80.626/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.306/143.771 GiB，均未发生显存不足。

2026-07-27 06:16 完成第十二次三作业联合检查。三个作业均为 `RUNNING`，发布目录、训练参数、W&B、GPU、唯一运行目录和各自唯一的 `after_lock` 正常，当前应用日志错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=7880`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。新版 `unet_c1` 到达 `global_step=9662`，最新验证总损失降至 `0.215768`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.550378`、`0.516296`、`0.109687` 和 `0.165010`，并保存新的 TOP 与 last checkpoints。旧版 Find_0 到达 `global_step=16733`，最近一次验证与 checkpoints 继续有效。训练中的 `BEST.ckpt` 继续落后最新 TOP 一轮。新版 Find_1 两张 H100 最高占用 80.682/81.559 GiB，旧版 Find_0 两张 H200 最高占用 143.014/143.771 GiB，均未发生显存不足。

2026-07-27 11:16 完成第十三次三作业联合检查。三个作业均为 `RUNNING`，发布目录、训练参数、W&B、GPU、唯一运行目录和各自唯一的 `after_lock` 正常，当前应用日志错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=8126`，最新验证总损失降至 `0.325117`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 升至 `0.682811`、`0.619510`、`0.993087` 和 `0.990706`，并保存新的 TOP 与 last checkpoints。新版 `unet_c1` 到达 `global_step=10520`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。旧版 Find_0 到达 `global_step=17354`，本次验证总损失 `0.311083` 未优于此前的 `0.307528`，但验证指标有限且新的 TOP、last checkpoints 完整保存。新版 Find_1 与 `unet_c1` 的 `BEST.ckpt` 仍分别落后当前最佳 TOP 一轮；旧版 Find_0 的 `BEST.ckpt` 已在本次验证开始时刷新为此前的 `0.3075` 最佳 TOP。新版 Find_1 两张 H100 最高占用 80.776/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.312/143.771 GiB，均未发生显存不足。

2026-07-27 16:17 完成第十四次三作业联合检查。三个作业均为 `RUNNING`，发布目录、训练参数、W&B、GPU、唯一运行目录和各自唯一的 `after_lock` 正常，当前应用日志错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=8726`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。新版 `unet_c1` 到达 `global_step=10604`，本次验证总损失 `0.216707` 未优于此前的 `0.215768`；受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.559758`、`0.512020`、`0.100211` 和 `0.167001`，新的 TOP 与 last checkpoints 完整保存。旧版 Find_0 到达 `global_step=18119`，最近一次验证与 checkpoints 继续有效。新版 `unet_c1` 与旧版 Find_0 的 `BEST.ckpt` 已分别指向当前最佳的 `0.2158` 与 `0.3075` TOP；新版 Find_1 的 `BEST.ckpt` 仍落后当前 `0.3251` 最佳 TOP 一次验证。新版 Find_1 两张 H100 最高占用 80.800/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.708/143.771 GiB，均未发生显存不足。

2026-07-27 21:17 完成第十五次三作业联合检查。三个作业均为 `RUNNING`，发布目录、最终配置、唯一运行目录、W&B、GPU 和各自唯一的 `after_lock` 正常，当前正式运行错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=9206`，新版 `unet_c1` 到达 `global_step=11432`，五项训练损失均有限，最近一次验证与 checkpoints 继续有效。旧版 Find_0 到达 `global_step=18728`，最新验证总损失降至 `0.302739`，受体区域与配体区域 PR-AUC 为 `0.712215` 和 `0.640414`，并保存新的 TOP 与 last checkpoints。旧版 Find_0 的 `BEST.ckpt` 暂时仍指向上一轮最佳 `0.3075`，新版 Find_1 的 `BEST.ckpt` 仍落后当前 `0.3251` 最佳 TOP；新版 `unet_c1` 的最佳别名继续正确。新版 Find_1 两张 H100 最高占用 80.854/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.280/143.771 GiB，均未发生显存不足。

2026-07-28 02:17 完成第十六次三作业联合检查。三个作业均为 `RUNNING`，发布目录、最终配置、唯一运行目录、W&B、GPU 和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=9575`，最新验证总损失降至 `0.317876`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.695172`、`0.638382`、`0.993398` 和 `0.993774`，并保存新的 TOP 与 last checkpoints。新版 `unet_c1` 最近一次摘要仍为 `global_step=11837`，但 W&B 事件文件、内部日志和 GPU 计算继续推进，五项训练损失有限。旧版 Find_0 到达 `global_step=19664`，最近验证与 checkpoints 继续有效。新版 Find_1 的 `BEST.ckpt` 已刷新为上一轮最佳 `0.3251`，但暂时落后新最佳 `0.3179`；旧版 Find_0 的 `BEST.ckpt` 继续落后当前 `0.3027` 最佳 TOP；新版 `unet_c1` 的最佳别名继续正确。新版 Find_1 两张 H100 最高占用 80.436/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.750/143.771 GiB，均未发生显存不足。

2026-07-28 07:20 完成第十七次三作业联合检查。三个作业均为 `RUNNING`，W&B、GPU、唯一运行目录和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=10178`，五项训练损失有限，最近验证与 checkpoints 继续有效。新版 `unet_c1` 到达 `global_step=12350`，最新验证总损失降至 `0.212835`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.579582`、`0.532469`、`0.119576` 和 `0.199941`，并保存新的 TOP 与 last checkpoints。旧版 Find_0 到达 `global_step=20270`，本次验证总损失 `0.305076` 未优于此前 `0.302739`；受体区域和配体区域 PR-AUC 为 `0.715987` 和 `0.658177`，新的 TOP 与 last checkpoints 完整保存。新版 `unet_c1` 的 `BEST.ckpt` 暂时落后新最佳 `0.2128`；旧版 Find_0 的 `BEST.ckpt` 已刷新到当前最佳 `0.3027`；新版 Find_1 的 `BEST.ckpt` 继续落后当前 `0.3179` 最佳 TOP。新版 Find_1 两张 H100 最高占用 80.752/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.336/143.771 GiB，均未发生显存不足。

2026-07-28 13:10 完成第十八次三作业联合检查。三个作业均为 `RUNNING`，W&B、GPU、唯一运行目录和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 最近摘要为 `global_step=10520`，五项训练损失有限，最近验证与 checkpoints 继续有效；摘要自 10:29 后暂未刷新，但 W&B 事件文件、内部日志和两张 H100 的计算持续推进到本次检查时刻，没有进程停滞证据。新版 `unet_c1` 到达 `global_step=13151`，旧版 Find_0 到达 `global_step=21053`，两者五项或既有训练损失有限，最近验证与 checkpoints 继续有效。新版 Find_1 两张 H100 最高占用 80.964/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.598/143.771 GiB，均未发生显存不足。三项 `BEST.ckpt` 状态没有变化。

2026-07-28 18:11 完成第十九次三作业联合检查。三个作业均为 `RUNNING`，W&B、GPU、唯一运行目录和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=11048`，最新验证总损失降至 `0.311816`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.699857`、`0.623672`、`0.995299` 和 `0.993932`。新版 `unet_c1` 到达 `global_step=13574`，最新验证总损失降至 `0.205013`，对应四项 PR-AUC 为 `0.597136`、`0.552964`、`0.122539` 和 `0.198476`。旧版 Find_0 到达 `global_step=21749`，最新验证总损失降至 `0.301744`，受体区域与配体区域 PR-AUC 为 `0.727060` 和 `0.644007`。三项训练均保存新的 TOP 与 last checkpoints；三项 `BEST.ckpt` 分别已刷新为上一轮最佳 `0.3179`、`0.2128` 和 `0.3027`，暂时落后新最佳一次验证。新版 Find_1 两张 H100 最高占用 80.214/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.866/143.771 GiB，均未发生显存不足。

2026-07-28 23:13 完成第二十次三作业联合检查。三个作业均为 `RUNNING`，训练进程、冻结发布目录、命令行参数、W&B 活动、唯一运行目录和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=11615`，新版 `unet_c1` 到达 `global_step=14465`，旧版 Find_0 到达 `global_step=22370`；三项训练损失有限，最近一次验证与 TOP、last checkpoints 继续有效，尚未产生下一次验证。三项 `BEST.ckpt` 仍分别对应 `0.3179`、`0.2128` 和 `0.3027`，暂时落后当前最佳 TOP 一次验证。新版 Find_1 两张 H100 最高占用 80.912/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.966/143.771 GiB，均未发生显存不足。

2026-07-29 04:12 完成第二十一次三作业联合检查。三个作业均为 `RUNNING`，训练进程、冻结发布目录、命令行参数、W&B 活动、唯一运行目录和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=11852`，本次验证总损失 `0.316934`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.682324`、`0.614820`、`0.996150` 和 `0.995899`，未优于当前最佳 `0.311816`。新版 `unet_c1` 到达 `global_step=14735`，本次验证总损失 `0.209346`，对应四项 PR-AUC 为 `0.588629`、`0.537688`、`0.119717` 和 `0.190546`，未优于当前最佳 `0.205013`。旧版 Find_0 到达 `global_step=23249`，最新验证总损失降至 `0.301204`，受体区域与配体区域 PR-AUC 为 `0.724694` 和 `0.671256`。三项训练均保存新的 TOP 与 last checkpoints；新版 Find_1 和 `unet_c1` 的 `BEST.ckpt` 已刷新为当前最佳，旧版 Find_0 的 `BEST.ckpt` 暂时落后新最佳一次验证。新版 Find_1 两张 H100 最高占用 80.666/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.910/143.771 GiB，均未发生显存不足。

2026-07-29 11:34 完成第二十二次三作业联合检查。三个作业均为 `RUNNING`，训练进程、冻结发布目录、命令行参数、W&B 活动、唯一运行目录和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=12752`，五项训练损失有限，最近一次验证与 checkpoints 继续有效。新版 `unet_c1` 最近一次摘要为 `global_step=15782`，五项训练损失有限；摘要自 09:43 后暂未刷新，但 W&B 事件文件、内部日志和 H100 计算持续推进到 11:34，没有进程停滞证据。旧版 Find_0 到达 `global_step=24242`，最新验证总损失降至 `0.299347`，受体区域与配体区域 PR-AUC 为 `0.722775` 和 `0.660897`。三项训练的 TOP 与 last checkpoints 完整；新版 Find_1 和 `unet_c1` 的 `BEST.ckpt` 继续正确，旧版 Find_0 的 `BEST.ckpt` 已刷新为上一轮最佳 `0.3012`，暂时落后新最佳一次验证。新版 Find_1 两张 H100 最高占用 80.594/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.830/143.771 GiB，均未发生显存不足。

2026-07-29 16:35 完成第二十三次三作业联合检查。三个作业均为 `RUNNING`，训练进程、冻结发布目录、命令行参数、W&B 活动、唯一运行目录和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 最近一次摘要为 `global_step=13151`，五项训练损失有限；摘要自 14:55 后暂未刷新，但 W&B 事件文件、内部日志和两张 H100 持续推进到 16:35，没有进程停滞证据。新版 `unet_c1` 到达 `global_step=16400`，本次验证总损失 `0.205393`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.593386`、`0.556990`、`0.134876` 和 `0.211881`，未优于当前最佳 `0.205013`。旧版 Find_0 到达 `global_step=25001`，训练损失有限，最近一次验证与 checkpoints 继续有效。三项 TOP 与 last checkpoints 完整；新版 Find_1 和 `unet_c1` 的 `BEST.ckpt` 继续正确，旧版 Find_0 的 `BEST.ckpt` 暂时落后当前最佳 `0.2993` 一次验证。新版 Find_1 两张 H100 最高占用 80.968/81.559 GiB，旧版 Find_0 两张 H200 最高占用 141.824/143.771 GiB，均未发生显存不足。

2026-07-29 21:36 完成第二十四次三作业联合检查。三个作业均为 `RUNNING`，训练进程、冻结发布目录、命令行参数、W&B 活动、唯一运行目录和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=13493`，最新验证总损失降至 `0.311039`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.705271`、`0.633604`、`0.995829` 和 `0.996088`。新版 `unet_c1` 最近一次摘要为 `global_step=17096`，五项训练损失有限，最近一次验证与 checkpoints 继续有效；W&B 事件文件、内部日志和 H100 计算持续推进到 21:36。旧版 Find_0 到达 `global_step=25724`，最新验证总损失降至 `0.297060`，受体区域与配体区域 PR-AUC 为 `0.726640` 和 `0.653690`。三项训练的 TOP 与 last checkpoints 完整；新版 Find_1 与旧版 Find_0 的 `BEST.ckpt` 分别暂时落后当前最佳 `0.3110` 和 `0.2971` 一次验证，新版 `unet_c1` 的最佳别名继续正确。新版 Find_1 两张 H100 最高占用 80.832/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.572/143.771 GiB，均未发生显存不足。

2026-07-30 09:04 完成第二十五次三作业联合检查。三个作业均为 `RUNNING`，训练进程、冻结发布目录、命令行参数、W&B 活动、唯一运行目录和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 最近一次摘要为 `global_step=14465`，五项训练损失有限；摘要自 06:47 后暂未刷新，但 W&B 事件文件、内部日志和两张 H100 持续推进到 09:04，没有进程停滞证据。新版 `unet_c1` 到达 `global_step=18413`，最新验证总损失降至 `0.202949`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.602145`、`0.551623`、`0.129276` 和 `0.216974`。旧版 Find_0 到达 `global_step=27569`，最新验证总损失降至 `0.294410`，受体区域与配体区域 PR-AUC 为 `0.726935` 和 `0.655561`。三项训练的 TOP 与 last checkpoints 完整；三项 `BEST.ckpt` 分别暂时落后当前最佳 `0.3110`、`0.2029` 和 `0.2944` 一次验证。新版 Find_1 两张 H100 最高占用 80.924/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.910/143.771 GiB，均未发生显存不足。

2026-07-30 14:04 完成第二十六次三作业联合检查。三个作业均为 `RUNNING`，训练进程、冻结发布目录、命令行参数、W&B 活动、唯一运行目录和各自唯一的 `after_lock` 正常，当前正式训练错误扫描为空；两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=14894`，最新验证总损失降至 `0.308903`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.713553`、`0.642585`、`0.996422` 和 `0.995750`。新版 `unet_c1` 到达 `global_step=19010`，本次验证总损失 `0.203519`，对应四项 PR-AUC 为 `0.603420`、`0.552125`、`0.144904` 和 `0.223982`，未优于当前最佳 `0.202949`。旧版 Find_0 到达 `global_step=28193`，最新验证总损失降至 `0.291351`，受体区域与配体区域 PR-AUC 为 `0.734282` 和 `0.661356`。三项训练的 TOP 与 last checkpoints 完整；新版 Find_1 与旧版 Find_0 的 `BEST.ckpt` 暂时落后当前最佳一次验证，新版 `unet_c1` 的 `BEST.ckpt` 已刷新为当前最佳。新版 Find_1 两张 H100 最高占用 80.912/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.812/143.771 GiB，均未发生显存不足。

2026-07-30 19:11 完成第二十七次三作业联合检查。三个作业均为 `RUNNING`，冻结发布提交、唯一运行标记、最终配置和各自唯一的 `after_lock` 保持不变；当前正式训练错误扫描为空，W&B 事件文件、内部日志和 GPU 计算持续更新，两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=15479`，新版 `unet_c1` 到达 `global_step=19727`，旧版 Find_0 到达 `global_step=28949`；三项当前训练损失均为有限值，最近验证和 checkpoints 继续有效，本次没有新验证。新版 Find_1 与旧版 Find_0 的 `BEST.ckpt` 分别仍对应 `0.3110` 和 `0.2944`，暂时落后当前最佳 `0.3089` 和 `0.2914`；新版 `unet_c1` 的 `BEST.ckpt` 正确对应当前最佳 `0.2029`。新版 Find_1 两张 H100 最高占用 80.858/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 143.064/143.771 GiB，均未发生显存不足。

2026-07-31 00:06 原定第二十八次三作业联合检查未完成。对服务器 `10.102.33.220:10022` 的两次 SSH 连接均超时，随后 TCP 端口探测也失败；00:39 和 01:09 的两次定时重试也都在建立 SSH 连接时超时。没有执行服务器写入、锁操作或作业控制命令，因而不能把此次连接失败解释为训练作业失败。最近一次可确认事实仍是 2026-07-30 19:11 的第二十七次健康检查。heartbeat 继续每 30 分钟重试；恢复连接并完成一次联合检查后，应恢复每 5 小时检查。

2026-07-31 01:40 服务器连接恢复并完成第二十八次三作业联合检查。三个作业均为 `RUNNING`，冻结发布提交、唯一运行标记、最终配置、W&B 活动、各自唯一的 `after_lock` 和当前正式训练错误扫描正常，两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=15977`，最新验证总损失降至 `0.303946`，受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.715185`、`0.646304`、`0.996413` 和 `0.996518`。新版 `unet_c1` 到达 `global_step=20525`，最新验证总损失降至 `0.201407`，对应四项 PR-AUC 为 `0.598554`、`0.551136`、`0.144395` 和 `0.230864`。旧版 Find_0 到达 `global_step=30218`，本次验证总损失 `0.294136`，未优于当前最佳 `0.291351`；受体区域与配体区域 PR-AUC 为 `0.734265` 和 `0.653966`。三项 TOP 与 last checkpoints 完整；新版 Find_1 与 `unet_c1` 的 `BEST.ckpt` 暂时落后新最佳一次验证，旧版 Find_0 的 `BEST.ckpt` 已刷新为当前最佳。新版 Find_1 两张 H100 最高占用 80.788/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.946/143.771 GiB，均未发生显存不足。heartbeat 已恢复为每 5 小时。

2026-07-31 10:51 原定第二十九次三作业联合检查再次遇到服务器连接中断。对 `10.102.33.220:10022` 的两次 SSH 连接均超时，随后 TCP 端口探测也返回不可达；没有执行服务器写入、锁操作或作业控制命令，不能据此判断训练作业状态。最近一次可确认事实仍是 01:40 的第二十八次健康检查。heartbeat 已临时改为每 30 分钟重试；恢复连接并完成联合检查后，应恢复每 5 小时检查。

2026-07-31 11:25 服务器连接恢复并完成第二十九次三作业联合检查。三个作业均为 `RUNNING`，冻结发布提交、唯一运行标记、最终配置、W&B 活动、各自唯一的 `after_lock` 和当前正式训练错误扫描正常，两个 Find 仍在 CPC1。新版 Find_1 最近摘要为 `global_step=17096`，五项训练损失有限；摘要暂缓刷新，但 W&B 事件文件、内部日志和两张 H100 持续推进到 11:25。新版 `unet_c1` 到达 `global_step=21869`，本次验证总损失 `0.201678`，未优于当前最佳 `0.201407`；受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.598142`、`0.565161`、`0.154531` 和 `0.255415`。旧版 Find_0 到达 `global_step=31877`，最新验证总损失降至 `0.284309`，受体区域与配体区域 PR-AUC 为 `0.740554` 和 `0.675477`。三项 TOP 与 last checkpoints 完整；新版 Find_1 的 `BEST.ckpt` 暂时落后当前最佳 `0.3039`，新版 `unet_c1` 的 `BEST.ckpt` 正确指向 `0.2014`，旧版 Find_0 的 `BEST.ckpt` 指向中间一次验证产生的 `0.2899` 最佳 TOP，暂时落后当前 `0.2843` 新最佳一次验证。新版 Find_1 两张 H100 最高占用 80.926/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.670/143.771 GiB，均未发生显存不足。heartbeat 已恢复为每 5 小时。

2026-07-31 16:35 完成第三十次三作业联合检查。三个作业均为 `RUNNING`，精确发布目录或固定运行代码、最终配置、W&B 活动、各自唯一的 `after_lock` 和当前正式训练错误扫描正常，两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=17681`，最新验证总损失降至 `0.299771`；受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.734120`、`0.656438`、`0.996792` 和 `0.996809`，已保存新的 `TOP_epoch_00_score_0.2998.ckpt` 与 `last.ckpt`。新版 `unet_c1` 最近摘要仍为 `global_step=22358`；摘要自 13:42 暂未刷新，但 W&B 内部日志与 H100 计算持续推进到 16:35，没有停滞证据。旧版 Find_0 到达 `global_step=32897`，最近验证总损失 `0.284309` 与 checkpoints 继续有效。新版 Find_1 与旧版 Find_0 的 `BEST.ckpt` 分别暂时落后当前最佳 `0.2998` 和 `0.2843` 一次验证；新版 `unet_c1` 的 `BEST.ckpt` 正确指向 `0.2014`。新版 Find_1 两张 H100 最高占用 80.966/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.974/143.771 GiB，均未发生显存不足。

2026-07-31 21:35 完成第三十一次三作业联合检查。三个作业均为 `RUNNING`，精确发布目录或固定运行代码、训练命令、最终配置、W&B 活动、各自唯一的 `after_lock` 和当前正式训练错误扫描正常，两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=18338`，五项训练损失有限，最近验证总损失 `0.299771` 与 checkpoints 继续有效。新版 `unet_c1` 到达 `global_step=23174`，最新验证总损失降至 `0.198961`；受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.600148`、`0.563620`、`0.156990` 和 `0.241991`，已保存新的 `TOP_epoch_00_score_0.1990.ckpt` 与 `last.ckpt`。旧版 Find_0 到达 `global_step=33779`，本次验证总损失 `0.287442`，未优于当前最佳 `0.284309`；受体区域与配体区域 PR-AUC 为 `0.733609` 和 `0.662756`，新的 TOP 与 last checkpoints 完整。新版 Find_1 与 `unet_c1` 的 `BEST.ckpt` 分别暂时落后当前最佳 `0.2998` 和 `0.1990` 一次验证，旧版 Find_0 的 `BEST.ckpt` 已正确刷新为当前最佳 `0.2843`。新版 Find_1 两张 H100 最高占用 80.750/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 143.052/143.771 GiB，均未发生显存不足。

2026-08-01 02:35 完成第三十二次三作业联合检查。三个作业均为 `RUNNING`，精确发布目录或固定运行代码、训练命令、最终配置、W&B 活动、各自唯一的 `after_lock` 和当前正式训练错误扫描正常，两个 Find 仍在 CPC1。新版 Find_1 到达 `global_step=18641`，本次验证总损失 `0.305648`，未优于当前最佳 `0.299771`；受体区域、配体区域、蛋白主链和核酸主链 PR-AUC 为 `0.720618`、`0.634045`、`0.996900` 和 `0.997245`，新的 TOP 与 last checkpoints 完整。新版 `unet_c1` 最近摘要为 `global_step=23672`，五项训练损失有限，最近验证与 checkpoints 继续有效；摘要自 23:45 暂未刷新，但 W&B 内部日志与 H100 计算持续推进到 02:35，没有停滞证据。旧版 Find_0 到达 `global_step=34724`，本次验证总损失 `0.290416`，未优于当前最佳 `0.284309`；受体区域与配体区域 PR-AUC 为 `0.734715` 和 `0.671311`，新的 TOP 与 last checkpoints 完整。新版 Find_1 与旧版 Find_0 的 `BEST.ckpt` 已正确对应当前最佳 `0.2998` 和 `0.2843`；新版 `unet_c1` 的 `BEST.ckpt` 暂时落后当前最佳 `0.1990` 一次验证。新版 Find_1 两张 H100 最高占用 80.928/81.559 GiB，新版 `unet_c1` 的 H100 占用 65.080/81.559 GiB，旧版 Find_0 两张 H200 最高占用 142.884/143.771 GiB，均未发生显存不足。

2026-08-01 11:20 完成第三十三次联合检查并确认真实训练故障。新版 Find_1 与 `unet_c1` 仍健康，分别到达 `global_step=19727` 和 `24989`；前者当前最佳验证总损失仍为 `0.299771`，后者本次验证总损失为 `0.199638`，未优于当前最佳 `0.198961`。旧版 Find_0 的 Slurm Job `321743` 仍占用两张 H200，但训练进程已经退出，GPU 空闲；`try_lock_321743`、`last_exit_code=137` 和正式启动的 `exit_code=137` 均已落盘。正式错误日志在退出前给出 `MisconfigurationException: You can't self.log() inside on_validation_end`：`src/train.py` 的学习率下降计数回调从 `on_validation_end` 调用 `pl_module.log()`，第一次真实学习率下降才进入该分支。旧版 Find_0 运行副本、新版 Find_1 发布 `1909267` 与新版 `unet_c1` 发布 `9417bcf` 的相关代码相同，因此两个仍在运行的新版训练也有确定性风险。此次没有修改服务器文件、删除锁、终止进程、覆盖运行目录或释放 allocation。旧版 Find_0 最近可用 `last.ckpt` 生成于 2026-08-01 00:41；当前 `init_from` 只加载模型参数，不能恢复优化器、学习率调度器和回调状态，因此不能直接用于故障续训。

2026-08-01 14:25 完成 Find_0 CPC2 启动后的第一次短周期联合检查。三个 Slurm Job 均为 `RUNNING`，各自只保留唯一 `after_lock`。新版 Find_1 到达 `global_step=20009`，最新验证总损失 `0.300548`、配体区域 PR-AUC `0.641525`，五项训练损失有限，两张 H100 继续计算。新版 `unet_c1` 到达 `global_step=25409`，最新验证总损失 `0.199726`、配体区域 PR-AUC `0.568633`，五项训练损失有限，H100 利用率为 100%。Find_0 CPC2 已从冻结运行代码和选定 checkpoint 真正进入训练，到达 `global_step=119`；训练总损失 `0.129193`，W&B `liibt044` 持续写入，最终配置与摘要确认 `warmup_steps=0`、`warmup_ratio=0`、学习率 `5e-5`、全局批量 48。两个训练进程和两张 H200 均存在，瞬时采样时其中一张卡利用率 100%；日志仅有 Lightning 同步建议与 DDP 参数检查警告，没有错误栈。短周期健康计数为 1/5，heartbeat 继续每 30 分钟检查。

2026-08-01 14:51 完成第二次短周期联合检查。三个 Slurm Job 均为 `RUNNING` 并各自只有 `after_lock`。新版 Find_1 的唯一运行标记仍为 `job321540_auxsup_1909267_warmup005_20260723T1545_CPC1`，推进到 `global_step=20069`，五项训练损失有限，两张 H100 分别占用约 80.360 GiB 和 80.218 GiB。新版 `unet_c1` 的唯一运行标记仍为 `job321107_auxsup_unet_9417bcf_warmup005_20260723T1545_formal`，推进到 `global_step=25493`，五项训练损失有限，H100 占用约 65.080 GiB、瞬时利用率 100%。Find_0 CPC2 的唯一运行标记仍为 `job321743_Find_0_CPC2_prauc0675477_20260801T132852_w1`，推进到 `global_step=218`；训练总损失 `0.150973`，摘要学习率 `5e-5`，最终配置仍为 `warmup_steps=0`、`warmup_ratio=0`、全局批量 48。两个训练进程与两张 H200 均存在，W&B 持续写入，当前正式错误日志没有错误栈。短周期健康计数为 2/5。

2026-08-01 15:20 完成第三次短周期联合检查。三个 Slurm Job 均为 `RUNNING` 并各自只有 `after_lock`；三个进程的唯一运行标记和冻结代码位置均与约定一致。新版 Find_1 推进到 `global_step=20135`，新版 `unet_c1` 推进到 `25595`，两者五项训练损失均为有限值，W&B 摘要持续更新。Find_0 CPC2 推进到 `global_step=326`，训练总损失 `0.180765`，学习率仍为 `5e-5`；最终配置继续保持 `warmup_steps=0`、`warmup_ratio=0`、全局批量 48。两个训练进程与两张 H200 均存在，正式错误日志自 13:50 后没有新增内容，也没有错误栈；CPC2 尚未保存首个 checkpoint。短周期健康计数为 3/5。

2026-08-01 15:50 完成第四次短周期联合检查。三个 Slurm Job 均为 `RUNNING` 并各自只有 `after_lock`；三个进程的唯一运行标记和冻结代码位置继续符合约定。新版 Find_1 推进到 `global_step=20204`，新版 `unet_c1` 推进到 `25703`，两者五项训练损失均为有限值，W&B 摘要持续更新。Find_0 CPC2 推进到 `global_step=443`，训练总损失 `0.119722`，学习率仍为 `5e-5`，最终配置继续保持无 warmup 和全局批量 48；正式错误日志没有新增内容或错误栈。两张 H200 分别占用约 126.888 GiB 和 142.812 GiB，第二张卡只剩约 0.959 GiB，但当前没有显存不足、进程退出或训练停滞证据。短周期健康计数为 4/5。

2026-08-01 16:20 完成第五次短周期联合检查。三个 Slurm Job 均为 `RUNNING` 并各自只有 `after_lock`；三个进程的唯一运行标记和冻结代码位置继续符合约定。新版 Find_1 推进到 `global_step=20270`，新版 `unet_c1` 推进到 `25805`，两者五项训练损失均为有限值，W&B 摘要持续更新。Find_0 CPC2 推进到 `global_step=545`，训练总损失 `0.115280`，学习率仍为 `5e-5`，最终配置继续保持无 warmup 和全局批量 48；正式错误日志没有新增内容或错误栈。两张 H200 分别占用约 135.082 GiB 和 143.068 GiB，第二张卡只剩约 0.703 GiB，但当前没有显存不足、进程退出或训练停滞证据。连续健康计数达到 5/5，`adaligand-stage1` heartbeat 已改为每 5 小时检查；Find_1 若转入 CPC2，则重新改为每 30 分钟检查并从零累计。

2026-08-01 21:26 完成恢复五小时周期后的第一次联合检查。三个 Slurm Job 均为 `RUNNING`，各自只有 `after_lock`；三个主训练进程的唯一运行标记与冻结代码位置正确。新版 Find_1 推进到 `global_step=20945`，五项训练损失有限，W&B 持续更新。新版 `unet_c1` 的摘要停在 `global_step=26303`，但主进程仍存在、H100 瞬时利用率为 97%，W&B 事件文件与内部日志持续更新到 21:26，因此没有训练停滞证据。Find_0 CPC2 推进到 `global_step=1334`，完成首次正式验证并保存 `TOP_epoch_00_score_0.1432.ckpt` 与 `last.ckpt`；验证总损失为 `0.143169`，受体区域与配体区域 PR-AUC 为 `0.740554` 和 `0.675477`。两张 H200 约占用 142.916 GiB，余量很小但没有显存不足；正式错误日志只有新增警告，没有错误栈。heartbeat 保持每 5 小时检查。

活动 ExecPlan 位于 `文档/exec_plan/Find辅助监督与新版Find1端到端实施.md`。主实现使用两个隔离工作树：

- AdaLigand：`C:\Users\15919\.codex\worktrees\019f8834-aux\AdaLigand`，分支 `codex/auxiliary-supervision-find1`，基点 `799293c`。
- Pocket_Plus：`C:\Users\15919\.codex\worktrees\019f8834-aux\Pocket_Plus`，分支 `codex/auxiliary-supervision-find1`，基点 `de6a89f`。

## Completed

- 完成 `grill with doc` 并获得最终实施授权。
- 确认删除和停用的只有仓库根目录 `CLAUDE.md`；`CLAUDE/memory/` 继续有效并允许新增。
- 读取 AdaLigand `AGENTS.md` 触发的规划、代码、表达、Git、服务器与项目记忆 skills。
- 确认两个原工作区都有未提交修改，因此不在原工作区实施。
- 初始化本轮 ExecPlan，并在 `文档/mapping/计划执行映射.md` 登记。
- AdaLigand 距离生产实现提交：`dde0685`、缺失坐标修正 `e5234c9`；专项测试 `5 passed`。
- Pocket_Plus 实现提交：辅助监督 `c462825`、完整快照 `acfb018`、快照推理与动态通道 `b54a28d`；本地合并回归 `78 passed`。
- 独立审计修复了缺失原子 NaN、比例抽样、空类别 PR-AUC、类别单一来源、五头固定宽度、Find_1 专属学习率和 DDP 快照竞争，最终结论无剩余代码级阻断或科学契约漂移。
- 只读核验 Job `321540` 仍为 `RUNNING`、两张 H100 和 `after_lock` 保留；尚未创建或触碰 `kill_lock`。
- 精确发布目录的 Linux 全套为 Pocket_Plus `380 passed`、AdaLigand `306 passed`；共享代码目录因安全同步不删除旧文件而保留历史测试，不能用于发布端点核验。
- `10ad` 的正式 `ligand_dist.npz` 已生成，CPU 作业 `323026` 验证真实 Dataset 的 56 通道输入、蛋白与核酸类别图、反距离图和固定五头模型构造。
- 历史三种 BEST checkpoint 均从各自补齐后的 `src_snapshot/src/` 严格恢复为完整 `VoxelPointStage1Wrapper`；补齐只增加缺失文件和 SHA 清单，没有覆盖已有模型源码。
- 正式距离生产为 Slurm array `323027`，12 个分片、每项 16 核、最多 192 核；当前 heartbeat `adaligand-stage1` 每 30 分钟唤醒本任务继续检查。
- 分片 2 的 `6k0a`、分片 3 的 `7pel`、分片 4 的 `9wqp`、分片 7 的 `7ojf` 与分片 11 的 `9yx6` 均因缺少 `density/{pdb_id}/exp.npz` 记录为 `unknown_failed`。
- 分片 6 的 1,865 个目标文件均已落盘；取消长时间写后验证的数组元素 `323027_6` 后，补验数组 `323275` 对 36 个候选执行每样本 90 秒硬超时，25 个通过、11 个跳过。完整证据位于 `reports/runs/adaligand_ligand_distance_20260723T0345/ligand_distance/`。
- `unet_c1` 对齐提交为 `6c798d6` 与 `eef5c5b`；后者修复 Dataset 原先只给 `Find_1` 返回三项新增监督的问题。最新精确发布目录 Linux 全套为 `380 passed`。
- Find_1 第一份单步 smoke 因 `max_steps=1` 使当时的 warmup 比例取整为 0，尚无验证阈值时错误进入自适应候选路径。模型和候选逻辑未修改；重试只给 smoke 显式设置 `warmup_steps=1`。后来用户确认正式训练的统一 warmup 比例应为 `0.005`，已经另行重启。
- 第一次正式 Find_1 在训练前验证末尾暴露 CPU PRAUC 与 NCCL 不兼容；旧实际运行快照已有 Gloo 通信组，提交 `9f4012d` 恢复该机制。
- `9f4012d` 仍遗漏旧快照中的 `log_scalar_payload(..., sync_dist=False)`；因此第一轮 `9417bcf` smoke 在 PRAUC 已由 Gloo 聚合后又被 Lightning/NCCL 二次同步并再次失败。提交 `1909267` 补全这一边界，精确发布目录的专项测试为 `8 passed`。
- Find_1 smoke 的 `refined`、`unrefined` 面板显示 `NaN`，原因是当前配置没有启用 `ligand_sparse_refine_loss`，对应诊断面板没有输入。它不参与损失、调度器、BEST checkpoint 或本次新增监督；smoke 的训练与验证损失、候选统计和已定义 PR-AUC 均为有限值。
- 独立审计发现旧 Job `321540` 实际使用 sigma=0.7 Gaussian scatter，而训练计划误写成三线性写入。用户确认以旧运行真实行为为准；提交 `9417bcf` 恢复 Gaussian 优先级并增加测试。

2026-08-01 23:04，用户根据 W&B 曲线判断 CPC2 收益较小，已主动停止 Find_0 CPC2，并取消本轮 Find_1 CPC2。只读核验确认 Job `321743` 已离开 Slurm；Job `321540` 的新版 Find_1 CPC1 与 Job `321107` 的新版 `unet_c1` 仍在各自冻结发布目录中运行，分别到达 `global_step=21041` 与 `26399`，训练进程、W&B 与 GPU 活动正常。后续 heartbeat 只监控这两个 CPC1 运行。本次决定不修改、删除或禁用 CPC2 代码、配置、checkpoint 与未来复用能力。

2026-08-02 02:35 完成范围收窄后的第一次两作业检查。Job `321540` 与 `321107` 均为 `RUNNING`，各自只有 `after_lock`；冻结发布目录、唯一运行标记、学习率、`warmup_ratio=0.005`、全局批量 48 和五项损失权重没有漂移。Find_1 到达 `global_step=21278`，验证总损失与配体区域 PR-AUC 为 `0.300379` 和 `0.646503`；`unet_c1` 到达 `27251`，对应数值为 `0.198103` 和 `0.575755`。两者启用的训练损失、验证损失和 PR-AUC 均为有限值，当前 W&B、GPU 与 checkpoint 持续更新；分配作业标准错误文件中的错误只属于 7 月 23 日已退出的启动尝试。

2026-08-02 07:37 完成第二次两作业五小时检查。Job `321540` 与 `321107` 继续在正确的冻结发布目录中运行，并各自只保留 `after_lock`。Find_1 到达 `global_step=22088`，训练总损失 `0.245146`，最近验证总损失与配体区域 PR-AUC 仍为 `0.300379` 和 `0.646503`；`unet_c1` 到达 `27620`，训练总损失 `0.217885`，最近验证总损失与配体区域 PR-AUC 仍为 `0.198103` 和 `0.575755`。两项启用损失与指标均为有限值。Find_1 的 W&B 摘要持续刷新；`unet_c1` 的摘要自 04:17 暂未刷新，但根训练进程从 7 月 23 日持续存在，W&B 二进制事件文件与内部日志更新到 07:37，H100 利用率为 85%，因此没有停滞证据。两项没有新验证或 checkpoint，也没有训练错误或显存不足。

2026-08-02 17:34 完成第三次两作业五小时检查。Job `321540` 与 `321107` 继续在正确的冻结发布目录中运行，并各自只保留 `after_lock`。Find_1 到达 `global_step=23171`，训练总损失 `0.292095`；最新验证总损失降至 `0.292820`，配体区域 PR-AUC 为 `0.660276`，并保存 `TOP_epoch_00_score_0.2928.ckpt` 与 `last.ckpt`。`unet_c1` 到达 `29060`，训练总损失 `0.175191`；最新验证总损失为 `0.195387`，配体区域 PR-AUC 为 `0.568288`，并保存 `TOP_epoch_00_score_0.1954.ckpt` 与 `last.ckpt`。两项启用损失与指标均为有限值，W&B、训练进程与 GPU 持续活动，没有训练错误或显存不足。Find_1 的 `BEST.ckpt` 暂时仍指向验证总损失 `0.2998` 的旧 TOP，属于冻结回调的一次验证延迟；`unet_c1` 的 `BEST.ckpt` 正确指向当前最低验证总损失 `0.1952` 的 TOP。

## Decisions

- `ligand_dist.npz` 保存与实验密度网格对齐的 `float16` 原始 Å 距离；无配体原子时全为正无穷。
- 蛋白和核酸各使用一个固定 softmax，类别与损失定义见活动 ExecPlan。
- 新 Find_1 使用 64 通道 `voxel_final`、关闭 RAUNet 末端多尺度输出块，并运行完整 CPC1→CPC2。
- 五项损失权重按配体区域、受体结合区域、配体反距离、蛋白原子、核酸原子顺序固定为 `1.0:0.1:0.3:0.05:0.05`。
- Find_1 受体特征写入复现旧 Job `321540`：sigma=0.7 的 `3×3×3` Gaussian scatter 优先于配置中同时保留的 soft 开关。
- Find_1 CPC1、CPC2 最高学习率为 `5e-5`；新版 `unet_c1` 最高学习率仍为 `1e-4`。
- Find_1 CPC1 与 `unet_c1` 的优化器学习率 warmup 比例统一为 `0.005`；Find_1 CPC2 继续无 warmup。
- 本轮 CPC1 产物按已经落盘的 TOP checkpoints 计算 `val_score/global/voxel_ligand_PRAUC`，取最大值作为阶段起点；不使用可能落后一轮的 `BEST.ckpt`。Find_0 与 Find_1 运行 CPC2，`unet_c1` 不运行 CPC2。
- Find_0 与 Find_1 的 CPC2 从第一步直接使用各自最高学习率，不执行 warmup；阶段切换只加载模型参数，用户明确接受不恢复 CPC1 的优化器、调度器、训练步数和回调状态。
- 当前三个冻结运行副本不换版。监控指标改为配体区域 PR-AUC 最大值、删除学习率下降停止回调非法日志调用的修正，只供下一次运行使用。
- 从 2026-08-01 23:04 起，本轮不再训练任何 CPC2：Find_0 CPC2 已由用户停止，Find_1 CPC1 结束后也不启动 CPC2。此决定只改变本轮运行和监控范围，不改变代码、配置或未来复用能力。
- 距离标签生产同时运行最多 192 个 CPU 核；正式 GPU 训练只接管 Job `321540`。
- 一个受限 subagent 负责快照推理、正式产物可变通道与 Selector 惰性输入；主 agent 负责接口、审查和合入。
- 复用 Job `321540` 时必须显式设置唯一 `POCKET_RUN_STAMP`，先证明新运行目录与旧 `job321540` 目录不同，再允许触碰 `kill_lock`；不得覆盖旧快照或放宽拒绝覆盖保护。

## Next Actions

1. 每 5 小时只检查 Job `321540` 的新版 Find_1 CPC1 与 Job `321107` 的新版 `unet_c1`，分别核对冻结发布目录、唯一运行标记、最终配置、进程、GPU、W&B、损失、验证指标、checkpoint、锁和当前错误。
2. 让两个运行继续使用各自冻结发布副本自然执行；不得把下一版修正部署到当前运行，也不得再控制或重启已经停止的 Job `321743`。
3. 任一运行结束后，只在实际存在的 TOP checkpoints 中比较 `val_score/global/voxel_ligand_PRAUC` 并记录最高者及其产物地址；不启动新的训练阶段。
4. 下一版 Pocket_Plus 修正已完成：实现端点 `4ae9f5f`，学习端点与 `Learn/CUMULATIVE` 为 `66469e2`，两端树对象相同且专项测试均为 `31 passed`。这些提交与保留的 CPC2 能力均不得部署到当前冻结运行。
5. 两项 CPC1 运行结束并记录最终产物后，收口 ExecPlan、mapping、项目记忆、运行检查记录与 heartbeat。

## Files To Reopen

- `文档/exec_plan/Find辅助监督与新版Find1端到端实施.md`
- `Data_Preprocessing/Ori_Data/README.md`
- `Data_Preprocessing/Ori_Data/adaligand_preprocessing/labels/ligand_distance.py`
- `C:\Users\15919\.codex\worktrees\019f8834-aux\Pocket_Plus\src\datasets\stage1_dataset.py`
- `C:\Users\15919\.codex\worktrees\019f8834-aux\Pocket_Plus\src\model\stage1_voxel_backbone.py`
