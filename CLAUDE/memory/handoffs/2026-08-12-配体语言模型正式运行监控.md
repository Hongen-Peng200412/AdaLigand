# 配体语言模型正式运行监控 handoff

> 更新时间：2026-08-12 20:25（Asia/Shanghai）  
> 当前阶段：prepared、SMI-TED 和确定性 MoLFormer 正式产物均已完成并通过核验；服务器计算、最终表达审计与双线 Git 均已收口。

## 1. 固定任务顺序

正式处理范围为 `all_existing`，按以下顺序提交和核验：

1. CPU prepared：数组 `0-11`，每任务 8 核，总计 96 核；
2. prepared 全局汇总与结果核验；
3. SMI-TED Light 289M：四张 A100，数组 `0-3`；
4. SMI-TED 全局汇总与结果核验；
5. MoLFormer：四张 A100，数组 `0-3`；
6. MoLFormer 全局汇总与最终结果分析。

前一阶段没有稳定完成并通过结果核验时，不提交后一阶段。代码只保存宽口径事实，不自动
决定产物是否发布。

## 2. 正式位置与离线环境

- 服务器项目：`/home/penghongen/My_Project/AdaLigand`
- 正式代码：`Data_Preprocessing/Ori_Data_upgrade/ligand_language_model/`
- 数据根：`/storage/penghongen/AdaLigand/Ori_Data`
- 目录清单：`/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_2`
- 正式输出：`stage1_preparation_box_pool_2/ligand_language_models/`
- 离线模型与补充依赖：`/storage/penghongen/AdaLigand/model_weights/ligand_language_models`
- CPU Python：`/home/penghongen/anaconda3/envs/AdaLigand_stage1_py310/bin/python`
- GPU Python：`/home/penghongen/anaconda3/envs/Pocket_Plus_centos7_cu121_allgpu/bin/python`

五组离线目录已经完成逐文件内容比较。CPU 化学依赖、两套模型依赖及各自冻结的
Transformers 版本均已实际导入；GPU Python 提供 PyTorch 2.4.1+cu121，并已在 A100
PCIE 40GB 上确认 CUDA 可用。

## 3. 已发生事件

### 3.1 首次 CPU 提交失败

Job `339800` 使用 README 中的 CPU 命令提交。十二个数组元素均在进入 Python 前失败，
共同错误是旧版 Bash 在 `set -u` 下展开空的 `overwrite_args[@]`。该次任务没有生成科学
产物。

三个正式处理 shell 已统一改为始终非空的 `python_args`，并通过服务器旧版 Bash 的普通
模式与覆盖模式参数探针。Job `339816` 另行证明 Slurm 数组元素本来就使用不同的
`SLURM_JOB_ID`；先前由监控目录匹配错误产生的“共享控制目录”判断已经撤回。

### 3.2 release 并发创建修复

独立审计发现 `create_release.sh` 存在并发创建竞态。单节点可用的 `flock` 经跨节点实测后
被否定；正式实现现以共享文件系统上的原子 `mkdir` 创建 `.release-id.lockdir`。32 进程
单节点测试通过。跨节点 Job `339835`（cnode01）和 `339836`（cnode04）也均完成，同一
release 恰好一次创建、一次复用，没有残留锁目录。等待超时不会删除所有者未知的锁。

### 3.3 CPU prepared 重跑成功

Job `339823` 再次使用 README 中的 CPU 命令：

```bash
bash 训练与运行/submit_task.sh \
  --sh Data_Preprocessing/Ori_Data_upgrade/ligand_language_model/prepare_smiles.sh \
  --resource cpu \
  --array 0-11 \
  --cpus 8 \
  --mem 768G \
  -- all_existing
```

十二个数组元素全部 `COMPLETED`，退出码均为 0。分片核验结果：22,386 个 PDB 恰好各
处理一次，全部完成；677,835 个五类正式 occurrence 全部得到非空 SMILES；166 个
`other` 只计数。来源计数为 CCD 636,299、CLC 41,532、present-graph 4。没有 PDB 级
失败、缺失完成文件或临时写入文件。

## 4. 已完成任务

2026-08-12 03:33 左右执行：

```bash
bash 训练与运行/submit_task.sh \
  --sh Data_Preprocessing/Ori_Data_upgrade/ligand_language_model/summarize_outputs.sh \
  --resource cpu \
  -- prepared all_existing
```

Slurm 返回 Job `339847`，在 cnode01 运行 7 分 51 秒后以退出码 0 完成。汇总核验确认：
22,386 个 PDB 全覆盖，677,835 条记录全部有 SMILES；五类计数为 ion 341,567、
small_molecule 219,216、sugar 109,267、nucleotide_like 6,155、peptide_like 1,630。
共有 3,588 种精确字符串；最高频 `[Mg+2]` 出现 280,916 次，只进入报告，不影响输入选择。

prepared 核验通过后，执行：

```bash
bash 训练与运行/submit_task.sh \
  --sh Data_Preprocessing/Ori_Data_upgrade/ligand_language_model/encode_smi_ted.sh \
  --resource a100 \
  --gpus 1 \
  --array 0-3 \
  -- all_existing
```

Slurm 返回 Job `339853`。四个分片读取的输入数分别为 163,119、172,598、169,796、
172,322，总和精确等于 677,835。冻结官方代码把 `normalize_smiles` 失败项变成 `None`；
一个 `None` 会使 tokenizer 拒绝整个大列表，四片因而都退化成全量单条调用。该问题不会
丢样本，但属于共同性能错误，因此已为四个 allocation 创建 `kill_lock`，让运行系统终止
进程并释放资源。四个数组元素均已退出，没有分片报告；中断时产生的少量孤立 NPZ 会在
下一次读取未完成 PDB 时自动清理。

正式修复把官方规范化成功项组成跨 PDB 全局列表；规范化失败项仍逐条调用公开接口，既不
筛除，也不再让 `None` 拖垮有效批量。规范化成功组若发生其他普通 batch 异常，仍保留逐条
重试。最新本地端到端回归通过。真实 A100 临时 Job `339858` 也以退出码 0 完成：`C` 与
`CC` 批量编码成功，`not-a-smiles` 被实际单条调用并按官方异常记录为 `model_failed`。
科学契约审计和注释/文档一致性审计均已 PASS。随后执行：

```bash
bash 训练与运行/submit_task.sh \
  --sh Data_Preprocessing/Ori_Data_upgrade/ligand_language_model/encode_smi_ted.sh \
  --resource a100 \
  --gpus 1 \
  --array 0-3 \
  -- all_existing --overwrite
```

Slurm 返回 Job `339861`。数组元素 0 在 gnode01，元素 1-3 在 gnode02。跨节点同时创建
新 release 时恰好一个任务创建、三个任务复用。四个元素均已完成独立规范化并进入全局
批量前向；各自隔离的规范化失败 occurrence 数为 237、79、106、153，共 575 个。这些
occurrence 没有被删除，批量完成后仍逐个调用官方公开接口。四个元素最终全部
`COMPLETED 0:0`，四份分片报告齐全；没有再次出现成功组全局 `encode` 失败或 CUDA 错误。

只读分片核验 Job `339894` 也以 `COMPLETED 0:0` 退出：22,386 个 PDB 恰好覆盖一次，
677,835 条输入全部有状态，其中 `encoded=677260`、`model_failed=575`；prepared 缺失、
跳过 PDB、临时文件均为 0，四片依赖与 A100/CUDA 事实一致。随后按 README 命令提交
SMI-TED 正式汇总 Job `340175`。该任务以 `COMPLETED 0:0` 退出：22,386 个预期 PDB
全部存在，677,835 条记录中 `encoded=677260`、`model_failed=575`；677,260 个成功
向量文件全部存在且有限性诊断均为 `all_finite`。模型实际输入共有 3,491 种精确
SMILES，最高频字符串为 `[Mg+2]`，出现 280,916 次且没有并列。

汇总通过后按 README 提交：

```bash
bash 训练与运行/submit_task.sh \
  --sh Data_Preprocessing/Ori_Data_upgrade/ligand_language_model/encode_molformer.sh \
  --resource a100 \
  --gpus 1 \
  --array 0-3 \
  -- all_existing
```

Slurm 返回数组 Job `340231`。四片输入数分别为 163,119、172,598、169,796、172,322，
合计 677,835。底层 allocation `340232`、`340233`、`340234`、`340231` 均以
`COMPLETED 0:0` 退出并写出分片报告。日志中的 `lm_head.* UNEXPECTED` 是官方
`AutoModel` 加载基础模型时丢弃掩码语言建模预测头的提示；本项目只读取
`pooler_output`，没有把该提示当作科学失败。全程没有 batch 回退、CUDA 错误或模型调用
失败。

只读分片核验 Job `340265` 以 `COMPLETED 0:0` 退出：22,386 个 PDB 恰好覆盖一次，
677,835 条输入全部为 `encoded`；prepared 缺失、跳过 PDB 和临时文件均为 0。四片依赖
与 A100/CUDA 事实一致。随后按 README 命令提交 MoLFormer 正式汇总 Job `340273`；该
任务以 `COMPLETED 0:0` 退出。最终汇总确认 22,386 个 PDB 全部存在，677,835 条记录
全部为 `encoded`，所有 NPZ 路径存在且有限性诊断均为 `all_finite`；精确模型输入共
3,588 种，最高频 `[Mg+2]` 出现 280,916 次且没有并列。

另有临时 NPZ 内容抽样核验。Job `340266` 因临时脚本中的 SMI-TED 模型名称拼写与正式
常量不一致，使用项目 `kill_lock_340266` 主动终止；该任务只读且没有触碰正式产物。修正
后的 Job `340277` 以 `COMPLETED 0:0` 退出：两套模型各稳定抽取 1,000 个 encoded
occurrence，2,000 个 NPZ 的字段、身份、SMILES、模型名、`float32 (768,)` 向量和
有限性诊断全部通过。

SMI-TED 失败归因 Job `340353` 以 `COMPLETED 0:0` 退出：575 条失败分布于 138 个
PDB、91 种精确 SMILES；`small_molecule=564`、`sugar=9`、`ion=1`、
`nucleotide_like=1`，来源 `ccd=458`、`clc=117`。它们均为官方规范化返回空值后仍逐条
调用公开接口、再由 tokenizer 报空输入类型异常，不是预检筛除或文件写入失败。

最终科学审计发现 MoLFormer checkpoint 默认 `deterministic_eval=false`，即使
`model.eval()` 也会在每次前向重抽线性注意力的随机特征映射；官方特征提取示例显式使用
`deterministic_eval=true`。现有 MoLFormer 产物在字段、路径、形状和有限性上均通过，
但重跑或改变 batch 边界时不保证逐值相同。CPU 探针 Job `340362` 先以
`COMPLETED 0:0` 退出。三个 SMILES 连续四次前向的实测中，
默认模式最大绝对差为 6.2211、5.7869、2.0226，确定性模式后三次与第一次逐元素完全
相同。checkpoint 自身保存十二层 `feature_map.weight`，确定性模式使用冻结权重中的矩阵。
随后获得 A100 的 Job `340358` 也以 `COMPLETED 0:0` 退出，确定性模式四次前向的最大
绝对差和逐 occurrence L2 差均为 0。

正式入口已经显式传入并核对 `deterministic_eval=True`；服务器静态检查与临时端到端回归
通过。2026-08-12 14:49（Asia/Shanghai）使用 README 的覆盖命令提交 Job `340367`：

```bash
bash 训练与运行/submit_task.sh \
  --sh Data_Preprocessing/Ori_Data_upgrade/ligand_language_model/encode_molformer.sh \
  --resource a100 \
  --gpus 1 \
  --array 0-3 \
  -- all_existing --overwrite
```

四个数组元素对应实际 Job `340473`、`340501`、`340505` 和 `340367`，均以
`COMPLETED 0:0` 退出。只读分片核验 Job `340560` 确认 22,386 个 PDB 恰好覆盖一次、
677,835 条输入全部为 `encoded`、prepared 不可用、跳过已有和临时残留均为 0。正式汇总
Job `340564` 确认 677,835 个向量全部有限且没有缺失 NPZ。抽样核验 Job `340594` 对两套
模型各实读 1,000 个稳定样本，2,000 个 NPZ 的字段、身份、SMILES、形状和有限性全部通过。
`7d3f/candidate 4` 的确定性正式向量数值也已回填真实端到端示例。
用户明确要求不建立 heartbeat。后续只在当前 goal 内约每 30 分钟低频检查上述 Job；
运行期间两次检查之间保持等待，不进行高频轮询，也不另建独立任务；上述服务器 Job 已
全部完成，因此不再需要继续监控队列。

## 5. 后续稳定命令

SMI-TED 和 MoLFormer 的提交命令均以
`Data_Preprocessing/Ori_Data_upgrade/ligand_language_model/README_run.md` 为准。SMI-TED
先使用四张 A100 的 `--array 0-3`；其数组与汇总核验完成后，MoLFormer 再使用四张 A100
的 `--array 0-3`。任何共同代码错误修复后可以按用户授权使用 `--overwrite` 重跑；针对
极少 occurrence 的个别化学问题不做全量重跑。

完整事件、验证证据和最终结果持续追加到
`文档/exec_plan/AdaLigand_v3基础数据升级实施.md`。
