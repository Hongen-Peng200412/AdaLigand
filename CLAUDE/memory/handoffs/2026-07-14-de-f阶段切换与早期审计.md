# Handoff: DE 闭合、F12 启动与早期产物审计

Date: 2026-07-14

## Current State

正式 run `adaligand_ag_20260711T154658` 已从 DE 安全切换到 F。原 DAG 仍为 `316114 → 316115 → 316116 → 316117`：`316115` 已于 `2026-07-14 01:26:42+08:00` 以 `COMPLETED 0:0` 结束，`316116` 同时启动并以 F12 运行；`316117` 继续等待 `afterok:316116`，只能执行 analyze。

`316116` 已复用冻结的 `/home/penghongen/run_cmd_316116.sh`，SHA-256 为 `8399d571bab44881facbeaa9dd1e738594255a4b4212605cfe418878744f7d13`。当前精确锁状态为 `after_lock_316116` 存在，`pre_lock_316116`、`try_lock_316116`、`kill_lock_316116` 均不存在。

## Completed

- Stage D 状态完整覆盖 22,386 个 PDB：22,339 `success`、3 `skipped`、44 `known_failed`，`unknown/duplicate/missing` 均为 0。状态 SHA-256：`263fa2af81198e686616d2c8b1022a32668a689a6a74438d72c6e294662d5e9f`。
- Stage E 状态完整覆盖 22,386 个 PDB：22,309 `skipped-valid`、77 `known_failed`，`unknown/duplicate/missing` 均为 0。状态 SHA-256：`3a0d4148c2001a56d43168c9b73db4dea9f2da07aa27a82061e2a23fe1e8c54c`。
- `de_release` 判定为 success，证据 SHA-256：`ab49f43cd60fc0490d4d45aad850aa64d773b39c87014e399958089fac348da6`。
- E 的三路风险审计均通过：
  1. `7b14/7nll` 的祖传 MRC 轴序、实际 voxel、origin、shape 与三维几何闭合；
  2. 长尾补足后保留的 `8j07/9dp7/9qwt` 均具有并通过验证的 `exp.npz`、`sim.npz`、`ligand_area.npz` 完整三件套；
  3. `8ckb/8glv/9e5c/9fqr` 四条 run-only exclusion 逐字段通过，manifest SHA-256 为 `380844d0b908b08707fada689f64b2fa4cc519f4771df92dec8b5bf0b2cd325f`；`2zhc` 的 `model_map_frame_mismatch` 确定性契约通过。没有伪造成功、删除样本宇宙或猜测坐标变换。
- F 于 `2026-07-14 01:26:42+08:00` 以 `F_N_JOBS=12` 启动。到 `04:05+08:00` 日志已完成 944 个 task；对较早完成的 874 个 PDB 逐一核验了 `quality/{pdb_id}.jsonl`、`quality/{pdb_id}.provenance.json`、`quality_atoms/{pdb_id}.npz`，三件套存在且契约验证通过。

## Decisions

- F 继续保留四种 CC、配体逐原子 Q、6 Å occurrence 口袋逐原子 Q、schema v3 空口袋 null/status、MapQ `sigma=0.4,np=8` 与完整 provenance；不得因早期抽查通过而放宽最终 gate。
- 四条 run-only exclusion 必须在 F 完成后分别形成可审计的 `known_failed:run_policy_excluded` 终态；当前早期 874 样本审计不能替代这项最终复核。
- `316117` 只运行 Stage G analyze：不得执行示例阈值，不得猜生产配置，不得写最终 `keep_list`。正式 map-level schema v2 filter 仍须在质量分布产生后另行显式运行。

## Open Questions

- 当前没有需要用户决定的科学契约问题。
- 尚未取得 F 全量终态、四条 exclusion 的 F 传播证据、F release gate 和全量 QC；这些属于运行中的待验收项，不是新的契约阻塞。

## Next Actions

1. 低噪声只读监控 `316116`、F 日志、run-scoped status、三类质量产物增量以及精确锁；运行正常时不改代码、命令或锁。
2. F 完成后先独立审计四终态、unknown/duplicate/missing、四条 exclusion、四 CC、配体 Q、6 Å 口袋 Q、空口袋 schema v3、MapQ 固定参数和 provenance，再确认 F release gate。
3. 只有 F 审计通过后，才确认 `316117` 由既有依赖自动启动；复核其仅执行 analyze，并审计候选扁平字段和质量分布，不产生 `keep_list`。
4. A–G 完成后更新 ExecPlan、mapping、代码邻近 README、项目记忆与最终 handoff，并完成独立全量 QC 证据报告。

## Files To Reopen

- `CLAUDE/memory/projects/adaligand.json`
- `文档/exec_plan/A-G数据流水线实现与全量运行.md`
- `文档/mapping/计划执行映射.md`
- `Data_Preprocessing/Ori_Data/code/readme.md`
- `/storage/penghongen/AdaLigand/Ori_Data/reports/runs/adaligand_ag_20260711T154658/`
