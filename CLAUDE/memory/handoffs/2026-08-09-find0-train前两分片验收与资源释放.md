# Handoff: Find_0 train 前两个分片验收与资源释放

Date: 2026-08-09

## Current State

Find_0 Stage1 的 calibration 概率图、正式阈值、calibration F1、validation F1、Gauss 参数搜索与 calibration forest 回填均已完成。train F1-centered 当前只完成全局分片 0/50 与 1/50；其余 48 个分片尚未提交。两个已完成分片对应的 A800 均已在验收后通过四锁执行器正常释放。

正式阈值保持为 `t_F1=0.9962158203125`、`min_voxels=10`、`max_voxels=2046`、`denominator=32768`。正式 Gauss 参数保持为 `lambda_positive=0.1`、`lambda_negative=0.001`、`tau_angstrom=1.0`、`gauss_score_min=1.0`、截断距离 5 Å。

## Completed

- Job 335116 完成 `train_pdb_ids.json[0::50]` 的 275 项：246 份完整 probability/components/F1-centered 产物，29 份合法根级 `_BLOB_EXCEED`。最终验收无缺失、冲突、活跃租约或临时文件；Job 最终为 `COMPLETED 0:0`。
- Job 335493 attempt a7 完成 `train_pdb_ids.json[1::50]` 的 275 项：247 份完整产物，28 份合法根级 `_BLOB_EXCEED`。最终验收无缺失、冲突、活跃租约或临时文件；Job 最终为 `COMPLETED 0:0`。
- Job 335493 的冻结 release 为 `/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_c65e77b0b031/Pocket_Plus`，检查点为 `TOP_epoch_00_score_0.2843.ckpt`，launch 为 `/home/penghongen/Feedback/AdaLigand/launches/335493/train_anchor_O_O_prime_job335493_20260806T034515_a7`。
- 两次释放都只删除对应 Job 自己的 `after_lock`，没有使用 `scancel`，没有修改正式产物，也没有追加 train 分片。

## Decisions

- train 0/50 与 1/50 只代表公共 train 清单的 2/50，不能记为 train 全量完成。
- 已完成并验收的单卡任务立即释放资源；释放前必须确认终态集合完整、`try_lock` 存在、无生产进程、无活跃 `_RUNNING`。
- validation/train 的 Gauss 增量回填继续使用已冻结参数，但尚未提交；不得把 calibration 已完成回填误记为三个数据划分均已回填。

## Open Questions

- 其余 48 个 train 分片使用哪些 GPU allocation、卡型和并发安排。
- 是否先对已经静止的 validation 与 train 0/50、1/50 执行正式 Gauss 增量回填。

## Next Actions

1. 等待用户决定后续 GPU 资源走向和 train 分片编号。
2. 新增分片前复核用户冻结脚本的模型、检查点、输出根目录、`global_shard_count=50` 与 `shard_index`。
3. 需要 Gauss 回填时使用正式 `训练与运行/sh/infer/Find_0_Gauss.sh`，只处理依赖齐全且成功取得 PDB 级租约的静止 forest。

## Files To Reopen

- `文档/exec_plan/Find_0推理与F1居中产物实施.md`
- `文档/exec_plan/Find_0高斯联合打分实施.md`
- `talk/Find_0推理与评估检查记录.md`
- `talk/Find_0高斯联合打分检查记录.md`
