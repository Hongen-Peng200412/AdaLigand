# Handoff: unet_c1 校准完成与三支线启动

Date: 2026-08-24

## Current State

主链辅助监督版 `unet_c1` 已完成 calibration 100 个 PDB 的完整图 probability map。Job `346737` attempt a6 正在生成 validation 200 个 PDB 的 probability map；CPU16 Job `353620` 与 `353621` 正在分别生成 calibration 的 F1、F2 semantic 与 blobs。三条支线均在运行，尚未出现科学或执行故障。

正式根为 `/storage/penghongen/AdaLigand_stage1_inference/UNET/unet_c1-mainchain-ligand_PRAUC_0.602950`。checkpoint、训练配置、两份清单和部署代码身份没有变化。

## Completed

- a5 于 2026-08-24 03:39:50+08:00 开始，07:49:48 以退出码 0 完成，墙钟 14,998 秒。
- calibration 为 100/100；完整图概率共 14,459,386,630 字节，合计 112,070 个窗口。全量审计没有发现缺项、额外项、临时项、非有限值、概率越界或几何漂移。
- a5 的 GPU 采样行从 allocation 输出回填；984 个活跃样本平均利用率 99.112%，峰值显存 43,930 MiB。a6 已修复采样重定向，CSV 会直接接收数据行。
- 第一次临时审计命令因嵌套 shell 引号剥离字典键而在科学读取前退出；独立只读脚本重跑成功，科学文件未修改。
- a6 launch 为 `tmp_stage1_mainchain_job346737_20260824T080211_a6`，动态命令 SHA-256 为 `eb986013b3ba221346445bc2c17ec1a5bdbae41b260c5a2b3069eed48e846919`。
- F1/F2 blobs Job 为 `353620/353621`，分别固定 `alpha=1/2`；每项使用 16 CPU，总额外 CPU 为 32。

## Decisions

- validation 不因 calibration 的高 GPU 利用率调整窗口参数，继续使用 batch 16、12 个物化 worker、12 个预取 batch 和 8 个待融合 batch。
- 任一 blobs Job 先完成，就立即提交对应 CPU16 basic tune，不等待另一项；同时运行的额外 CPU 始终不超过 32。
- F1 basic 固定 `alpha=1, objective_beta=1, prefiltered_min_voxel=8`；F2 basic 固定 `alpha=2, objective_beta=2, prefiltered_min_voxel=8`。
- Job `346737` 完成 validation 后仍保留 `try_lock_346737` 和 `after_lock_346737`；不得删除 `after_lock` 或执行 `scancel`。
- AdaLigand 主工作区仍属于另一项 Find1 工作。本轮执行记录继续只写隔离分支 `codex/unet-c1-calibration-inference-log`。

## Next Actions

1. 监视 a6 首个 validation PDB，并核对 probability 字段、shape、有限性、范围、几何、性能和完成标记。
2. 监视 Job `353620/353621`；进入 blobs 并行阶段后用 `sstat` 与进程 CPU 核对多核利用率。
3. 任一 blobs Job 正常结束后立即提交对应 basic tune，并记录 Job、release、launch、动态命令、日志和产物路径。
4. 最终验收两套 semantic/blobs/basic 互不覆盖以及 validation 200/200。

## Files To Reopen

- `C:\Users\15919\Desktop\AdaLigand_unet_c1_calibration_log\文档\exec_plan\Stage1第三版unet_c1校准与validation推理实施.md`
- `C:\Users\15919\Desktop\AdaLigand_unet_c1_calibration_log\文档\mapping\计划执行映射.md`
- `C:\Users\15919\Desktop\AdaLigand_unet_c1_calibration_log\CLAUDE\memory\projects\adaligand.json`
- `/storage/penghongen/AdaLigand_stage1_inference/UNET/unet_c1-mainchain-ligand_PRAUC_0.602950/feedback/`
- `/storage/penghongen/AdaLigand_stage1_inference/UNET/unet_c1-mainchain-ligand_PRAUC_0.602950/monitoring/`
