# Handoff: CryoAtom2 校准集分片 0 已验收，分片 1 继续排队

Date: 2026-09-13

## Current State

截至 14:24，calibration.json 原清单 100 项中，分片 0 的 50 项已全部运行成功并完成局部验收；分片 1 的 50 项尚未开始。goal 保持 active，不能把 50 项通过当成全量完成。

379402_0 位于 gnode09，批处理正常退出，当前通过 after_hold 保留单卡 A800。379403_1 仍 PENDING(Resources)。两个任务各 16 CPU，不传 --mem，不设时间限制，QOS 分别为 h200g4、a100g2，均 nvlink 分区。用户已允许其他有效 QOS。不要重复提交，也不要释放或借用 379402 的资源。

## Completed

- 50 项 latest.json 均 success、returncode=0、error=null，与各自 status.json 一致。
- 独立重读 100 份最终/raw CIF，均可解析、非空、坐标有限，原子和残基统计与状态记录一致。
- 三份源文件 SHA256、原始压缩图路径/大小/修改时间、全部实体与 FASTA 序列/链副本标题、实际命令均核对通过。临时解压图已清理，产物目录没有残留 .log。
- `slurm/allocations/try_lock_379402` 与 `slurm/allocations/379402/after_lock_379402` 均存在，批处理 out 明确记录第 1 次执行成功。
- 证据写入 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/calibration/运行日志与统计/分片0完成验收.json`。它只覆盖分片 0，不能替代全量最终验收。

## Decisions

完整原序清单按 `[0::2]`、`[1::2]` 分给两个单卡任务；原始密度图仅解压，输入完整下载的蛋白质/RNA/DNA 序列，包括短序列，不输入真实结构坐标，不改官方科学参数。逐 PDB 实际产物位于 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/calibration/cryoatom2_artifact`，日志和统计位于同级 `运行日志与统计`。

正式短命令已执行，见执行记录的“正式运行命令”部分。验收使用单独的内联检查，不是重新预测。稳定状态继续每轮 60 或 90 分钟、每次 Start-Sleep 300 秒静默等待，不设 heartbeat，不因排队结束任务。

## Next Actions

1. 继续检查 379403_1 的真实 Slurm 状态；启动后核对它的 release、launch、单 A800/16 CPU 身份和生产代码摘要。
2. 分片 1 完成后验收全部 100 项及 200 份最终/raw CIF，核对两个 50 项集合完整且不重叠、来源、科学参数、冻结代码和运行证据，生成最终 summary 与审计记录。
3. 保留两项 after_hold 资源，更新任务日志、服务器镜像、映射和完成交接，核验 Git 后才标记 goal 完成。本地累计分支已由其他工作推进至 4f55abe988775a427839d52b33b94946628092e5，本任务没有改写历史。

## Files To Reopen

- [完整执行记录](../../../文档/exec_plan/CryoAtom2校准集受体重建执行记录.md)
- [当前代码与产物契约](../../../测评数据代码/cryoatom2原始预测结构/README.md)
- [两个最终 Job 的提交交接](2026-09-12-cryoatom2校准集双A800重新提交.md)
- 服务器日志根的 `分片0完成验收.json`、`提交前输入核验.json`、`提交前代码摘要.json`、`轮询记录.jsonl`、`runs/`、`slurm/`。
