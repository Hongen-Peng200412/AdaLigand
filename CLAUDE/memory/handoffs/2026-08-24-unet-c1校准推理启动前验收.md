# Handoff: unet_c1 校准推理启动前验收

Date: 2026-08-24

## Current State

主链辅助监督版 `unet_c1` 的 calibration/validation 推理代码、并行 basic/Gaussian 调参、隔离部署和服务器 smoke 已完成。Job `346737` 仍在 `hnode02` 保留 1×H100 和 16 CPU；`try_lock_346737` 与 `after_lock_346737` 都存在，没有 `kill_lock`，正式 attempt a5 尚未触发。

正式根为 `/storage/penghongen/AdaLigand_stage1_inference/UNET/unet_c1-mainchain-ligand_PRAUC_0.602950`。`inputs/` 已冻结 calibration 100 个 PDB、validation 200 个 PDB、训练配置、推理配置和 `production_contract.json`；`artifacts/` 当前为空。

## Completed

- Pocket Plus 实现端点 `5476a33` 与学习累计端点 `4e5325d` 的 tree 均为 `ca21a939`。
- basic 保持全局实际分数阈值串行累计；多 PDB 事实、Gaussian 原子项、Gaussian 粗搜/细搜和最终 `min_voxels` 使用 16-worker 外层并发。
- 独立逻辑复核确认 25 个随机样例的旧串行实现、当前 `workers=1` 和 `workers=4` 共 50 组结果逐字段相同；乱序并列测试保持原配置首项获胜。
- 部署删除隔离任务根中的 1,971 个旧文件/目录，最终保留 458 个当前 Git 文件与 1 个固定 `TASK_PATH` 兼容脚本。四个关键文件哈希与本地一致，反向同步差异为 0。
- checkpoint SHA-256 为 `341c3aa...feed6`，训练配置为 `28d0d6...7884`，calibration 与 validation 清单分别为 `b14c9f...0fe7a` 和 `86a8e8...1c4cdf`。
- hnode02 上 44 项 Linux CPU 回归、配置解析、编译、Shell 语法与 2 项真实 H100 CUDA smoke 全部通过。

## Decisions

- a5 只生成 calibration probability；使用 `training_snapshot`、窗口 batch 16、12 个物化 worker、12 个预取 batch 和 8 个待融合 batch。
- a5 完成后，a6 生成 validation probability；F1 与 F2 各用一个 CPU16 Job 生成 semantic/blobs，随后各提交一个 CPU16 basic tune Job。
- 本轮不生成 centered/evaluate，不正式运行 Gaussian tune；Gaussian 并发能力只完成实现与测试验收。
- allocation 结束时仍保留 `try_lock_346737` 与 `after_lock_346737`，不得删除 `after_lock` 或执行 `scancel`。
- AdaLigand 主工作区当前有另一项 Find1 监视的未提交改动。本轮日志位于隔离分支 `codex/unet-c1-calibration-inference-log`，不得覆盖主工作区文件。

## Next Actions

1. 写入并核对 a5 的完整 `run_cmd_346737.sh`、SHA-256 与 15 秒 GPU 采样路径。
2. 只删除 `try_lock_346737` 触发 a5；保存自动生成的 release 与 launch 身份。
3. 首个 calibration PDB 完成后核对 probability NPZ、几何、有限性、范围、性能文件与 `_COMPLETE`。
4. calibration 100/100 完成后启动 a6 与 F1/F2 CPU 支线，并整理第二个关键 handoff。

## Files To Reopen

- `C:\Users\15919\Desktop\AdaLigand_unet_c1_calibration_log\文档\exec_plan\Stage1第三版unet_c1校准与validation推理实施.md`
- `C:\Users\15919\Desktop\AdaLigand_unet_c1_calibration_log\文档\mapping\计划执行映射.md`
- `C:\Users\15919\Desktop\AdaLigand_unet_c1_calibration_log\CLAUDE\memory\projects\adaligand.json`
- `C:\Users\15919\Desktop\Pocket_Plus\src\inference\pipeline.py`
- `C:\Users\15919\Desktop\Pocket_Plus\src\inference\calibration.py`
