# Handoff: Stage E 长尾截止与正式恢复

Date: 2026-07-13

## Current State

- 正式 run 是 `adaligand_ag_20260711T154658`，DAG 保持 `316114 → 316115 → 316116 → 316117`，没有取消或重提。
- `316114` 已完成。`316115` 于 `2026-07-13T21:04:43+08:00` 在原 96 CPU allocation（`cnode04`）进入正式、无过滤、无 `--overwrite` 的 Stage E，日志已确认恢复脚本和 run_cmd 哈希、`frozen initial` 分支及 `Parallel(n_jobs=24)`。
- 当前 `316115` 为 `RUNNING`；精确 `/home/penghongen/after_lock_316115` 存在，`try_lock_316115` 与 `kill_lock_316115` 均不存在。`316116`、`316117` 保持 `PENDING (Dependency)`。
- Stage F 仍须以 F12 计算四种 CC、配体逐原子 Q、6 Å occurrence 口袋逐原子 Q、schema v3 空口袋 `null/status`、MapQ `sigma=0.4,np=8` 和完整 provenance。Stage G 只运行 analyze，不猜阈值、不写最终 `keep_list`。

## Completed

### 316415 截止收口

- 用户授权的绝对截止点为 `2026-07-13T19:54:19+08:00`。补足作业 `316415` 使用 CPU48、`n_jobs=12` 和标准 Chimera molmap 处理 `8glv,8j07,9dp7,9e5c,9fqr,9qwt`。
- 截止审计按完整公共三件套 `exp.npz + sim.npz + ligand_area.npz` 判定：`8j07,9dp7,9qwt` 完成；`8glv,9e5c,9fqr` 未完成。partial/scratch 不计完成。
- 精确 `kill_lock_316415` 触发 core 收口后，仍发现三个外部 Chimera：PID 160147（9e5c）、160179（9fqr）、160191（8glv）。逐一核对用户、PID、完整命令行和 scratch 路径后终止，并复核无残留 `e_density`/Chimera 进程，才删除精确 `after_lock_316415`。
- `316415` 最终为预期的用户截止 `FAILED 9:0`，elapsed `06:04:02`；它不是正式 DAG 的依赖。没有伪造 6/6 success、没有把 partial 提升为公共产物。

### Run-scoped exclusions 与证据链

- 正式 exclusions 保留既有 `8ckb`，新增 `8glv,9e5c,9fqr`。四个样本仍在 22,386 样本宇宙和 E/F 审计中，以 `known_failed:run_policy_excluded` 传播，并从训练、推理和 G 候选排除。
- 完成的 `8j07,9dp7,9qwt` 不进入排除清单，其完整产物由正式 E 复核/复用。
- 冻结证据目录：`/storage/penghongen/AdaLigand/Ori_Data/reports/runs/adaligand_ag_20260711T154658/stage_e_long_tail_cutoff_20260713T195419`。
- SHA-256 证据链：
  - `predecision.json`: `40e7c949df528b81e1c4a8ee8bbd60daec5ec4d06089037e64958f08fd5458a8`
  - `posttermination.json`: `0f20f20cae3b9958cfe3fd3085233782b2e2e5533144cec704fa22dec2d97397`
  - `exclusions.before.jsonl`: `b586cab20644c3cc8fb1f4e0eaa7eead4cff0d496a862c2313b5e0c1847257fe`
  - `exclusions.after.jsonl` 与正式 manifest: `380844d0b908b08707fada689f64b2fa4cc519f4771df92dec8b5bf0b2cd325f`
  - `summary.json`: `f4a26a9359519e4b94b9f28ecdb21645929c018a014729feadb44b29eadf4761`
  - `/home/penghongen/e_long_tail_cutoff_release_316115`: `cd06ec335ab109f25a9da9660a0ce943888c72546a11dbb3852d4ff897764cfa`
  - `Data_Preprocessing/Ori_Data/sbatch/resume_de_316115_e_repair_v3.sh`: `eabfad6bd33b7ae3bfca619000cf120f9585796a8dc7fc497e8f2328f7009626`
  - `/home/penghongen/run_cmd_316115.sh`: `6e8c88a18472c07c76d6c9caf64472548d39db65ea3aef26d6540024e8e3d828`
- 本地与远端全套均为 207 tests passed；`VALIDATE_ONLY=1` 路径实际执行并通过后，才删除精确 `try_lock_316115` 放行正式 E24。
- 首版 release marker 的 `released_at` 来自 Windows，带 7 位小数，服务器 `datetime.fromisoformat` 安全拒绝；当时 `try_lock_316115` 仍在且正式 E 尚未启动。将时间格式收紧为 6 位小数并重新绑定 marker 后，`VALIDATE_ONLY=1` 通过。该事件只涉及证据格式兼容，没有改变科学契约或数据产物。

## Decisions

- `CPU48/n_jobs=12` 是本次六样本纠偏的运行事实，不是新的通用 repair 最优值。未来并发必须复用已冻结的实证结论；任何偏离须先记录依据和关键路径影响。
- deadline 排除仅属于本次正式 run 的运行政策，不新增通用科学 KnownFailureCode，也不改变坐标、密度、质量或 Stage G 科学契约。
- 停止外部工具作业时，`kill_lock` 之后必须独立检查归属明确的孤儿进程；只有进程归零才能释放补足 allocation 或切换 writer。

## Next Actions

1. 低噪声监控 `316115` 的正式 Stage E 状态、日志、公共三件套增量和精确锁；不得改运行中代码、run_cmd 或 manifest。
2. Stage E 完成后运行 DE gate；独立审计 D/E 四终态、unknown/silent missing、E1/E2/E3 schema v2、Pocket lineage、actual voxel/origin/shape、严格 HETATM removal 与三维内容。
3. 只有 DE gate 和独立审计通过后，确认 `316116` 以既有预置命令和 F12 启动；F 完成后审计四 CC、两类逐原子 Q、6 Å 口袋、空口袋、MapQ 参数和 provenance。
4. F 审计通过后允许 `316117` 运行 G analyze；不要执行示例阈值或生成最终 `keep_list`。
5. A–G 完成后审计全量 QC 与 G 分布，更新 ExecPlan、mapping、README、项目记忆和最终 handoff，再停止 heartbeat。

## Files To Reopen

- `文档/exec_plan/A-G数据流水线实现与全量运行.md`
- `文档/mapping/计划执行映射.md`
- `Data_Preprocessing/Ori_Data/code/readme.md`
- `Data_Preprocessing/Ori_Data/sbatch/resume_de_316115_e_repair_v3.sh`
- `Data_Preprocessing/Ori_Data/code/long_tail_cutoff.py`
- `CLAUDE/memory/projects/adaligand.json`
- `CLAUDE/memory/learnings/gotcha-2026-07-13-repair资源不得偏离冻结吞吐.md`
