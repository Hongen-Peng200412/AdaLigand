# Handoff: Stage1 第三版 unet_c1 训练完成验收

Date: 2026-08-24

## Current State

Slurm Job `346737` 上的 Stage1 第三版单卡主链版 `unet_c1` a4 已正常完成训练并通过最终验收。训练主体已经退出；Job 仍显示 `RUNNING`，仅因为 runner 按 `--after_hold` 保留 allocation。

- release：`Pocket_Plus_8139f528eebc`。
- launch：`tmp_stage1_mainchain_job346737_20260818T035126_a4`。
- W&B：`hqumqkex`，已完成最终同步；摘要为 `trainer/global_step=34,992`、`epoch=0`。
- 正常停止原因：实际学习率衰减达到冻结配置 `stop_after_lr_reductions=3`。
- 最终配体体素、受体、蛋白主链和核酸主链 PRAUC：`0.602950`、`0.667168`、`0.181585`、`0.241873`。
- 最佳 checkpoint：`TOP_epoch_00_score_0.6030.ckpt`；内部 `best_model_score=0.602950275`、`global_step=34,993`。
- `BEST.ckpt`、最佳 TOP 和 `last.ckpt` 均为 499,668,966 字节，SHA-256 同为 `341c3aa1f383b3920964980bb410bd69f0c0e10e2169f417cce0cf1cf34feed6`。
- a4 错误切片为空；训练进程为 0，H100 显存仅 1 MiB。
- 根目录 `try_lock_346737` 与 Job 专属 `after_lock_346737` 保留。用户明确要求暂时不要释放资源。

## Completed

- 核对 runner 结束输出：实验结果已保存、`unet_c1` mainchain 正式训练完成、第 4 次执行成功。
- 核对冻结配置：单卡、16 workers、batch 8、全局 batch 48、每 epoch 40 次 validation、主链损失 `0.05/0.05`、阈值 `0.003`、3 次学习率衰减后停止。
- 核对 W&B 最终同步、最终指标与有限损失。
- 读取 `BEST.ckpt` 内部 ModelCheckpoint 状态，确认监控字段为 `val_score/global/voxel_ligand_PRAUC`，模式为 `max`，最佳分数和最佳路径正确。
- 核对 checkpoint 目录：10 个 TOP、`BEST.ckpt` 与 `last.ckpt`，没有 `.tmp` 或 `.part`；最终三份文件内容一致。
- 核对节点：训练进程已经清空，GPU 不再占用训练显存。
- 更新训练执行记录、主规格、计划执行映射与项目记忆。

## Decisions

- 本对话的持续训练监视目标已经完成；不再为已退出的训练进程安排自然睡眠，也不创建 heartbeat。
- allocation 的保留不是训练未完成。没有新的明确授权，不删除 `try_lock_346737` 或 `after_lock_346737`，不执行 `scancel`，不触发 runner 再次运行。
- 后续推理或实战应使用 `BEST.ckpt` 或 `TOP_epoch_00_score_0.6030.ckpt`；两者当前内容完全一致。
- 无主链双卡 U-Net、Find 系列训练和新版推理程序属于各自独立任务，不纳入本次完成验收。

## Next Actions

1. 等待用户未来明确授权后，再单独释放 Job `346737` 的保留 allocation。
2. 新版推理程序验收通过后，可使用本次最佳 checkpoint 进行约定的实战测试。

## Files To Reopen

- `C:\Users\15919\Desktop\AdaLigand\文档\exec_plan\Stage1第三版训练启动与监视记录.md`
- `C:\Users\15919\Desktop\AdaLigand\文档\规划文档\Stage1第三版训练IO实施计划.md`
- `C:\Users\15919\Desktop\AdaLigand\CLAUDE\memory\handoffs\2026-08-24-stage1-v3-unet-c1训练完成验收.md`
- `C:\Users\15919\Desktop\Pocket_Plus\训练与运行\sh\unet_c1.sh`
