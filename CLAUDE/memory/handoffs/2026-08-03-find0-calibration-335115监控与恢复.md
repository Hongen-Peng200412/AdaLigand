# Find_0 calibration Job 335115 监控与恢复交接

## 当前目标

使用已经完成训练的 Find_0 检查点，为 calibration 数据划分的 100 个 PDB 生成完整图配体概率。正式产物根目录是：

`/storage/penghongen/AdaLigand_stage1_inference/Find_0-CPC1-ligand_PRAUC_0.675477/artifacts/Find_0/calibration`

当前先监控、必要恢复并验收概率图。用户已经授权验收后继续正式阈值冻结和 F1 居中阶段，并直接冻结 `min_voxels=10`；不再执行候选值比较。

## 作业身份

- 旧 A100 数组 Job 334936 已由用户取消。它曾短暂运行，合法 `_COMPLETE` 产物继续复用；无完成标记的目录不能计入完成集合。
- 当前 A800 父数组是 Job 335115，使用 `QOS=cpu96`、每个数组元素 1 张 A800 和 8 个 CPU 核。
- 分片 0 的真实数字 Job ID 是 335116。
- 分片 1 的真实数字 Job ID 是 335115。
- 两个元素当前共用冻结 release：`/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_05c8e4d8216d/Pocket_Plus`。
- 分片 0 launch：`/home/penghongen/Feedback/Pocket_Plus/launches/335116/Find_0_calibration_probability_job335116_20260803T163413_a1`。
- 分片 1 launch：`/home/penghongen/Feedback/Pocket_Plus/launches/335115/Find_0_calibration_probability_job335115_20260803T163415_a1`。
- 实际 Python 命令使用 `--window-batch-size 10`、`--cache-max-bytes 107374182400`、`--shard-count 2`。

## 最近状态

2026-08-03 17:12+08:00，两个数组元素都在 `gnode10` 运行，已有 9 个完整概率图。`_RUNNING` 是 PDB 级租约，准确位置在 PDB 目录根部：`6w2t/_RUNNING` 由分片 0 的当前进程 `gnode10:46435` 持有，`7bl6/_RUNNING` 由分片 1 的当前进程 `gnode10:46863` 持有；`28um/_RUNNING` 的 owner 是已取消旧 A100 进程 `gnode07:75525`。第一轮仍在运行，三个租约都不得清理。

两个 A800 进程当时都存在。分片 0 显存约为 76.2/81.9 GiB；分片 1 一次采样约为 80.9/81.9 GiB，尚未出现 OOM。继续检查日志和完成数量，不因瞬时 GPU 利用率为零或接近满显存单独判错。

2026-08-03 19:45+08:00，统一 SSH 入口修复后重新取得只读快照：两个分片均运行约 3 小时 9 分钟，完整概率图、概率数组和几何文件数量均为 35；PDB 级租约共有三个，其中 `28um` 仍为旧 A100 残留，另外两个由当前 A800 进程持有。没有 `try_lock`、OOM 或异常栈。Heartbeat 已改为每 2 小时检查。

2026-08-04 01:39+08:00，分片 0 的 Job 335116 已成功结束第一次推理，标准输出记录创建 `/home/penghongen/Feedback/Pocket_Plus/allocations/try_lock_335116`。分片 1 的 Job 335115 仍在运行，当前租约是 `9k3b/_RUNNING`，owner 为 `gnode10:46863`。完成集合为 94/100；缺少 probability `_COMPLETE` 的 PDB 是 `28um`、`9k3b`、`9mbz`、`9r66`、`9rwp`、`9wmu`。`28um/_RUNNING` 仍是旧 A100 残留。当前没有 OOM 或异常栈；不得在 Job 335115 结束前清理任何租约或锁。

2026-08-04 02:22+08:00，完成集合推进到 96/100，缺少 `28um`、`9r66`、`9rwp`、`9wmu`。Job 335115 正在处理 `9r66`；PID 46863、GPU 100% 利用率和约 76.9/81.9 GiB 显存占用证明进程健康，错误扫描为空。`28um/_RUNNING` 仍是旧 A100 残留，`9r66/_RUNNING` 是当前活跃租约；没有执行清理。

2026-08-04 02:55+08:00，完成集合推进到 97/100，缺少 `28um`、`9rwp`、`9wmu`。当前活跃租约已经推进到 `9rwp/_RUNNING`，owner 为 `gnode10:46863`；旧 `28um/_RUNNING` 不变。Job 335115 尚未出现 `try_lock`，错误扫描为空，因此仍只读等待，不清理租约或锁。

2026-08-04 04:27+08:00，两个 A800 分片均已成功结束首轮并进入各自 `try_lock`；首轮最终完成 99/100，唯一缺失 `28um`。独立只读复核确认没有生产者，唯一租约 `28um/_RUNNING` 内恰好只有旧 A100 owner `gnode07:75525` 的 `owner.json`。主 agent 按授权精准删除该文件与空租约目录，再删除 allocation 根目录的 `try_lock_335116`、`try_lock_335115`，保留两个 `after_lock`。恢复证据为 `/home/penghongen/My_Project/tmp/find0_calibration_recover_335115/recovery_20260804T042726.txt`。正式命令已经原样第二次执行：分片 1 跳过完整产物并再次进入 `try_lock`；分片 0 正在以新 owner `gnode10:6115` 补算 `28um`。不得再自动执行第三次；下一临界点是两分片再次进入 `try_lock` 后进行一次最终验收。

2026-08-04 06:28+08:00，概率图阶段最终验收完成。两个分片第二轮均成功结束并再次进入 `try_lock`，完成集合为 100/100，无 `_RUNNING` 或临时文件。独立只读复核确认 a1/a2 四个 launch、同一 release `Pocket_Plus_05c8e4d8216d`、参数、日志和 50+50 分片覆盖无误。临时 CPU16 Job 335494 在 43 秒内深读 100 份 NPZ 并以 `COMPLETED 0:0` 退出；精确字段、`float32` 三维有限 `[0,1]`、原点、体素尺寸和 `geometry.json` 全部通过。验收证据为 `/home/penghongen/My_Project/tmp/find0_calibration_recover_335115/final_acceptance/acceptance.json`。Job 335115、335116 的两个 `after_lock` 仍保留，下一阶段应复用或按正式计划处理这两张 A800，不再监控概率图生产。

下一版代码审计发现并最小修复了冻结阈值拟合评估中的契约漂移：`_BLOB_EXCEED` PDB 原先仍进入平均精确率、Dice、实例与 top-K 指标。修复后阈值扫描仍读取全部 calibration 概率图，冻结 `t_F1` 后先构建单层组件，并在所有拟合指标前排除超限 PDB；`metrics.json` 同时记录清单总数、排除数和实际评估数。该修复不会部署到当前概率图冻结 release，但必须在正式 `freeze-thresholds` 前进入所用 release。

2026-08-04 07:09+08:00，服务器工作树已安全同步并逐文件核对到 Pocket_Plus 本地学习端点。独立只读复核确认 `_BLOB_EXCEED` 修复、`min_voxels=10`、100/100 输入和无输出碰撞后，正式 CPU16 阈值冻结 Job 335495 已启动。实际 release 为 `/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_c65e77b0b031/Pocket_Plus`，内容 SHA-256 为 `c65e77b0b0315fc42216563211312213a7cf492852cdd6bb2d289373a1dac72f`；launch 为 `/home/penghongen/Feedback/Pocket_Plus/launches/335495/Find_0_freeze_thresholds_job335495_20260804T070920_a1`。早期检查显示任务在 `cnode01` 运行，只有 `after_lock_335495`，尚无阈值输出或错误。完成后必须验收 `thresholds.json`、`threshold_scan.npz`、`metrics.json` 与 calibration `_COMPLETE`，再复用 Job 335115/335116 保留的两张 A800。

2026-08-04 07:29+08:00，Job 335495 成功并进入 `try_lock`。主 agent 与独立复核均确认四项产物、release/launch 和日志通过：`t_F1=0.9962158203125`、`min_voxels=10`、`max_voxels=2046`、`denominator=32768`；扫描为七条 $F_\alpha$ 曲线、每条 32769 点且全部有限。`metrics.json` 为 `calibration_fitted`，`n_total_pdb=100`、`n_evaluated_pdb=86`、`n_blob_exceed_pdb=14`。CPU allocation 的 `after_lock_335495` 仍保留，没有被释放或重复执行。

2026-08-04 07:38+08:00，Job 335115/335116 的两张保留 A800 经独立只读边界复核后原位切换到 F1 主线。主 agent 原子改写两份动态 `run_cmd`，先发布和验语法，再只删除两个精确 `try_lock`，保留 `after_lock`：335115 顺序运行 calibration F1 后运行 validation F1；335116 运行 train 全局分片 0/50。证据为 `/home/penghongen/My_Project/tmp/find0_f1_reuse_335115_335116/activation_20260804T073854.txt`。两者 attempt a3 使用 release `Pocket_Plus_c65e77b0b031`；launch 名仍继承原 probability allocation 名称，但冻结的 `run_cmd.sh` 是实际身份依据。初始 calibration 与 train 租约已经出现，components 开始落盘，错误扫描为空。train 本次只覆盖 0/50，不能误记为全量完成。

2026-08-04 08:01+08:00，第 1 次 F1 健康检查通过。calibration 为 components 20、F1-centered 19、`_BLOB_EXCEED` 6，当前 owner `gnode10:8655`；train 0/50 为 probability/components/F1-centered 各 3，当前 owner `gnode10:8656`。两项都没有 `try_lock` 或 `kill_lock`，各自 `after_lock` 保留，错误扫描为空；validation 尚未启动，符合 calibration 成功后才进入下一脚本的顺序契约。

2026-08-04 08:31+08:00，第 2 次 F1 健康检查通过。calibration 为 components 42、F1-centered 41、`_BLOB_EXCEED` 7；train 0/50 为 probability 4、components/F1-centered 各 3、`_BLOB_EXCEED` 1。两个 owner PID 未变且租约由 `7too/3j6b` 推进到 `8cue/5a9z`，锁与错误扫描正常，validation 尚未启动。

2026-08-04 09:01+08:00，第 3 次 F1 健康检查通过。calibration 为 components 73、F1-centered 72、`_BLOB_EXCEED` 10；train 0/50 为 probability 5、components/F1-centered 各 4、`_BLOB_EXCEED` 1。当前租约为 `9gkl/5j8k`，两个 owner PID、release、动态命令与锁身份未变，错误扫描为空。

2026-08-04 09:33+08:00，calibration F1 最终验收通过。100 项公共清单精确分成 86 份 components/F1-centered 完成产物与 14 份 `_BLOB_EXCEED`，两组互斥、合计 100 且无 `_RUNNING`。独立只读复核完整读取 86 份 forest 与 centered NPZ，确认字段、offset、来源节点、冻结阈值身份、概率范围及有限性正确；`8tu8` 与 `9jeq` 是符合契约的零候选空产物。Job 335115 的同一 attempt a3 已进入 validation，当前处理 `11co`；Job 335116 的 train 0/50 继续处理 `5j8k`。两项日志无 OOM 或未处理异常。Gauss scorer 已解除“等待 calibration F1”的前置条件，可以在不阻塞 validation/train 的前提下准备 CPU 参数搜索。

Heartbeat 已重新启用为每 2 小时。主 agent 负责普通只读轮询；不再每次自动派 subagent。只有正式提交、恢复操作或最终验收前才节制地派一个只读 subagent，所有修复和写操作仍由主 agent 负责。

锁的实际目录分为两处：`try_lock_<job_id>` 位于 `allocations/` 根目录，`after_lock_<job_id>` 与 `run_cmd_<job_id>.sh` 位于 `allocations/<job_id>/`。后续恢复检查必须同时核对这两个位置。

## 已授权恢复边界

1. 两个数组元素第一次执行都结束并分别出现 `try_lock` 以前，只读监控，不修改任何锁或产物。
2. subagent 只能读取和汇总证据；bug 必须由主 agent 修复。
3. 陈旧租约的准确位置是 `calibration/<pdb_id>/_RUNNING`；`_COMPLETE` 才位于产物角色的状态目录，不能把两者路径混淆。
4. 只有旧任务和当前两个生产者都不存在、没有其他任务写相同目录，且 `_RUNNING` 内恰好只有 `owner.json` 时，主 agent才能删除 `owner.json` 并用 `rmdir` 删除空目录。禁止递归删除。
5. 100 项全部完整时不续跑；只有陈旧租约对应缺失 PDB 时，才删除 Job 335116 与 Job 335115 各自的 `try_lock`，让正式命令原样执行第二次。
6. 不删除 `after_lock`，不修改 `run_cmd`，不自动执行第三次。
7. 没有陈旧租约却仍缺少 PDB，或者出现 OOM、代码错误、数据错误、模型身份错误时，主 agent负责诊断；真实科学契约问题必须向用户汇报。

## 最终验收

最终验收必须证明公共 calibration 清单恰好包含 100 个有序、无重复 PDB；完成集合与清单相等；每个 PDB 具有 `probability_map.npz`、`geometry.json` 与 probability `_COMPLETE`；数组字段、类型、范围和几何信息符合契约；正式目录中没有 `_RUNNING` 或原子写入临时文件；两条分片合并后无遗漏。验收失败只报告具体 PDB 和失败类别，不自动第三次运行。

两个 `after_lock` 在验收后继续保留，由用户决定是否释放 A800。完成验收后把 heartbeat 改写到阈值冻结和 F1 正式作业，不保留过时 Job 指令。正式阈值必须使用 `min_voxels=10`；三个 F1 人类脚本只有在真实 OOM、参数无效或其他硬错误时才允许修改。

独立 Gauss scorer 由 `文档/exec_plan/Find_0高斯联合打分实施.md` 管理。它只消费已经完成的 F1-centered A 原子概率，不阻塞主线，不改写 `candidate_eligible`，也不限制 CLG/Selector 候选。

2026-08-04 10:01+08:00，第 5 次 F1 健康检查通过：Job335115 的 validation 已完成 probability/components/F1-centered 各4份，当前租约为 `5lmq`；Job335116 的 train 0/50 为 probability 5、components/F1-centered 各4份、`_BLOB_EXCEED` 1，当前租约仍为 `5j8k`。两项进度和错误扫描正常，heartbeat 已恢复为每2小时。

Gauss calibration 参数搜索已正式启动：父数组 Job335529 对应真实数字 Job335529至335536，每项CPU16，共享 release `Pocket_Plus_58afbf1dbaea`，唯一临时目录 `/storage/penghongen/tmp/find0_gauss_scorer_job335529`。网格为四轴各3值，共81组正参数加1组基线，SHA-256为 `85dec01cddbf992e7f8170a0d87e5626a90252487bdab002b8a21beeddb6f8dd`。提交前独立只读审计通过；八份part全部完成前不得merge，参数冻结前不得回填forest。

Job335529至335536均已 `COMPLETED 0:0`，八份part完整。主agent与独立复核均确认82组覆盖、86/14集合、有限指标和目标函数重算；基线目标函数1.0722594872，最优配置59为lambda_positive=0.1、lambda_negative=0.001、tau_angstrom=1.0、gauss_score_min=1.0，目标函数1.5214190961；semantic Dice micro、coverage F1@0.3、one-to-one F1@0.3为0.4633845442、0.5343108282、0.5237237237。CPU16合并Job335539使用同一release，在31秒内COMPLETED 0:0；完整结果位于/storage/penghongen/tmp/find0_gauss_scorer_job335529/tuning_results.json，正式参数已冻结到正式根下的artifacts/Find_0/gauss_scorer/calibration.json并通过重读验收。尚未回填forest。

2026-08-04 12:35+08:00，calibration forest 正式回填完成。CPU16 Job335572 使用 release `Pocket_Plus_34a6b476fc71` 和 launch `Find_0_calibration_Gauss_job335572_20260804T123137_a1`，4分16秒后 `COMPLETED 0:0`。86份可消费forest都只新增 `gauss_score` 与 `gauss_selected`，14份 `_BLOB_EXCEED` 未触碰；3363个F1来源节点具有有限分数，1466个达到冻结阈值。回填前后17个旧字段的临时验收哈希同为 `83558c5427adec87b75958a2b9c1b9a12543eff5a27d21ade8fb0e396ef7f3d0`；该哈希没有进入主线代码或产物，目录无 `_RUNNING`。

用户要求 CPU Gauss 回填入口可以在 GPU 主线期间随时使用。Pocket_Plus 工作树已最小调整：`src/inference/Gauss_Scorer/cli.py` 对前置角色未完成的PDB记录 `pending`，对租约被占用的PDB记录 `skipped_running`，继续扫描其余项；真实字段冲突和科学契约错误仍然失败。正式人类入口为 `训练与运行/sh/infer/Find_0_Gauss.sh`，可选择 calibration/validation/train 与分片；相同分片重复运行会增量补齐。相关20项测试通过，推理README、产物README、BOX-level契约和执行记录已更新，全部改动保持未提交且未改Git索引。

同一时刻，Job335115 validation为probability/components/F1-centered各9份，活跃租约`6az1`；Job335116 train 0/50为probability 6份、components/F1-centered各4份、`_BLOB_EXCEED` 2份，活跃租约`5lzy`。两项均RUNNING、各自只有after_lock、错误扫描为空。Heartbeat改为每3小时；后续只在validation/train对应PDB已经完成且CPU成功取得同一根租约时增量回填，不得修改两个冻结GPU release或用户F1脚本。

2026-08-06 03:45+08:00，用户授权复用 Job335493 保留的单张A800运行train全局分片1/50。本地只新增未暂存入口 `C:/Users/15919/Desktop/Pocket_Plus/训练与运行/sh/infer/Find_0_train_F1_shard_01.sh`，固定`shard_index=1`、`shard_count=50`，其余参数与原train脚本一致。主agent以精确kill_lock结束原Matcher v2 attempt6，等runner进入try_lock后原子替换动态命令，再只删除try_lock激活attempt a7；after_lock始终保留，没有scancel。新命令显式绑定冻结release `/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_c65e77b0b031/Pocket_Plus`，继续使用同一Find_0 checkpoint和正式产物根。launch为`/home/penghongen/Feedback/AdaLigand/launches/335493/train_anchor_O_O_prime_job335493_20260806T034515_a7`，启动证据在`/home/penghongen/My_Project/tmp/find0_train_shard01_job335493/`。03:57时首个PDB `10ay` 已完成probability、components与F1-centered，活跃租约推进到`11ta`；A800占用约79.2/81.9GiB，错误日志为空。Heartbeat仍每3小时，但现在联合检查Job335115 validation、Job335116 train 0/50和Job335493 train 1/50；不得恢复Matcher或把两个train分片误记为全量完成。

2026-08-06 20:25+08:00，Job335115 attempt a3 成功完成 validation 并进入 `try_lock_335115`；`after_lock_335115` 保留。200 项 validation 清单精确分成 183 份 probability/components/F1-centered 完成产物和 17 份根级 `_BLOB_EXCEED`，两组互斥合计 200，目录无 `_RUNNING`。动态命令、release `Pocket_Plus_c65e77b0b031`、allocation 成功日志和错误扫描均符合契约。不得删除 Job335115 的锁或重复执行 validation；后续 heartbeat 只监控 Job335116 的 train 0/50 与 Job335493 的 train 1/50，并在需要时确认 validation 仍停留在静止终态。21:35 时两个 train 分片分别达到 probability 215/49、F1-centered 192/42、`_BLOB_EXCEED` 23/7，各自仍有一个活跃 PDB 租约且无错误。

2026-08-07 11:19+08:00，用户明确要求完成并验收的单卡任务释放相应资源。主 agent 重新确认 Job335115 存在 `try_lock`、没有 validation 进程、A800 空闲且没有 `kill_lock` 后，只删除其 `/home/penghongen/Feedback/Pocket_Plus/allocations/335115/after_lock_335115`。runner 正常退出，数字 Job335115（数组元素`335115_1`）最终为 `COMPLETED 0:0`，活动锁被清理，正式产物与运行证据未改动。后续不再监控或复用 Job335115；heartbeat 只检查 Job335116 的 train 0/50 和 Job335493 的 train 1/50。任一单卡 train 分片自然结束并完成独立最终验收后，按用户最新原则释放其对应 allocation，不自动追加其他 train 分片。
