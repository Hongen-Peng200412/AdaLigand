# Handoff：双线 Git、学习注释与项目最新状态重建

Date: 2026-07-24

## 当前状态

本轮同时整理个人 Git 双线 skills、Pocket_Plus Stage1 学习历史和 AdaLigand 完整学习历史。所有 Git 操作只发生在本地；没有向 GitHub 或其他远端推送。

AdaLigand 的最新实现分支是 `codex/auxiliary-supervision-find1`，最新累计学习分支是 `Learn/CUMULATIVE`。交接后应使用以下命令读取精确端点，而不要依赖本文写入前的候选哈希：

```powershell
git rev-parse codex/auxiliary-supervision-find1
git rev-parse Learn/CUMULATIVE
git log --oneline --decorate --graph --all
```

训练状态没有因 Git 整理而关闭。Job `321107`、`321540`、`321743` 仍由 `文档/exec_plan/Find辅助监督与新版Find1端到端实施.md` 和 `adaligand-stage1` heartbeat 监控。

## 此前 56 个文件冲突如何收口

Pocket_Plus 曾有 56 项未提交修改挂在旧学习提交上，同时存在两条各自包含最新事实的实现线。不能整批采用任一侧，也不能直接把脏工作区覆盖到学习分支。

只读审计后，以 `de6a89f38e48553edfd440c76bf61823229d9205` 为共同基点逐文件比较两条实现线。`aa1b9e92a95e346b9a8139270854392a981ae4d7` 独有的 32 个文件和另一条实现线独有的 39 个文件保留原 blob；17 个重叠文件按实际科学契约、训练入口和运行快照逐一合并。最终形成双亲实现提交 `ebadbeac882faea40779f31e441988b118f7c6fa`，恰好覆盖两条实现线涉及的 88 个文件。

独立审计确认以下事实均保留：

- Find_1 使用 Gaussian 受体体素散射。
- Find_2 使用调整后的 56 通道密度分支。
- 蛋白、核酸和配体距离辅助监督契约完整。
- checkpoint 源码快照恢复、Selector 惰性输入和 CPU/Gloo 指标聚合保持有效。
- Stage1 producer 的盘上字段和读取契约没有漂移。

Pocket_Plus 后续从 `de6a89f` 重建六个学习提交，并在 11 个生产 Python 文件中增加中文学习注释。最终本地学习端点为 `23761e9b510927ed8dad1dd4327dee41f875ef4a`；`Learn/auxiliary-supervision-find1` 指向快照恢复阶段，`Learn/stage1-consolidated` 与 `Learn/CUMULATIVE` 指向最终核验阶段。远端 `origin/Learn/CUMULATIVE` 仍是旧历史，未经用户允许不得推送。

## 本轮 AdaLigand 整理

1. 删除并忽略用户指定的旧 Codex 辅助工作树；这些工作树不再作为事实来源。
2. 主工作树的 19 项修改全部按用户决定保留，包括 Markdown 文件的新增、修改和删除，以及两个服务器同步脚本。它们先保存为 `3a6a92a4b066ed53c0c1daadce3c6dfd2ae5ef38`。
3. 将该提交纳入 `codex/auxiliary-supervision-find1@af894df742e32855c54768bfac8a66563ef94cff`。唯一手工冲突是 `CLAUDE/memory/learnings/gotcha-2026-07-23-CLAUDE根文件与项目记忆边界.md`，采用主工作树中已经获准保留的版本。收口文字写入前的实现内容端点为 `e3404dd0ed5d886bbb0ec7eed7ff7b57dfe87c48`。
4. 从 `817940a1682216cb54308a0a0aa87fd76e1216f0` 重建完整线性学习历史，依次说明旧目录退出、产物契约、包基础、结构与受体、实验和模拟密度、质量与候选、运维工具、命令入口、配体距离标签、集群脚本、项目记忆，最后集中提交全部测试文件。
5. 收口文字写入前的学习候选端点为 `72e6105eb09e0e08808093c6d1fe43b89dfbb9a6`，其完整 Git 树与 `e3404dd` 一致。

## 验证

- AdaLigand 实现端点和学习候选端点分别运行完整测试，均为 `302 passed, 4 skipped`。
- 四项跳过来自 Windows 缺少 Linux Bash 进程树或符号链接权限。
- 收口文字、计划映射和项目记忆应以相同内容进入实现线与学习线；最终推进分支前再次执行完整树比较。
- Pocket_Plus 配置和测试在实现端点与学习端点逐字一致；11 个注释文件去除 Docstring 后的抽象语法树一致。
- Pocket_Plus 两端对当前 Windows 环境能加载的 15 个本轮测试文件均为 `96 passed`。另一个测试需要当前机器没有安装的 `torch_cluster`，两端受到同一环境限制。

## Skill 规则

`dual-track-git-workflow` 已增加两种可直接触发的流程：

- 把工作区、暂存区或指定提交融入用户点名的学习分支。
- 当学习提交顺序或注释不合要求时，重新整理用户点名的学习分支。

若修改涉及可执行逻辑、配置、测试、脚本或契约，必须先把同一改动作为实现线最后一个语义提交，再重建等价学习线。学习历史的全部测试文件放在最后一个提交。未经用户明确允许，不得把本地提交或重写后的分支推送到 GitHub 或其他远端。

`ai-code-workflow` 已加入上述任务的触发映射。两个 skill 的结构和 `agents/openai.yaml` 已通过校验。

## 下一步

1. 继续按 heartbeat 监控三个 Stage1 训练作业；不要把旧 Find_0 缺少新增辅助监督误判为错误。
2. 等两个 Find 完成 CPC 阶段切换和训练收口后，再关闭活动 ExecPlan 和 heartbeat。
3. 只有用户明确授权时，才讨论把本地重建后的学习历史推送到远端。

## 需要重新打开的文件

- `文档/exec_plan/Find辅助监督与新版Find1端到端实施.md`
- `文档/exec_plan/双线Git与学习注释重建.md`
- `文档/mapping/计划执行映射.md`
- `CLAUDE/memory/projects/adaligand.json`
- `C:/Users/15919/Desktop/Pocket_Plus/CLAUDE/memory/handoffs/2026-07-24-stage1-git-learning-rebuild.md`
