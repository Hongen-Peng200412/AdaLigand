# Handoff: CryoAtom2 calibration 首分片已提交

日期：2026-09-12

## 当前状态

calibration.json 的 100 个 PDB 原始结构预测已经准备完成。分片 0 成功提交为 379399_0（Job 379399），2026-09-12 11:56:20 在 nvlink/h200g4 PENDING(Priority)。单 A800、16 CPU、96G、TimeLimit=UNLIMITED，仅 after_hold，获得资源后直接开始。分片 1 尚未提交，不能误记为两个任务已提交。

## 已完成

输入清单 `/storage/penghongen/AdaLigand/Ori_Data/stage1_preparation_box_pool_3/split/pdb_split/calibration.json` 为 100 个不同小写 PDB 的 JSON 列表。原始图 `/storage/penghongen/AdaLigand/Ori_Data/raw/emdb_maps` 及 pair_list.jsonl 全覆盖，完整序列 `/storage/penghongen/AdaLigand/held_out/sequence_catalog.jsonl` 包含 715 蛋白质、38 RNA、10 DNA entity，短序列保留。predict.py 只新增两处列表读取兼容，官方参数、只解压原始图、不读取真实坐标的边界不变。8 项测试、两遍主代理自查、两轮独立审查及参数化后的窄查通过。

## 决定与资源限制

nvlinkg8、h200g4 是 QOS，A800 选 nvlink 分区。nvlinkg8 全组 GrpSubmitJobs=4 已满，h200g4=2 只剩一个名额，原双元素数组提交被拒绝且没有作业号。改为每个分片独立提交一个单元素数组，不设分片依赖。a100g2 的同硬件 test-only 通过，但尚未获用户选择；不得把只读检查当作正式提交。

已执行正式短命令（服务器 AdaLigand 项目根）：

```bash
bash 测评数据代码/cryoatom2原始预测结构/submit_calibration.sh 0 h200g4
```

分片 1 的待执行命令为 `bash 测评数据代码/cryoatom2原始预测结构/submit_calibration.sh 1 nvlinkg8`；若用户允许其他 QOS，核对通过后再改为该 QOS。不要重复提交分片 0。

## 下一步

接收用户对第二个 QOS 的选择，提交分片 1，记录新 Job 编号与实际资源。继续监视两分片到 100 项验收完成。稳定等待每轮 60/90 分钟，由多次 Start-Sleep -Seconds 300 组成，期间静默，无 heartbeat。任务结束后保持 after_hold，用户尚未授权释放。旧 test_0 的 179 项已完成，旧作业不属于本轮操作范围。

## 重新打开的文件

- `文档/exec_plan/CryoAtom2校准集受体重建执行记录.md`：完整任务日志、正式命令、测试命令和后续验收。
- `测评数据代码/cryoatom2原始预测结构/README.md`：产物与字段；submit_calibration.sh 的必填参数依次为分片编号和 QOS。
- 服务器 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/calibration/运行日志与统计/`：任务日志.md、提交前输入核验.json、提交前代码摘要.json、单分片提交参数检查.txt；未来 release/launch 在 slurm/ 下。实际结构仅保存同根 cryoatom2_artifact/<pdb>/<run_stamp>/。

Git 开始点 Learn/CUMULATIVE=1fcd70063ce0652b406ed634cd9a0f68b3e32319，开始工作区干净且按提交者时间唯一最新。本轮小型适配按明确例外留累计分支工作区，不自行提交。当前线程 goal 仍 active，不能在首分片排队时标记完成。
