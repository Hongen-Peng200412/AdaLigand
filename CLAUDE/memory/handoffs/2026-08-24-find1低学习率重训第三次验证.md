# Handoff: Find_1 低学习率重训第三次验证

Date: 2026-08-24

## Current State

Job `351295` 的第 2 次执行仍在 `hnode01` 使用两张 H100 训练。当前只有 `/home/penghongen/Feedback/Pocket_Plus/allocations/351295/after_lock_351295`，没有 `try_lock` 或 `kill_lock`。08:59 检查时 W&B `pencounkdual-111/AdaLigand_Stage1/a87fe1ef` 已从第三次 validation 的 `global_step=3386` 恢复到 `global_step=3404`，服务器错误扫描为空。

## Completed

第三次正式 validation 的总损失为 `0.341108`。主要 PR-AUC 如下：

- 配体体素：`0.561822`
- 受体：`0.636451`
- 蛋白主链：`0.958468`
- 核酸主链：`0.000172`
- 原子：`0.650053`
- 伪原子：`0.608337`

配体体素 PR-AUC 比第二次 validation 的 `0.552578` 提高约 `0.009244`，超过调度器绝对改善阈值 `0.003`。`TOP_epoch_00_score_0.5618.ckpt` 与 `last.ckpt` 已于 08:53--08:54 完整落盘，大小均为 1,466,223,991 字节。`BEST.ckpt` 已刷新为上一轮的 `TOP_epoch_00_score_0.5526.ckpt`；当前最高 TOP 按冻结回调语义等待后续 validation 刷新别名。

## Decisions

继续保持当前科学契约与训练参数，不触发重启或学习率修正。`refined/unrefined` 面板的 `NaN` 仍来自未启用的稀疏精修路径，不参与损失、调度器判断或 checkpoint 选择。

## Open Questions

无新增科学契约问题。后续只需观察第四次 validation、`BEST.ckpt` 是否刷新到 `0.5618` TOP，以及调度器在连续不足 `0.003` 的改善后是否按配置降低学习率。

## Next Actions

继续使用多段独立的 300 秒命令低耗守护 Job `351295`。只在 validation、checkpoint、学习率下降、故障恢复或训练退出等明确事件时合并更新执行记录、映射索引与 handoff。

## Files To Reopen

- `文档/exec_plan/Find辅助监督与新版Find1端到端实施.md`
- `文档/mapping/计划执行映射.md`
- `CLAUDE/memory/projects/adaligand.json`
- `CLAUDE/memory/handoffs/2026-08-23-find1双卡降低学习率重启.md`
- `CLAUDE/memory/handoffs/2026-08-24-find1低学习率重训第二次验证.md`
