# Find_1 第三版双卡重训启动

## 当前结论

2026-08-22 09:30:26，Slurm Job `351295` 在 `hnode01` 获得两张 H100 和 64 CPU，并启动第三版 Find_1 CPC1 重训。冻结发布、最终配置、两个 NCCL rank、在线 W&B、训练损失和两张 GPU 均已完成首轮验收；09:48 的 W&B 摘要到达 `trainer/global_step=5`，当前没有显存不足、NCCL、DataLoader 或非有限值错误。

## 资源转换事件

- 触发门槛是 H100 分区没有既有待调度任务。
- 09:17 提交 Job `351295` 后，二次检查确认待调度队列中只有新 Job；随后按用户授权取消 Job `346003` 与 `350185`。他人的 Job `350573` 未被操作。
- Slurm 回收旧任务时一度把 `hnode01` 标记为 `MIXED+DRAIN`，原因是 `Kill task failed`，新 Job 暂时显示 `ReqNodeNotAvail`。没有改投节点、取消新 Job 或操作他人任务；管理员在 09:30 前恢复节点，新 Job 随即在原定 `hnode01` 启动。

## 冻结运行身份

- Slurm Job：`351295`
- 发布副本：`/home/penghongen/Feedback/Pocket_Plus/releases/Pocket_Plus_fdb8a30fa196/Pocket_Plus`
- launch：`/home/penghongen/Feedback/Pocket_Plus/launches/351295/Find_1_job351295_20260822T093108_a1`
- 运行目录：`/home/penghongen/Feedback/Pocket_Plus/logs/AdaLigand_Stage1-Find_1-CPC1/Find_1-CPC1____Find_1_job351295_20260822T093108_a1_CPC1`
- W&B：项目 `pencounkdual-111/AdaLigand_Stage1`，运行 ID `yyz7c55e`
- `Find_1.sh` SHA-256：`2f239a4e5770d27f701fbaba033d39a70c499356bcfae92dcdefb1b151bef248`

最终 `config.yaml` 和训练命令共同确认：

- 单节点、`train.devices=2`、`train.nnodes=1`；
- 64 CPU、每 rank `train.num_workers=30`；
- 每卡 batch 6、梯度累积 4、全局 batch 48；
- `train.optimizer.lr=1.0e-4`；
- `train.gradient_clip_val=1000`；
- `train.val_per_epoch=40`；
- `train.scheduler.threshold=0.003`。

## 启动验收证据

- 两个进程以 `GLOBAL_RANK 0/1` 完成 NCCL 注册，`world_size=2`。
- W&B `yyz7c55e` 在 09:48 报告 `trainer/global_step=5`、epoch 0、训练总损失 `0.709794`；各启用分项均为有限值。
- 三次 GPU 采样中，两张 H100 均参与计算，显存约为 79--81 GiB；当前没有显存不足。
- 启动阶段主进程先顺序读取 BOX pool 和完整体 NPY，随后建立两个 rank 与 60 个 DataLoader worker；这段等待有持续磁盘读取和内存增长证据，不是停滞。
- allocation 标准错误只有 Lightning 同步建议和弃用警告，没有错误栈。

## 后续守护规则

1. 训练稳定后使用多条 `Start-Sleep -Seconds 300` 组成 30 分钟或 60 分钟等待，不创建 heartbeat；每 5 分钟都能接收用户的新指令。
2. 醒来后核对 Job 状态、W&B 步数和有限损失、两卡活动、错误增量、validation 与 checkpoint。
3. 不记录无变化轮询。只在新 validation、TOP/BEST/last checkpoint、故障恢复、学习率变化或训练退出等明确事件时，合并更新执行记录、计划映射和本 handoff 的后继记录。
4. 可恢复故障可在同一训练目标内自行处理；若需要改变科学契约，必须记录原因、实际参数和恢复证据。

## 相关本地记录

- `文档/exec_plan/Find辅助监督与新版Find1端到端实施.md`
- `文档/mapping/计划执行映射.md`
