# Handoff: CryoAtom2 实现完成，等待 A100 资源

Date: 2026-09-08

## Current State

179 个 PDB 的 CryoAtom2 批量重建代码与审查完成，正式预测尚未提交。独立 GPU 门控 Job `374469` 连续三次核实为 `PENDING(Priority)`，尚无分配节点；Slurm 预计 2026-09-13 13:31:48 开始，此时间不是保证。Codex goal 因等待外部资源标记 blocked，完整验收目标仍未完成。保留排队作业，不重复提交门控。

## Completed

本地代码在 `测评数据代码/cryoatom2原始预测结构/`，已无删除上传到服务器 `/home/penghongen/My_Project/AdaLigand/` 的同名目录。三个生产脚本已核实上传前后逐字节一致。主代理两遍自查、三个代理两轮全面审查及问题窄口径复核均通过；两端各 7 项契约测试通过。真实整图推理尚未验证。

实现端点 `54201da290e739f57b6eb8a2f53e2821d0585e2c`，学习与累计端点 `34a64815340246a0ef17da6ccb6130d158f99e14`，共同基点 `854e4ebcf8f467c98c5ab7315b50c3b346a444e7`。两端完整树相同，累计已快进并为按提交者时间唯一最新提交。运行记录及本 handoff 按项目小型文字修改例外留在累计工作区；不擅自提交其他未提交修改。

## Decisions

输入为 `/storage/penghongen/AdaLigand/held_out/split/held_out_06_chain/test_0.json` 的 179 个 PDB，使用 `/storage/penghongen/AdaLigand/Ori_Data/raw/pair_list.jsonl` 对应的原始 `.map.gz`，仅解压、不提前重采样。完整蛋白质、RNA、DNA 序列来自 `/storage/penghongen/AdaLigand/held_out/sequence_catalog.jsonl`，保留短序列及未知字符，未知字符接受官方读取器行为并记录。不引入真实受体或配体坐标。

正式根为 `/storage/penghongen/Adaligand_infered_receptor_data/cryoatom2/test_0_chain06`。`cryoatom2_artifact` 只放实际逐 PDB 运行产物；日志、输入、来源、统计和 Slurm 记录统一放 `运行日志与统计`。每个 PDB 保留官方最终及 raw CIF，失败保留并独立统计，不能从分母删除。

正式任务使用 3 个数组元素，各 1 张 A100、8 核 CPU、96 GiB 内存，无时间限制，启用 after_hold。门控 374469 是单独作业，最多 20 分钟，结束即释放；其命令及结果只放任务日志的“测试与门控命令”，不能加入正式入口。

## Next Actions

1. 通过用户级 `C:/Users/15919/.codex/tools/Invoke-ProjectSsh.ps1` 查询 Job 374469。若仍排队，等待资源变化；不要因观察超时而重启。若已结束，检查 `运行日志与统计/门控/374469.out` 的 `RUNTIME_GATE_PASSED` 及退出码。门控只验证主权重反序列化与 ESM2、RNA-FM 短序列前向。
2. 门控通过后，在服务器 `/home/penghongen/My_Project/AdaLigand` 使用以下唯一正式命令，单独执行并记录正式 Job ID：

   ```bash
   bash 测评数据代码/cryoatom2原始预测结构/submit.sh
   ```

3. 核实三份 allocation、资源参数、release 和 launch，观察真实 PDB 运行。此时更新提交里程碑 handoff。不要擅自释放 after_hold 资源或触碰其他训练作业。
4. 全部 PDB 完成后执行独立状态汇总，逐项验收最终及 raw CIF 可解析、非空、坐标有限；核对 179 项完整分母及失败原因。最后更新执行记录、映射与最终 handoff。Find_1 受体特征和模拟密度替换不在本任务范围。

## Files To Reopen

- `文档/exec_plan/CryoAtom2测试集受体重建执行记录.md`，服务器副本为正式根下 `运行日志与统计/任务日志.md`。
- `测评数据代码/cryoatom2原始预测结构/README.md`。
- `文档/规划文档/CryoAtom2测试集受体重建.md`。
